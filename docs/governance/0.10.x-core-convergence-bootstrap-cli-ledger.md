# Bootstrap and CLI Core Convergence Ledger

Status: verified implementation checkpoint for issue #233. This checkpoint
completes every mutually compatible deletion that can be performed without an
Application or Presentation contract redesign. It does **not** claim that the
literal wiring-only outcome is complete; the remaining cross-layer blockers
below must be resolved by separately approved architecture work before #233 or
the #229 umbrella can close.

## Authority and scope

- Ticket: GitHub issue #233.
- Audited base: `d7d03e22375d47afafde05dcacdaf622fbae9e31`.
- Entry measurement: 18,651 Bootstrap + CLI nonblank runtime lines and 68,922
  total runtime lines. The 18,653 value in the issue intake is stale by two
  lines; the accepted #230 ledger and both later Core ledgers already record
  18,651.
- Mutable production scope: `NvtFwCombiner.Bootstrap` and
  `NvtFwCombiner.Cli`. Focused composition-root, CLI, golden, and architecture
  tests may change. Domain, Profiles, Application, Infrastructure, Contracts,
  CRC worker, and Presentation production are frozen.
- Risk: R2. Firmware maps, profiles, ranges, processors, output bytes, support
  state, report schema, and naming behavior are unchanged.

Gross removal and required additions below are exact production nonblank-line
measurements from the frozen diff. Test helpers and test migrations are not
counted as production reduction.

## Compatible implementation portfolio

| Id | Final owner and callers | Compatibility decision | Gross removal | Required additions | Net | Evidence | Disposition |
| --- | --- | --- | ---: | ---: | ---: | --- | --- |
| B01 | Base `CompositionOutputNaming.cs:12-25,149-215` (`CreateFlashCodeOutputFileName`, `FindDpVersionToken`, `FindTpVersionToken`) had zero production callers; current tests use `FirmwareInspectionTestSupport` and `OutputNamingTestSupport`, while production retains the inspection-backed overload at current `CompositionOutputNaming.cs:12`. | Delete the path reread owner; no compatible candidate conflicts. One immutable batch inspection remains authoritative. | 65 | 0 | -65 | Output-naming/direct-golden metadata tests and exact absence guard; R2 naming/hash parity | Implemented |
| B02 | Base `FirmwareInspectionAdapter.cs:23-34` and `.Metadata.cs:9-68` exposed single-file inspection plus DP/CMI/context readers with zero production callers. The live production caller `CompositionExperienceAdapters.cs:434-437` already uses `InspectFirmwareBatch`. | Delete only the zero-production shells. Retain batch reading, FirmwareConfig, CtrlRAM projection, the internal shared core, accepted ranges, and hashes. | 71 | 0 | -71 | Firmware-inspection, CtrlRAM evidence, output-naming, and UI batch-smoke tests; R2 inspection parity | Implemented |
| C01 | Base `CliApplication.AbMerge.cs:1-13` forwarded one call from base `CliApplication.cs:52`; current `CliApplication.cs:52` calls `AbMergeCliCommandHandler.RunAsync` directly. | Delete the one-call shell; no parser, exit, report, or naming behavior changes. | 13 | 1 | -12 | AB CLI/root dispatch tests and deleted-file guard; R1 dispatch parity | Implemented |
| C02 | Base `MergeCliCommandHandler.cs:11-23` accepted a redundant `command`; base `CliApplication.cs:54` could invoke it only after dispatching the closed General Merge identity. Current handler line 10 has no command argument. | Delete the impossible unknown-command branch and unused import. It does not conflict with the retained General parser diagnostics in R09. | 8 | 0 | -8 | General Merge CLI grammar/exit tests and source guard; R1 diagnostic parity | Implemented |
| C03 | Base `CliApplication.StandardMerge.cs:17-19` mapped AB-only `DpAb`, `TpA`, and `TpB`; the Standard parser at current lines 38-43 admits only DP/TP/LDC options. | Delete unreachable rows; AB dispatch owns its own slot grammar. | 3 | 0 | -3 | Standard Merge CLI tests and source guard; R1 parser parity | Implemented |
| A01 | Current `CanonicalCapabilityProjection.DpReplace.cs:103-146` now requires `AuthoringRevision`; callers are `CompositionExecutionAdapter.Replace.Dp.cs:35-44` (accepted session revision or ephemeral 1) and `ReplaceCliCommandHandler.DpWorkbench.cs:77-86` (CLI 1). | Replace the final revision-zero bridge. It intentionally adds four lines and does not claim to remove the duplicated readiness pass classified in R12. | 1 | 5 | +4 | DP selection/readiness tests assert revision 7 preservation; global no-revision-zero and private-seam guards; R2 publication identity | Implemented |

The frozen production diff removes 161 nonblank lines and adds 6 required
nonblank lines, for an exact Bootstrap + CLI and runtime reduction of 155.

## Residual candidates and stop decisions

| Id | Exact owner, current lines, and live caller evidence | Gross | Required additions | Net | Conflict, evidence, and disposition |
| --- | --- | ---: | ---: | ---: | --- |
| R01 | `CompositionExperienceAdapters.cs:1-432` and `CompositionExecutionPort.cs:7-133`; `WorkbenchHostServices` and Presentation consume their Application-port instances. | 559 | Unbounded without an owner-instance rewrite | Unproven | The seven targets are large static partial owners and cannot implement the instance ports. **STOP / Application + Presentation:** deleting them either recreates forwarding or inverts the UI dependency. |
| R02 | `CanonicalCapabilityResolution.cs:344-409` (`ResolveCanonicalCapabilityForRun`), called from `CompositionExecutionAdapter.cs:173`, `CompositionOutputNaming.AbMerge.cs:71`, and `CliApplication.StandardMerge.cs:160`. | 66 | At least 87 in the rejected exact attempt | **+21 measured** | Correct AB naming/delivery propagation grew production and the current path remains fail-closed across catalog reloads. **STOP / not net-negative.** |
| R03 | `CanonicalCapabilityCatalogMigrationSource.cs:11` plus its bundle/route/registration partials; sole construction is `WorkbenchHostServices.cs:14`, with about sixty registry consumers. | 1,731 | No accepted upstream source exists | Unproven | It still constructs map/compiler/fingerprint/publication authority. **STOP / Profiles, Infrastructure, and Application R2-R3:** moving it can change maps, diagnostics, fingerprints, or bytes. |
| R04 | `CompositionMemoryProjection.AbMerge.cs:7` and the other `CompositionMemoryProjection*` partials plus `WorkbenchMemoryDisplayProjection.cs:8`; UI-facing ports still pass primitive IC/path/length/draft inputs. | at least 1,292 | No accepted session-bearing port exists | Unproven | Application's projector cannot consume the present port shape. **STOP / Application + Presentation:** first migrate the port to accepted session/capability inputs. |
| R05 | `CompositionPlanningAdapter.GeneralMerge.Mapping.cs:6` and the other planning partials; `CompositionAuthoringSessionAdapter.GeneralMerge.cs:9` and the remaining session partials are live UI/CLI owners. | about 2,815 | No complete typed replacement exists | Unproven | Current Application ports do not carry every accepted mapping/readiness result. **STOP / Application + Presentation:** requires the same session-contract migration as R04. |
| R06 | `WorkbenchCompositionModels.cs:347-404` exposes only `WorkbenchRunResult.ReportJson`; CLI interpreters remain at `MergeCliCommandHandler.Result.cs:49`, `AbMergeCliCommandHandler.cs:270`, and `ReplaceCliCommandHandler.WorkbenchReport.cs:52`. | about 228 | Typed Application envelope and UI/CLI migration not yet designed | Unproven | A Bootstrap DTO/parser would duplicate semantics. **STOP / Application + Presentation:** add one typed run/report result before deleting CLI parsing. |
| R07 | CLI selectors, required-slot binders, and profile loops inside current handlers. Exact audited surfaces were 162 gross lines. | 162 | at most 99 | about -63 | They would create a new short-lived helper inside handlers expected to disappear after R04-R06. **STOP / conflict with final migration:** delete the handlers after typed session adoption rather than polish them now. |
| R08 | `CompositionExecutionAdapter.StandardMerge.cs:11-27` (`RunStandardMergeAsync`); live callers are GoldenRegression and Bootstrap evidence tests, not product dispatch. | about 20 | 0 production, broad golden harness migration | about -20 | **STOP / disproportionate R3 evidence cost:** retain until golden support has an accepted-session harness; do not expose private execution internals. |
| R09 | General parser diagnostics, runtime generation refresh, process entry/cancellation, bundle materialization, workflow/issue constants. Representative owners include `CliApplication.cs:9-69` and `CanonicalCapabilityCatalogHost.cs:9-91`. | Not coherently removable | Unproven | Unproven | **STOP / durable boundaries:** parser errors, process lifetime, release trust, and Presentation-neutral tokens are independent responsibilities, not duplicate owners. |
| R10 | Execution convenience gateways at `CompositionExecutionAdapter.StandardMerge.cs:30`, `AbMerge.cs:14,74,135`, `Replace.cs:235,252,362`, and `Replace.CtrlRam.cs:50,76`. | 254 | 11 in the rejected patch | -243 syntactically | The exact attempt required raw cores, capability/session inputs, and General Replace strategies to become friend-visible and forgeable. Independent review classified that as a P1 admission-authority regression. **STOP:** wrappers and private cores are restored; guard locks the boundary. |
| R11 | AB CLI reruns naming/admission and alias projection at `AbMergeCliCommandHandler.cs:98-144`; execution repeats naming/admission at `CompositionExecutionAdapter.AbMerge.cs:117-167`. | 28 | Accepted Application naming/result contract unavailable | Unproven | Removing either pass changes concrete auto-name preflight, alias timing, `ParamName`, and delivery-report behavior. **STOP / Application typed-result prerequisite.** |
| R12 | DP CLI recomputes selection readiness at `ReplaceCliCommandHandler.DpWorkbench.cs:65-91`; execution repeats it at `CompositionExecutionAdapter.Replace.Dp.cs:31-67`. | 27 | Accepted readiness result is not carried by the current CLI execution request | Unproven | A01 fixes revision provenance only; it does not remove the duplicated readiness pass. **STOP / Application accepted-session prerequisite.** |
| R13 | Standard CLI compiles, inspects, and constructs direct execution at `CliApplication.StandardMerge.cs:64-166` instead of consuming an accepted session. | 86 | Accepted Standard CLI session/result migration not designed | Unproven | Removing this path now changes inspection/error/report behavior. **STOP / Application typed-session prerequisite.** |

The audited surfaces above contain no other classified compatible deletion at
this checkpoint. The literal issue
criteria that demand removal of all Workbench facades, Bootstrap semantic
catalog construction, memory projection, planning/session adapters, and CLI
report interpretation remain unsatisfied because the frozen upstream
Application/Presentation contracts do not provide replacements. This ledger
therefore blocks a false #233 completion claim and identifies the architecture
work needed for the next checkpoint.

## Frozen measurement and evidence

| Slice | Entry | Frozen checkpoint | Delta |
| --- | ---: | ---: | ---: |
| Domain + Profiles ratchet | 20,287 | 20,287 | 0 |
| Application ratchet | 23,071 | 23,071 | 0 |
| Bootstrap + CLI ratchet | 18,651 | 18,496 | -155 |
| Infrastructure + Contracts + worker ratchet | 6,913 | 6,913 | 0 |
| Runtime total ratchet | 68,922 | 68,767 | -155 |

The four frozen slice values sum exactly to 68,767. No receiving production
slice grows, no logic is moved into Presentation or an excluded path, and both
exact ratchets are lowered in this change.

Completed evidence before the final integrated verifier:

- Final naming/inspection, DP/Standard routing, and focused composition tests:
  101/101 PASS after the rejected execution-gateway deletion was fully rolled
  back.
- The migrated UI batch-inspection smoke: 1/1 PASS; the UiSmoke project builds
  without warnings or errors.
- The focused architecture convergence guard: 1/1 PASS and now rejects any
  reopening of the private Standard, AB, Replace, CtrlRAM, or General Replace
  execution seams.
- Complete canonical verifier: PASS. Release tests passed Domain 402/402,
  ProfileContract 387/387, Application 480/480, Infrastructure 401/403 with
  two platform skips, GoldenRegression 17/17, Architecture 195/195, Bootstrap
  963/963, and UiSmoke 442/442. Python verification passed 348 script tests
  with four platform skips and 30/30 CRC-worker tests at 100% coverage.
- `git diff --check`: PASS.

Structure-only validation, scoped Polytail, independent R2 architecture review,
and the complete canonical verifier are recorded and pass. PR/CI evidence
remains a publication gate; this checkpoint does not satisfy the cross-layer
residuals above. No schema, profile, golden fixture, firmware byte, range,
processor, report schema, CLI exit code, or support state changed.
