# Repository Bootstrap Status

## Locked identity

| Field | Value |
| --- | --- |
| Owner | `Dennis40816` |
| Repository | `nvt_fw_combiner` |
| Visibility | Private |
| License | MIT |
| Default branch | `main` |
| Branch model | version integration branch plus short-lived feature branches |
| Initial tag | `v0.1.0-dev.0` |

## Included in init

- root `SPEC.md`, layered AGENTS and nine repo skills;
- project-scoped Codex config and four bounded agents;
- .NET 10/Avalonia solution structure and exact SDK installer scripts;
- active CI and stable-tag release skeleton;
- profile/request/report/worker contracts and ADRs;
- exactly two Python reference snapshots and no NFCG TypeScript;
- minimal domain/architecture proof code, not a parity claim.

## GitHub settings still applied after remote creation

- protect `main`, require PR/reviews/checks/linear history;
- create protected `release` environment;
- configure CODEOWNERS identities and required check names;
- enable Dependabot/security scanning according to company policy;
- confirm runner, private golden storage, artifact retention and code signing;
- push the already-created annotated `v0.1.0-dev.0` together with the verified init commit.

## Blocking firmware item

Protocol 2 transform implementation remains blocked until owner provides exact Python invocation, header fields, order, applicability and minimum write ranges. Codex must not infer them.
