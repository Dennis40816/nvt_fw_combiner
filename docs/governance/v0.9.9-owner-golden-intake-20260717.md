# v0.9.9 Owner Golden Intake — 2026-07-17

## Decision

The repository owner explicitly approved committing every supplied golden
sample to public Git while retaining technical project and `AUTO_PRJ`
identifiers and excluding personal information. The recovered archive has
SHA-256
`9027e9038f51c5421f922afdf73d1f16fc5f7dd582e23e25547727fda585205c`.

The committed snapshot contains 118 BIN payloads and 13 supporting evidence
files. Every file is pinned by path, size, original filename, role, and SHA-256
in `testdata/golden/ctrlram-replace/manifest.20260717.json`. Repository
validation rejects missing, changed, extra, duplicated, or path-escaping
snapshot files.

This is R3 evidence intake. It does not promote runtime support or approve a
range, command, processor write, alias, or firmware result.

## CtrlRAM Replace artifact contract

CtrlRAM Replace consumes a TP BIN work image, not a full FlashCode image. The
product-level evidence sequence is:

1. Standard Merge the Initial DP and original TP and compare the complete
   FlashCode with the owner expected output.
2. Apply selected physical CtrlRAM inputs and Legacy Combiner 1.13 to a staged
   copy of the original TP.
3. Standard Merge the same Initial DP and the updated TP, then compare the
   complete final FlashCode with the owner expected output.

The host must not apply TP offsets directly to a larger FlashCode artifact.
Supporting a full-FlashCode input would require an explicit owner-approved TP
slice/reinsert contract and is not part of this intake.

The NT51927 single case replays this three-stage contract. The initial DP/TP
merge is byte-identical to the supplied FlashCode. The updated final output has
zero payload differences; its 24 residual bytes are confined to known Header
CRC and Header Copy CRC fields. The real tool sample independently changes
exactly the selected NF bytes, four firmware-version bytes, and known
Header/Header Copy CRC fields, with no other mutation.

## Evidence classification

| IC / mode | Intake result | v0.9.9 decision |
| --- | --- | --- |
| NT51920 single/cascade | All supplied physical CtrlRAM bytes match their declared target sections; short Vector uses prefix semantics | Remove from the completely-missing list; owner command/range review remains |
| NT51923 single/cascade | All formal physical inputs match their declared target sections | Remove from the completely-missing list; owner command/range review remains |
| NT51926 1.4.1 single/cascade | All supplied physical inputs match their declared target sections | Range-parity candidate; 2.0.0 remains separate |
| NT51927 single | Direct real replacement sample plus complete TP-only replay | Strong direct evidence; owner R3 promotion review remains |
| NT51929 single | New non-AB expected output matches NF/Normal/VN target bytes | Standalone single candidate; do not use the AB first half as its golden |
| NT51930 1.3 cascade | NF/Normal/MP/VN match, but one DiffDLM 4 KiB slot has 4,090 mismatching bytes | Not closed |
| NT51931 cascade | Owner BAT names `NT51931BASED_NORMAL_MODE` and consumes only `0xC800` DiffDLM bytes; `InsertSID.py` and its output are absent | Keep CtrlRAM Replace Not Supported |
| NT51932 cascade | Normal/VN/NF prefix match, but one DiffDLM 4 KiB slot has 4,095 mismatching bytes; supplied NF composite equals only `NF_Diff_0.bin` | Not closed; DiffNFMerge completion is not proven |

Except for the NT51927 real sample, the new complete FlashCode sets establish
owner-expected-output plus expected-derived range parity. They do not create a
historical pre-replacement base or independently prove unchanged bytes.

## Owner family facts

- NT5191x entries use their owner-declared NT5192x perfect-family aliases; no
  duplicate evidence folder is required unless the fact scope changes.
- NT51928 non-NB is a partial NT51927-family alias; NT51928 NB remains out of
  scope.
- NT51919, NT51929, and NT51932 are one owner-declared family. NT51929 single
  is the single-product case. NT51932 owns the cascade product path.
- NT51932 AB may use the NT51929 AB golden only as an explicitly recorded
  family alias. A code occupies `[0x00000, 0x40000)` and B code occupies
  `[0x40000, 0x80000)`; the tool must update the selected start address.
- NT51930 1.3 cascade is secondary support and remains evidence-gated.

These facts may remove duplicated declarations only after the typed V2 query
and execution path proves equivalent behavior. They do not themselves promote
runtime support.

## DiffNFMerge intake

The incoming `DiffNFMerge.exe` is byte-identical to the existing hash-pinned
repository package at `external-tools/diff-nf-merge/1.0.0`:

`f611af7e315d46341e15cd7140eb3962f6ac05d337121e5554022ef5e69a2bbe`

It is intentionally not duplicated in the golden snapshot, executed,
registered at runtime, or added to the release ZIP. Its NF directory and
filename contract remain deferred.

## Remaining stable v0.9.9 evidence gaps

- NT51926 Common FW 2.0.0 single/cascade, unless explicitly removed from the
  v0.9.9 migration scope.
- NT51930 1.3 cascade: the same-run DiffDLM or command/log that explains how
  the supplied Diff variants are composed.
- NT51932 cascade: the same-run DiffDLM and completed DiffNFMerge
  `NF_Ctrlram.bin`, or equivalent output hash/log evidence.
- NT51950 cascade.
- NT51951 single corrected same-run inputs/final and NT51951 cascade.
- NT51951 direct-product AB, if it remains in the stable scope.

NT51931 CtrlRAM Replace is intentionally not a request for promotion evidence;
it stays fail-closed.
