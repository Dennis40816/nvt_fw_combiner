# v0.10.5 preload baseline and implementation ledger

Status: Frozen implementation baseline for PL-01 (#373)

This document freezes the predecessor evidence, current production paths, work
units, lifecycle grammar, and ticket accounting boundary before any `v0.10.5`
lifecycle migration. It is descriptive evidence for the intake base
`6d002868094719d9420820d703cc51a7a09e601e`; the normative behavior remains
[`SPEC.md`](../../SPEC.md), the
[`v0.10.5` specification](../specs/v0.10.5-unified-preload-lifecycle.md), and
[ADR 0049](../adr/0049-unified-preload-lifecycle.md).

PL-01 changes no production path. A later ticket that removes a characterized
path must keep this historical baseline intact, replace its current-shape
architecture assertion with deletion and behavioral evidence, and record the
exact production gross/add/net delta.

## Stable predecessor identity

The predecessor is the immutable stable `v0.10.4` release, not an earlier local
candidate or the current `0.10.5` integration branch.

| Evidence | Exact value |
| --- | --- |
| annotated tag | `v0.10.4` |
| source commit | `8c3cc51dc95cea2fbae8ec5ac0287db730d1b37b` |
| source tree | `48ee0fd87d942674acf83c910de78681db748458` |
| successful release workflow | run `31623880051`, attempt 2 |
| package | `NvtFwCombiner-v0.10.4-win-x64.zip`, 77,025,377 bytes |
| package SHA-256 | `06f9c63ef0d7384dfe7c6b850e80fec94b7a7c29368e4499dfb781f1187dcf3f` |
| application SHA-256 | `3ad2051d75730a5e1409e6398a83c11bfbab6ead05c497846e8a750d243299d4` |
| candidate-manifest SHA-256 | `cc63b5fa49bc6c27466bc1320e928c6d34ed169e2ef96f32157ed82762104f6c` |
| SBOM SHA-256 | `c51a69f0e1783919532eef85336e249c8f8f9e920bc456777534602bd56ca0ac` |
| provenance SHA-256 | `88782b1a77f41002eede3779e4fc438bc876166ca4cb0a73db56a5407e344620` |
| outer checksum-list SHA-256 | `9da227e2acf585e389d630f8741c19309f333e9ca708cd725b5724968237d19c` |

The canonical nonblank code-size baseline and intake ratchets are:

| Code-size metric | Nonblank lines |
| --- | ---: |
| predecessor full production | 97,306 |
| runtime production | 67,186 |
| Domain plus Profiles | 20,619 |
| Application | 29,383 |
| Bootstrap plus CLI | 3,255 |
| Infrastructure plus Contracts plus CRC worker | 13,929 |

The four runtime slices sum to 67,186. The full-tree review threshold in
`scripts/code_size_policy.py:86` remains 96,044; it is not substituted for the
measured predecessor baseline.

## Startup evidence boundary

The exact downloaded stable package was measured twice after publication. Its
five-run process-to-window medians were **777.090 ms** and **806.930 ms**. In
the same later environment, the unchanged release-identity control measured
**782.368 ms**. The corresponding measurement JSON SHA-256 values are:

- first published-package observation:
  `f5c2062c3cec80299641be9cae2113f0cc199dd6c88e4503ed71e53374fa79a5`;
- repeat published-package observation:
  `c921b58beea244504aa06e32556b22ab4fe20057bc485005d88ebc6ecaaab41d`;
- unchanged control observation:
  `ddd27f07311fd599b258c9e41e26c424b3bb8bae02b33e89a304b6f9b1f5b142`.

Neither stable-package median satisfies the 700 ms absolute gate. The control
prevents attributing the miss to one code change without evidence, but it does
not waive the gate. Earlier 661.784, 656.444, 687.180, or 696.166 ms candidate
observations belong to different package identities and cannot be reused as a
`v0.10.5` pass. The full provenance and environment record remains in
`docs/references/v0.10.4-startup-700ms-evidence.md`.

## Lifecycle grammar ratchet

The implementation must preserve all of these closed rules:

1. Presentation owns exactly one shell preload lifecycle. Feature data and
   semantic publication stay in the existing typed catalog, report/history,
   diagnostics, inspection, filesystem, and external-environment owners.
2. Every queued progress, terminal, dispatcher, and accessibility callback for
   an allocated attempt carries `(shell session generation, stable stage id,
   monotonically increasing attempt number)`. Pending or dependency-blocked
   stage-state updates still carry the shell-session generation and stable
   stage id, but expose no current attempt identity until work is admitted.
   Selection inspection and external requests keep their separate never-reused
   request generations.
3. Every allocated attempt has exactly one terminal outcome. Per stage,
   Presentation retains at most the current attempt plus the immediately prior
   terminal summary; a third attempt deterministically evicts the older summary.
   Summaries contain lifecycle identity/state/diagnostic only, never copied
   feature data.
4. Catalog is the sole required startup stage and runs alone. After its typed
   publication is applied, optional non-UI work uses at most **two workers**;
   UI materialization stays serial on the background dispatcher.
5. Percentage is monotonic and local to one named stage's truthful work units.
   The UI may show `stage index / admitted stage count`, but heterogeneous
   route, byte, manifest, inspection, and view units are never summed into a
   fake aggregate percentage. Unknown totals remain indeterminate; timers and
   duration estimates do not invent progress.
6. Required failure offers Retry and Cancel startup/exit. Optional failure is
   isolated and offers Retry, Skip, or Continue; cancelling remaining optional
   preload cannot cancel an active Preview or Build.
7. Shell preferences remain a bounded pre-window prerequisite and contribute
   trace evidence only. Selection-triggered inspection uses a separate workflow
   lifecycle. Application-owned Preview/Build progress remains excluded from
   the shell lifecycle.
8. Closing cancels the active shell session and waits at most five seconds for
   drain. Stale, decreasing, non-finite, duplicate-terminal, and post-terminal
   updates cannot mutate observable state or publish a feature result.

These rules are architecture constraints, not a demand for one untyped result
envelope, service locator, plugin registry, ambient progress slot, or second
verifier.

## Current production inventory

Line anchors are for intake base `6d002868`; they identify displaced owners and
must not be treated as durable public APIs.

| Path | Trigger and callers | Semantic owner | Cancellation and progress | Bound/cache/failure today | Deletion or migration target |
| --- | --- | --- | --- | --- | --- |
| Canonical catalog startup | `MainWindow.axaml.cs:219-252` starts `ContinueStartupAsync`; `:267` starts `TryWarmCanonicalCatalogAsync`; Retry returns through `:410-412` | Application catalog owns candidate validation, atomic publication, token and LKG; Presentation coordinator only maps the typed stream | One window CTS at `MainWindow.axaml.cs:22`; exact route work enters at 0, then `route/(N+1)`, followed by one typed terminal | 78 routes plus one disclosure unit; required failure retains blocking Retry/Cancel surface | PL-03 deletes `CanonicalCatalogStartupCoordinator.cs` and MainWindow catalog-specific fields/methods after the shell lifecycle consumes the same typed loader |
| Manual report input | `MainWindow.Report.cs:11-35` from the storage-provider picker | Existing Report parser/projection | `StreamReader.ReadToEndAsync` has no explicit raw-byte admission or coherent identity contract | Unbounded standalone read; parser failure is shown on the Report surface | PL-02 routes it through one bounded Infrastructure reader, then deletes this duplicate reader |
| Startup history restore | `MainWindow.Report.cs:38-45` calls `ReportHistoryFileStore.LoadAsync` | Report-history schema/order and Presentation projection stay in their existing owners | `ReportHistoryFileStore.cs:49-51` only schedules synchronous `Load` with `Task.Run` | Persisted file bound 64 MiB at `ReportHistoryFileStore.cs:10`; history projection has its own latest-generation cancellation | PL-02 supplies cooperative streaming read/identity and removes scheduling-only cancellation |
| Explicit startup report | `MainWindow.Report.cs:108-120` is called by deferred launch-option handling | Existing Report parser/projection | `Task.Run(File.ReadAllText(...))`; caller cancellation does not make the synchronous read cooperative | No standalone 10 MiB admission; failure is traced/diagnosed through current caller | PL-02 converges with manual input and startup history; PL-04 later schedules the resulting typed stage |
| Startup diagnostics | `MainWindow.axaml.cs:245` calls `MessageCenterViewModel.RefreshAfterStartupAsync`; `MessageCenterViewModel.cs:215-241` runs the typed service refresh | Application `ISystemInformationService` owns the snapshot | Startup CTS reaches an off-dispatcher `Task.Run`; UI has only `IsRefreshInProgress`, no exact numeric work | No duplicate cache is introduced; one exception in the serial startup block currently skips later optional work | PL-04 moves invocation/failure isolation into the shell session without interpreting the snapshot |
| Deferred view warm-up | `MainWindow.axaml.cs:252`; `MainWindow.StartupWarmup.cs:9-52` admits five views and `:71-92` materializes each at background priority | Presentation owns view identity/materialization only | Startup CTS checked before/after each step; five truthful view-count units; internally serial | Yields when a run is active; failure is swallowed by the outer serial startup trace path | PL-04 migrates the five-item stage and deletes `WarmDeferredShellAsync`/obsolete wrappers |
| External environment discovery | First `ExternalProcessorFactory.AcquireCurrent` through `RuntimeDependencyReadinessLeaseProvider.cs:11` or `CompositionHostServices.cs:61`; explicit production refresh has no caller | Retained Bootstrap acquisition wiring is `ExternalProcessorFactory.cs:11-22`; displaced environment/root/manifest construction is `:23-143`, and its process-wide lazy generation/refresh owner is `:155-219` | Lazy synchronous first access; no cancellation/progress; `Refresh` at `:197-203` replaces the lazy generation | `CreateUncached`, `FindExternalToolsRoot`, and `LoadManifest` own unbounded recursive enumeration/parsing; generation lease/stale check exists, but request identity is not observable | PL-05 moves the exact 170 nonblank displaced owner lines to one bounded Infrastructure loader, retains only composition/lease wiring, and deletes the unused refresh seam |
| Selected-file inspection | `WorkflowSessionPresentationViewModel.FirmwareInspection.cs:14-86` on selection and `:126-190` for refresh; all six workflows share the partial | Application/Infrastructure own inspection facts; Presentation currently owns generation and display projection | `Task.Run` around a synchronous batch at `:167`; pre/post cancellation and stale generation rejection; no percentage | Presentation path caches at `FirmwareInspectionSession.cs:11-14`; Infrastructure IC-only static compiled cache at `BuiltInFirmwareInspection.cs:19,475-535`; refresh paths use `CancellationToken.None` at `:444` and `:504` | PL-06 fixes coherent content identity/cache/strategy ownership; PL-07 adopts one workflow-session inspection lifecycle and deletes the ad hoc loading/task/cache path |
| General Merge/Replace preparation | `SetSlotFileAsync` branches at `WorkflowSessionPresentationViewModel.FirmwareInspection.cs:23-58` to `GeneralMergeReadinessRefreshTask` or `GeneralReplaceReadinessRefreshTask`; the actual owners are `MergePresentationViewModel.General.cs:14,66-115` and `ReplacePresentationViewModel.General.cs:16,93-157` | Application General authoring owns typed admission/readiness/session preparation; the two Presentation ViewModels own separate queues and publication today | Each path uses its own `SerialTaskQueue`, `CancellationToken.None`, reference/path stale checks, and task property; neither reports progress nor cancels obsolete queued work | No firmware cache, but two workflow-specific scheduling/loading owners sit beside the common selection path | PL-07 moves both typed preparation calls under the one per-workflow inspection lifecycle, preserves result application, and deletes both queues/task wrappers |
| Shell preferences | `DesktopApplication.cs:21-45` begins the bounded preference read; `App.axaml.cs:53` obtains the immutable result before MainWindow | Presentation preference file store | Best-effort bounded prerequisite; not controlled by visible shell preload | 64 KiB policy already owns first-frame language/theme/motion and fallback | Observe in startup trace only; do not move behind loading or count in preload percentage |
| Preview/Build progress | Application `CompositionRunProgress.cs:82-132` owns a bounded typed run feed | Application composition run | Run-specific cancellation, bounded channel and seven typed phases | Existing one-run authority; not startup/preload work | Explicitly excluded; no PL ticket may absorb or replace it |
| CtrlRAM run metadata | Build entry `MainWindow.Build.cs:88` and Replace modal entry `Views/ReplaceSelectionModal.axaml.cs:31` call public open path `ReplacePresentationViewModel.CtrlRamFirmwareVersion.cs:85-115`; confirmation at `Views/CtrlRamFirmwareVersionModal.axaml.cs:25,34,49` calls public edit/revalidation paths at `:117-187`; private reads are `:227-341` | Application/Infrastructure inspection owns metadata and coherent file identity; the Replace ViewModel owns the modal lease and run-bound request generation | Caller token, dedicated generation, off-dispatcher read, current-context/file-identity revalidation, and loading state | Demand-driven Build preflight; its result is scoped to the modal lease and is revalidated before creating the typed edit | Explicitly excluded by the specification and ADR 0049; PL-07 must not absorb this run-bound lifecycle |

## Work-count and characterization baseline

| Surface | Truthful current unit or state | Existing evidence seam |
| --- | --- | --- |
| catalog | 78 static/dynamic routes plus one disclosure unit; source progress is `completed/(routes + 1)`, then one terminal | `CanonicalCapabilityCatalogMigrationTests.Progress.cs:10-43`; exact 78-row checks in `CanonicalSupportMatrixHostTests.cs:116-118` and `HeadlessCanonicalConvergenceTests.cs:37` |
| report/history | raw bytes are the future determinate unit; current standalone readers do not expose them | `ReportHistoryPersistenceTests`; `ShellViewModelTests.ReportHistory.cs`; report projection/history generation tests |
| diagnostics | indeterminate until its typed owner exposes a truthful work total | Message Center refresh tests and System Information service tests |
| deferred views | five completed-view units, serial on the UI dispatcher | startup warm-up ordering/trace and XAML/UI smoke tests |
| external environment | future filesystem entries/manifest bytes under PL-05 bounds; current lazy load is unobservable | `ExternalProcessorLifetimeTests.cs` generation/lease tests |
| selected-file inspection | distinct paths/items when known; otherwise indeterminate | `ShellViewModelTests.FirmwareInspection*.cs`, Bootstrap file-identity tests, Application inspection tests |
| General Merge/Replace preparation | one typed General authoring preparation per admitted draft/reference; current queues expose no progress | General admission/readiness UiSmoke and Application authoring-session tests |
| lifecycle presentation | stage-local percent or indeterminate; bounded announcement phases/deciles | `ShellPreloadSessionTests`, `ForegroundLoadingStateTests`, startup loading/focus/accessibility UI smoke |

This is a characterization matrix, not proof that current paths already satisfy
the final lifecycle. Each implementation ticket must start with a discriminating
red test for the missing behavior and retain typed-owner parity.

## Dependency-ordered ticket ledger

The values below are conservative **planning envelopes**, not LOC targets or
permission to minify, relocate, or delete unique behavior. `Gross` is displaced
production surface expected to be removed; `add` is the maximum planned new
production surface; `net = add - gross`. Each PR recounts exact physical
nonblank production lines from its fixed base, updates the descending ratchets,
and stops for redesign if the behavior-compatible addition exceeds the listed
cap. Paths are intake anchors.

| Ticket | Current owner anchors | Gross | Add | Expected net | Dependency and evidence |
| --- | --- | ---: | ---: | ---: | --- |
| PL-01 #373 | this document; `RepositoryBoundaryTests.StartupDiagnostics.cs` | 0 | 0 | 0 | Freeze only; no production mutation |
| PL-02 #374 | `MainWindow.Report.cs:11-45,108-120`; `ReportHistoryFileStore.cs:10-51`; `ReportPresentationViewModel.History.cs:94-175` | 110 | 90 | -20 | Blocked by PL-01; Report/history, Infrastructure file and UiSmoke parity; PR must be net-negative |
| PL-03 #375 | `CanonicalCatalogStartupCoordinator.cs:1-139`; `MainWindow.axaml.cs:22-33,219-318,337-412` | 260 | 230 | -30 | Blocked by PL-01; catalog token/LKG/progress plus loading/a11y tests; PR must be net-negative |
| PL-04 #376 | `MainWindow.axaml.cs:227-265`; `MainWindow.Report.cs:38-120`; `MainWindow.StartupWarmup.cs:9-92`; `MessageCenterViewModel.cs:147-241` | 220 | 190 | -30 | Original planning envelope, superseded only by the named PL-04 implementation amendment below; no later growth budget |
| PL-05 #377 | `ExternalProcessorFactory.cs:23-143,155-219` (exactly 170 nonblank owner lines); callers/wiring retained or migrated at `RuntimeDependencyReadinessLeaseProvider.cs:6-16` and `CompositionHostServices.cs:36-64` | 170 | 145 | -25 | Original planning envelope, superseded only by the named PL-05 implementation amendment below; no later growth budget |
| PL-06 #378 | `FirmwareInspectionSession.cs:8-163`; `BuiltInFirmwareInspection.cs:19,470-537`; `BuiltInFirmwareInspection.FileIdentity.cs:19-74` | 220 | 190 | -30 | Blocked by PL-01; coherent identity, cache-on/off, selective dispatch and six-workflow parity; PR must be net-negative |
| PL-07 #379 | `WorkflowSessionPresentationViewModel.FirmwareInspection.cs:8-11,126-211,341-539`; `MergePresentationViewModel.General.cs:14,66-115`; `ReplacePresentationViewModel.General.cs:16,93-157` (375 nonblank candidate lines before retained result application is separated) | 250 | 220 | -30 | Blocked by PL-03/06; six workflow lifecycle/stale/readiness/Build-reinspection evidence; PR must be net-negative |
| PL-00 #380 | `StartupTraceFileSink.cs`; `StartupTraceSession.cs`; `MainWindow.axaml.cs`; `ShellTextResources`; shared button/navigation roles; CtrlRAM relocalization; immutable Application DTO backing-field aliases; `scripts/measure-startup.ps1` | 295 | 358 | +63 | Exact package/startup/private-memory/lifecycle evidence, explicit waiting-for-artifact copy, fully project-owned semantic-button and shell-navigation interaction states, two CI runs within five minutes, accessibility and release gates |

The ledger does not promise the sum of each maximum. Overlapping displaced
surfaces are charged once by their owning ticket, and a later PR must cite this
baseline plus its exact base/head measurement. PL-00 accepts `v0.10.5` only
when the then-current exact full/runtime/slice ratchets have resumed descending
from the latest named PL-05 replacements, their four-slice sum is exact, and no
later ticket reuses PL-02, PL-04, PL-05, or PL-00's non-transferable allowance.

## PL-02 implementation accounting

Purpose: make startup history, explicit `--load-report`, manual report input,
and existing preference persistence use one bounded platform file adapter while
leaving report/history JSON schema and projection in Presentation.

The fixed implementation base is `12be1228f96fd481a8666ec142803245ccbbb278`.
The exact physical nonblank production ledger is 284 gross removed, 281
required additions, net **-3**. Full production changes 97,306 to 97,303.
The old 216-line `BestEffortLocalJsonFileStore.cs`, unbounded standalone report
reads, scheduling-only history read, and delegate-only report-load overloads are
deleted. `ILocalFileStore` and `LocalFileStore` become the one typed
Application/Infrastructure I/O boundary; Presentation retains only JSON codec,
schema, order, budgets, generation, and UI publication.

The planning add cap of 90 was arithmetically incompatible with that required
ownership move because it counted neither the typed port nor the receiving
adapter, and the runtime metric excluded the displaced Presentation code. The
owner-approved ADR 0021 amendment replaces that cap for PL-02 only. Runtime is
rebaselined 67,186 to 67,371: Application 29,383 to 29,404; Bootstrap plus CLI
plus Desktop 3,255 to 3,267; Infrastructure plus Contracts plus CRC worker
13,929 to 14,081; Domain plus Profiles remains 20,619. These values create no
allowance and become descending ratchets immediately.

Non-goals remain unchanged: no report wire/schema/layout change, no firmware or
Golden mutation, no remote storage, and no new live-report projection. Required
evidence covers the exact 10 MiB boundary, typed oversize/missing/unreadable/
invalid-text failures, cancellation, stable read handles across mutation and
replacement, startup/manual parity, malformed history fallback, 16/64 MiB
retention/envelope rules, literal-`<` expansion, metadata fallback,
oldest-first pruning, and previous-file preservation.

## PL-03 implementation accounting

Purpose: give shell startup one Presentation-owned lifecycle identity, bounded
attempt history, cancellation/drain policy, and reusable foreground projection
while the Application catalog remains the sole validation, publication, token,
and last-known-good owner.

The fixed implementation base is `1472ad0f7bb555f2b6bfc303216e95101bf8af25`.
The exact physical nonblank production ledger is 285 gross removed, 280 required
additions, net **-5**. Full production descends from 97,302 to 97,297; runtime
production and all four runtime slices remain unchanged at 67,371. The
executable full-production ratchet descends to 97,297 with the implementation.

The planning add envelope of 230 omitted the immutable observable stage and
attempt snapshots, bounded prior-terminal retention, explicit Cancel action,
and fail-closed typed-stream grammar that the accepted specification requires.
After redesigning the implementation to remove forwarding APIs and duplicate
catalog phases, the required behavior remains 50 lines above that planning
envelope while the complete PR remains net-negative. This recorded amendment
creates no reusable allowance for later tickets and does not weaken any runtime
slice ratchet.

The deleted `CanonicalCatalogStartupCoordinator` has no forwarding replacement.
`ShellPreloadSession` now owns one closed required catalog stage, session and
attempt identity, current plus one prior terminal snapshot, exact stage-local
progress, retry freshness, cooperative cancellation, and five-second drain.
MainWindow only projects immutable stage snapshots into the existing foreground
loading surface and applies the typed catalog publication on the UI dispatcher.
The offset also removes one construction-only BIN-inspector array alias while
preserving the same cloned array, read-only wrapper, order, and reference checks.
No optional preload stage, aggregate percentage, catalog semantic change,
Preview/Build inclusion, or selected-file inspection migration is included.

## PL-04 implementation accounting

Purpose: make the shell usable immediately after catalog publication while its
closed optional plan remains observable, cancellable, retryable, and isolated
from active Preview/Build work.

The fixed implementation base is `c98b6f80b5b5a64aba1f951340bcc0ca21119f12`.
The exact physical nonblank production ledger is 1,154 gross removed and 1,283
required additions, net **+129**. Full production changes from 97,297 to 97,426.
Truthful report-byte progress adds the focused Application port and
Infrastructure adapter contract that the planning envelope omitted, so runtime
production changes once from 67,371 to 67,404 = 20,619 + 29,410 + 3,267 +
14,108. The executable ratchets are those exact values.

The Presentation portion removes 1,149 lines and adds 1,245, net +96. Within
it, the ViewModel boundary removes 894 lines and adds 231, net -663, while the
session, localized Retry/Skip/Cancel state, determinate/indeterminate
projection, bounded prior-attempt evidence, and behavioral seams required by
ADR 0049 replace the serial startup path. ViewModels and UI projection helpers
have no reusable production API consumer outside Presentation; 159
accidental-public type/member declaration sites become internal. Exactly 735
one-line XML summaries that merely restated their internal member names are
deleted and two focused boundary/typed-contract summaries are added.
Documentation for semantic ownership, firmware/ranges, identity/generation,
bounds, cancellation/concurrency, fail-closed behavior, and other non-obvious
invariants remains. This is the accepted SPEC Decision 31 convergence path, not
minification, excluded-path relocation, test/evidence deletion, or reusable
allowance.

The +129 full-production and +33 runtime deltas are the owner-approved PL-04
accounting exception recorded
in SPEC Decision 30, ADR 0021, and the unified-preload specification. It adds
the typed `LocalFileReadProgress` contract needed to show admitted bytes over a
stable total and isolates observer failures from source failures. It does not
move equivalent Presentation code into a counted slice, and it creates no
budget for PL-05 or later tickets. The original -30 planning envelope was
disproved by the complete lifecycle behavior, observer isolation, bounded close
semantics, and Decision 31 information-retention audit; every later PR resumes
from the exact replacement ratchets.

The former `ContinueStartupAsync` and `ApplyDeferredLaunchOptionsAsync` path has
no forwarding replacement. Launch-page selection occurs first; history precedes
the explicit startup report; diagnostics run beside that chain; deferred views
remain serial on the dispatcher and wait for active operator/run work. Optional
failure records its diagnostic without disabling the shell, retry keeps the
session and reruns only one stage, Skip preserves an explicit dependency-blocked
successor, and Cancel drains only remaining optional work. Report, diagnostics,
catalog, and view-result interpretation remain with their existing typed owners.
External discovery and selection-triggered inspection remain deferred to PL-05
and PL-07.

## PL-05 implementation accounting

Purpose: replace Bootstrap's synchronous, unbounded external-tool discovery
with one observable, cancellable, bounded Infrastructure environment lifecycle
used by startup, explicit Message Center refresh, CLI dispatch, readiness, and
execution leases without launching a process during preload.

The fixed implementation base is `c7967166c2a9ebf203d158852ffcf359e8ab1706`.
The exact physical nonblank production ledger is 250 gross removed and 984
required additions, net **+734**. Full production changes from 97,426 to 98,160.
Runtime changes from 67,404 to 68,018 = 20,619 Domain plus Profiles +
29,585 Application + 3,074 Bootstrap plus CLI plus Desktop + 14,740
Infrastructure plus Contracts plus CRC worker. The executable ratchets become
those exact values.

The original 170/145 envelope counted only the old factory body. It omitted the
accepted depth 16 / 4,096-entry / 256-manifest / 1 MiB-per-manifest / 16 MiB
cumulative bounds, reparse/escape rejection, stable regular-file and cancellable
SHA validation, deterministic order, typed request and publication generations,
single-flight supersession, last-known-good retention, generation-zero blocked
readiness, exact stage-local progress, and shared Desktop/Message Center/CLI
adoption. The implementation deletes `ExternalProcessorFactory` and the
Bootstrap lease wrapper; Bootstrap retains only composition wiring. Preload
does not create staging or launch a processor, and processor protocols,
firmware bytes/ranges/names, report wire, profiles, schemas, and Golden data do
not change.

The +734 full-production and +614 runtime deltas are the owner-approved PL-05
accounting exception recorded in SPEC Decision 30, ADR 0021, and the unified
preload specification. It is not reusable by PL-06, PL-07, or PL-00 and does
not permit minification, unrelated offsets, cross-slice relocation, or weaker
tests/evidence. Every later PR resumes from these exact descending ratchets.

## Message Center readability accounting

Purpose: make the existing Report and System Information content easier to
scan without changing its typed owners, commands, data, or persistence. The
Presentation-only change replaces the mixed System Information grid with four
aligned two-column fact cards and gives Current Report and Report History equal
action cards.

The exact physical nonblank production ledger is 67 added and 38 removed, net
**+29**. Full production changes from 98,160 to 98,189. Runtime remains 68,018
and the four runtime slices remain exactly 20,619 / 29,585 / 3,074 / 14,740.
The full-production ratchet becomes 98,189 immediately.

This owner-requested visual exception is non-transferable. It changes no
report, diagnostics, firmware, profile, schema, processor, Golden, persistence,
coverage, or release contract and cannot fund PL-06, PL-07, PL-00, or later
work.

## PL-06 implementation accounting

Purpose: converge selected-file inspection on one coherent complete-content
identity, delete unsafe path/IC cache ownership, and dispatch only the
Application role named by each typed input without changing accepted firmware
semantics.

The fixed implementation base is `1f6f3ac3155b63eb342dd63dc43d64e0c69e4dcf`.
The exact physical nonblank production ledger is 403 gross removed and 289
required additions, net **-114**. Full production changes from 98,189 to 98,075.
Runtime changes from 68,018 to 68,016 = 20,619 Domain plus Profiles + 29,584
Application + 3,074 Bootstrap plus CLI plus Desktop + 14,739 Infrastructure
plus Contracts plus CRC worker. These become the executable descending ratchets.

The implementation removes `BuiltInFirmwareInspection.FileIdentity.cs`, the
Presentation `_fileProjections` and `_baseCache` dictionaries, and the static
IC-only compiled-classification cache. `FileContentSnapshotInspector` now owns
one bounded complete read, exact length/SHA identity, short/trailing/change
rejection, and cancellation. Application returns the same immutable bytes and
identity to inspection; Presentation retains only current slot projection and
generation. Artifact classification recomputes from the current canonical
publication, so reload of the same IC with a new capability cannot reuse stale
compiled state.

Typed input roles are the only strategy selectors. The characterized
all-strategy path remains test-only differential evidence; production retains
input/result order, diagnostics, exact health/metadata, and one read per
distinct path. Same-path/same-length replacement, change during read,
concurrent request isolation, cancellation, unreadable input, exact/short/
trailing content, catalog reload, and CtrlRAM Build reinspection are covered.
Bootstrap 934, Infrastructure 439 with 2 declared skips, and UI 482 contribute
to the exact eight-project 3,406 inventory. Firmware bytes/ranges/order,
metadata semantics, output naming, profiles, schemas, report wire, processors,
and Golden data do not change.

## PL-07 implementation accounting

Purpose: adopt one observable, cancellable, retryable selected-file inspection
lifecycle across Standard Merge, AB Merge, General Merge, DP Replace, CtrlRAM
Replace, and General Replace while deleting their duplicate generation, loading,
queue, cache-wrapper, and task ownership.

The implementation base is `9845e6804f527974d1fd50d89bd430537eeb3b6c`.
The exact physical nonblank production ledger is 950 gross removed and 949
required additions, net **-1**. Full production changes from 98,075 to 98,074.
Runtime changes from 68,016 to 67,997 = 20,619 Domain plus Profiles + 29,571
Application + 3,074 Bootstrap plus CLI plus Desktop + 14,733 Infrastructure
plus Contracts plus CRC worker. These become the executable descending ratchets.

One `WorkflowInspectionLifecycle` contract owns request admission, a never-reused
generation, cancellation/drain, monotonic exact work, localized retry/cancel,
terminal state, and stale callback rejection. Merge and Replace retain three
workflow-local lifecycle instances each; the shared set only removes duplicated
array traversal and never becomes a cross-workflow semantic cache. Application
and Infrastructure report truthful work through `IProgress<AuthoringInspectionProgress>`
and remain the sole owners of preparation, content identity, metadata, health,
readiness, and inspection results.

The implementation deletes `SerialTaskQueue`, `FirmwareInspectionReader`, the
global firmware-loading flag, separate preparation task/generation fields, and
duplicate foreground surface event glue. The catalog surface and workflow rail
reuse one loading template; exact visible percent, reduced motion, localized
detail, bounded decile announcements, Retry, and Cancel remain presentation-only.
New selection/revision/catalog/compilation invalidates obsolete work before any
result application, and Build still performs its authoritative reinspection.

Bootstrap 934, UI 490, and Architecture 214 pass locally and contribute to the
exact eight-project 3,414 inventory with 2 declared skips and Golden 17/17. No
firmware bytes/ranges/order, profile/schema, report wire, processor protocol,
output naming, CLI behavior, support truth, or Golden data changes.

## PL-00 measurement instrumentation accounting

Purpose: make the exact packaged predecessor/candidate comparison capture peak
working set, peak private bytes, and the terminal state plus truthful work units
of every admitted preload stage without changing an ordinary application launch.

The implementation base is `bb95c20b3aec5cc91f5e7705d0526bde44c8e53f`.
The exact physical nonblank production ledger is 295 gross removed and 358
required additions, net **+63**. Full production changes from 98,074 to 98,137.
Runtime changes from 67,997 to 67,981 = 20,619 Domain plus Profiles + 29,555
Application + 3,074 Bootstrap plus CLI plus Desktop + 14,733 Infrastructure
plus Contracts plus CRC worker. The 2026-08-14 owner-requested semantic-control
amendment permits this named Presentation-only increase; 98,137 and the unchanged
runtime values become the executable descending ratchets and create no allowance
for later work. The Application reduction removes only redundant private array aliases
from immutable classes; existing copies, validation, ordering, read-only public
properties, and execution semantics remain unchanged. Presentation assigns the
existing project button roles explicitly. The shared secondary role now owns
normal, hover, pressed, keyboard-focus, and disabled visuals, while every other
semantic role now owns the same interaction-state boundary. Startup Cancel/Retry/
Skip and adjacent actions therefore do not fall back to the framework appearance.
The shell navigation owns the same states and replaces the default Avalonia focus
rectangle shown when startup completion moves keyboard focus to Home.
CtrlRAM relocalization preserves the current inspected slot projection while
refreshing its localized memory text. A superseded
startup environment attempt also cannot clear the active operator-refresh
presentation. The gross ledger counts each one-for-one semantic button-class
migration while the shared style deletes displaced duplicate dimensions,
alignment, typography, corner-radius setters, and four identical role-local
disabled-opacity rules.

The PL-00 accessibility closure moves the bounded optional-stage status from an
outer host that has no Windows automation peer to the existing Expander peer and
its keyboard-operable template header. Reusing the shared Expander template style
and removing the peerless host lowers the current full-production ratchet from
98,137 to **98,135**; runtime and all four runtime slices remain unchanged.

The Settings and workflow-readability closure replaces ambiguous matrix icons and
generic pending-layout copy with localized verification text and exact
`Waiting for DP BIN` / `TP BIN` / `Base BIN` states. It also removes duplicated
same-file Avalonia style bodies without changing their selectors or precedence.
The exact full-production ratchet descends from 98,135 to **98,133**; runtime and
all four runtime slices remain unchanged.

Diagnostic startup traces use schema v3 only when the existing opt-in trace
environment variable is present. The trace contains the bounded public lifecycle
facts `id`, terminal `state`, and nullable `completedWork`/`totalWork`; it excludes
localized text, diagnostics, paths, report content, firmware facts, and private
data. The measurement runner accepts the stable predecessor's v2 trace. Release
mode requires the exact lowercase Home workload, at least one warm-up plus five
scored launches, schema v3, the exact five ordered stage IDs, `Succeeded` for
every stage, deferred-view work `5/5`, one ordered window/catalog-ready/completed
summary, and all five ordered deferred-view start/ready pairs. It rejects
duplicates, missing work, unsuccessful terminals, and malformed ordering; its
output records `validation.mode=preload-release` and
`validation.releaseAdmissionPassed=true` as well as working-set/private-byte
values and the first-window-to-catalog-ready interval. A focused uncompressed
developer run proves the contract only; it is not package performance or 700 ms
release evidence.
The same Presentation boundary replaces the ambiguous user-facing `Pending input`
copy with the typed prerequisite (`Waiting for DP BIN`, `Waiting for Reference
FlashCode`, or the bounded multi-input fallback) without changing the internal
`PendingInput` state or inferring an artifact from its filename.

PL-00 remains open until the exact packaged `v0.10.4` and candidate have one
warm-up plus at least five same-machine scored launches, the two exact-head CI
runs are each within five minutes, clean-package accessibility and UI checks pass,
and release notes, SBOM, provenance, hashes, and owner release approval are
recorded.

### Historical PL-00 candidate evidence (superseded for release admission)

The following ancestor packages remain immutable diagnostic and performance
evidence. Their source trees differ from the final reviewed PR tree, so none of
their verifier, CI, package, provenance, hash, or timing records can close the
current PL-00 release gate. The final frozen tree must refresh every named gate.

Candidate `6b1c874dbf031713733089e0459e5ac123cf8fcb` passed the canonical
verifier with 387 Python tests (four declared skips), 3,414 .NET tests (two
declared platform skips), UI Smoke 490/490, Architecture 214/214, and Golden
17/17. Exact-head CI run `31782340809` passed twice in 253 and 299 seconds.
The portable package and closed-package smoke passed; its ZIP is 77,464,389
bytes with SHA-256
`2a189a6f9347f941c38ba768ea0d836ba9c7472dd7436d8f65ad72b6c8801571`,
and its 72,351,504-byte executable has SHA-256
`b896dd6e3bea098f16dc715b6433bb9c30b22d05c7b6d39ecad23298e8f1e235`.
Packaged Light, Dark, reduced-motion, and keyboard-focus review passed. The
screen-reader review and release-owner approval remain human gates.

The same controlled session used one warm-up plus five scored Home launches.
Official `v0.10.4` measured 709.271 ms process-to-window, while the candidate
measured 768.097 ms and 803.214 ms in two standard-window observations. The
candidate's five preload stages were unique, terminal, and `Succeeded` in both
observations. The absolute 700 ms target is therefore not accepted. Measurement
SHA-256 values are
`9e758de996b14e3b578e05cd415f6a2b34ae540e11782241bf43ae2aa97b806b`
for the predecessor, `9152506df3c60b0051db3482dd27f1a16e9c351deb0e8f0a609b89bfed20eeb9`
for the candidate's first standard-window observation, and
`8f154db0e6af879d7ca77fefa05c5f4250dfb4aab917206dbaaf5b232583a6ff`
for its repeat.

A same-session differential rebuilt predecessor head `de62544f` and measured
it immediately before the exact candidate. Process-to-window medians were
770.645 ms and 787.509 ms; managed-entry-to-opened medians were 459.724 ms and
470.645 ms; native-host-gap medians were 312.800 ms and 319.420 ms. The new
head's measured synchronous first-frame UI work was 117.279 ms versus 118.932
ms for the predecessor. The differential does not show the project-owned
navigation selectors as the cause of the ambient absolute miss; it does not
waive the absolute gate.
The differential JSON SHA-256 values are
`90f574177d464ba01f776fcc60731317d0b0e12794d4498ebee55954a7051a38`
and `1eb7a9277ed1b3f28134db4bdd5ac4fba3b5d15ac87f7650e05d5664d61c35b`.

The trace also isolated a retained first-frame scheduling regression from
PL-02: its canonical `ILocalFileStore.ReadAsync` performs file-open,
stable-handle, length, and last-write admission before its first asynchronous
read, while the predecessor scheduled that bounded operation on a worker.
`PrepareStartup` now schedules the same one loader before host construction and
framework initialization still awaits the exact returned task before creating
the window. A controlled behavioral test blocks the loader's synchronous
preamble and proves host construction proceeds, one preference load remains in
flight, and the same completed snapshot reaches the first-frame ViewModel. The
change adds no reader, cache, fallback, test case, or production nonblank line.
Replacement production candidate
`7e7a31bb3937245fb68c259b29ec6914d7507780` passed the canonical full
verifier with the same closed 387-Python / 3,414-.NET / Golden-17 inventory and
produced package `NvtFwCombiner-v0.10.5-pl00.7e7a31b-win-x64`. Closed-package
smoke passed. The 77,464,776-byte ZIP has SHA-256
`c58f533adf1e42b24b42e99b222c0149a34ba9d375e0f9ce731d45936212c96d`;
the 72,351,159-byte executable has SHA-256
`b82fab356601547edac931513c78356cd8ad1b95d8ba566acb75355146e81f40`.
Its SPDX, provenance, release-manifest, and package-hash-list SHA-256 values are
`2c7755da76e21e1cd4143b6a75e99d45afb121c3cdf8f2577a72db9aae3dabb5`,
`2e8f70cb6e729dfc69b8f80d8a9eebf31e9a1c8e011a81e240a788d5bc1e34ff`,
`8ccf5f4be29bea8d8275822e85bc7ada546287c9f967293c3e34fcb5873020cd`,
and `1e8cfbd1b4fe430d6a6a569ee25ade3e3df81bef057da6bf3571771033078d98`.

Exact-head CI run `31787126975` passed attempt 1 in 290 seconds. Attempt 2
was cancelled and rejected after its core producer exceeded the five-minute
ceiling; it is not acceptance evidence. Consecutive replacement attempts 3 and
4 passed in 255 and 250 seconds, including the stable `dotnet / build-test`
finalizer. Those two consecutive attempts satisfied the five-minute CI gate for
that ancestor only.

Two independent standard-window observations of that exact package each used
one warm-up plus five scored Home launches and measured 631.132 ms
(626.246--639.646 ms) and 634.589 ms (625.864--665.213 ms)
process-to-window. Synchronous first-frame UI medians were 109.676 ms and
109.723 ms, while the preference-start-to-host-start interval fell to 2.020 ms
and 1.960 ms. All five lifecycle stages were unique, terminal, and `Succeeded`.
The ancestor package therefore met 700 ms without redefining the target, but the
current reviewed tree must be measured again. Measurement JSON SHA-256 values are
`383fea133855c06647abca306b15aa428ed549d2c0cc44ab25d5ddf6b388a9bb`
and `7ab0a2c4ccf1af68b7dceefc2dd03469e42f77c10dced7f5506c66ea22795022`.

The final frozen-tree rerun is controlled on Microsoft Windows 11 Enterprise
10.0.26200 x64 (build 26200), Intel Core i9-10900F, 20 logical processors,
34,279,567,360 bytes physical memory, Ultimate Performance power scheme
`e9a42b02-d5df-448d-aa00-03f14749eb61`, .NET SDK 10.0.303, and PowerShell
7.6.3. The exact reviewed-tree package, release-admitted measurement JSON,
two same-SHA CI attempts, screen-reader review, release-owner approval, protected
stable workflow, and downloaded-asset audit remain open until freshly recorded.

## PL-01 evidence and release impact

- Production, Domain, Profiles, Contracts, schemas, external protocols, report
  wire, golden fixtures, expected BIN bytes/hashes/names, and support truth are
  unchanged.
- The architecture ratchet binds the predecessor identity, measured misses,
  lifecycle grammar, inventory owners, excluded run progress, and ticket rows.
- PL-01 does not claim a startup improvement. PL-00 owns the exact packaged
  predecessor/candidate comparison and the truthful 700 ms result.
- Required local gates are the focused Architecture test, structure validation,
  scoped R2/Polytail review, and the canonical full verifier.
