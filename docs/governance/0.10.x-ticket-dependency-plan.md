# 0.10.x Ticket Dependency Plan

Status: owner-approved dependency graph. GitHub issues and PRs are the sole live
execution-state records.

This document anchors the reviewed GitHub issue inventory for the `0.10.x`
maintainability program so the repository can be reviewed without reconstructing
planning dependencies from GitHub. It does not duplicate open/closed,
completion, review, or current-frontier state. If an edge or approved outcome
changes, update the issue and this table in the same planning phase.

`ready-for-agent` means that a ticket is sufficiently specified for agent
execution. It does not override a listed blocker, evidence gate, branch rule,
review requirement, or owner approval. A ticket is on the executable frontier
only when every listed blocker is complete and its own intake gates are
satisfied.

Dependency depth is a topological planning aid, not a release number. Tickets
at the same depth may run independently only when they do not contend for the
same files, ownership seam, private evidence, or human reviewer. The owner
allocates dependency-ready tickets to an actual later `0.10.x` release; this
plan does not assign `0.10.1`, `0.10.2`, or any other version.

| Depth | Wave | Issue | Approved outcome | Blocked by |
| ---: | --- | ---: | --- | --- |
| 0 | Baseline | #170 | Establish executable Support Matrix baseline | — |
| 1 | Baseline | #171 | Record coverage baseline and enforce ratchets | #172 |
| 0 | Baseline | #172 | Add bounded concurrency to the canonical verifier | — |
| 0 | Canonical pilot | #173 | Deliver the NT51929 Standard Merge canonical capability tracer | — |
| 1 | Headless data | #174 | Deliver canonical DPCMI inspection through NT51929 DP Replace | #173 |
| 1 | Headless runtime | #183 | Unify CtrlRAM runtime readiness | #173 |
| 0 | Headless retirement | #221 | Retire NT51920/NT51925/NT51930/NT51931 production capabilities | — |
| 2 | Headless data | #175 | Model FirmwareConfig General Parameters | #173, #174 |
| 2 | Headless state | #178 | Isolate the DP Replace authoring session | #174 |
| 2 | Headless data | #186 | Introduce the TP Flash Header metadata pilot | #173, #174 |
| 3 | Headless data | #176 | Resolve NT51950 and NT51951 TP-prerequisite metadata | #174, #175 |
| 3 | Headless state | #179 | Migrate all Merge modes to isolated authoring sessions | #178 |
| 3 | Headless state | #180 | Migrate remaining Replace modes to isolated sessions | #178, #183 |
| 3 | Headless projection | #184 | Deliver the pure Memory Layout projector | #173, #174, #178 |
| 3 | Headless firmware | #189 | Deliver symmetric NT51929-family AB Merge | #186, #259 |
| 3 | Headless firmware | #219 | Establish the canonical NF-preserving DiffDLM scatter contract for the NT51919/29/32 family through an NT51932 4-IC pilot | #173, #183 |
| 4 | Headless firmware | #239 | Model NT51928 dual-capacity Standard Merge and DP Replace with one selection-group contract | #219 |
| 4 | Headless data | #177 | Migrate remaining admitted metadata family bindings | #174, #175, #176, #221 |
| 4 | Headless authoring | #181 | Unify General mapping authoring | #179, #180 |
| 4 | Headless firmware | #188 | Migrate NT51950/NT51951 onto their owner-confirmed 2-IC NF-preserving DiffDLM geometry; retain TP-only POSTBUILD | #176, #186, #219 |
| 4 | Headless output | #193 | Unify output naming from the accepted inspection snapshot | #174, #175, #176 |
| 5 | Headless firmware | #259 | Canonicalize source projections and FlashCode admission | #219, #239 |
| 5 | Headless authoring | #248 | Bind General inputs to immutable content snapshots | #181 |
| 5 | Headless authoring | #249 | Canonicalize General Merge output initialization | #181 |
| 5 | Headless authoring | #250 | Unify General occupancy and resource admission | #181 |
| 6 | Headless firmware | #187 | Migrate admitted legacy TP Header families | #186, #221, #259, and matching #177 family slice |
| 6 | Headless firmware | #190 | Deliver NT51950 and NT51951 AB Merge | #176, #188, #259 |
| 6 | Headless reuse | #251 | Complete Saved Rule v2 lifecycle for General Merge | #248, #249, #250 |
| 7 | Headless reuse | #252 | Execute General Replace Saved Rules through exact Trusted Parent | #250, #251 |
| 8 | Headless diagnostics | #253 | Add plan-only General Replace Diagnostic Preview | #252 |
| 9 | Headless convergence | #194 | Migrate remaining admitted headless routes to canonical resolution | #177, #179, #180, #181, #183, #187, #188, #189, #190, #193, #221, #239, #248, #249, #250, #251, #252, #253, #259 |
| 10 | Deferred UI | #182 | Introduce shared slot and information-card presentation | #174, #178, #194, #239 |
| 10 | Runtime deletion | #196 | Retire the legacy profile and compiler runtime | #187, #188, #189, #190, #194 |
| 11 | Deferred UI | #185 | Reduce MainWindowViewModel to a shallow shell | #179, #180, #181, #182, #183, #184, #194 |
| 12 | Deferred UI | #191 | Extract the read-only Hex Viewport | #170, #185 |
| 12 | Deferred UI | #207 | Present Support Matrix in focused Settings UI | #185, #194 |
| 13 | Deferred UI | #192 | Reuse Hex Viewport in Report Diff and BIN Inspector | #174, #175, #185, #191 |
| 14 | Deferred UI | #208 | Migrate desktop routes to canonical Application contracts | #185, #192, #194 |
| 15 | Deferred UI | #214 | Deliver Message Center and System Information diagnostics | #173, #185, #208 |
| 15 | Compatibility deletion | #254 | Delete superseded General and Saved Rule compatibility owners | #194, #208, #248, #249, #250, #251, #252, #253 |
| 16 | Integration | #247 | Complete canonical General authoring and Saved Rule v2 | #248, #249, #250, #251, #252, #253, #254 |
| 16 | Compatibility deletion | #195 | Delete Workbench facade and parallel support catalogs | #194, #207, #208, #214, #254 |
| 17 | Core convergence | #230 | Converge Domain + Profiles to one canonical firmware model | #195, #196, #259 |
| 18 | Core convergence | #231 | Converge Application on capability-centered use cases | #195, #196, #230 |
| 18 | Core convergence | #232 | Converge Infrastructure, Contracts, and CRC worker protocol ownership | #195, #196, #230 |
| 19 | Core convergence | #233 | Converge Bootstrap + CLI to wiring-only composition | #195, #196, #230, #231, #232 |
| 20 | Core convergence | #229 | Complete Canonical Core Convergence under the hard production-size gate | #230, #231, #232, #233 |
| 21 | Integration | #197 | Close the 0.10.x integration gate and allocate releases | #171, #172, #229 |

## Stable execution ordering

Live completion and dependency-ready frontier state are queried from GitHub;
they are not committed here. File ownership and the one-writer rule still
govern which dependency-ready tickets may run concurrently. #221 is an early
removal ticket, not a late compatibility cleanup: it must close before #177
migrates remaining metadata families, before #187 migrates remaining TP Header
families, and before #194 closes headless convergence.

The ordering deliberately keeps the headless data/Application/CLI migration
ahead of Presentation work:

1. #173 establishes the canonical seam.
2. #221 removes retired routes while #174-#194 migrate metadata, sessions,
   firmware routes, mapping, readiness, naming, and only the remaining admitted
   headless consumers. #259 freezes section projection/complete-container
   admission before #187, #190, and their #194 batches. #248, #249, and #250 may proceed in parallel after #181
   only with disjoint selected-file, initializer, and occupancy/limit writers;
   #251, #252, and #253 then close reusable Saved Rule and diagnostic behavior
   before the General batch of #194 completes.
3. #182, #185, #191, #192, #207, #208, and #214 consume those frozen contracts
   in the deferred UI wave.
4. #254 deletes the superseded General/Saved Rule compatibility owners after
   #208 parity; #195 and #196 then delete broader Workbench and legacy runtime
   owners.
5. #230 freezes the Core semantic seam; #231 and #232 may then proceed in
   parallel with disjoint writers; #233 removes remaining Bootstrap/CLI
   duplication; #229 closes the four-slice hard gate.
6. #197 is the final integration/release gate.

#183 precedes #180; it is not blocked by #180. #194 has no Presentation
dependency. #254, rather than #194, waits for #208 before deleting compatibility
owners. These rules prevent the former readiness cycle and keep UI work from
delaying headless convergence.

#183 may therefore use one named CtrlRAM compiled-route bridge with
`AuthoringRevision(0)` while #180 still owns the session migration. The bridge
must carry the existing admitted route into the shared readiness gate, bind
runtime results to an explicit environment generation, and extract only
processor/tool references already present in the compiled plan. #180 replaces
the revision-zero input; #194 deletes the bridge when CtrlRAM resolves from the
canonical capability catalog. It is never an alternate firmware, evidence, or
publication owner.

## Family and FirmwareConfig allocation

ADR 0039 and ADR 0041 distinguish the strong perfect-family invariant from one
typed, fact-scoped partial-sharing contract:

- #175 defines the one all-IC FirmwareConfig General Parameters structure and
  pilots map-bound locators through NT51927/NT51928 without making the global
  definition a TP-family fact. It also freezes their
  Initial-Code-shared and TP-shared author vocabulary, but does not create
  dedicated runtime relationship subclasses or migrate their DPCMI runtime
  binding.
- #176 binds the same global FirmwareConfig structure for NT51950/NT51951
  topology prerequisite without creating another field table.
- #177 owns the remaining metadata bindings. Its slices include the
  family-owned NT51919/NT51929/NT51932 perfect-like DPCMI migration,
  NT51917/NT51927 perfect-family plus NT51917/NT51927/NT51928 shared-part
  bindings, and NT51923/NT51926 exact-reference consumers.
  NT51950/NT51951 retain only their TP-region relationship; FirmwareConfig and
  DPCMI remain global definitions with map-specific locator bindings.
- #221 removes NT51920/NT51925/NT51930/NT51931 before #177/#187/#194. Their
  former locators, including NT51920 `0x3E014` and NT51931 `0x3E018`, remain
  historical traceability only and are not migration input, runtime authority,
  or a reason to retain a compatibility route.

The #177 deployed evidence matrix therefore covers NT51919/29/32 as one
perfect-like family, NT51917/27/28 as Initial-Code/TP shared parts, and
NT51923/26 as exact-reference consumers. Consumer-wide workflow convergence
remains #194 and is not a reason to duplicate these family facts.

No later slice may redefine FirmwareConfig fields, duplicate perfect-like
firmware semantics per member, or treat shared TP/Initial Code as authority for
another artifact part.

## NT51928 dual-capacity allocation

- #239 owns one NT51928 Standard Merge capability and one DP Replace capability
  with declared `0x40000` shared Initial-Code/TP and `0x80000` LDC variants.
  It consumes the already accepted `initial-code-shared` and `tp-shared`
  `SharedFactRelationship` scopes; it cannot promote NT51927/NT51928 to a
  perfect family or copy their shared geometry.
- For Standard Merge, LDC absence selects the `0x40000` candidate; supplied
  LDC selects the `0x80000` candidate and must then pass structural validation.
  Failure blocks and never falls back to absence. DP Replace selects its variant
  from the accepted Reference length. Neither mechanism infers IC identity or
  creates another route/Support Matrix row.
- #239 owns the canonical replacement selection group, conditional operation
  materialization, warning-only non-uniform Initial Code/DP/LDC validation, and
  headless Application/CLI/report evidence. It is ordered after #219 to avoid
  concurrent ownership of shared profile/compiler integration surfaces.
- #194 consumes #239 during remaining route convergence. #182 later presents
  its typed LDC `PendingInput`/`NotApplicable`/Optional and Warning states
  without deriving capacity or firmware validity in Presentation.

## Source projection and FlashCode admission allocation

#259 owns the ADR 0045 R2/R3 migration before remaining TP Header/AB/headless
consumers converge:

- one generic `source-view-coverage` requirement replaces workflow-named
  Normal-DP/TP length semantics;
- Initial Code, DP, TP, LDC, TPA, and TPB remain address-bearing section
  sources and may consume compatible same-IC FlashCode windows;
- TPA is same-coordinate and TPB is bank-relocated; only compact CtrlRAM uses
  built-in payload-byte-zero to nonzero-target projection;
- Replace Reference and complete DP AB seeds remain exact declared container
  variants;
- FlashCode classification composes declared capacity, DP/TP coverage, and
  profile validations without inventing a magic signature or route inference.

#187, #190, and #194 consume this contract rather than recreating
family/workflow-specific admission. #230 later deletes the migrated
compatibility semantic types with zero-caller evidence.

## General authoring and Saved Rule v2 allocation

The owner-approved #247 program extends the completed #181 typed draft rather
than reopening its merged baseline:

- #248 owns selected-file content identity, Source Slice / From File Start
  presets, full-file-length materialization, explicit Reload/Rebind, and
  build-time length/SHA-256 re-verification.
- #249 owns exact General Merge output capacity plus one arbitrary blank byte
  with default `0x00`, including schema/request/Preview identity and the
  deletion of the fixed-fill compatibility owner.
- #250 owns the one order-independent user-writer occupancy ledger and the
  intersection of Application technical ceilings, exact Trusted Parent
  semantic/slot limits, and Saved Rule narrowing.
- #251 owns Saved Rule v2 General Merge initialization, exact separately
  installed Parent reference, canonical content hash, Draft invalidation,
  local Save-in-place, and immutable Catalog working-copy lifecycle.
- #252 owns General Replace Saved Rule execution through the exact Parent.
  Rules may narrow but cannot introduce, remove, reorder, or widen protected
  ranges, conditional POSTBUILD, tools, stages, parameters, or allowed writes.
  TP-touching admission remains R3 and requires exact golden/write-range owner
  evidence.
- #253 owns the explicit plan-only Diagnostic Preview when required General
  Replace POSTBUILD authority or runtime tooling is unavailable. It produces no
  BIN and makes no final Header/CRC/hash claim.
- #194 consumes the frozen headless contracts. #208 later consumes the same
  Application results for desktop behavior and the accepted visual grammar.
  #254 then deletes V1/inclusive-end/fixed-fill/timestamp-stamp and
  workflow-specific compatibility owners. #195 consumes that zero-caller
  result instead of creating another broad facade.

#248, #249, and #250 may execute concurrently only when their file ownership
remains disjoint and a single writer coordinates any shared request/schema
surface. `ready-for-agent` on the seven child issues records specification
readiness; the blockers above still control execution.

## DiffDLM and Diff NF allocation

The 2026-07-27 owner correction supersedes any interpretation of a legacy
DiffDLM outer envelope as one contiguous writable range:

- Every profile whose canonical DiffDLM record composition includes Diff NF
  must bind explicit writable DLM and preservation-mask subranges whose
  non-overlapping union covers each complete active target record. Missing, `unknown`,
  overlapping, or incomplete mask authority blocks compilation; it is not a
  warning or compatibility fallback. A DiffDLM filename or outer envelope alone
  does not trigger this contract; DLM-only routes retain their separately
  declared write geometry.
- #219 owns the canonical full-stride-source-to-masked-target contract for the
  NT51919/NT51929/NT51932 perfect family. It pilots the owner-confirmed
  NT51932 4-IC geometry (`0x0B90` DLM bytes in a `0x1400` source/target stride,
  with the `0x0870` Diff NF tail preserved) and binds cascade IC Count `N` to
  exactly `N - 1` active DLM subranges. Source admission requires the complete
  `(N - 1) * 0x1400` active prefix; merely reaching the final DLM end is not a
  complete-record contract.
- #219 must prove that preview, execution, and mutation audit preserve every
  NF byte, ignore invalid source NF filler, and reject any required DLM
  subrange that is entirely one repeated byte. Validation covers every required
  record. AE records after the active `N - 1` prefix are inactive dummy content:
  they are not copied, cannot enlarge the compiled DiffDLM source/read or
  scatter/write set, and leave inactive target records identical to the
  reference before postbuild. For this family, postbuild owns FWConfig Backup
  placement; runtime compares the marker-derived actual start with
  `AlignUp(0x2D100 + (N - 1) * 0x1400, 0x1000)`. An in-authority mismatch is a
  typed Build Report warning, while missing/ambiguous or out-of-authority
  placement fails closed. The Backup may therefore occupy inactive capacity
  inside the maximum `[0x2D100,0x35D00)` DiffDLM envelope;
  `[0x35D00,0x37000)` is only the upper authority extension needed by the
  8-IC case and must not be modeled as a fixed Backup region. Its affected DiffDLM
  authoring route keeps independent NF selection unavailable until a separate
  NF-mode/`DiffNFMerge.exe` contract is approved.
- #219 distinguishes placement-candidate authority from final mutation
  permission. After the unique actual Backup is located, only the original
  Reference Backup envelope and actual Backup envelope may differ; every other
  inactive DLM, Diff NF, and extension byte is audited against Reference.
  Dynamic DiffDLM requires a positive FWConfig `Chip_Num` at `0x17` and blocks
  before processor invocation when it is zero. Count-invariant routes retain a
  shared warning-only diagnostic. The NT51932 4-IC suite includes full-prefix
  boundary tests, inactive-mutation negatives, and an executable rejected
  contiguous-overlay corruption oracle.
- #188 owns the NT51950/NT51951 migration using the independent owner-confirmed
  2-IC geometry: target `0x33200`, stride `0x1400`, writable Diff CtrlRAM
  `[+0x000,+0x910)`, and preserved Diff NF `[+0x910,+0x1400)`. Their declared
  End Flag `0x36FFC` fixes the canonical FWConfig Backup start at `0x36000`;
  postbuild copies the primary FWConfig from `0x22200` to that destination.
  No implementation may reuse NT51932's split, wider IC Counts, or
  count-derived Backup placement formula by analogy. Direct expected-output
  and exact processor mutation evidence remain promotion gates.
- #194 migrates remaining consumers only after #219/#188 expose this behavior
  through the canonical compiled plan. #184/#194 project DiffDLM as one
  renderer-neutral segment with typed preservation details; #208 presents the
  compact hover/focus summary and lateral kept-range details. Presentation
  never owns the byte mask.

## Change control

Any dependency change must preserve all of the following:

1. the issue body and this repository anchor agree;
2. no ticket is moved ahead of its characterization, evidence, or ownership
   prerequisite;
3. a dependency depth change is not presented as an automatic release
   allocation;
4. `ready-for-agent` remains a specification-readiness label rather than a
   bypass for blockers; and
5. the final integration gate remains downstream of coverage, verifier,
   compatibility-owner deletion, legacy-runtime retirement, and #229 Canonical
   Core Convergence.
