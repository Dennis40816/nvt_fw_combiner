# v0.9.7 Integration Evidence

Status: release-candidate integration record; not a firmware-support promotion.

## Baseline and retained history

The `0.9.7` integration branch fast-forwards the existing local milestone branch
to `origin/0.9.6` at `6213e434`, then retains only these independently reviewed
forward-port phases:

- AB evidence applicability and runtime-admission guards from
  `feature/0.9.3/ab-evidence-forward-port`;
- compiled final-output validation and the non-routed NT51926 CtrlRAM candidate
  from `feature/0.9.5/ctrlram-v2-convergence`; and
- the four audited semantic-state token slices from
  `feature/0.9.7/ui-token-consolidation-audit`.

The stale `feature/0.9.3/ab-combiner` history is excluded because it mixes
private and out-of-scope changes. The older full UI-token branch is excluded
because its broad palette hierarchy would duplicate tokens already owned by
the current base. Candidate-intake and DP-retirement feature histories are not
replayed because their retained outcomes already exist on the `0.9.6` lineage.

After the final fetch, `origin/0.9.5` points to `f7d8bb60`
(`fix(release): prove external tool package exclusion`). The cumulative lineage
contains `6213e434` with the same subject, stable patch id
`36a8192043210843fcd01d825d284af37fd17e9a`, and an identical resulting tree.
The integration therefore retains the verified change without adding an empty
ancestry-only merge or importing stale private history.

The final pre-merge refactor phase retires the duplicate
`DpPerspectiveCatalog` in `5da4134c`. NT51950/NT51951 Standard Merge and DP
Replace projections now read capacities, named ranges, operations, and IC
registration from the existing trusted V2 bundle. This phase does not change
bytes, ranges, operation order, runtime admission, external processing, or the
Legacy Combiner contract.

## Owner milestone intake snapshot

This table records the owner's integration input before the final `0.9.7`
review. Percentages are planning estimates, not tag or firmware-support claims.

| Milestone | Function complete | Tag ready | Intake status |
| --- | ---: | ---: | --- |
| `v0.9.1` | 100% | 100% | tagged |
| `v0.9.2` | 100% | 100% | tagged |
| `v0.9.3` | 75% | 45% | AB/CtrlRAM candidates largely established; R3 evidence not fully closed |
| `v0.9.4` | 85% | 55% | Candidate Intake developed on a feature branch; integration outcome must be verified from the retained cumulative tree |
| `v0.9.5` | 68% | 40% | three principal legacy definitions retired; remaining compiler/map/postbuild replacement stays evidence-gated |
| `v0.9.6` | 78% | 35% | package allowlist P2 integrated; clean-machine, signing, and legal gates remain open |
| `v0.9.7` | 82% | 25% | six UI-audit commits supplied for review; the retained four-slice outcome is integrated on the cumulative branch |

No retroactive `v0.9.3` through `v0.9.6` tags are created from this snapshot.
The only intended final release target in this integration is `v0.9.7`, after
review, protected CI, and the explicit residual gates below.

## Commit-log quality gate

Every commit after annotated tag `v0.9.2` must have one reviewable phase and a
Conventional Commit subject using an explicit type and, where useful, a narrow
scope. The final integration audit rejects subjects longer than 72 characters,
leading or trailing whitespace, `WIP`, `TODO`, temporary/checkpoint wording,
and `fixup!` or `squash!` commits. Published feature history is not force
rewritten; duplicate or obsolete branches are excluded at integration instead.

The audit is performed over `v0.9.2..HEAD` immediately before the PR handoff,
and its count/result is recorded in the PR evidence.

## Firmware and release gates retained

- All five AB candidates remain rejected by the Application run-request
  boundary. NT51950 and NT51951 initialize from the complete DP container,
  consume no `map.txt`, and leave AB header CRC writes to Combiner 1.13.0.
- NT51919 has fact-scoped NT51929 alias evidence but no direct product golden.
  NT51932 has named synthetic parity but still needs a direct product golden or
  owner-approved fact-scoped alias. NT51951 has synthetic Python/Combiner
  parity but still needs its own direct product golden.
- NT51926 CtrlRAM remains an executable candidate with direct-golden,
  firmware-owner-review, and runtime-route blockers. No candidate profile is
  promoted by this integration.
- Stable publishing still requires protected CI, independent review, approved
  support-matrix evidence, signing/legal approval, and clean Windows package
  smoke evidence.
