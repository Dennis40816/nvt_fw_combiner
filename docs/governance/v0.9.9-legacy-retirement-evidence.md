# v0.9.9 Legacy Retirement Evidence

## Decision

At baseline `c3b77363`, no remaining legacy authority can be deleted safely as
a whole. The remaining duplication is architectural rather than byte-identical:
trusted V2 profiles own the migrated Standard Merge and DP Replace paths, while
CtrlRAM and General Replace still depend on Workbench-built profiles, flash-map
projections, metadata aggregation, and the static Combiner postbuild catalog.

The original inventory was R0. ADR 0023's TP-only candidate correction is R3
because it narrows a profile map and processor read/target range; it does not
change a command, write authority, runtime route, or support claim.

## Measured retirement surface

The accepted `v0.9.8` production ratchet is 56,742 nonblank C#/AXAML lines.
The four remaining compatibility authorities account for 2,141 nonblank C#
lines before caller cleanup:

| Authority | Files | Nonblank lines | External production caller files |
| --- | ---: | ---: | ---: |
| `CompositionProfileCompiler` | 8 | 776 | 2 |
| `TpFlashMapCatalog` | 4 | 497 | 13 |
| `IcMetadataFacade` | 1 | 123 | 5 |
| `LegacyCombinerPostbuildCatalog` | 7 | 745 | 4 direct, plus facade/map consumers |

Deleting only those definitions would leave 54,601 lines and would not meet
the owner-approved `v0.9.9` ceiling of 54,000. The current planning range is
53,000-53,800 lines, with 53,300 as the working target. Reaching it requires
removing the replaced Workbench adapters and duplicated projections in the
same reviewed migrations; it must not come from deleting evidence or weakening
validation. This is a forecast, not a deletion claim or ratchet update.

## Current consumers and exact replacement conditions

| Current authority | Current production responsibility | V2 replacement required before deletion |
| --- | --- | --- |
| `CompositionProfileCompiler` | Lowers the synthetic CtrlRAM Replace profile and General Replace explicit mappings built by `WorkbenchCompositionService` | Trusted V2 registrations must express both workflows, preserve optional CtrlRAM slot behavior and General Replace safety policy, and produce equivalent operations, diagnostics, reports, and bytes |
| `TpFlashMapCatalog` | Supplies CtrlRAM/General Replace region planning, display, coverage, IC-number choices, postbuild-to-region binding, binary-model projection, and one General Merge capacity projection | Resolved V2 family/map/profile facts must supply every consumer without copying ranges, aliases, number policy, or postbuild mapping into another C# catalog |
| `IcMetadataFacade` | Joins IC support, flash maps, number choices, and postbuild profiles for Workbench/UI catalog and firmware-metadata selection | One typed V2-backed query boundary must expose the same catalog, number-selection, version-based postbuild choice, and readiness information |
| `LegacyCombinerPostbuildCatalog` | Owns cataloged Combiner 1.13.0 command sequences, per-IC/count branches, firmware-version selection, processor ids, staged source names, and write authority used by CtrlRAM/General Replace; unresolved cases such as NT51931 remain gated | Equivalent manifest-pinned V2 processor declarations need the exact command/tool/staging/read/write facts, negative validation, full-output golden parity, and firmware-owner review for every migrated IC/mode |

The Legacy Combiner 1.13.0 executable and constrained staged runner are not
retirement targets. Only the duplicated static declarations may move, and the
runner remains the sole writer for approved Combiner-owned CRC/header changes.

## Existing V2 coverage

The only built-in CtrlRAM Replace V2 candidate is
`nt51926-ctrlram-replace-fw141-cascade`. It is restricted to NT51926,
Common FW 1.4.1, cascade selection, and an exact `0x3C000` TP-work base. It
declares the five Normal/DIFF/MP/VN/NF inputs as required and records the
Combiner 1.13.0 processor staging, TP-work read view, and allowed writes.

The validated legacy product path continues to handle both full-Flash and
TP-work artifacts. This non-routed V2 candidate deliberately rejects a
`0x40000` reference: full-Flash V2 needs a generic processor-subrange contract
that stages `[0,0x3C000)`, audits it, and reinserts it into the full clone while
preserving the tail. No full-Flash V2 support or runtime promotion is claimed.

It remains `executable-candidate` and deliberately has no UI/CLI runtime route.
Its profile records three open blockers: direct input/output golden evidence,
firmware-owner review of the exact command and writes, and runtime-route review.
The current Workbench path is not semantically equivalent because it also:

- permits a non-empty subset of the mapped CtrlRAM regions;
- catalogs, routes, and plans all 13 IC rows and their single/cascade/numeric
  branches without making a support claim;
- selects versioned NT51926 and NT51930 postbuild categories from the base;
- exposes a Build-only TP firmware-version edit that remains R3-gated on its
  existing field/range, expected-output, and firmware-owner evidence;
- plans display, coverage, warnings, command evidence, reports, and naming; and
- admits General Replace mappings only through its current protected-range and
  postbuild selection policy.

The committed NT51926 and NT51927 self-replacement fixtures prove that the
Workbench route and staged tool can execute. They intentionally omit expected
postbuild outputs, so they do not establish full-byte parity.

## R3 owner-evidence gaps

Every row below remains a firmware-owner/golden gate. Command-shape tests or a
self-replacement input fixture do not substitute for the expected final bytes.

| IC/family | Evidence already recorded | Evidence still required before its declarations move |
| --- | --- | --- |
| NT51926 | Direct 1.4.1 cascade base and sliced replacement inputs; non-routed V2 candidate declares one exact `0x3C000` TP-work map, five slots, staging, command binding, and unchanged write ranges | Expected final TP-work output for 1.4.1 cascade; exact legacy/V2 full-byte parity; owner approval of command order/read-write authority; generic prefix-stage/reinsert contract plus full-Flash expected output before any `0x40000` V2 route; separate evidence for 2.0.0 and any non-cascade route selected for migration |
| NT51927 | Direct two-chip and three-chip bases plus sliced replacement inputs; cataloged Combiner command sequence | Expected postbuild outputs for the selected count modes, allowed-diff proof, exact command/tool review, and firmware-owner parity approval |
| NT51917 / NT51928 non-NB | Cataloged owner aliases to the NT51927 reference flow; NT51928 NB is excluded | Owner-approved fact scope for each released mode/count and either direct expected output or reviewed alias output evidence; no NB promotion |
| NT51932 | Direct command declaration and range model; no CtrlRAM expected output | Direct expected output, allowed-diff proof, exact command/tool review, and firmware-owner parity approval |
| NT51929 / NT51919 | Cataloged NT51929-to-NT51932 and NT51919-to-NT51929 reference relationships | CtrlRAM-specific owner-approved fact scope plus expected-output evidence. AB evidence does not prove CtrlRAM Replace parity |
| NT51930 | Versioned 1.x/2.0 command declarations and reviewed current cascade-count branch policy | Expected output for every migrated firmware-version/count branch, allowed-diff proof, and firmware-owner approval; unapproved extended-count behavior remains closed |
| NT51931 | The official inspected `NT51930BASED_NORMAL_MODE` shape crashes with Combiner 1.13.0; diagnostic `NT51931BASED_NORMAL_MODE` avoids the crash but has an unexplained non-CRC-only 108-byte drift | Owner decision on the correct tool/version/hash and compatible input/header state, or explicit approval of the diagnostic mode; then direct expected output, allowed-diff proof, and firmware-owner parity approval |
| NT51950 | Cataloged CtrlRAM command declarations | CtrlRAM expected output and owner-reviewed command/write parity. Standard Merge, DP Replace, and AB goldens are different workflow evidence |
| NT51951 | Cataloged owner reference to the NT51950 CtrlRAM flow | Owner-approved CtrlRAM alias scope plus expected output and parity review. Its Standard Merge/DP Replace evidence does not close this gate |
| NT51920 / NT51923 | Cataloged CtrlRAM command declarations and argv-shape tests | Direct map/slot/command/read-write evidence, expected final output, allowed-diff proof, and firmware-owner parity approval |
| CtrlRAM TP FW version edit | Workbench exposes the Build-only mutation and requires the canonical NVT Backup fields to match before commit | Owner-approved editable fields/ranges and source/backup relationship, expected output for every migrated IC/category, allowed-diff proof, and firmware-owner review; its current execution path is not parity evidence |
| CtrlRAM base artifact shape | Validated legacy product runtime handles TP-work and full-Flash artifacts; the NT51926 V2 candidate accepts only exact `0x3C000` TP work | Generic processor-subrange staging and prefix reinsertion before any full-Flash V2 base is accepted; the full clone tail must be preserved and no capacity or offset may be inferred from existing fixtures |
| General Replace cross-cutting policy | Workbench currently blocks header/customer/project/backup rows and appends postbuild for selected TP/CtrlRAM mappings | Owner-approved protected ranges, explicit-mapping safety envelope, overlap/alignment rules, TP-touching expected outputs, allowed diffs, and final release-scope IC/mode approvals |

For every migrated case the evidence package must name the base and replacement
hashes, Common FW version where applicable, IC-number selection, Combiner
1.13.0 binding, ordered argv, staging bindings, read/write ranges, expected
output hash, allowed diff ranges, and owner approval.

## Retirement order

1. Close NT51926 Common FW 1.4.1 cascade TP-work golden and owner review. Add a
   generic processor-subrange stage/reinsert contract and full-Flash golden
   before considering a `0x40000` V2 path. Do not promote runtime support as
   part of evidence intake.
2. Migrate one directly evidenced IC/version/count family at a time. In the same
   phase, replace its flash-map, number-policy, and postbuild projections and
   delete the exactly replaced C# declarations.
3. Route all CtrlRAM planning, display, coverage, metadata, Preview/Build, and
   report consumers through the reviewed V2 boundary. Retire
   `IcMetadataFacade` and the covered parts of `TpFlashMapCatalog` only after no
   production consumer remains.
4. Obtain owner approval for General Replace protected ranges, the explicit-
   mapping envelope, overlap/alignment policy, TP-touching expected outputs,
   and release-scope IC/modes. Express only those reviewed safety envelopes and
   postbuild triggers in trusted V2 data, prove operation/report/byte parity,
   and remove the remaining `CompositionProfileCompiler` caller.
5. Remove `CompositionProfileCompiler`, `TpFlashMapCatalog`, and the static
   `LegacyCombinerPostbuildCatalog` only when architecture tests prove they have
   no production consumers. Keep the Legacy Combiner executable, constrained
   runner, staging-copy isolation, host diff enforcement, and tool packaging.

## Protected invariants

- No range, command, processor write, alias, IC-count rule, or runtime support
  is inferred from this inventory.
- C# does not reimplement Combiner CRC/header algorithms. In particular, C#
  never writes the AB header CRC.
- NT51950/NT51951 AB use the full DP container as the base and do not require
  `map.txt`; Python parity is compared with the exact Legacy Combiner 1.13
  command.
- NT51919/NT51932 AB promotion still requires direct evidence or the explicitly
  approved fact-scoped alias evidence documented for that workflow.
- Runtime/support promotion remains separate and requires owner review after
  byte evidence closes.
- The NT51926 CtrlRAM V2 candidate is TP-work-only; validated legacy full-Flash
  product handling is not evidence that the current V2 executor can preserve
  and reinsert a full artifact's tail.

## Evidence sources

- [Legacy retirement matrix](legacy-retirement-matrix.md)
- [ADR 0023: NT51926 CtrlRAM V2 TP-work boundary](../adr/0023-nt51926-ctrlram-v2-tp-work-boundary.md)
- [Supported IC/workflow matrix](../architecture/supported-ic-matrix.md)
- [Integrity processing matrix](../architecture/integrity-processing-matrix.md)
- [NT51926 CtrlRAM V2 candidate](../../profiles/built-in/nt51926-ctrlram-replace-candidate/profiles/nt51926-ctrlram-replace-fw141-cascade.json)
- [CtrlRAM golden handoff](../../testdata/golden/ctrlram-replace/README.md)
- [v0.9.8 code-size policy](v0.9.8-code-size-policy.md)
