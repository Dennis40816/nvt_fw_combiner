# v0.9.9 Legacy Retirement Evidence

## Decision

At baseline `c3b77363`, no remaining legacy authority can be deleted safely as
a whole. The remaining duplication is architectural rather than byte-identical:
trusted V2 profiles own the migrated Standard Merge and DP Replace paths, while
CtrlRAM and General Replace still depend on Workbench-built profiles, flash-map
projections, and metadata aggregation. The static Combiner Postbuild command
and TP flash-map fact declarations have now been retired into hash-pinned
repository data catalogs.

The original inventory was R0. ADR 0024's TP/full-Flash convergence is R3
because it adds a schema/compiler processor-prefix contract and a second exact
map capacity. It does not change a command, write authority, runtime route, or
support claim.

## Measured retirement surface

The accepted `v0.9.8` production ratchet is 56,742 nonblank C#/AXAML lines.
The v0.9.9 TP semantic/Workbench/UI convergence, bilingual-resource
consolidation, drop-zone convergence, CLI report-writer convergence, and
firmware-region ordering convergence and config-authority phases lower the interim ratchet to
55,201 lines, keep the
Workbench aggregate at 4,476, and lower the MainWindowViewModel aggregate to
2,717. They retire only unbound projections, wrappers, and duplicated
bilingual assignment and region-ordering structures plus the static Postbuild
command/range declarations; they do not promote any IC/mode. The remaining gap to the owner-approved
54,000-line stable gate is 1,201 lines.
The remaining compiler/facade plus the config-backed map query adapter account
for 1,189 nonblank C# lines before caller cleanup:

| Authority | Files | Nonblank lines | External production caller files |
| --- | ---: | ---: | ---: |
| `CompositionProfileCompiler` | 8 | 776 | 2 |
| `BuiltInTpFlashMapCatalog` config loader/query | 3 | 333 | 8 |
| `IcMetadataFacade` | 1 | 80 | 6 |

Deleting those definitions without migrating their callers would not compile.
Their nominal subtraction would leave 54,012 lines, so reaching the owner-
approved `v0.9.9` ceiling of 54,000 requires the evidence-backed caller
migrations and authority deletion to land together. It must not come from
deleting evidence or weakening validation. This is a measured boundary, not a
deletion claim or runtime-promotion approval.

## Current consumers and exact replacement conditions

| Current authority | Current production responsibility | V2 replacement required before deletion |
| --- | --- | --- |
| `CompositionProfileCompiler` | Lowers the synthetic CtrlRAM Replace profile and General Replace explicit mappings built by `WorkbenchCompositionService` | Trusted V2 registrations must express both workflows, preserve optional CtrlRAM slot behavior and General Replace safety policy, and produce equivalent operations, diagnostics, reports, and bytes |
| `BuiltInTpFlashMapCatalog` query adapter | Loads hash-pinned TP/full-Flash shapes and TP Overview regions, then supplies CtrlRAM/General Replace planning, display, coverage, and postbuild-to-region binding | Resolved V2 family/map/profile facts must supply every consumer without copying ranges, aliases, number policy, or postbuild mapping into another C# catalog |
| `IcMetadataFacade` | Joins IC support, flash maps, number choices, and postbuild profiles for Workbench/UI catalog and firmware-metadata selection | One typed V2-backed query boundary must expose the same catalog, number-selection, version-based postbuild choice, and readiness information |

The Legacy Combiner 1.13.0 executable and constrained staged runner are not
retirement targets. The duplicated static declarations have moved to
`profiles/built-in/ctrlram-postbuild-v2/catalog.json` and `flash-map.json`; the loaders reject hash,
unknown-field, enum, range, duplicate-id, and reviewed argument-allowlist drift.
The runner remains the sole writer for approved Combiner-owned CRC/header changes.

## Existing V2 coverage

The only built-in CtrlRAM Replace V2 candidate is
`nt51926-ctrlram-replace-fw141-cascade`. It is restricted to NT51926,
Common FW 1.4.1 and cascade selection. It accepts exactly `0x3C000` TP-work or
`0x40000` full-Flash base forms. Both select the same five required
Normal/DIFF/MP/VN/NF inputs and the pinned Combiner 1.13 invocation profile. For
full Flash, the shared engine stages only `[0,0x3C000)` and preserves
`[0x3C000,0x40000)` byte-for-byte.

This is a compiler/executor contract and synthetic tail-preservation proof,
not direct full-Flash product golden evidence. No runtime promotion is claimed.

It remains `executable-candidate` and deliberately has no UI/CLI runtime route.
Its profile records three open blockers: direct input/output golden evidence,
firmware-owner review of the exact command and writes, and runtime-route review.
The current Workbench path is not semantically equivalent because it also:

- permits a non-empty subset of the mapped CtrlRAM regions;
- catalogs, routes, and plans all 13 IC rows and their single/cascade/numeric
  branches without making a support claim;
- selects versioned NT51926 and NT51930 postbuild categories from the base;
- exposes a Build-only TP firmware-version edit that remains R3-gated on its
  existing field/range, expected-output, and firmware-owner evidence;
- plans display, coverage, warnings, command evidence, reports, and naming; and
- admits General Replace mappings only through its current protected-range and
  postbuild selection policy.

The committed NT51926 and NT51927 self-replacement fixtures prove that the
Workbench route and staged tool can execute. For NT51926 Common FW 1.4.1
cascade, regression commit `6eb0db6e` also runs the current Workbench compiler
and the V2 candidate from the same owner-approved inputs through the actual
Combiner 1.13 staged runner and requires their complete output bytes and
SHA-256 to match. This is cross-route migration parity, not an independently
owner-approved expected output. The fixtures intentionally omit expected
postbuild outputs, so the direct golden and firmware-owner gates remain open.

The same hash-pinned NT51926 family now includes the non-routed
`nt51926-general-replace-dp-single-candidate`. Its dedicated physical map
accepts only a `0x40000` full-Flash reference and authorizes only explicit
DP mappings within `[0x3E000,0x40000)`. It has no metadata, static operation,
validation, or processor authority. Direct regression uses the same immutable
base/source with the current Workbench/V1 compiler and the V2 candidate and
requires complete output-byte equality; separate tests reject a `0x3C000`
TP-only base and TP/CtrlRAM targets. This is the first concrete General
Replace migration slice, but it is single-selector and DP-only and has no
production UI/CLI route.

## R3 owner-evidence gaps

Every row below remains a firmware-owner/golden gate. Command-shape tests or a
self-replacement input fixture do not substitute for the expected final bytes.

| IC/family | Evidence already recorded | Evidence still required before its declarations move |
| --- | --- | --- |
| NT51926 | Owner expected outputs and physical inputs now exist for 1.4.1 single/cascade, and all selected payload bytes match their declared target sections. Candidate v0.4.0 accepts exact `0x3C000` TP work and `0x40000` full Flash, stages only the TP prefix, preserves the Flash tail, and proves complete current-Workbench/V2 TP-output parity through Combiner 1.13 for the owner 1.4.1 cascade inputs. | Direct postbuild TP/full-Flash expected-output golden plus owner approval of command/read-write and prefix authority; separate 2.0.0 evidence or explicit v0.9.9 scope exclusion |
| NT51927 | Owner expected outputs and inputs exist for single, two-chip, and three-chip. Single has a direct real-tool sample and a TP-only product replay with zero payload drift; two/three-chip replay leaves only Header CRC/Header Copy CRC fields. | Owner approval of command/tool authority, family/count mapping, and the recorded allowed-diff ranges before runtime promotion |
| NT51917 / NT51928 non-NB | Owner confirms the NT5191x-to-NT5192x perfect-family rule and the NT51928 non-NB partial NT51927-family rule; NT51928 NB is excluded | Typed fact-scoped alias migration and owner R3 review; no duplicate evidence folder or NB promotion |
| NT51932 | Owner expected cascade output and inputs now exist. Normal/VN/NF prefix bytes match, but one DiffDLM 4 KiB slot has 4,095 mismatches and `NF_Ctrlram.bin` equals only `NF_Diff_0.bin`. | Same-run DiffDLM and completed DiffNFMerge NF output, or equivalent log/hash proof; exact command/tool and owner review |
| NT51929 / NT51919 | NT51929 now has a true non-AB single expected output whose NF/Normal/VN sections match. Owner confirms NT51919/NT51929/NT51932 as one family and permits NT51929 AB evidence as the fact-scoped NT51932 AB alias. | Typed family/AB-offset migration and owner R3 review; the AB first half must not be presented as the NT51929 single golden |
| NT51930 | Owner expected 1.3 cascade output exists and NF/Normal/MP/VN match, but one DiffDLM 4 KiB slot has 4,090 mismatches | Same-run DiffDLM or command/log explaining the three supplied Diff variants; unapproved branches remain closed |
| NT51931 | Owner BAT names `NT51931BASED_NORMAL_MODE CRC8` and consumes only `0xC800` DiffDLM bytes, correcting the prior catalog fact. It also invokes an absent `InsertSID.py`; the prior diagnostic still has 108 unexplained bytes. | Keep CtrlRAM Replace Not Supported; command evidence may correct diagnostics but cannot promote runtime behavior |
| NT51950 | Owner final output and original physical inputs now exist for single. Expected-derived-sentinel replay proves zero payload differences and only 16 Header CRC/Header Copy CRC bytes. It also directly exercises a 2,816-byte NF against the `0x2A10` maximum and prefix-only consumption of an oversized Normal source. | Owner acceptance of the expected-only evidence method, same-run provenance, short-source/tail and oversize-prefix facts, exact command/write authority, allowed-diff ranges, and firmware-owner parity approval. Cascade remains unevidenced. |
| NT51951 | A single final output and physical input set was supplied, but replay has 3,773 unexplained Normal payload differences plus 16 Header CRC/Header Copy CRC bytes; supplied Initial/TP metadata also conflicts with the final. | A matching same-run Normal/Initial/TP/final set, or an owner-corrected package, followed by exact command/write and allowed-diff review. The NT51950 flow alias cannot hide the current mismatch; cascade remains unevidenced. |
| NT51920 / NT51923 | Owner expected outputs and physical inputs now exist for single/cascade, and all formal physical payload bytes match their declared target sections, including short-source prefix behavior | Owner approval of command/read-write authority and expected-derived evidence before declaration migration or runtime promotion |
| CtrlRAM TP FW version edit | Workbench exposes the Build-only mutation and requires the canonical NVT Backup fields to match before commit | Owner-approved editable fields/ranges and source/backup relationship, expected output for every migrated IC/category, allowed-diff proof, and firmware-owner review; its current execution path is not parity evidence |
| CtrlRAM base artifact shape | Owner requires both TP BIN and full Flash. ADR 0024 and the NT51926 candidate now clone either exact shape, stage only the zero-based TP prefix for full Flash, and prove the remaining container tail is unchanged. | Direct expected-output parity for both shapes and firmware-owner review remain required before runtime/support promotion. |
| General Replace cross-cutting policy | Workbench currently blocks header/customer/project/backup rows and appends postbuild for selected TP/CtrlRAM mappings. NT51926 now has a hash-pinned, non-routed single-selector DP-only V2 candidate with complete V1/V2 output-byte parity and fail-closed TP/base tests. | Extend exact physical maps and parity across all release ICs and number modes; close report/naming/CLI/UI parity; declare TP postbuild stages and expected outputs; obtain owner approval of protected ranges, explicit-mapping safety envelope, overlap/alignment rules, allowed diffs, and final release scope |

For every migrated case the evidence package must name the base and replacement
hashes, Common FW version where applicable, IC-number selection, Combiner
1.13.0 binding, ordered argv, staging bindings, read/write ranges, expected
output hash, allowed diff ranges, and owner approval.

## Retirement order

1. Close NT51926 Common FW 1.4.1 cascade TP/full-Flash golden and owner review
   against the ADR 0024 zero-based prefix contract. Do not promote runtime
   support as part of evidence intake.
2. Migrate one directly evidenced IC/version/count family at a time. Static
   flash-map facts are already retired; replace their config query projections
   only after the resolved V2 map supplies every current consumer.
3. Route all CtrlRAM planning, display, coverage, metadata, Preview/Build, and
   report consumers through the reviewed V2 boundary. Retire
   `IcMetadataFacade` and `BuiltInTpFlashMapCatalog` only after no
   production consumer remains.
4. Obtain owner approval for General Replace protected ranges, the explicit-
   mapping envelope, overlap/alignment policy, TP-touching expected outputs,
   and release-scope IC/modes. Expand the existing NT51926 DP-only candidate
   only through those reviewed safety envelopes and postbuild triggers, prove
   operation/report/byte parity for every routed shape, and remove the
   remaining `CompositionProfileCompiler` caller.
5. The static `LegacyCombinerPostbuildCatalog` is retired; keep its hash-pinned
   data projection under parity tests. Remove `CompositionProfileCompiler` and
   the config-backed flash-map query adapter only when architecture tests prove they have no
   production consumers. Keep the Legacy Combiner executable, constrained
   runner, staging-copy isolation, host diff enforcement, and tool packaging.

## Protected invariants

- No range, command, processor write, alias, IC-count rule, or runtime support
  is inferred from this inventory.
- For zero-offset CtrlRAM sources, the declared section length is an upper
  bound: copy stops at source EOF, preserves the untouched base tail, and
  rejects a source larger than the declared maximum. This owner-approved
  compatibility behavior is not whole-input exact-length authority.
- C# does not reimplement Combiner CRC/header algorithms. In particular, C#
  never writes the AB header CRC.
- NT51950/NT51951 AB use the full DP container as the base and do not require
  `map.txt`; Python parity is compared with the exact Legacy Combiner 1.13
  command.
- NT51919/NT51932 AB promotion still requires direct evidence or the explicitly
  approved fact-scoped alias evidence documented for that workflow.
- Runtime/support promotion remains separate and requires owner review after
  byte evidence closes.
- The NT51926 CtrlRAM V2 candidate accepts only exact TP-work/full-Flash shapes;
  its prefix/tail tests prove the executor boundary but do not replace direct
  product golden parity.
- The NT51926 General Replace V2 candidate accepts only a full-Flash base and
  DP-only single-selector mappings. It is not registered in production, and
  it cannot be used to bypass TP/CtrlRAM Legacy Combiner postbuild.

## Evidence sources

- [Legacy retirement matrix](legacy-retirement-matrix.md)
- [ADR 0023: NT51926 CtrlRAM V2 TP-work boundary](../adr/0023-nt51926-ctrlram-v2-tp-work-boundary.md)
- [ADR 0024: CtrlRAM TP/full-Flash base convergence](../adr/0024-ctrlram-tp-and-flash-base-convergence.md)
- [Supported IC/workflow matrix](../architecture/supported-ic-matrix.md)
- [Integrity processing matrix](../architecture/integrity-processing-matrix.md)
- [NT51926 CtrlRAM V2 candidate](../../profiles/built-in/nt51926-ctrlram-replace-candidate/profiles/nt51926-ctrlram-replace-fw141-cascade.json)
- [NT51926 General Replace DP-only V2 candidate](../../profiles/built-in/nt51926-ctrlram-replace-candidate/profiles/nt51926-general-replace-dp-single-candidate.json)
- [CtrlRAM golden handoff](../../testdata/golden/ctrlram-replace/README.md)
- [v0.9.9 owner CtrlRAM evidence audit](v0.9.9-owner-ctrlram-evidence-audit.md)
- [2026-07-17 owner golden intake](v0.9.9-owner-golden-intake-20260717.md)
- [v0.9.8 code-size policy](v0.9.8-code-size-policy.md)
