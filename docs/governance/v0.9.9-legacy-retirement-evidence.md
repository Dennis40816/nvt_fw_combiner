# v0.9.9 Legacy Retirement Evidence

## Decision

At baseline `c3b77363`, no remaining legacy authority can be deleted safely as
a whole. The remaining duplication is architectural rather than byte-identical:
trusted V2 profiles own the migrated Standard Merge and DP Replace paths, while
CtrlRAM and General Replace still depend on Workbench-built profiles and flash-map
projections. The forwarding metadata facade and the static Combiner Postbuild command
and TP flash-map fact declarations have now been retired into hash-pinned
repository data catalogs.

The original inventory was R0. ADR 0024's TP/full-Flash convergence is R3
because it adds a schema/compiler processor-prefix contract and a second exact
map capacity. It does not change a command, write authority, runtime route, or
support claim.

## Measured retirement surface

The accepted `v0.9.8` production ratchet is 56,742 nonblank C#/AXAML lines.
The v0.9.9 TP semantic/Workbench/UI convergence, bilingual-resource
consolidation, drop-zone convergence, CLI report-writer convergence, and
firmware-region ordering convergence, config-authority, and dormant V1 compiler
retirement phases previously lowered the interim ratchet to 55,031 lines.
After the later support/UI phases and this facade retirement, the current
working branch measures 55,366 production lines, a 4,609-line Workbench
aggregate, a 2,731-line MainWindowViewModel aggregate, and 1,055 exact-duplicate
JSON lines. These are measurements, not accepted higher ratchets. The phases
retire only unbound projections, wrappers, and duplicated
bilingual assignment and region-ordering structures plus the static Postbuild
command/range declarations. The latest phase also removes the raw IC-number
list projection and forwarding-only metadata/map overloads while retaining
grouped UI choices and branch-rule request validation; none of these phases
promote an IC/mode. The remaining gap to the owner-approved 54,000-line stable
gate is 1,366 lines. The retained CtrlRAM compatibility path additionally fails
closed unless exactly one processor stages source bytes into processor-owned
CtrlRAM ranges within declared postbuild write authority.
The remaining compiler plus the config-backed map query adapter account for
990 nonblank C# lines before caller cleanup:

| Authority | Files | Nonblank lines | External production caller files |
| --- | ---: | ---: | ---: |
| `CompositionProfileCompiler` | 8 | 699 | 2 |
| `BuiltInTpFlashMapCatalog` config loader/query | 3 | 291 | 8 |

Deleting those definitions without migrating their callers would not compile.
Their nominal subtraction from the current working measurement would leave
54,376 lines before caller cleanup. The `IcMetadataFacade` forwarding layer has been retired independently because
it owned no firmware fact or execution policy; projection parity remains locked
at `WorkbenchCompositionService`. Reaching the owner-approved `v0.9.9` ceiling
of 54,000 still requires the evidence-backed compiler/map caller migrations and
authority deletion to land together. It must not come from
deleting evidence or weakening validation. This is a measured boundary, not a
deletion claim or runtime-promotion approval.

## Current consumers and exact replacement conditions

| Current authority | Current production responsibility | V2 replacement required before deletion |
| --- | --- | --- |
| `CompositionProfileCompiler` | Lowers only the staged CtrlRAM Replace profile and General Replace explicit mappings built by `WorkbenchCompositionService`; dormant Merge, DP Replace, CopyRange, and fixed ReplaceRange authority is retired | Trusted V2 registrations must express both workflows, preserve optional CtrlRAM slot behavior and General Replace safety policy, and produce equivalent operations, diagnostics, reports, and bytes |
| `BuiltInTpFlashMapCatalog` query adapter | Loads hash-pinned TP/full-Flash shapes and TP Overview regions, then supplies CtrlRAM/General Replace planning, display, coverage, and postbuild-to-region binding | Resolved V2 family/map/profile facts must supply every consumer without copying ranges, aliases, number policy, or postbuild mapping into another C# catalog |

The Legacy Combiner 1.13.0 executable and constrained staged runner are not
retirement targets. The duplicated static declarations have moved to
`profiles/built-in/ctrlram-postbuild-v2/catalog.json` and `flash-map.json`; the loaders reject hash,
unknown-field, enum, range, duplicate-id, and reviewed argument-allowlist drift.
The runner remains the sole writer for approved Combiner-owned CRC/header changes.

## Existing V2 coverage

The built-in routed CtrlRAM Replace V2 profiles cover NT51926 Common FW 1.4.1
cascade and Common FW 2.0.0 single/cascade without a TP firmware-version edit.
They accept exactly `0x3C000` TP-work or `0x40000` full-Flash base forms and
selected physical Normal/DIFF/MP/VN/NF sources through the pinned Combiner
1.13 invocation profile. For full Flash, the shared engine stages only
`[0,0x3C000)` and preserves `[0x3C000,0x40000)` byte-for-byte.

The owner-authorized TP-base case now proves full-output V2-candidate parity
with its archived Legacy Combiner 1.13.0 output. The exact full-Flash owner
intake also proves V1/V2 byte parity and tail preservation, but remains an
expected-derived self-replacement without an independent pre-replacement base.
No support promotion is claimed.

The final 2026-07-18 NT51926 2.0.0 direct cases close exact single/cascade
engineering parity: Standard Merge decodes Common FW 2.0.0, PID `0x1309`, and
chip count 1 or 3 from FWConfig Backup; V1 and V2 output bytes and SHA-256 are
identical after one two-command processor session. Each output differs from the
owner expected by exactly 16 bytes at the two Header CRC/Header Copy CRC words
and their copied-header counterparts. CtrlRAM payload difference is zero,
inputs remain immutable, and the full-Flash tail is preserved. These facts
route only the exact profiles and do not promote support.

It remains `executable-candidate` and is routed through the shared UI/CLI
Workbench boundary for that exact slice. The scoped Legacy Combiner and route
parity blockers are closed. Its remaining blocker is firmware-owner review of
the exact command, prefix authority, and writes. The residual Workbench path
still:

- permits a non-empty subset of the mapped CtrlRAM regions;
- catalogs, routes, and plans all 13 IC rows and their single/cascade/numeric
  branches without making a support claim;
- selects versioned NT51926 and NT51930 postbuild categories from the base;
- exposes a Build-only TP firmware-version edit that remains R3-gated on its
  existing field/range, expected-output, and firmware-owner evidence;
- plans display, coverage, warnings, command evidence, reports, and naming; and
- admits General Replace mappings only through its current protected-range and
  postbuild selection policy.

The committed NT51926 and NT51927 self-replacement fixtures prove that the
Workbench route and staged tool can execute. For NT51926 Common FW 1.4.1
cascade, commit `67b8144b` routes the runtime profile, requires the TP-base
result to match the archived Legacy Combiner 1.13 full output, and forces the
residual V1 path for independent full-Flash byte/tail parity. This closes the
scoped route gate, not firmware-owner approval of exact write authority or the
remaining version/count/edit cases.

The same hash-pinned NT51926 family also includes the routed
`nt51926-general-replace-dp-single-candidate`. Its dedicated physical map
accepts only a `0x40000` full-Flash reference and authorizes only explicit
DP mappings within `[0x3E000,0x40000)`. It has no metadata, static operation,
validation, or processor authority. Direct regression uses the same immutable
base/source with a forced V1 Workbench run and the V2 candidate and requires
complete output-byte equality; CLI and UI tests lock the routed profile,
report, naming, and immutable base. Separate tests reject a `0x3C000` TP-only
base and TP/CtrlRAM targets. This is the first concrete General Replace runtime
migration slice, but it is limited to single-selector, full-Flash, file-backed
DP mappings. Patches/fills and every other IC/count/base/target retain their
existing behavior.

## R3 owner-evidence gaps

Every row below remains a firmware-owner/golden gate. Command-shape tests or a
self-replacement input fixture do not substitute for the expected final bytes.

| IC/family | Evidence already recorded | Evidence still required before its declarations move |
| --- | --- | --- |
| NT51926 | Owner expected outputs and physical inputs exist for 1.4.1 and 2.0.0 exact cases. Candidate family v0.6.0 accepts exact `0x3C000` TP work and `0x40000` full Flash, stages only the TP prefix, and preserves the Flash tail. The 2.0.0 single/cascade V1 and V2 outputs are byte-identical and differ from owner expected only at four approved CRC words. | Exact engineering parity is closed for routed 1.4.1 cascade and 2.0.0 single/cascade without version edits. Firmware-owner support promotion review remains; other versions/counts/edits stay fail-closed or on the retained fallback. |
| NT51927 | AUTO_PRJ-529/PID `0x5709`/Common FW 1.4.1/single has exact V1/V2 SHA `fdb8fef0...20ab9`, one seven-command session, and six owner-reference CRC words. JIRA-0251/PID `0x1615`/Common FW 1.3.2/two-chip routes only its full base SHA through V2; V1/V2 SHA is `6f0bbde7...5f58`, one ten-command session, with 100 expected-derived differences confined to 25 complete header/CRC words. PID `0x570A`/Common FW 1.4.0/three-chip likewise routes only its full base SHA through V2; V1/V2 SHA is `dc1ee892...fe16`, one 13-command session, with 116 bytes in 29 complete header/CRC words and 22 bytes in four declared VN replacement ranges. | Single/two-/three-chip route engineering is closed and support-neutral. The multi-chip results are explicitly not direct owner expected-output parity; no owner input is pending. Other projects, base hashes, versions, counts, and selectors retain the fallback. |
| NT51917 / NT51928 non-NB | Owner confirms the NT5191x-to-NT5192x perfect-family rule and the NT51928 non-NB partial NT51927-family rule; NT51928 NB is excluded | Typed fact-scoped alias migration and owner R3 review; no duplicate evidence folder or NB promotion |
| NT51932 exact AUTO_PRJ-525 | PID `0x5601`/Common FW 2.0.0/cascade 3 plus exact reference SHA `3eb556e0...08fd` now has an exact V2 route. V1 and V2 are byte-identical (`0e59a2fb...2566`), with only four CRC words different from the owner expected. The direct NF composite is hash-pinned and used as supplied; DiffNFMerge derivation is not claimed. | Exact-route retirement closed without support promotion. A different base with the same metadata tuple, or another PID/version/count/selector shape, retains V1; release-wide V1 caller removal and final R3 gate remain. |
| NT51929 exact AUTO_PRJ-594 / NT51919 alias scope | NT51929 PID `0x4703`/Common FW 2.0.0/single has a true 256 KiB non-AB expected that Standard Merge reconstructs byte-for-byte. Exact-base V1 and V2 output SHA are both `d23f53a1...198f`; the 15 owner-reference differences are confined to four Header/Header Copy CRC words, physical NF/Normal/VN payload drift is zero, and one ordered two-command session is locked. The 512 KiB AB expected remains separate. Owner confirms NT51919/NT51929/NT51932 as one family for the already approved fact-scoped AB alias. | Exact NT51929 single route retirement is closed without support promotion; other bases/PIDs/versions/counts/selectors retain V1. NT51919 typed CtrlRAM alias migration and release-wide caller removal remain engineering work, not missing owner input. |
| NT51930 | Direct INX AUTO_PRJ-302/PID `0x110D`/Common FW 1.3.0/cascade-3 evidence is committed. Exact V1/V2 output is byte-identical at SHA `6725c501...ff48f`, with one ordered registered-1.13 command. The owner final is used as the immutable reference sentinel because no separate pre-replacement FlashCode exists; its 4,397 current-output differences are confined to 8 CRC bytes, 1 header-copy byte, and 4,388 declared DiffDLM-range bytes. NF/Normal/MP/VN output bytes match the physical inputs; 29 NF_Diff files and the direct 577-byte NF composite are independently hash-pinned without claiming an unobserved DiffNFMerge derivation. | Exact project/version/topology runtime migration is closed and wrong shapes retain V1. Independent R3 release review and explicit support promotion remain; no-Jira content is not a missing case and cannot imply support. |
| NT51931 | The 2026-07-17 BAT names `NT51931BASED_NORMAL_MODE`; the final 2026-07-18 BAT names `NT51930BASED_NORMAL_MODE` and consumes `0x17C00` DiffDLM bytes. AUTO_PRJ-158/PID `0x131B`/cascade 6 expected-derived control has zero replacement-payload drift and 108 bytes in eight classified header/header-copy CRC ranges. On identical inputs, registered 1.13.0/51931-based and owner 1.2.0.4/51930-based both exit 0 and produce SHA `f38fdecd...c594`. The exact V2 route now produces the same SHA as retained V1. The supplied D8DfT82 FlashCode is not the same build as D8DT83 expected. InsertSID is an out-of-scope nonblocking pre-step. | Tool/mode and exact V2 route parity are closed. CtrlRAM Replace remains NotAvailable by support-neutral/no-promotion policy; no owner input or 1.2.0.4 package is required. |
| NT51950 exact AUTO_PRJ-676 | PID `0x4A06`/Common FW 2.0.0/single has a canonical Standard Merge base SHA `ccda75d0...e38e`. V1 and V2 are byte-identical at SHA `a32e6896...d5c4`; the 16 owner differences are four Header/Header Copy CRC words and CtrlRAM payload drift is zero. One ordered two-command session, input immutability, a 2,816-byte NF against the `0x2A10` maximum, preserved NF tail, and prefix-only consumption plus the canonical warning for the oversized Normal source are locked. | Exact route engineering is closed without support promotion. Other bases/PIDs/versions/counts/selectors retain V1. Cascade has no product case and is a release-scope exclusion, not missing evidence. |
| NT51951 exact AUTO_PRJ-695 | Corrected PID `0x5901`/Common FW 2.0.0/single DP, TP, physical inputs, and final output are committed. Standard Merge reconstructs the exact owner base SHA `c1cd54d9...b6b69`; V1 and V2 outputs are byte-identical at `64ffa21a...d1ea`, with zero NF/Normal/VN payload drift. The 1.11-produced owner expected differs from registered 1.13 only at four CRC words. | Exact project/version/topology route retirement is closed without support promotion. Admission requires the exact reference SHA; other bases/PIDs/versions/counts/selectors retain V1. Cascade is excluded from v0.9.9 release scope because no product case exists, not because owner evidence is missing. |
| AB family applicability | NT51929 and two NT51950 products have direct owner golden evidence. On 2026-07-18 the owner approved NT51929 -> NT51932 family applicability and NT51950 -> NT51951 workflow-logic applicability; independent named/synthetic tests retain the distinct NT51932 configuration and NT51951 `0x80000` placement. | No duplicate NT51932 or NT51951 AB product golden is requested. Firmware-owner review and explicit runtime-promotion approval remain required. |
| NT51920 | AUTO_PRJ-72/PID `0xF401`/Common FW 1.2.0/single and AUTO_PRJ-55/PID `0x1403`/Common FW 1.2.0/cascade 2 now use exact-base V2 routes. V1 and V2 output bytes, ordered two-command `CRC_Enable` argv, read/write authority, report identity, and immutable inputs match. Both owner expected controls have exactly 16 allowed CRC-only differences in four source/header-copy words and zero CtrlRAM payload drift. | Exact routes are engineering-complete and support-neutral; other projects, base hashes, versions, counts, and selectors retain the fallback until separately evidenced. |
| NT51923 | AUTO_PRJ-662/PID `0x6005`/Common FW 1.4.1/single and AUTO_PRJ-734/PID `0x4C03`/Common FW 1.4.1/cascade 3 now use exact-base V2 routes. Canonical Standard Merge reconstructs both owner expected bases byte-for-byte. V1 and V2 output bytes, ordered two-command `CRC_Enable` argv, read/write authority, report identity, and immutable inputs match; each recalculated output has exactly 16 CRC-only differences in four source/header-copy words and zero CtrlRAM payload drift. | Exact routes are engineering-complete and support-neutral; other projects, base hashes, versions, counts, and selectors retain the fallback until separately evidenced. |
| CtrlRAM TP FW version edit | Workbench exposes the Build-only mutation and requires the canonical NVT Backup fields to match before commit | Owner-approved editable fields/ranges and source/backup relationship, expected output for every migrated IC/category, allowed-diff proof, and firmware-owner review; its current execution path is not parity evidence |
| CtrlRAM base artifact shape | Owner requires both TP BIN and full Flash. ADR 0024 and the NT51926 candidate clone either exact shape. The TP case matches its archived Legacy Combiner 1.13.0 full output; the exact full-Flash owner self-replacement matches V1 byte-for-byte while preserving the container tail. | Independent pre-replacement full-Flash product parity, selective-slot/report/version-edit route parity, and firmware-owner review remain required before runtime/support promotion. |
| General Replace cross-cutting policy | Workbench blocks header/customer/project/backup rows and appends postbuild for selected TP/CtrlRAM mappings. NT51926 single/full-Flash/file-backed DP-only mappings now route through the hash-pinned V2 candidate with forced-V1 full-byte parity plus CLI/UI/report/naming coverage. | Extend exact physical maps and parity across remaining ICs/counts/base shapes, patches/fills, and TP postbuild; obtain owner approval of protected ranges, explicit-mapping safety envelope, overlap/alignment rules, allowed diffs, and final release scope |

For every migrated case the evidence package must name the base and replacement
hashes, Common FW version where applicable, IC-number selection, Combiner
1.13.0 binding, ordered argv, staging bindings, read/write ranges, expected
output hash, allowed diff ranges, and owner approval.

## Retirement order

1. Keep the reviewed NT51926 1.4.1 cascade and 2.0.0 single/cascade routes
   pinned to ADR 0024, exact golden tests, and support-neutral profiles. Do not
   widen them while migrating the next exact case.
2. Migrate one directly evidenced IC/version/count family at a time. Static
   flash-map facts are already retired; replace their config query projections
   only after the resolved V2 map supplies every current consumer.
3. Route all CtrlRAM planning, display, coverage, metadata, Preview/Build, and
   report consumers through the reviewed V2 boundary. The forwarding
   `IcMetadataFacade` is already retired; retire `BuiltInTpFlashMapCatalog`
   only after no production consumer remains.
4. Obtain owner approval for General Replace protected ranges, the explicit-
   mapping envelope, overlap/alignment policy, TP-touching expected outputs,
   and release-scope IC/modes. Expand the existing NT51926 DP-only candidate
   only through those reviewed safety envelopes and postbuild triggers, prove
   operation/report/byte parity for every routed shape, and remove the
   remaining `CompositionProfileCompiler` caller.
5. The static `LegacyCombinerPostbuildCatalog` is retired; keep its hash-pinned
   data projection under parity tests. Remove `CompositionProfileCompiler` and
   the config-backed flash-map query adapter only when architecture tests prove they have no
   production consumers. Keep the Legacy Combiner executable, constrained
   runner, staging-copy isolation, host diff enforcement, and tool packaging.

## Protected invariants

- No range, command, processor write, alias, IC-count rule, or runtime support
  is inferred from this inventory.
- For zero-offset CtrlRAM sources, the declared section length is a consumption
  and write-authority upper bound: copy stops at source EOF and preserves the
  untouched base tail. A larger owner-supplied physical container is accepted
  only through the explicit CtrlRAM truncate-with-warning policy; only its
  leading declared span is staged, and trailing bytes never expand processor
  read/write authority. This owner-approved compatibility behavior is not
  whole-input exact-length authority.
- C# does not reimplement Combiner CRC/header algorithms. In particular, C#
  never writes the AB header CRC.
- NT51950/NT51951 AB use the full DP container as the base and do not require
  `map.txt`; Python parity is compared with the exact Legacy Combiner 1.13
  command.
- NT51919/NT51932 AB evidence applicability is closed by the explicitly
  approved NT51929 fact-scoped aliases; NT51950/NT51951 AB applicability is
  likewise closed by the approved workflow-logic alias. These decisions do not
  replace firmware-owner promotion review or the independent topology tests.
- Runtime/support promotion remains separate and requires owner review after
  byte evidence closes.
- The NT51926 CtrlRAM V2 candidate accepts only exact TP-work/full-Flash shapes.
  Scoped TP Legacy Combiner golden parity and full-Flash V1/V2 self-replacement
  parity are closed. Common FW 1.4.1 cascade without a version edit is routed;
  exact owner range review, other versions/counts/edits, and support promotion remain open.
- The NT51926 General Replace V2 candidate accepts only a full-Flash base and
  file-backed DP-only single-selector mappings. That exact slice is routed; it
  cannot be used to bypass TP/CtrlRAM Legacy Combiner postbuild, and patches,
  fills, other shapes, counts, and ICs retain their existing path.

## Evidence sources

- [Legacy retirement matrix](legacy-retirement-matrix.md)
- [ADR 0023: NT51926 CtrlRAM V2 TP-work boundary](../adr/0023-nt51926-ctrlram-v2-tp-work-boundary.md)
- [ADR 0024: CtrlRAM TP/full-Flash base convergence](../adr/0024-ctrlram-tp-and-flash-base-convergence.md)
- [Supported IC/workflow matrix](../architecture/supported-ic-matrix.md)
- [Integrity processing matrix](../architecture/integrity-processing-matrix.md)
- [NT51926 CtrlRAM V2 candidate](../../profiles/built-in/nt51926-ctrlram-replace-candidate/profiles/nt51926-ctrlram-replace-fw141-cascade.json)
- [NT51926 General Replace DP-only V2 candidate](../../profiles/built-in/nt51926-ctrlram-replace-candidate/profiles/nt51926-general-replace-dp-single-candidate.json)
- [CtrlRAM golden handoff](../../testdata/golden/ctrlram-replace/README.md)
- [v0.9.9 owner CtrlRAM evidence audit](v0.9.9-owner-ctrlram-evidence-audit.md)
- [2026-07-17 owner golden intake](v0.9.9-owner-golden-intake-20260717.md)
- [v0.9.8 code-size policy](v0.9.8-code-size-policy.md)
