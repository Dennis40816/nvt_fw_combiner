# Infrastructure, Contracts, and CRC Worker Core Convergence Ledger

Status: verified review candidate for issue #232. The compatible portfolio,
exact ratchets, narrow evidence, independent R2 reviews, and final canonical
verifier are complete. PR-head CI and merge evidence remain before closure.

## Authority and scope

- Ticket: GitHub issue #232.
- Audited base: `e94752bfbeaecc9d4b5d0ec6b12c89a386526b54`.
- Entry measurement: 7,029 Infrastructure + Contracts + CRC worker nonblank
  runtime lines and 69,038 total runtime lines.
- Mutable production scope: `NvtFwCombiner.Infrastructure`,
  `NvtFwCombiner.Contracts`, and `tools/crc-worker`. Focused caller and
  architecture tests may change; no Domain, Profiles, Application, Bootstrap,
  CLI, or UI production file may change.
- Risk: R2. CRC/header algorithms, processor plans, executable protocols,
  tool identity, firmware ranges, and firmware bytes are R3 and prohibited.

The implementation preserves exact raw-byte SHA admission before parsing,
strict JSON/schema behavior, immutable snapshots, deterministic ordering and
diagnostics, process-tree cleanup, staging isolation, immutable artifacts,
host-side mutation audit, protocol limits, and the CRC-32/MPEG-2 vector. Gross
removal and required additions below are measured from the frozen production
diff; tests are not counted as production reduction.

## Compatible implementation portfolio

| Id | Final owner and callers | Compatibility decision | Gross removal | Required additions | Net | Evidence | Disposition |
| --- | --- | --- | ---: | ---: | ---: | --- | --- |
| I01 | `PinnedJsonCatalogLoader.cs:56-76` at the audited base: zero-caller generic `Load` overload | Delete the unused `JsonTypeInfo<T>` overload. Active catalogs retain their canonical raw-byte hash and strict deserialization paths. | 21 | 0 | -21 | Architecture absence guard; Infrastructure full suite | Implemented |
| I02 | `ExternalCombinerProcessor.Staging.cs:148-163` and `LegacyCombinerPostbuildProcessor.cs:263-278`: duplicate run-directory cleanup | Reuse one Infrastructure cleanup helper from the manifest and legacy Combiner adapters. Preserve recursive cleanup and the exact swallowed `IOException`/`UnauthorizedAccessException` behavior. | 34 | 21 | -13 | Processor failure/cleanup tests; Bootstrap integration smoke | Implemented |
| I03 | `ProfileBundleManifestSchema.cs:19-26`, `ProfileBundlePackageTrustIndexSchema.cs:20-31`, and `SavedCompositionRuleV2Schema.cs:29-40`: embedded-schema loaders | Reuse `ProfileBundleSchemaValidator` as the embedded-resource loader while retaining each resource name, `Lazy` owner, schema id, and failure text. Schema bytes do not change. | 39 | 30 | -9 | Manifest, trust-index, Saved Rule schema tests | Implemented |
| I04 | `SavedRuleDocumentIdentityReader.cs:26-29,57-80`: second duplicate-property walker | Consume `StrictJsonDocumentReader`, the existing strict JSON owner, and delete the recursive mirror. Retain maximum depth, exact rejection, and identity extraction. | 24 | 6 | -18 | Saved Rule identity/storage strict-JSON tests | Implemented |
| I05 | `ProfileBundleManifest.cs`, `ProfileBundlePackageTrustIndex.cs`, `TrustedProfileBundleDocumentProjection.cs`, `ProfileBundleEntrySnapshotCollection.cs:63-75,126`, and `ProfileBundleLoader.cs:86-95`: construction-only/test-only state | Assign the same immutable snapshots directly and remove projections with no production reader. Preserve ordering, read-only exposure, counts, and entry identity. | 29 | 20 | -9 | Bundle snapshot, loader, trust, and projection tests | Implemented |
| I06 | `TrustedProfileBundleDocumentProjection.cs:5-60`: three internal document transport wrappers | Use compact primary-constructor classes with the same signatures, types, properties, references, and class identity semantics. | 35 | 19 | -16 | Projection property/reference tests; architecture guard | Implemented |
| C01 | `ExternalCombinerToolManifest.cs:3-80`: public external Combiner manifest transport | Use a sealed primary-constructor class, not a record. Preserve the exact 14-parameter public constructor, property types/names, collection references, and reference equality. | 47 | 29 | -18 | Application contract 9/9; processor 19/19; Bootstrap 8/8; architecture guard | Implemented |
| W01 | `__main__.py:3,18-27,71-72` and `protocol.py:3,31-37,54-64,125-133,171-179`: package-only protocol plumbing and error bound | Inline one-use helpers, use one flushed JSON print, simplify request-id/base64 handling, and enforce the schema's existing 512-character error-message maximum. Preserve CRC, success envelope, exit/error codes, correlation, payload bound, and one-response stdout. | 21 | 9 | -12 | Worker 30/30; Ruff, Pyright, Pylint; process-bound regression | Implemented |

The frozen production diff removes 250 nonblank lines and adds 134 required
nonblank lines, for an exact slice and runtime reduction of 116.

## Residual candidates and stop decisions

| Id | Candidate | Potential reduction | Why it is not in this compatible batch | Disposition |
| --- | --- | ---: | --- | --- |
| R01 | Merge the manifest Combiner and legacy postbuild adapters into one class | No proven net | They share the process runner and cleanup owner already, but implement different reviewed invocation, multi-command, pasteback, short-output, staged-source, and diagnostic contracts. A common adapter would either reproduce both branches or create common-mode mutation risk. | **STOP:** retain one adapter per executable protocol family; any semantic unification needs separate R2/R3 evidence. |
| R02 | Merge Windows and Unix atomic-write scopes | No proven net | Their security authority is platform-specific: Windows handle/reparse checks and Unix descriptor/openat/renameat checks are not interchangeable boilerplate. | **STOP:** durable platform security boundary. |
| R03 | Delete or reshape public profile/firmware DTO constructors | Up to about 46 | The wire transports and the original 18-parameter composition-profile ABI are explicitly locked for compiled consumers and lossless JSON values. | **STOP:** durable public/binary and serialized contract. |
| R04 | Minify, merge, or delete schema/history files | No runtime benefit | Active manifests pin exact raw bytes and hashes; historical versions remain migration and schema-boundary evidence. | **STOP:** exact trust and compatibility authority. |
| R05 | Remove the CRC worker package because no C# runtime route exists | Apparent 240 | Protocol 1 is an accepted, packaged calculation contract; the catalog intentionally marks it package-only and Protocol 2 not routed. | **STOP:** durable release contract; runtime routing or Protocol 2 is a new separately reviewed capability. |
| R06 | Inline or alter the CRC algorithm/startup vector | At most de minimis | This would touch integrity authority for negligible density gain. | **STOP/R3:** retain `123456789 -> 0x0376E6E7` and the reviewed algorithm implementation. |
| R07 | Delete live registries, routers, runtime-dependency inspection, bundle trust, or bounded caches | No proven net | Each has production callers and owns process dispatch, exact identity, security admission, or bounded optional caching; replacement moves or recreates the same owner. | **STOP:** durable Infrastructure adapter boundary. |
| R08 | Raise the canonical hash allocation micro-test threshold after one CI outlier | 0 | The exact code and test passed locally and in adjacent identical CI runs; the failed 3,720-byte sample was a single profiler/testhost fluctuation and the retry passed without code changes. | **STOP:** do not weaken the performance regression. Reopen only if differential-size evidence shows recurring test-design instability. |

Every remaining item is a durable contract, has no proven compatible net
reduction, would relocate equal logic, or carries disproportionate security,
firmware, compatibility, release, or evidence risk. No safe candidate is
skipped solely because it is small.

The residual caller audit is anchored to the frozen implementation tree:

- R01 covers `ExternalCombinerProcessor.cs:9`,
  `LegacyCombinerPostbuildProcessor.cs:9` plus its staging/results partials,
  and the already shared `SystemExternalProcessRunner.cs:6`. Bootstrap creates
  the two reviewed adapters; all remaining branches carry distinct protocol
  inputs or mutation evidence. Gross removal and replacement cost cannot be
  separated into a positive compatible net.
- R02 covers `WindowsAtomicFileWriteScope.cs:10` and
  `UnixAtomicFileWriteScope.cs:8`. Their only common shape is the required
  `IAtomicFileWriteScope`; platform-native authority and error handling occupy
  the remaining implementations, so compatible net is zero.
- R03 covers `CompositionProfileDocument.cs:29-74` (the explicit legacy
  constructor alone could remove about 46 lines) and the remaining Contracts
  DTOs. `CompositionProfileDocumentTests` locks that binary constructor and
  lossless JSON values; no other DTO has a zero-production/wire caller.
- R04 covers the checked-in schema resources and the package trust index. Their
  exact bytes are selected and hash-pinned by Bootstrap/release policy, so
  source minification has zero runtime benefit and a nonzero trust migration.
- R05/R06 cover `tools/crc-worker/src/nfc_crc_worker/__init__.py:3`,
  `crc.py:3`, and the retained typed request/result fields in `protocol.py`.
  The apparent two-line package-export saving is public compatibility; CRC
  loop/startup changes are R3 and at most de minimis.
- R07 covers live owners `ExternalProcessorRouter.cs:7`,
  `ExternalProcessorRuntimeDependencyInspector.cs:13`,
  `ProfileBundleSnapshotSource.cs:4`, `BuiltInPostbuildProfileCatalog.cs:6`,
  `BuiltInTpFlashMapCatalog.cs:9`, `SavedRuleDocumentStorage.cs:9`,
  `FileArtifactReader.cs:6`, `FileContentSnapshotInspector.cs:11`,
  `OverlayArtifactReader.cs:6`, `SystemRuntimeProbe.cs:8`,
  `JsonSystemDiagnosticsExporter.cs:8`, `SystemClock.cs:6`, and
  `WindowsExplorerFileRevealService.cs:9`. Each either has a production port
  caller, preserves a trust/TOCTOU/OS seam, or would move the same code into
  Bootstrap/#233; no compatible positive reduction was found.
- R08 is `BuiltInPostbuildProfileCatalogTests.cs:46-66`; it changes no
  production metric and adjacent identical CI runners plus attempt 2 passed.

## Frozen measurement and evidence

| Slice | Entry | Frozen batch | Delta |
| --- | ---: | ---: | ---: |
| Domain + Profiles ratchet | 20,287 | 20,287 | 0 |
| Application ratchet | 23,071 | 23,071 | 0 |
| Bootstrap + CLI ratchet | 18,651 | 18,651 | 0 |
| Infrastructure + Contracts + worker ratchet | 7,029 | 6,913 | -116 |
| Runtime total ratchet | 69,038 | 68,922 | -116 |

The four frozen slice values sum exactly to 68,922. No receiving production
slice grows, and the exact slice/runtime ratchets are lowered in this change.

Completed narrow evidence before integrated review:

- Infrastructure: 401/401 PASS with two OS-specific skips.
- Infrastructure-adjacent Bootstrap smoke: 10/10 PASS.
- Contracts/Application manifest contract: 9/9 PASS; focused Infrastructure
  processor/runtime dependency 19/19 and Bootstrap AB 8/8 PASS.
- Architecture: 194/194 PASS after the Infrastructure convergence guards;
  the Contracts structure guard passes independently.
- CRC worker: 30/30 PASS, 100% line and branch coverage, Ruff PASS, Pyright
  zero findings, and Pylint 10.00/10.
- `git diff --check`: PASS for each scoped batch.

Integrated review and structure evidence on the frozen candidate:

- `python scripts/verify.py --structure-only`: PASS at runtime ratchet 68,922
  and Infrastructure + Contracts + worker ratchet 6,913.
- Independent accounting review reproduced 250 removed, 134 added, net -116,
  exact four-slice closure, and no cross-slice production relocation.
- Independent Infrastructure architecture/contract review: PASS, no P0-P3.
  The cleanup guard locks both `finally` placements, recursive deletion, and
  the exact empty `IOException` and `UnauthorizedAccessException` handlers.
- Independent Contracts and CRC worker review: PASS, no P0-P3. The public
  manifest constructor ABI and reference semantics remain exact; Protocol 1.0
  response, correlation, error, limit, and CRC behavior remain compatible.
- Integrated scoped Polytail: PASS, no P0-P3, fake/disabled test, private
  fixture, executable, profile, schema, golden, or suspicious-secret finding.
- `python scripts/verify.py --all`: PASS. Structure, Python, and .NET lanes
  passed; golden regression 17/17, Architecture 194/194, Infrastructure
  401/401 with two OS-specific skips, UI smoke 442/442, and CRC worker 30/30
  with 100% line and branch coverage all passed.

## Protocol and release impact

- CRC worker Protocol remains `1.0` and the package component version remains
  `0.1.0`; the change is an implementation refactor plus conformance to the
  already-published 512-character error bound, not a new wire capability.
- A later product package rebuilds the generated worker executable from this
  reviewed source and therefore records new package hashes/provenance and reruns
  the existing packaged-worker smoke. It must not replace an already-published
  product archive or bypass release/signing policy.
- No support state, IC route, processor id, transform authority, executable
  catalog entry, firmware range, or firmware byte is added or promoted.

Before closure this section records PR head/tree, CI, and merge evidence. No
schema/profile/golden fixture, processor plan, executable catalog, firmware
byte, range, or downstream production slice may change.
