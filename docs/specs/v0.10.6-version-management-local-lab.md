# v0.10.6 local managed-upgrade lab

This document is the normative local-lab companion to the
[v0.10.6 managed version experience](v0.10.6-version-management.md).

The ignored workspace lab `artifacts/version-update-source-lab/` contains only
synthetic/release-safe generated packages and catalog data. Each package puts
the compiled `NvtFwCombiner.ReadyProbe` process behind the production
`NvtFwCombiner.exe` package entry and supplies synthetic closed-package
inventory. The current production launcher is built separately for the managed
root. This combination exercises the real catalog, package-verification,
installation, activation-channel, and launcher adapters without claiming that
the packaged payload is the desktop application.

The environment-bound source-relocation/offline evidence run:

1. generates a fresh managed root containing a trimmed stable launcher, one
   `version-manager.seed.v1.json`, and one verified synthetic `0.10.5` closed
   payload;
2. proves first-run seed import, relocates the identical update source and
   confirms it still works, then proves a second managed root sharing the state
   file is rejected before inventory or mutation;
3. discovers and consents to the synthetic `0.10.6` protocol candidate;
4. stages, promotes, activates, receives the ReadyProbe handshake, and confirms
   the test version identity;
5. disconnects/renames the source and switches offline between installed
   verified versions;
6. tampers with the installed candidate, proves one LKG rollback, deletes the
   damaged version, reinstalls it from the relocated source, and completes an
   offline switch and final deletion.

Separate deterministic adapter and process tests cover corrupt packages, start
failure, ready timeout, no-partial-install cleanup, retention review, damaged
deletion, and protected-path guards. Neither those tests nor this ReadyProbe
lab prove visible desktop startup, Settings UI/accessibility, firmware
Preview/Build/report behavior, CRC-worker integration, or a distributable
application package.

Every lab run must record the env-bound test command and exact catalog/package
SHA-256 values, plus relevant managed-state snapshots, process exits, and
cleanup outcomes. Generated executables, catalogs, packages, and private
firmware remain uncommitted.
