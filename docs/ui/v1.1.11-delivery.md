# 1.1.11 交付紀錄

更新日期：2026-09-25。狀態：929 AB Event Buffer、AB 完全對稱宣告與 1 MiB OSD AB Base 修正已本地實作；已核准的 OSD AB Merge Golden 已登錄及執行。1.1.11 Candidate 版本／release notes 已準備；新 Normal CtrlRAM AB Golden 的 32 bytes backup／CRC 差異依 owner 指示移至 1.1.12 檢查，保留未認證的已知差異；固定來源驗證與封存未完成，尚未正式整合或發布。SVN 共用建模依最新指示移至 1.1.12。
版本分配仍由 [roadmap](../architecture/nfc_roadmap.md) 擁有；
[1.1.10 交付紀錄](v1.1.10-delivery.md) 保留已發布版本的歷史證據。
以下搬入的日期紀錄維持原文；最新狀態以本節為準，不將歷史測試當成新來源通過。

## 最新可完成工作收尾 — 2026-09-25

- `a3c80bef8`：929 AB TP A/B 從既有 canonical General Parameters projector／consensus 取得 read-only Event Buffer，與 AB 格式准入分開。保留同一 accepted bytes、FileStamp、publication、locator 與讀後過期檢查；admitted observation 優先，最多一個 Event，DP 不增加。Application 12/12、Bootstrap 14/14（含下列 layout cases）、UI 19/19；獨立 fixed-head review PASS，無 P0–P2。
- `8e79ce558`：schema 2.17 在既有 AB profile 宣告 `ab.isFullySymmetric`，929 perfect family 三份為 true，950／951 五份（含 inactive Desay）為 false。AB CtrlRAM 從原 AB layout profile 繼承；Application 的唯一 `CanViewIndividualBanks` 決定 toggle、裁切、focus、subtitle 與 view state。950 顯示完整 512 KiB／對應 cascade 容量，切換 IC 或清空不保留舊 bank view。未新增頁面 profile、改 bytes／CRC／地址／support，16 個 policy fingerprint 與 16 個既有 evidence pins 僅同步來源身份。
- Symmetry 局部驗證：normalizer 12/12、schema／2.16 相容 22/22、無宣告但有兩個等長 bank 的負向 1/1、真實 English/Light 與繁中/Dark window 6/6。首輪 schema 測試 fixture 64 KiB 上限不足容納 65,675-byte 新 schema，僅調整 test capture limit，未調整產品 resource limit；首輪跨 IC window 測試未載入新 IC 的 CtrlRAM，補齊 fixture 後通過，沒有因此修改產品 admission。獨立 fixed-head review PASS-WITH-HUMAN-GATE，無 P0–P2；R3／Golden 最終 gate 仍在。
- `261dd1aee`：VERSION、SPEC 與既有 derived version header 對齊 1.1.11；CHANGELOG 完整 Candidate 說明已可由 release-note renderer 產生，1.1.10 頂端狀態校正為已發布，歷史 release section 保留。未修改 tag／CI／發布政策或發布資產。
- 已保存 **14-case／28 張**載入 FW 的 Details 開／關截圖，來源 `261dd1aee`、畫面版本 1.1.11：`D:/NvtFwCombiner-TestArea/evidence/v1111-final-ui-review/index.md`。後續複核發現 927 single 範例漏載 CtrlRAM、AB Merge 展開 viewport 會截掉 TP B Details，因此這批不是最終驗收圖；修復及重拍狀態見下列 `680ded90e`。已確認的 929 Event／950 完整 layout 證據仍保留，截圖不等於 firmware Golden。
- 測試／產物紀錄在 Test Area artifacts：`ab-symmetry-{normalizer,schema,missing,ui}.log`、`ab-symmetry-actual-fingerprints.json`、`v1111-final-screenshots.log`、`v1111-release-notes.md`。Event 三條窄測結果保留於該單元 task 工具紀錄；後續完整驗證另保存固定來源 log。

- `27ca67e3f`：已核准的 `nt51950-ab-osd-d03t02-20260924` 納入既有 canonical／public-host runner，原 1 MiB DP／完整 expected output 保持不變，canonical map 仍為 512 KiB。新案例與既有 shared-TP CLI 完整輸出測試 2/2 通過；Python 317/317、canonical 85、AB fixture 10 均通過；獨立 fixed-head review 無 P0–P2。Release Golden discovery 現有 26 個 Direct 案例，這只是 inventory，仍須在最終 candidate 全部實際執行。未把新案例加入 release redistribution allowlist。
- `680ded90e`：927 single canonical manifest 補齊四個既有 slotId，CMD／截圖沿同一 manifest 載入；NF／VN 沿原 shared slot，不新增 master alias。截圖測試要求每個指定 CtrlRAM 都成功載入，AB Merge viewport 提高到 1600，TP B Details 不再被截掉。927 red 1 → green 1、AB expanded viewport 1、CMD binding/hash 檢查通過；受影響 package/source-pin Python 107 項及正確的 pytest smoke-policy 27 項通過。最初測試命令誤用 module 名／unittest 的 loader 錯誤保留於 log，不算產品失敗或通過。獨立 scoped review 無 P0–P2；兩組修正圖保存在 `evidence/v1111-927-preload-fixed`，後續完整重拍結果見下列 afdcd3d05 截圖紀錄。
- `3bcc1ce01`：補齊既有 Memory Layout endpoint caption 修正的目前整合紀錄，不虛構原先 preflight、不重做已完成修正。`4fe9cb9bb`：依 owner 受限核准修復 VERSION cutover 與 record 真實 Git path／ASCII 大小寫查核；179/179 通過、獨立 fixed-head review 無 P0–P2。這不解除 design-active finalization、Golden 或 R3 gate。
- `afdcd3d05`：1 MiB OSD AB Base 重用既有 captured source envelope，保留完整 Reference 與 canonical A／B bank／512 KiB finalizer，不新增 profile 或 CRC 行為。Bootstrap 101/101、Application Memory Layout 55/55 通過；含 A-only／B-only／Both 真實 Combiner、未選 bank／OSD tail 保留、無 extent／短 Base 拒絕、Memory Layout／Report 實際長度。獨立 fixed-head review 找到一個 P1：1 MiB 多候選的 metadata 預篩可能將損壞 AB 誤降為 Standard；A／B 各五種 metadata 損壞已重現，正在另案修正，原窄測不代表該邊界已通過。其餘範圍無 P0–P2。Logs：`artifacts/v1111-osd-envelope-{bootstrap-focused,application-memory}.log`、`v1111-osd-damaged-route-red.log`。
- `ffd7f0cd9`：依 owner 最新指示，TP Header SVN 共用建模移至 [1.1.12 handoff](../architecture/post-v1.1.8-audit-handoff.md#tp-header-svn-modeling-deferred-to-1112--2026-09-25)，待 Header 內容確認後另行實作。本輪未改 SVN profile／讀寫；文件獨立複審通過。1.1.11 Replace 保留 Base SVN 的既有決定不變。
- 最新已重拍 **14-case／28 張**，來源 `afdcd3d05`：`D:/NvtFwCombiner-TestArea/evidence/v1111-afdcd3d05-ui-review/index.md`，同目錄 `image-hashes.json` 保存來源與 PNG SHA-256。截圖測試 14/14 通過；927 single 4/4、3 IC 8/8 均載入，AB Merge 展開不再裁掉 TP B，Event 在上半部。Root 與唯讀 reviewer 已複核變動圖，10 張與既有已審圖片 SHA 相同；未見新增畫面阻擋。這是健康輸入畫面，不涵蓋上述損壞 AB P1 或 Golden 認證。

- `f89dbaf30`：修正上述 AB 分類 P1，沿用 Profiles 的既有 AB 結構驗證，typed adapter 將結構證據與 native metadata issues 分開交付；移除 classifier 額外 count 預篩。受損的 A／B Backup、雙 bank Backup 或 relocation 保留 AB 終止診斷；單一有效 bank 不足以將非 AB 大檔認成 AB，Number 不決定來源種類。窄測 15/15、相關 Bootstrap CtrlRAM／inspection 207/207 通過；新 record 在產品修改前 stage、base 為封存 `3db701b70`。Fixed-head 獨立 correctness／architecture／Polytail 複審 PASS-WITH-HUMAN-GATE，無 P0–P2；沒有改 CRC／Golden，32-byte CRC 診斷後續依 owner 決定移至 1.1.12，其他最終驗證 gate 保留。Logs：`artifacts/v1111-osd-classifier-green-target.log`、`v1111-osd-classifier-focused-bootstrap.log`。

目前剩餘由 agent 處理：固定來源整合／26 個 Direct Golden 執行、既有 record 的 final ownership／證據封存準備。最新 loaded-FW 截圖已完成，來源與限度如上。新 AB CtrlRAM 實檔已取得，不再泛稱 owner 未提供任何 Golden；其精確未結案事項見下一節。OSD AB Merge 原核准不再列 owner 待辦；尚未覆蓋的其他 family／topology／bank-selection 證據不能由新單一案例自動升格。

### 新 NT51950 Normal CtrlRAM AB Golden — 2026-09-25

以 HackMD API 取得 `51950_ab_ctrlram_replace` 最新 source，經既有 CJK14 工具解密；
archive `51950_ab_replace_ctrlram-20260925T105635.7z`，SHA-256
`4a051d3873a9cf29b2d1e49837eb8c76d01154bdbe90b18e1d7407cf4245bf2a`。
私有 11-file intake／SHA 清單保存在
`D:/NvtFwCombiner-TestArea/evidence/v1111-ab-ctrlram-intake-20260925/intake.json`；
本輪未將這批私有 BIN 入 Git，也未更改上傳 Golden。

Owner 已接受：1 MiB 支援缺口納入 1.1.11；Replace 保留 Base SVN，先用提供的
`nt51950_fw_expected_diff_svn_revision.bin` 經既有 AB Merge 建立 SVN 對齊 Base。
原 DP + changed TP 經 AB Merge 已與 owner 完整 AB expected 逐 byte 相同；
這項結果不代替 CtrlRAM Replace 的完整比較。

修正後完整 Replace Both 執行成功，輸出 1,048,576 bytes：
candidate SHA-256 `1d0ba3e16e767ba90302196c94f3ea230984261c5eb33a3824cc7ebba8382c19`；
owner expected SHA-256 `e58fcdae7eae983a25161aeb7d49d68427c452537537f0cf8f3b4126e2952a6f`。
Normal、SVN 及其餘 bytes 相同，OSD `[0x80000,0x100000)` 原樣保留；
僅差 A bank `[0xA11C,0xA120)`、`[0xA130,0xA134)`、`[0x2D428,0x2D42C)`、
`[0x2D43C,0x2D440)`，以及 B bank 同位置加 `0x40000`，共 32 bytes。
完整結果與範圍在 intake 的 `ab-replace-comparison.json`，CLI report/log 同目錄。

獨立只讀核對：兩份的 A/B 主 DLM／Header CRC 都成立。Backup CRC words 位於 DLM
範圍中，backup 保存值不同會連動主 DLM 與 Header CRC；只在記憶體以 owner backup
兩個 words 代入並重算主 CRC，完整 TP 即與 owner expected 一致。既有兩輪
Header copy → DLM CRC → Header CRC 可完整重現 candidate，但未證實 owner 的生成順序。
實際執行 report 亦顯示每個 bank 呼叫兩次既有 Combiner：先替換／複製 Header 並計算
CRC，再複製 Header 並計算 CRC。每個 bank 的 backup 兩個 CRC words 共 8 bytes，
其差異連動主 DLM CRC 4 bytes，再連動主 Header CRC 4 bytes，合計每 bank 16 bytes、
A／B 共 32 bytes。這解釋差異傳遞，並未證明 Golden 的 backup 初始值／生成順序。
因此**仍是 Candidate，不是與上傳 Golden byte-identical**。Owner 隨後明確要求將
此序列與 firmware 影響檢查移至 [1.1.12 handoff](../architecture/post-v1.1.8-audit-handoff.md#header-backup-crc-investigation-deferred-to-1112--2026-09-25)，
1.1.11 保留此已知差異並繼續發布準備，不重複要求同一延期確認。未改上傳 Golden 或
現行 CRC 行為；不將本例列為完全通過，也不豁免其他 owner-certified Golden／固定來源
CI／結構與發布 gate。後續若接受限定差異，32 bytes 仍須獨立逐 byte 算驗，不能只 mask
或憑主 CRC valid 放行。此 Both 證據不自動認證 A-only／B-only、其他 family 或 topology。

## 最新 owner 驗收清單更正 — 2026-09-25

- **OSD 原人工核准第 3 項已完成**：owner 明確更正「3 已經 OK 了吧 不用再列上來了」。對應既有 `51950_1IC_OSD_AB_Code_Golden.7z` 的 950 1-IC 完整 input/output 案例，沿用[實檔證據](#evidence-2)的 archive／output 身份與完整比較結果。不再要求重新提供檔案或重複核准。認證 metadata 登錄及固定候選版本的驗證／封存由 agent 完成，不列為 owner 待辦；不將此核准延伸成 950 AB CtrlRAM 不同案例的 Golden。
- **Event Buffer 最新呈現預期**：owner 要求上半部主資訊區可見，不在 Details，並明確要求先改 UI。本輪共用 formatter 的移位已完成，保留 `Name (0x??)`、bank 標籤與缺值說明；不在 DP slot 新增 TP 欄位。最新圖與既有資料供應限制見下一節，較早的 Details 圖保留為歷史。

## 2026-09-25 畫面驗收追加修正

### Event Buffer 上半部 UI 修正結果

- 產品只修改 `CreateEventBufferFact` 有值／缺值兩個分支的 Priority：Standard TP/Base、AB TP 與 AB Base 共用 Primary 分類；沒有新增 IC／頁面規則、讀取 offsets、修改 firmware bytes 或格式准入。
- 修改既有回歸後、修產品前：21 項中 13 失敗／8 通過，包含實際 card 在 Details 關閉時的可見性。首輪另有測試 discard 寫法的 IDE0058 編譯錯誤，已修正，未當成產品 red 證據。
- 修後相關 UI **75/75 通過**，包含 English/Light、繁中/Dark 的實際文字可見與正尺寸、DP 排除，以及 IC Count／DP／range 留在 Details。五組實檔截圖 **5/5 通過**；926 Standard、929/950 AB Merge、929/950 AB CtrlRAM 各兩張開／關圖，位於 `D:/NvtFwCombiner-TestArea/evidence/v1111-eventbuffer-primary-review/index.md`，最終 source／PNG hashes 在同目錄 `image-hashes.json`。已目視確認供應的 Event Buffer 位於上半部；950 的 toggle 仍待對稱宣告另案修正。
- **新發現、尚未修復的既有缺口：929 AB Merge TP A/B 無 Event Buffer 值**，上下區域都沒有。`AbMergeAuthoringExperience.Format.cs` 僅在 `family.AbFormatPolicy` 存在時產生格式驗證 observation；`AbMergeAuthoringExperience.InputInspection.cs` 只將該 observation 放入 AB facts。AB input 又未走 Standard 共用 display 供應，因此沒有 policy 的 929 缺值；950 AB Merge 及 929/950 AB Base 已有值且正確移位。這是供應缺口，不是 Priority 回歸，亦不表示五組截圖證明全部欄位供應完備。後續須延伸共用 metadata display facts，不可偽造認證 observation、替 929 加格式 policy，或在 UI 讀 offsets。
- Logs：Test Area `artifacts/eventbuffer-primary-red.log`（測試編譯問題）、`eventbuffer-primary-red-behavior.log`、`eventbuffer-primary-green.log`、`eventbuffer-primary-screenshots.log`。本輪為局部 UI／文件修正，沒有重跑無變更的全套或宣稱正式整合通過。

### 本輪移位准入紀錄

本輪 Event Buffer 主資訊區修正准入（來源 `630618fa6`）：owner 已明確要求先改 UI。
沿用 `UiCompositionRunner.CreateEventBufferFact` 的唯一共用呈現 owner，僅將有值及
缺值分支改成 Primary，保留資料、名稱、bank 標籤與缺值說明。此為 bounded local R1，
不改 metadata／profile／Build／firmware bytes。產品路徑僅
`src/NvtFwCombiner.Presentation.Avalonia/UiCompositionRunner.FirmwareFacts.cs`；測試限定
`CtrlRamBaseFactsTests.cs`、`AbDpMetadataTests.cs`、`ShellViewModelTests.FirmwareSlots.cs`、
`XamlControlStyleContractTests.ApprovedInfoLayout.cs`。驗收為 Standard TP、AB TP A/B、
Standard/AB Base 在 Details 關閉時可見 Event Buffer，DP 不新增該欄位，IC Count 與
DP／range 細項留在 Details；中英文、Light/Dark 與實檔截圖驗證。Roadmap 舊指示另依
文件 R2 准入同步；既有整合／AB 證據 gate 不因局部 UI 修正解除。

- 共用 Event Buffer：既有 canonical General Parameters facts 加入 optional display byte；Standard／Replace／AB bank 沿用同一 projector，取消 display 對各 IC profile fieldIds 的依賴，沒有新增 raw offset、修改 profile、AB 格式准入或 firmware bytes。width／encoding／範圍與 consensus provenance 保留。舊主要 FWConfig legacy reader 尚在，本次不是所有 metadata 全面改為單次解析。
- 新增 10 IC × 三種 byte（0、0xA3、未知 0x7F）共用供應測試；正確使用完整 declared capacity 與 compiler 輸入後通過。初次 red 中包含測試 fixture 長度／編譯輸入不完整，不能把全部失敗都算成產品缺陷；獨立「缺少 field target」case 已重現原產品錯誤。Bootstrap 72/72、Application 30/30 通過，包含 NT51926 canonical 真實 TP snapshot（Event 0x82、DP 不呈現 TP Event）。獨立 R2／Polytail review PASS。
- 950 AB Base 額外缺值已以實檔重現（預期 0x84，原為 null）：沒有 Standard exact 編譯時，改由既有 full-image metadata-only authority 提供 bank 的 common facts。929 沿用已驗證的 Standard exact plan（其 full-image view 只有 DPCMI）；先選 authority 再解析，讀取失敗不換來源重試。AB eligibility 與 Build 行為未修改。
- AB Base Info 保持 TP 主資訊，Details 順序改為 IC Count、Event Buffer A／B、DP version／Jira、bank ranges。中英文排序 regression 修前 2 失敗，相關 UI 修後 28/28 通過。
- Memory Layout 藍線文字：初次 Arrange 時父 Bounds 尚為零，子文字被排成零寬；改用本次 Arrange 的實際 rail width。首次載入渲染 regression 修前 3/3 失敗，修後連同 geometry／popup 103/103 通過。既有短線「•」仍是 Base／Other 中性標記。
- Full-image 額外負向與既有 metadata 測試 30/30 通過；最新 926 Standard、929 AB Replace、950 AB Replace 截圖 3/3 通過（6 張 Details 開關圖），位於 `D:/NvtFwCombiner-TestArea/evidence/v1111-common-info-label-review`。已目視確認 926 Common (0x82)、929/950 AB Common (0x84)、TP Details 排序及 Master 藍線文字。950 toggle 尚未按下列新契約修改。
- 局部 log 位於 Test Area artifacts：eventbuffer-common-green.log、eventbuffer-projector-green.log、eventbuffer-926-real-green.log、ab-info-order-green.log、endpoint-label-green.log。既有 b28 完整驗證只適用其固定來源，不代表本追加修正重新跑完全套。

### 已接受、待獨立實作：AB Code 完全對稱宣告

- AB 支援沿用 trusted capability；不另增 supportsAb 布林值。AB profile 應明確宣告 isFullySymmetric（AB Code 是否完全對稱），929 perfect family 為 true，950／951 為 false；不修改 bank bytes／地址。
- toggle、overview 裁切、focus lane 篩選、subtitle 與 view state 必須共用同一 typed Application decision。不得依 IC 名稱、family 名稱、bank 大小或 byte 幾何自行推定是否完全對稱；缺宣告不啟用對稱功能，無效值拒絕。
- 目前 UI 僅以 Banks.Count==2 啟用 toggle／裁切，尚未實現此宣告。僅藏 toggle 不足以修正；此項另立 schema/profile 契約單元，不混入共用 Event Buffer 准入紀錄。

## 最新整合修正

- 固定來源 `ec0093089` 的非 structure 驗證已結束：.NET 7,500 項中 7,483 通過、17 失敗；另有 release package、release smoke、sync-derived 三條 Python lane 失敗。該輪不能記為全套通過。
- Hex Editor 成功載入 bytes 後 UI 可能因取消／後續失敗而顯示舊檔名：實際 adapter 四情境先重現 3 失敗／1 通過；`c40267704` 修正後相關 20/20 通過，獨立 scoped R2／Polytail PASS，無額外人工 gate。
- `45f324f16`：Merge 選單維持同一個 observable collection，修正 AB 選擇被重設；更新已核准的 Event Buffer Details／簡潔名稱與共用 owner 斷言後，479/479 相關 UI 測試通過。這包含前輪 15 項 UI 失敗；並未變更 layout 預期範圍或 Golden bytes。
- `b75a9b74e` 已同步三處 policy hash；相關 Python 134 項通過（132 項在等同該提交的工作區，另 2 項從 HEAD 建立快照，提交後重跑通過）。固定來源完整 UI **1,645/1,645**、Hex adapter **14/14** 通過；Architecture 兩項定向檢查通過。未把這些結果稱為全套 `verify --all` 通過。
- 固定產品來源 `b75a9b74e` 的 14-case／28 張截圖經複核無功能版面退步；其中 926 關閉 Details 圖曾拍到 80% 啟動列。截圖測試改為在既有 45 秒內等待 optional preload 完成，重拍 **14/14 通過**，926 畫面已確認沒有啟動列。最新索引與 SHA-256 位於 `D:/NvtFwCombiner-TestArea/evidence/v1111-settled-ui-review/index.md`；950 AB CtrlRAM 仍標示 Evidence open，DP／TP／DP 已呈現。
- `b75a9b74e` 固定來源的 Structure 檢查列出六份 design-active 紀錄待封存及三個 governed path 重疊。已在外部 `artifacts/v1111-final-integration-plan.json` 準備完整且唯一的歸屬及 path digest（現行 `b28c1f3c5` 為七份紀錄、41 個 governed path）；原始准入欄位保持不變。不得在 AB R3 owner／獨立 Golden 缺件時宣稱正式封存或整合通過；本次操作 skill／範例狀態校正新增一份獨立 R2 紀錄，亦待封存。
- 最終固定來源 **`b28c1f3c51bdbd55c3ca21bf73af5f340e8de3c2`** 執行 `python scripts/verify.py --skip-structure`：**PASS**。八個 .NET 專案 **7,504/7,504** 通過、0 失敗／0 略過；其餘 **38 條 Python／repository-script lanes 全部 PASS**。.NET coverage lines 91.65%、branches 80.56%；CRC worker Python lines／branches 均 100%。完整 log：Test Area `artifacts/v1111-b28c1f3c5-integration.log`；八份 TRX 雜湊與計數：`artifacts/v1111-b28c1f3c5-dotnet-summary.json`。
- 上述明確排除 Structure；它仍需要七份紀錄的最終封存及 R3 外部 authority，**不是 `verify --all` 通過、不是 1.1.11 整合／發布核准**。局部複審及固定來源技術整合審查均已完成，未留下產品 P0/P1 技術發現；不把這些結果代替 firmware-owner／獨立 Golden。
- 驗證器已自行移除本輪 session `s-67ipcgqdr37maakskai4ph3vti`，確認路徑不存在；必要 log、TRX／coverage 和待 owner 查看圖片保留。D 槽可用約 40.68 GiB。
- Test Area 每輪驗證收尾清理可重建輸出，保留原始 FW／Golden、必要診斷與待確認截圖。2026-09-25 的舊 session 快取刪除被自動審批拒絕（僅回報 blocked by policy），未執行，亦未改道繞過。

## 本輪交付後需要 owner 的事項

1. **畫面驗收**：Event Buffer 依最新預期移到上半部主資訊區、950／951 對稱宣告及 layout 修正完成後，由 agent 更新截圖供最終驗收。確認 slot 資訊順序、預設可見的 Event Buffer、950 完整 DP／TP layout 與 Customized 入口隱藏；既有截圖只供已完成部分檢視，不要求 owner 核准已知尚未符合的新要求。
2. **AB firmware 證據**：firmware owner 審閱 929 A-only／B-only／Both 的完整 oracle 結果、Header／CRC、未選 bank 與寫入範圍，以及 950 family 實檔 Candidate。未覆蓋路由仍須相應獨立預期輸出／認證證據，不能只靠同一程式結果升格。
3. **正式整合的 owner 簽核**：以上證據及 agent 的剩餘修正／驗證就緒後，再呈交固定候選版本所需的 R3 firmware／release authority；紀錄整理及封存由 agent 處理，不要求 owner 重複批准已確認的 OSD 案例。此項不等於授權發布 1.1.11，目前不建立其 release。

這些是尚未滿足的人工／外部 authority，不是要求再次批准已完成的本地實作。若 owner 檢核產生修改，針對新差異驗證；無新差異不重跑已通過的整套。

## 操作入口與證據位置

- 已備妥 `open-firmware-example`、`capture-firmware-ui`、`locate-golden-evidence` 三份 repo skill。前兩份的 950 舊狀態說明及 CMD evidence 訊息已校正，案例 ID 保持相容；skill inventory **23/23** 通過，CMD dry-run 的載入參數保持一致。
- 在專案根目錄執行 `cmd /c scripts\open-golden-example.cmd nt51926-gen-flash` 可開啟載入 DP／TP 的 926 Standard Merge；`--list` 列出其他案例，`--dry-run` 僅列印命令。
- Canonical Golden 目錄：`C:/Dennis/dev/work/novatek/nvt_fw_combiner/testdata/golden/canonical`。先讀各 case 的 `provenance/case.json`，區分完整預期輸出、限定差異及 input-only。
- 私有實檔／候選執行紀錄在 `D:/NvtFwCombiner-TestArea/evidence`；AB CtrlRAM Candidate 不等於獨立 Golden。

## 各單元歷史證據

<a id="evidence-1"></a>

## 1.1.11 AB Memory Layout 本地修正 — 2026-09-25

本地整體狀態：F04/F05、共享 Info、Event Buffer compact／range、Customized UI 隱藏及 AB DP 顯示均已分單元提交；Flash BIN 六組完整輸出、OSD 完整輸出及 AB Candidate 實檔證據已保存。尚待目前固定來源的整合測試、整合紀錄／獨立複審收尾及下列人工驗收，未發布 1.1.11。

真正需要 owner 的確認：

1. 審閱本節連結的 14-mode Details 開／關截圖，確認各 slot 的資訊順序、DP／TP layout 及 Customized 入口隱藏符合預期。
2. Firmware owner 審閱 NT51929 A/B/Both 獨立 oracle 候選與 NT51950 實檔 Candidate 的 Header／CRC／未選 bank／精確寫入範圍；目前 Candidate 通過不等於各 route 已有獨立 Golden。
3. 確認 OSD 上傳的完整預期輸出是否正式認證為該案例 Golden，並留下認證來源；目前完整 bytes 相符，但不自行代簽。

這是交付驗收清單，不是要求再次核准已授權的本地修正。全套自動驗證與文件整理仍由 agent 完成。

- 本地 commit `25a697940`：AB CtrlRAM 重用既有 Standard companion map，950 overview 正確顯示 DP／TP／DP；兩個 bank（含未選 bank）沿用 canonical placement。沒有新增 profile、IC 判斷或執行路徑。
- 新增 950 A/B/Both 回歸先得到 3 個預期失敗；修正後擴充組合 33/33 通過（含 950/951 single/cascade 顯示、原有 929 與三個既有實檔執行案例）。再補同容量錯誤 TP 幾何及錯誤容量拒絕案例，相關 3/3 通過。
- 六條既有路由只同步顯示引用 fingerprint、policy hash pin 及 manifest routeEvidence pin；完整 JSON 差異核對確認未改 expected output hash、decision 值／來源或證據分級。
- 獨立 scoped Polytail `/root/ab_layout_context_review` 未發現 P0–P2；R3 owner／獨立 Golden／最終固定來源整合 gate 仍待完成，不把本地通過視為正式認證。
- 本次截圖測試 14/14 通過，28 張 Details 開／關圖片保存於 `D:/NvtFwCombiner-TestArea/evidence/v1111-ab-dp-review-20260925`；已目視確認 950 AB CtrlRAM 展開圖的 DP／TP／DP。檔名 `nt51950-ab-ctrlram-unsupported` 是歷史 case ID，當前畫面可接受且仍標示 Evidence open；app 版本標籤尚未升至 1.1.11。
- 測試紀錄在 Test Area `artifacts/v1111-ab-dp-red.log`、`v1111-ab-dp-extended.log`、`v1111-ab-context-rejection.log`、`v1111-ab-dp-screenshots.log`。保留報告與待檢核截圖，測試 workspace 自行清理。


<a id="evidence-2"></a>

### 1.1.11 實檔驗證進展 — 2026-09-25

來源 `282ab7f4c` 執行 `python tests/scripts/nt51929_ab_ctrlram_oracle.py --fresh`：
A-only／B-only／Both 三個 NT51929 實檔 Candidate 均完整符合獨立 Header／CRC oracle。
此 oracle 限固定案例；仍需 firmware-owner review，未提升為 owner-certified Golden。
新證據：Test Area `evidence/v1110-ab-full-input-20260924T190434Z-85f287a9/oracle-report.json`，
執行 log：`artifacts/v1111-ab-oracle-fresh.log`。目錄的 v1110 是既有工具前綴，來源為上述當前 commit。

已檢查 owner 提供的 `51950_1IC_OSD_AB_Code_Golden.7z`，archive SHA-256
`d7fa71719e0ed16fbfdddc774c3ed573ebf191d407f4ed21587e0ae1107989ce`。
私有 intake 位於 `evidence/v1111-osd-intake-20260925`，三個 BIN 的大小／雜湊見 `intake.json`。
當前 CLI AB Merge 使用原 DP 與同一 TP A/B 成功輸出 1,048,576 bytes，
逐 byte 等於 owner 提供的完整 FlashCode，SHA-256
`71de58a5f9a2ca2cb0d51789794af64586435b09e4ca61d176b6cd41b4990136`。
`DP_NONSTANDARD_SIZE_WARNING` 正確保留整個 1 MiB DP；比較與報告見 `comparison.json`／`candidate-report.json`。
此為 owner-supplied complete-output parity；尚未修改 canonical manifest 或認證 metadata。

Flash BIN 輸入實測六組全數成功且完整輸出相同：950 OSD AB 分別以完整 FlashCode
替代 DP_AB、TPA、TPB；926／927／929 Standard 分別以既有 Golden FlashCode 作 TP 輸入。
證據在 `evidence/v1111-flash-input-20260925` 的 `comparison.json`、`run.json` 與各 CLI report／log。
六個重複輸出 BIN 已在完整比較及雜湊複核後刪除；保留原始 FW 與必要小型紀錄。
結論限這六組 fixture，不推定所有 Flash BIN 組合或擴張 firmware 支援範圍。

另以 NT51950 BOE AB canonical Base 與 Standard postbuild NF CtrlRAM 執行當前 CLI Replace：
成功輸出 524,288 bytes，SHA-256 `bf05bfa3b7652d4183a69862619957d1e5b3c8753c22ef31ac3eaa245f174b85`。
證據 `evidence/v1111-950-ab-ctrlram-20260925/report.json`／`execution.log` 與 Candidate BIN；
此為實際雙 bank 執行成功，尚無此組合的獨立完整 Golden，不宣稱 byte parity。


<a id="evidence-3"></a>

### 1.1.11 Output picker local correction — 2026-09-25

`282ab7f4c` 補 Hex native picker 的選取世代與 panel context：新載入、
DataContext／visual tree 離開再返回均拒收過期 picker。延伸既有 F05 載入順序保護，
紅測新增案例失敗／原 2 件通過；綠測 4/4 通過。
紀錄 `v1111-f04-hex-picker-{red,green}.log`；Scoped local R1 Polytail：PASS。
Settings update-source picker 的 cancel/reopen 漏洞亦已補正：由 Settings draft 擁有 browse generation，
MainWindow 回傳時驗證 instance／modal／generation，draft 編輯、退出與 modal 關閉使舊結果失效。
新增兩件 stale 回歸及既有四件正常互動共 6/6 通過；紀錄 `v1111-update-source-final-green.log`，
原失敗紀錄為 `v1111-f04-update-source-red.log`。Scoped local R1 Polytail：PASS。
Toolchain picker 原有 operation generation／modal guard 已覆蓋；未重寫該流程。


<a id="evidence-4"></a>

### 1.1.11 Loaded-FW screenshots — 2026-09-25

來源 `282ab7f4c` 的實際 UI assembly 以 `ReleaseExampleScreenshots` 執行 14/14 通過，
產生 28 張 Details 開／關截圖，位於 Test Area `evidence/v1111-info-review-20260925/`；
`index.md` 提供每組連結、`image-hashes.json` 固定圖片雜湊，log 為 artifacts `v1111-info-screenshots.log`。
仍顯示 1.1.10 版本號（1.1.11 尚未 bump／發布），AB CtrlRAM 保留 Evidence open。
既有 case ID `nt51950-ab-ctrlram-unsupported` 已過時：目前畫面可辨識 AB FlashCode，
不能再把檔名當成當前 unsupported 證據。
本次視覺檢查確認 NT51950 AB Merge 的 DP AB 區段／legend 存在；
NT51950 AB CtrlRAM Replace overview 的部分區域仍呈 Unmapped，需沿 canonical map／投影追查。
14 項通過證明載入與截圖產生，不代表全部畫面已完成人工驗收或 firmware bytes 認證。


<a id="evidence-5"></a>

### 1.1.11 Firmware and Output picker evidence — 2026-09-25

Firmware／General mapping picker 同時完成本地修正：selection lease 在開啟 picker 時
捕捉既有 workflow context 與目標版本，切頁／mode／IC／count 往返、清除、較新選檔及
Same TP 連結／取消連結均使舊結果失效。共用 `SetAbSameTpFileAsync` 在更新雙槽前
推進兩個目標版本，不靠 FilePath 字串是否變更判定。
`v1111-f04-sametp-red.log`：21 件中新增 Same TP 案例失敗、其餘 20 通過；
`v1111-f04-sametp-green.log`：21/21 通過。Scoped local R1 Polytail：PASS。
這批測試使用的 TempWorkspace 已自行清除；Hex／Settings 等其餘 F04 入口仍待檢核。

Local commit `58cb18fbd` 重用 Output confirmation 原有 preparation generation，
在主輸出、附加輸出與 bundle parent directory picker 回傳後檢查同一個視窗世代。
取消或重新開啟確認視窗會使舊結果失效，不執行輸出，也不覆寫新視窗的資料夾。
資料夾案例修正前 2 failed；修正後相關 Output confirmation／rename 選取共 35 passed、0 skipped。
紀錄：test-area artifacts `v1111-parent-picker-red.log`、`v1111-output-picker-green.log`。
Scoped local R1 Polytail：PASS；generation 仍由原 ViewModel 擁有，未增加 firmware 或輸出命名判斷。
這是本地單元完成證據，F04 其他 picker 與整合 gate 尚未宣稱完成。


<a id="evidence-6"></a>

### 1.1.11 Compact TP facts local correction — 2026-09-25

後續共用 Info 單元已集中至 `FirmwareInspectionProjection.GetFirmwareFacts`：載入、
AB 設定重套與語言切換使用同一個 slot-role／typed inspection 投影，無 IC 名稱分支。
DP_AB 不再附加 TP PID／Common FW／IC Count；一般 TP 保留 TP 事實；
Base 使用既有 typed Base／bank facts，DP 在 Details。語言切換保留 Details 展開狀態。
DP 混入 TP 資料的回歸修正前 1 failed；定向測試修正後 27 passed。
共用 consumer 較廣選取的 191 件通過，另 1 件新測試的建構資料錯誤已修正並包含於上述 27 件；
此紀錄不宣稱重新執行完整 192 件。紀錄為 `v1111-slot-info-{red,green,targeted-green}.log`。
Scoped local R1 Polytail：PASS；未變更 Application 的分類、bank 選擇、profile 或輸出 bytes。
各 mode 實檔截圖與 Flash BIN 接受範圍仍待整合驗證。

Local commit `f8a7aae00` 將 Event Buffer 投影共用為 `Name (0xXX)`，Common 使用短名稱，
固定放 Details；移除 AB Base 的 FWConfig range 顯示，保留 bank ranges。
`v1111-picker-info-green.log` 的 78 passed 含 CtrlRamBaseFacts／AbDpMetadata 相關案例。
此單元不代表 slot-role／input-kind 共用 Info 投影已完整實現。


<a id="evidence-7"></a>

### 1.1.11 F04/F05 初步診斷 — 2026-09-25

此為來源檢視，不是已重現或已修復。1.1.10 發布來源 `a90c640f2` 的產品
內容與本次檢視的原 checkout 相同；新規劃文件不在發布來源內。

- F05：`RawBinaryEditorFileSession.LoadAsync` 於 await reader 後直接呼叫
  `_editor.Load` 並更新 `SourcePath`；`HexEditorWorkspaceViewModel.Persistence`
  同樣直接接受 Load 結果。尚未看到載入 generation／接受 token 的卡控。
  後續測試須控制先發 A、後發 B、B 先完成、A 後完成，核對 editor bytes、
  file-session path 與 UI 三者一致；也覆蓋新讀取失敗保留先前已接受文件。
- F04：`GeneralMappingRow` picker 回傳後透過當時的 `WorkflowSession`
  設定原 mapping ID；Hex picker／drop 都可呼叫 Load；Output Delivery 的
  picker await 前後讀取可變 viewModel。這些是待測入口，不能只憑 await
  斷定已有缺陷。先控制 Cancel／Reopen、slot 或 context 改變與延遲回傳，
  檢查既有 Application acceptance 是否已拒絕舊請求，再只修真實缺口。


<a id="evidence-8"></a>

### 1.1.11 F05 local correction — 2026-09-25

Source base `a90c640f2`，本地修正 commit `9d607860c`，分支
`feature/1.1.11/reliability-and-info`；不屬於 1.1.10 發布來源。
Owner 的 1.1.11 完成目標授權此 bounded R1 correction。既有 owner 為
`RawBinaryEditorFileSession` 的文件提交及 `HexEditorWorkspaceViewModel.Persistence`
的畫面接受；mutable surfaces 僅這兩個 production files 及新增的
`WorkbenchRawBinaryEditorLoadTests`／`HexEditorLoadOrderingTests`。

- adapter 在同一 lock 內比較 load generation 並提交 bytes/path；取消或
  過期讀取不能覆蓋文件。ViewModel 忽略過期結果，避免舊錯誤覆蓋新狀態。
- 測試重現：adapter 兩例、UI 兩例先失敗；修正後 Bootstrap raw editor
  14/14、UI Hex／pending-load 32/32 通過。另涵蓋最新選取失敗時保留
  accepted bytes/path 並拒絕較舊的 pending read。
- 執行：`dotnet test`，Bootstrap filter `FullyQualifiedName~WorkbenchRawBinaryEditor`；
  UiSmoke filter `FullyQualifiedName~HexEditorLoadOrderingTests|FullyQualifiedName~RunAndHexEditorTests|FullyQualifiedName~PendingHexLoadAndSettings`。
  logs 位於 test-area artifacts 的 `v1111-f05-{red,ui-red,bootstrap-green,ui-green}.log`。
- Scoped local Polytail：PASS；原始檔案讀取仍經既有 FileArtifactReader，
  無公開 port、firmware、profile 或支援狀態變更；`git diff --check` 通過。
  這不是 1.1.11 整合或發布認證；final path ownership、固定整合來源驗證
  及 F04 picker/context 修正仍待完成。


<a id="evidence-9"></a>

### 1.1.11 Customized UI 隱藏 — 2026-09-25

Local commit `471d782bc` 延伸既有 `WorkflowModeDisplayConverters` 的顯示責任，
以單一 `CustomizedEntriesVisible` 規則隱藏 Home 兩個入口與普通模式選單。
canonical mode choices、指令、底層執行、草稿與歷史相容性保留；不新增
firmware 支援判斷，也不依版本號自動重新開放。直接啟動／CLI 是否另行限制
仍可由 owner 決定，不作為此已授權 UI-hide 單元的阻擋。

R1 mutable surfaces：既有 display helper、Merge/Replace State 的 visible choices、
MainWindow mode bindings、Home page template，以及原有 Home geometry／mode-binding
tests。正式 Home 模板先重現仍有六張卡的失敗，再驗證只呈現四張；真實
Merge/Replace selectors 驗證隐藏 Customized 且 retained canonical choices 不變。
Home geometry、ModeSelectorBinding、CanonicalCatalogRefresh 共 20/20 PASS；
logs：`v1111-hide-customized-red.log`／`v1111-hide-customized-green.log`。
一次測試資料修正：Replace 保留相容性斷言改用既有支援 General 的 NT51926，
不把 NT51950 原本未提供的 General route 當成應存在的能力。
Scoped local Polytail：PASS，`git diff --check` 通過；全版整合及 owner 截圖驗收仍待完成。
