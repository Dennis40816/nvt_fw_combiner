# v1.1.x CtrlRAM selector visual contract

Status: owner-approved reference; local implementation in progress.

Target: [the consolidated `v1.1.4` UI milestone](../architecture/nfc_roadmap.md#114-ui-corrections-and-ab-dummy-dp).
The owner approved the second actual-baseline preview on 2026-09-06, followed
by the title amendment below. Approval does not claim implementation or tests.

## Actual-baseline preview — 2026-09-06

Owner requested the next screenshot-based preview after the exit-confirmation
slice. The following reference is **owner-approved, not yet fully implemented**:

- [Actual published v1.1.3 screenshot](references/v1.1.3-ctrlram-selector-baseline.jpg).
- [Edited proposal, second revision](references/v1.1.4-ctrlram-selector-proposed.png).

The baseline was captured from the actual Windows portable `v1.1.3` app,
not the development executable: `NT51929`, `2–8 IC`, CtrlRAM Replace,
Light/English, 1920 × 1032 full window, Cascade and Common expanded. All inputs
are empty. This deliberately shows the real two-line Base title and nested
group outlines without inventing selected files, inspection results or
firmware metadata. It is not the verified-input/compact acceptance state below.
The previous AB baseline window and its selected file were preserved; a
separate clean baseline window was opened for this inspection.

Built-in `imagegen` edited this screenshot with the following prompt contract:
preserve the complete shell, right Output layout, exact texts/icons, input
state and controls; align Base/Cascade/Common outer edges; remove only the
intermediate group-content border; retain the outer section and individual
input borders; give the complete wrapped Base title a 10 px gap before its
badge, 12 px before following text, and consistent card padding. Revision two
used a single corrective prompt: preserve everything in revision one and move
Common down enough to separate its outline from Cascade by 12 px; allow the
lower content to clip naturally at the existing viewport.

The generated raster conveys the proposed arrangement, not measured production
pixels or hover behavior. Existing numeric geometry, keyboard, theme, scaling,
long-filename and viewport acceptance below remain authoritative for the later
implementation. No production code or firmware behavior changed in this
preview step; no product test run was needed.

### Accepted title amendment — 2026-09-06

Use `Base firmware` / `基底韌體` as the primary CtrlRAM Base heading and
`FlashCode / TP FW` as a separate secondary-color line, with a 6–8 px gap.
This replaces the awkward automatic split of the parenthesized full title.
Retain complete type wording, normal font sizes, shared card/fact columns,
actions and all firmware behavior. Other mode labels remain unchanged.
Mode and language changes must update or clear the subtitle through the
existing localization projection. This explicit exception changes only the
Base heading hierarchy; the approved alignment/border/spacing direction and
its remaining acceptance evidence are retained.

### Local title slice evidence — 2026-09-06

The heading amendment is locally implemented on branch `1.1.4` (uncommitted;
not integrated or published). [Actual full-window capture](references/v1.1.4-ctrlram-heading-actual.jpg)
uses the development build, Light/English, NT51929 `2–8 IC`, all files empty,
1920 × 1032. The retained `1.1.3 desktop` shell label is not a publication claim.
The heading is one primary line and one secondary line; existing facts,
actions and groups retain their layout. Shared consumers such as Browse and
the missing-input summary use the same shorter slot title; their existing
action, required-input explanation and Build blocking remain unchanged.

- Red: `ReplaceBaseSlotNamesAcceptedFirmwareKindsWithoutBadges` fails against
  the old parenthesized title (`ctrlram-title-red.trx`).
- Final narrow run: **103 passed, 0 failed, 0 skipped, 22 seconds** with
  `dotnet test tests/NvtFwCombiner.UiSmoke.Tests/NvtFwCombiner.UiSmoke.Tests.csproj --no-restore`
  and filter `FullyQualifiedName~CtrlRamHeadingUsesCompletePrimaryAndSecondaryLines|FullyQualifiedName~CtrlRamWorkflowTests|FullyQualifiedName~FirmwareSlot|FullyQualifiedName~IssueCards|FullyQualifiedName~AbDpMetadata`.
  Evidence: `D:/NvtFwCombiner-TestArea/evidence/v114-ctrlram-selector/ctrlram-title-acceptance-final.trx`.
- Eight rendered heading cases cover 480/900 px cards, English/Traditional
  Chinese and Light/Dark. They measure aligned complete single-line headings,
  6–8 px title/subtitle separation, no Browse overlap, secondary color,
  accessible action naming, selected-file persistence and subtitle clearing.
  Existing workflow regression covers CtrlRAM → DP → General → CtrlRAM and
  live language changes. Earlier intermediate runs exposed stale title
  assertions and one incorrect new test assumption about the unchanged
  missing-input detail; those failed TRX files are retained, not fresh passes.
- Desktop build: **0 warnings, 0 errors**. Primary-agent scoped R1 Polytail:
  **PASS** for this title slice after inspecting projection callers, exact
  changed hunks and rendered evidence; no firmware/profile or execution edits.

Remaining: the group anchors, intermediate-outline removal, hover-invariant
geometry, full spacing correction, long-filename and complete viewport/theme/
scaling matrix below are **not implemented or certified by this title slice**.
No full verifier, release Golden execution, commit or integration is claimed.

### Accepted empty-input guidance amendment — 2026-09-06

The owner supplied the cluttered DiffDLM/Normal guidance screenshot, removed
the suggested filename from the desired content, and chose `Max Size` and
`Target Addr`. Single-section input guidance now uses two aligned columns:
92 px labels, 12 px column gap, complete byte values with thousands separators
and a nonbreaking space before `B`, and hexadecimal target addresses. Chinese
labels are `大小上限` and `目標位址`. `Target Addr` is the typed target offset in
the output image, not an inferred CPU address. Do not repeat the slot name,
suggested filename or arrow in this compact guidance. Actual selected
filenames remain visible. Shared inputs retain their physical target count;
DiffNFMerge prerequisites and the complete selection/report mapping detail
remain available through their existing owners.

### Local geometry and guidance evidence — 2026-09-06

Locally changed, uncommitted on `1.1.4`; not integrated or published.
[Actual desktop capture](references/v1.1.4-ctrlram-guidance-actual.jpg):
1920 × 1032, Light/English, NT51929 `2–8 IC`, all inputs empty, expanded
Cascade/Common. The shell still says `1.1.3 desktop`; this is a development
build, not a release claim. The shared card, group outline and selector
anchors are reused; no Application/profile/firmware behavior changed.

- Geometry red evidence exposed a 4 px Base/group anchor mismatch, a zero
  wrapped-title/badge gap, and Fluent `ExpanderContent`'s extra `1,0,1,1`
  border. The shared styles now remove that intermediate border, align the
  outer selectors, reserve focus geometry, and use 12 px group gaps.
- Guidance red: **6 failed** against old cluttered descriptions
  (`guidance-red.trx`). New measured real-card cases verify EN/zh-TW,
  480/900 px, varying sizes/addresses, complete single-line values and equal
  value-column X positions. Additional cases retain shared prerequisites,
  missing-facts fallback and full technical mapping detail.
- Final relevant shared regression: **186 passed, 0 failed, 0 skipped,
  40 seconds**, `guidance-shared-acceptance.trx`, under
  `D:/NvtFwCombiner-TestArea/evidence/v114-ctrlram-selector`.
  Command: `dotnet test tests/NvtFwCombiner.UiSmoke.Tests/NvtFwCombiner.UiSmoke.Tests.csproj --no-restore`
  with filter `FullyQualifiedName~CtrlRam|FullyQualifiedName~FirmwareSlot|FullyQualifiedName~SelectorWrapped|FullyQualifiedName~AbDpMetadata|FullyQualifiedName~ModeSelectorBinding|FullyQualifiedName~IssueCard`.
  The intermediate 183/186 run retained three old-description assertions;
  they were updated to the approved wording or original typed facts, retaining
  IC/mode isolation and exact technical-report checks. Failures remain in TRX.
- Desktop build: **0 warnings, 0 errors**, 10.64 seconds. Terra/high read-only
  scoped review found no new correctness issue in the guidance delta; primary
  inspection confirmed the actual aligned labels/values and retained controls.
  Scoped guidance Polytail: **PASS**. No full verifier or release Golden run
  is claimed for this development slice.

The broader selector acceptance remains **open**, not a full visual-contract
pass: High Contrast is not implemented in the current Light/Dark theme system;
genuine 125% scaling is unverified. The text amendment does not waive these
requirements or authorize a new theme. The selected-state and stronger hover
evidence gaps were subsequently closed by the re-review below.

### Local logic re-review — 2026-09-06

Owner accepted the actual text arrangement and requested another overall
logic review before the next item. Primary-agent review scope is the CtrlRAM
heading, selector geometry and compact guidance delta over
`e5202e2707d272076d24216222188d478314a07d` on dirty branch `1.1.4`, including
shared card consumers; not a re-certification of every accumulated UI change.
Production sources were unchanged during this re-review. SHA-256 snapshots:
`ShellTextResources.ReplaceGrouping.cs` =
`C363EACC901A621E1EB2315C6EC937919F51DBC1CF7172F864EF7F3B9407F650`,
`FirmwareSlotCard.axaml` =
`FC5073781A9C410C3B1D1400BF0F38448615C71EBA3CD773614CC5630324F45B`,
`MainWindowStyles.axaml` =
`3C409DC83FF4FC20ABB865A8477503B5AAC7B55E6712EFC59BFC6F41D44143B5`.

- Traced `MemoryLayoutProjector.CreateCtrlRamInputSlot` → typed description
  facts → existing text projection → shared card. Size/target/grouping are
  not inferred from filename or IC labels; technical report mappings remain
  unchanged. Shared/multiple-section fallback and DiffNFMerge prerequisites
  retain their facts. Language refresh uses declared source text and refreshes
  guidance rows; real selected filenames, readiness and Mode state are retained.
- Extended the existing full-window test to 16 cases: both viewports,
  English/zh-TW, Light/Dark, empty and selected states. Selected cases use
  unchanged certified fixture `nt51926-fw200-cascade3-auto-prj-597-20260718`:
  expected-output as the inspected reference and `vn-ctrlram-input` as one
  Common selection, with Cascade unselected. This is a real NT51926 cascade
  scenario, not invented NT51929 cascade evidence or modified firmware bytes.
  It asserts the actual Base `Verified` state and preserves all selected paths
  through language/hover/keyboard expansion. Golden output execution is not
  claimed by loading fixtures for UI inspection.
- Every rendered group card now has its own outline checked. Pointer hover
  compares every card bound/outline, header/content border thickness and
  header-content origin, in addition to the existing group geometry and
  keyboard-visible focus/Space expansion assertions. **16/16 passed** in
  `selector-review-expanded.trx`; no production correction was needed.
- Final affected regression: **194 passed, 0 failed, 0 skipped, 43 seconds**,
  `selector-review-final.trx`, same filter/test-area as the prior 186-case run.
  Full-window renders are retained in that evidence directory's
  `review-renders/`. `git diff --check` passed. No full verifier, commit,
  integration or release is claimed.

Scoped logic Polytail: **PASS**; no new correctness finding. Complete selector
visual acceptance is still **open** for High Contrast/125% above. The next
independent Report-entry work may proceed to its screenshot preview, not
silently treat those remaining selector gates as passed.

## Earlier annotated proposal

Canonical proposed annotated geometry board:
[`v1.1.5-ctrlram-selector-reference.svg`](references/v1.1.5-ctrlram-selector-reference.svg)

The retained SVG filename records the original proposal, not its current
release allocation. The reference bytes are unchanged by this resequencing.

The SVG is an annotated design board, not a production viewport render or
pixel oracle. Its numeric annotations define the proposed contract. Exact
reference-versus-actual comparison must use production renders captured at
`980 x 640` and `1440 x 900` logical pixels.

## Source observation

The product owner supplied four Light-theme English screenshots on
2026-09-01. They show the CtrlRAM Replace input surface in constrained and
wider states. The screenshots themselves came from transient clipboard paths,
so this checked-in SVG is the proposed durable reference rather than a copy of
those temporary files.

The observed defects are:

1. selector title, metadata, and selected-state rows become vertically crowded
   as the viewport narrows;
2. Base firmware and the Cascade/Common group surfaces do not share the same
   left and right anchors; and
3. the expanded group can display three visually competing outlines, while
   hover changes which outline is emphasized; and
4. when a file/type title or filename wraps to two lines, the wrapped text,
   `Verified` or other badges, and the following filename/status row are packed
   too tightly.

## Approved selector direction

- Base firmware, Cascade, Common, and every equivalent primary selector
  surface share the same horizontal start and end anchors.
- An expanded group has exactly one persistent section outline. Each selected
  input remains one inset card with its own outline. There is no intermediate
  bordered content wrapper.
- Hover never adds, removes, thickens, or moves an outline. It may change only
  the section-header fill, chevron/accent color, and existing focus/hover token.
  Keyboard focus retains a separate accessible focus indicator.
- The narrow-viewport change is vertical spacing only. It does not re-order
  facts, change columns, abbreviate content, alter controls, or change any
  Application/profile/firmware semantics.
- Owner-approved amendment (2026-09-08, `UI-114-SLOT-FILENAME-22`): the
  selected filename now occupies a full-width bottom row in the shared
  `FirmwareSlotCard`, below the identity/facts/actions region with a 16 px
  gap and 16 px bottom inset. This supersedes the earlier identity-column
  filename placement only. At that checkpoint Browse/clear remained centered
  on the upper row; the 2026-09-09 amendment below replaces that alignment.
  All shared card consumers retain the same
  styling, commands, complete filename, path tooltip and responsive facts.
  Empty inputs reserve no filename row. Longer names still wrap without
  truncation; the footer starts after the complete upper region, including
  facts or expanded status text, rather than at a fixed badge-relative gap.
- Owner-requested action alignment amendment (2026-09-09,
  `UI-114-SLOT-CENTER-54`): Browse and clear now share the vertical center of
  the complete card, including the selected filename. Reserve one right-hand
  action column spanning both content rows; identity, facts and the complete
  wrapping filename use the remaining left area without painting underneath
  the buttons. This replaces the preceding full-width-footer and upper-row
  action alignment, not the filename content or reveal command. Preserve the
  existing button styles, red enabled/gray disabled trash, 10 px action gap,
  36 px action height, 820 px breakpoint and two/four fact columns. Empty cards
  still reserve no filename row. No hard-coded Y offset or per-page copy.
- File/type titles and filenames remain content-sized flow items rather than
  fixed one-line slots. When either wraps, its complete line box is reserved;
  the badge row starts `8–12 px` after the wrapped text, and the next
  filename/status row starts `12–16 px` after the badge row. A badge never
  overlays, touches, or vertically centers against only the first line of a
  wrapped label.
- Use a stable vertical rhythm: title to metadata `6–8 px`, metadata to
  selected-state text `10–12 px`, section-header lines `4–6 px`, card vertical
  padding at least `16 px`, and at least `12 px` between primary surfaces.

## Reference state

| Property | Value |
| --- | --- |
| Composition kind | Replace |
| Experience | CtrlRAM Replace |
| Viewport | `980 x 640` logical pixels, plus a `1440 x 900` regression |
| Theme | Light reference; identical geometry in Dark and High Contrast |
| Language | English reference; English and `zh-TW` acceptance |
| Scale | 100% reference; 100% and 125% acceptance |
| Data | Verified Base firmware with a two-line type title, expanded Cascade with no DiffDLM selected, expanded Common with one selected input |
| Interaction | Cascade header hovered in the reference; focus is not shown |

## Red-capable acceptance evidence

- Render the complete CtrlRAM Replace surface at both reference viewports; do
  not validate cropped controls alone.
- Assert the Base firmware, Cascade, and Common outer left/right bounds match
  within `0.5` logical pixel.
- Assert each expanded group has one section outline and one outline per input
  card, with no intermediate bordered wrapper.
- Compare normal and hovered geometry: all bounds, border thicknesses, and
  content positions remain identical.
- Assert title/metadata/status vertical gaps meet the ranges above at both
  viewports in English and `zh-TW`.
- Force both a two-line file/type title and a two-line filename; assert the
  badge row begins `8–12 px` after the wrapped text and the following
  filename footer begins `16 px` after the complete upper region under the
  2026-09-08 amendment. Longer filenames may occupy more than two lines;
  assert full text and line-height reservation without overlap or clipping.
- Treat the SVG positions as illustrative only. Geometry evidence comes from
  the exact production viewport renders and measured bounds above.
- Cover keyboard focus, screen-reader naming, Light, Dark, High Contrast,
  100%, and 125%. Token searches or XAML string checks alone are insufficient.

The eventual production change must reuse the shared selector/group/card
owners. It must not add a CtrlRAM-specific duplicate control or infer selector,
region, topology, processor, or firmware facts in Presentation.

## Approved narrow Replace header correction (2026-09-08)

The later 2026-09-09 shared action amendment is locally implemented, not
published. [Scoped evidence](../../tests/README.md#shared-slot-action-alignment--2026-09-09)
records the eight old-center failures, 17 passing targeted cases and 117
passing shared regressions (overlapping sets). The parent-grid correction
preserves commands/styles; no firmware or Memory Layout range changed.
Full native accessibility/scaling and release acceptance remain open.

`UI-114-REPLACE-HEADER-23` retains the wide Header geometry and existing
Mode/badge/Targets controls. When the Replace content container is at most
700 logical pixels wide, the title/subtitle span the first row; the existing
actions span a second row with a 12 px gap, left aligned. The output panel
and card layouts remain unchanged. This fixes the 980x640 title clipping
without reducing fonts, truncating labels or creating alternate controls.
Acceptance measures title/subtitle full text, action containment and vertical
separation, plus narrow-to-wide-to-narrow restoration without Mode/file-path
mutation. The 1440x900 frames must retain the preceding exact geometry.
