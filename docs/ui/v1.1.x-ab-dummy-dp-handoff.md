# v1.1.x AB Dummy DP handoff

Status: Dummy DP, naming and Report information handling are committed locally at
`30c91905d4f66fd55c74918300343fac7857e5f0`;
six complete-output map-based cases and scoped UI/Application/profile/trust
checks pass. Structure-only verification passed on 2026-09-07 (155.8 seconds).
Frozen-head independent technical reviews are complete with no new P0-P2 findings.
Final integration verification and exact-head R3 owner evidence remain pending;
this is not integration-ready, a release or a support-promotion claim. Version allocation is owned by the
[NFC roadmap `v1.1.4` milestone](../architecture/nfc_roadmap.md#114-ui-corrections-and-ab-dummy-dp).

## Retained owner requirement

### Frozen implementation checkpoint (2026-09-07)

- `ui_runtime_review` reviewed all changed non-Presentation authorities;
  `ui_presentation_review` reviewed the 21 changed Presentation files and three
  UiSmoke files. Both returned scoped `PASS-WITH-HUMAN-GATE` for the commit above.
- Existing P3 wording `Output range uses bytes from Reserved` remains a later
  typed-projection wording correction. Dummy cards and Report already identify
  `0xFF`; do not claim the generic memory-layout text was corrected here.
- Six independent whole-output map cases pass, including NT51950 single/cascade
  and NT51951 with the pinned processor. These are not certified Dummy Golden.
  Scoped test counts overlap; do not sum them into a unique full-suite count.
- The final record binds the reviewed source and canonical path-state digest.
  Its technical completion does not supply firmware-owner authority. The current
  validator requires an exact final-evidence-head owner attestation before its
  R3 final record can pass. Obtain that confirmation, then execute the final
  `python scripts/verify.py --all`; no fresh full pass is claimed yet.
- Native Windows DPI, OS High Contrast and screen-reader acceptance remain
  unverified by the headless evidence. No push or release is authorized here.

Owner scope amendment (2026-09-06): implement and verify NT51919, NT51929,
NT51932 **and NT51950 / NT51951** in the same Dummy DP batch. The owner's
latest clarification includes both 51950 and 51951, not 51952; it supersedes
the earlier exclusion of 51950. This follows the shared AB execution direction:
initialize the complete output with 0xFF, omit DP input/seed, retain existing
TP overlays, relocation and declared header/integrity processing, and leave the
entire non-TP complement at 0xFF. Do not create a fake DP file or a second
executor. Scope authorization is not proof that any new Golden passed.

NT51950 verification includes its declared single and cascade maps, including
the non-TP container tail. NT51951 verification must separately cover the ordinary Off path, the Dummy On
path and the three TP B postbuild imports at output 0x8A100, 0x8A110 and
0x8A130 (four bytes each), across the complete 0x100000-byte output. The profile
still records missing direct NT51951 AB input/output Golden and firmware-owner
review; CtrlRAM Golden and NT51950 workflow-logic evidence do not close those
AB gates. Retain this limitation until independent expected output and the
required review are actually admitted.

- Add an AB Code **Dummy DP checkbox**, **unchecked by default**.
- When enabled, fill **all output sections that do not belong to TP** with
  `0xFF`. This preserves the owner's correction: the scope is not only the
  section named DP.
- Do not allow DP input while enabled. A visual-only disabled input is not
  sufficient; the accepted typed request must enforce the same rule.

The owner moved the checkbox and its complete firmware behavior into
`v1.1.4` on 2026-09-05; this is not merely a visual control change.
The feature was deferred past `v1.1.2` and is not a `v1.1.3` change. Scheduling
does not certify output bytes, admit a new IC/mode or promote support.

## Decisions and evidence before implementation

### Accepted map-based evidence contract (2026-09-07)

Owner clarification: "Dummy DP 基本上沒有 golden 按照你的 map 去輸出就好".
The owner accepts the map inventory below as the Dummy output contract.
Do not wait for a nonexistent certified Dummy Golden before implementation.
Verify complete outputs against independently specified map-based expectations:
exact capacity, every TP/non-TP boundary, all non-TP bytes equal to `0xFF`,
and existing TP overlay, relocation and declared postbuild behavior. Expected
ranges/results must not be copied from the candidate plan or candidate output.
For 950/951, retain the declared three TP B imports and test their results;
the acceptance does not authorize omitting processing or exempting non-TP bytes.
Label this evidence as map-based contract verification, not certified Golden.
Off-mode existing Golden regression and existing candidate/support limitations
remain unchanged. This decision supersedes the earlier requirement below to
obtain independent Dummy Golden files before implementation; exact-range review
and applicable firmware review remain required.

### Accepted interaction contract (2026-09-06)

Owner naming decision (2026-09-07): use `Dummy` for both DP filename tokens and
show `0xFF` provenance in Report. Preserve TP versions, UTC date and override
behavior. This supersedes the pending naming choice in the earlier inventory;
the implementation follows the existing naming owner and ADR 0036 amendment.

The owner authorized implementation after approving these interaction rules:

- Place `Dummy DP (0xFF)` beside `Use the same TP for A and B` in one horizontal
  checkbox row below the AB description and above the input cards. Reuse existing
  checkbox styling/spacing; wrap only when the supported narrow viewport needs it.
- The option starts off. Requesting On opens confirmation **before** changing
  the accepted selection. DP remains selected until confirmation is accepted.
- Explain that non-TP regions use Dummy Byte (`0xFF`), the current DP selection
  will be cleared and DP input disabled. Explicitly state that original files
  will not be deleted. Disabling later requires selecting DP again.
- Cancel/Escape leaves the option and selected inputs unchanged. Accept clears
  DP selection (never deletes the file), enables Dummy DP and disallows DP input
  through both UI and shared typed admission. Off restores input availability
  without restoring the previous file; TP selections are preserved.
- Reuse `semanticAction secondary` for Cancel and `semanticAction primary`
  for Enable Dummy DP, with the existing focus/hover/disabled states. Do not add
  an orange confirmation-button style or the generated preview's square focus
  frame. Initial keyboard focus stays on Cancel.

The generated image was a process preview, not implementation or byte evidence;
the owner subsequently corrected its button styling and checkbox arrangement.
The approved interaction does not settle the firmware questions below.

### Remaining firmware admission

1. Identify the existing AB request/profile/compiler/planner owners and each
   affected profile's explicit TP and non-TP sections, including address
   spaces, topology and complete output bounds. Do not infer them from a
   filename, observed Golden layout or UI state.
2. Obtain the firmware owner's exact treatment of headers, integrity and
   postbuild processing where it intersects the requested non-TP fill. Do not
   silently exempt non-TP bytes or invent new processor semantics.
3. Enforce the accepted toggle contract above through the existing accepted
   session owner. No stale DP input may affect a Dummy DP build.
4. Use the owner-approved map-based contract above to define independent
   expected outputs and exact write-range checks for each IC/topology case.

## Acceptance outline

### Read-only implementation inventory (2026-09-06)

Existing AB authoring is owned by `AbMergeAuthoringExperience`, canonical
capability/profile compilation by `CanonicalCapabilityCompiler.AbMerge` and
`V2CompositionPlanCompiler`, execution by the shared `CompositionRunService`.
Current profiles require `dp-ab-input` and seed the output from DP. Dummy mode
therefore needs a typed, profile-compiled DP-free input/initializer selection;
it cannot be implemented by disabling Browse, creating a fake DP file or
overwriting the completed output afterward.

Owner-approved Dummy map inventory (2026-09-07), half-open ranges in
`output-image` projected from `flash`:

| IC / map | TP A | TP B | Non-TP complement required to be 0xFF |
| --- | --- | --- | --- |
| 51919 / 51929 / 51932; output ends 0x80000 | [0x7000,0x40000) | [0x47000,0x80000) | [0,0x7000), [0x40000,0x47000) |
| 51950 single; output ends 0x80000 | [0xA000,0x37000) | [0x4A000,0x77000) | [0,0xA000), [0x37000,0x4A000), [0x77000,0x80000) |
| 51950 cascade >=2; output ends 0x100000 | [0xA000,0x37000) | [0x4A000,0x77000) | [0,0xA000), [0x37000,0x4A000), [0x77000,0x100000) |
| 51951; output ends 0x100000 | [0xA000,0x37000) | [0x8A000,0xB7000) | [0,0xA000), [0x37000,0x8A000), [0xB7000,0x100000) |

Map sources: [919/929/932 family](../../profiles/built-in/nt51919-nt51929-nt51932-ab-merge/families/nt51919-nt51929-nt51932-ab-merge.json)
and [950/951 family](../../profiles/built-in/nt51950-ab-merge/families/nt51950-ab-merge.json).
The complement includes declared unknown/unmapped and container-tail regions;
filling only DP-owned regions is not equivalent to the owner's all-non-TP rule.

Existing TP B relocation must be distinguished from copying raw TP unchanged:
919/929/932 relocate ILM/DLM/DIFF address words; 950/951 retain DIFF relocation
and import three four-byte TP header results at B-relative 0x100, 0x110, 0x130.
The declared final postbuild imports are inside TP. The owner selected the
shared-execution direction above and added NT51950/NT51951 to its first verification
batch; map-based expected-output tests and exact-range review remain pending.

No certified AB Dummy output case was found. Existing AB Golden certifies the
ordinary DP-seeded mode, not this new contract. 950/951 retain their declared
candidate/evidence limitations; adding Dummy must not promote them. Without DP,
the current `AbCodeOutputNameResolver` would produce missing-DP metadata
placeholders/warnings; explicit Dummy naming/report provenance remains a product
decision, not a fabricated DP version. The 2026-09-07 decision removes the
missing Dummy Golden prerequisite, not independent map-based expectations or
exact write-range review.

Separately, the preceding UI batch still has committed `design-active` records
and unresolved final integration/code-size evidence. The current capability
lifecycle does not let those records authorize a new production batch. No
record finalization, gate waiver or Dummy production edit is claimed here.

### Read-only architecture assessment (2026-09-07)

Independent `ui_runtime_review` recommends reusing the existing optional input
selection group and compiler lowering, not a sibling Dummy map or executor.
The existing schema permits a 0..1 DP selection group; TP remains required.
Initialize the existing output with FF and retain the complete DP seed only
when DP is active. Ordinary mode still requires DP through explicit typed AB
admission; absence of DP must never silently select Dummy mode.

AB currently uses static unique-topology publication. Extend the existing
dynamic selection registration/admission to bind AB input selection together
with its real topology/map subset, including separate 950 single/cascade
capacities. Do not fabricate a map suffix or bypass publication. A toggle must
invalidate the old authoring revision/session and its readiness evidence.
Inactive DP metadata bindings must also be excluded through their existing
owner; verify this compiler seam before implementation. Preserve the existing
TP operations and real pinned Combiner execution for 950/951.

Planned tests cover explicit ordinary/Dummy admission, stale session replay,
missing TP, rejected DP in Dummy, inactive DP reads/seed/bindings, all map
boundaries and full output, ordinary Golden parity, same/independent TP, and
confirmation cancellation/file preservation. This is design evidence only,
not production admission or a claim that these tests have run.

### Ordinary AB baseline verification (2026-09-07)

The selected Bootstrap tests completed on 2026-09-06 at 23:59:30 +08:00:
19 executed, 19 passed, zero failed or skipped. The TRX execution interval was
13.674 seconds (not total restore/build/command wall time). The tested checkout
was `f46afe16`, with no production edits for Dummy DP.

Selection: `AbMergeGoldenRegressionTests`, `Nt51951AbMergeCandidateProfileTests`
and `AbMergeTopologyRuntimeAdmissionTests` in
`tests/NvtFwCombiner.Bootstrap.Tests/NvtFwCombiner.Bootstrap.Tests.csproj`.
Local evidence: `D:/NvtFwCombiner-TestArea/evidence/v114-dummy-dp/dummy-ab-baseline.trx`.
This includes ordinary AB direct/alias Golden and synthetic/reference checks;
it is neither a full-suite pass nor Dummy DP certification. NT51951's reference
parity does not replace its missing direct product AB Golden.

### Implementation checkpoint (2026-09-07, after `d937747a`)

The preceding UI batch completed its full verifier on `2934c269`; reviewed
records were committed at `d937747a`. That prerequisite is closed. Dummy DP
changes below remain local and uncommitted, not an integrated feature.

The five AB profiles now declare optional DP selection. The compiler retains
active processor-referenced views and conditionally lowers a TP first write
from ReplaceExisting to Reject only when a proven omitted whole-region seed
previously covered it. Existing Domain overlap validation remains unchanged;
ordinary DP seeds and 950/951 postbuild imports retain their original policies.

Evidence in `D:/NvtFwCombiner-TestArea/evidence/v114-dummy-dp/`:

- `dummy-compilation-conditional-overlap.trx`: six Dummy IC/topology compilation
  cases passed after the original six failures were corrected.
- `dummy-and-normal-compilation.trx`: 12 Dummy/ordinary cases passed, including
  required TP slots, capacities, DP seed presence, first-write policies and
  all three 950/951 postbuild imports retaining ReplaceExisting.
- `optional-seed-contract-declared-length.trx`: 47 scoped compiler tests passed,
  including omitted/selected seed, missing seed, late/equal sequence, source
  selector and insufficient source capacity rejection. Earlier fixture failures
  remain in separate TRX files; a later added target-selector case is not
  covered by this particular run.
- `optional-seed-contract-target-selector.trx`: 48 scoped tests passed after
  adding the target-selector negative case.
- `profile-contract-dummy-checkpoint.trx`: all 397 ProfileContract tests passed
  with the freshly built binaries (`--no-build --no-restore`); this is a full
  test-project run, not the repository-wide verifier or Golden execution.

Independent compiler diff review found no implementation defect in the admitted
conditional lowering; final acceptance still awaits remaining negative tests.
These checks do not prove complete output bytes or public runtime readiness.
Dynamic AB publication, typed Application mode/session enforcement, UI switch,
whole-output map comparisons, ordinary Golden rerun and final verification
remain incomplete. No external Dummy Golden is required by the latest owner
decision; independent map-derived complete expectations are still required.

The existing dynamic compilation port now accepts an optional explicit topology,
and its built-in adapter forwards AB requests to the existing AB registration.
Non-AB topology rejection remains owned by that registration. This is adapter
support only, not publication or public Dummy admission.

- `ab-dynamic-adapter-red.trx` reproduced the missing AB adapter dispatch before
  the change; `ab-dynamic-adapter-first.trx` then passed 13 tests.
- `ab-dynamic-adapter-canonical-metadata.trx`: all 17 AB compilation/adapter
  cases passed (six IC/topology pairs in Normal and Dummy, null-selection
  compatibility, metadata and topology/capacity conflicts). Three additional
  classification tests failed before classification because catalog publication
  is not yet synchronized. The combined run is therefore **failed**, not green.
- `ab-adapter-application-consumers.trx`: 34 Application capability catalog tests
  passed with the extended port and updated test doubles.
- `ab-adapter-publication-diagnostic.trx` identifies the first publication
  failure: NT51919 AB's current definition fingerprint differs from the policy.
  Resolve dynamic route identities/topology first, then synchronize affected
  policy/evidence and source-derived pins through their existing owners.

The initial expanded metadata test incorrectly expected bindings on all ICs;
the retained failed `ab-dynamic-adapter-topology.trx` records that test error.
Current declarations contain five TP bindings for 929/932 and none for
919/950/951; none declare DP metadata. Tests now assert those actual contracts,
not fabricated metadata. Generic inactive-binding filtering still needs its own
synthetic positive/negative coverage before final acceptance.

`optional-seed-admission-boundaries.trx` additionally passes all nine optional
seed tests, including immutable-target rejection at declaration admission.
This rejection occurs before lowering, like duplicate sequence rejection;
the test asserts that actual boundary instead of requiring a later overlap issue.

### Dynamic topology and inventory checkpoint (2026-09-07)

Existing dynamic route payloads now retain an AB-specific typed topology choice.
The shared AB projection validates disclosure, builds selector choices and checks
bound compilation category: single is 1; cascade disclosure is 2 but bound
counts >=2 remain valid. Selector-free and non-AB definitions cannot carry this
choice. Existing allowed-map binding checks remain in force.

- `dynamic-ab-choice-red.trx`: two missing-choice tests failed before the change.
- `dynamic-ab-choice-binding.trx`: 51 Application catalog tests passed, including
  actual dynamic route binding, cascade 2/3 acceptance and wrong-map rejection.
  A later non-AB-choice negative test is not included in this exact run.
- Independent `ui_runtime_review` approved the scoped Application diff and read
  the 51-case evidence; this is not full R3 or publication acceptance.
- `dynamic-ab-inventory-red.trx`: all six AB inventory cases failed before wiring.
- `dynamic-ab-inventory-schema12.trx`: 23 AB tests passed after inventory wiring,
  including exact six map subsets and topology disclosure. NT51950 uses shared
  map-set `nt51950-ab-merge-maps`, partitioned by its existing count axes; the
  four selector-free ICs retain their original map IDs as singleton map-sets.

The trust-index contract is revised to schema 1.2 / instance 1.1.4.1 to allow
AB's map-set field. Build materializer and runtime schema both retain the closed
workflow/token/bounds checks. The materializer's reviewed-schema hash was derived
from the changed schema bytes. `ab-trust-schema12-ordered.trx` passed all 19
loader tests, including AB acceptance and General Replace map-set rejection.

Still pending: six policy route/fingerprint transitions and evidence consumers,
remaining materializer negative checks, source pin synchronization, public AB
authoring/session/inspection integration, UI, full bytes and Off Golden. The
current policy is deliberately not reported as successfully published.

### Dynamic publication and optional-map regression checkpoint (2026-09-07)

The six AB policy definitions now use the actual dynamic inventory identities
and fingerprints. Their expanded Normal/Dummy contract is temporarily
`contract-only`; existing Normal Golden cases, alias facts, expected bytes and
release execution requirements remain unchanged. Independent scoped review
approved the policy/evidence payload before `sync_derived.py --write --only
reviewed-source-pins` synchronized four existing consumers. Canonical Golden
cross-link validation passed; this is not output-byte execution.

- `dynamic-ab-policy-loader.trx`: 20/20 passed.
- `dynamic-ab-policy-reload.trx`: 23/26 passed; all three failures originated
  from catalog load rejecting NT51928's compact map, before the intended
  classification scenarios could execute.
- Root cause: omitted-seed proof retained an optional LDC output view even when
  its region did not exist in the selected map. Retaining proof views now also
  requires that canonical region to exist. No IC-specific exception was added.
- `dynamic-ab-optional-map-region.trx`: 26/26 passed after that condition,
  including the three original classification regressions and 23 AB cases.
- `dynamic-ab-inspection-regression.trx`: 59/60 passed. The remaining
  `SelectiveDispatchMatchesAllStrategyBaselineForEveryWorkflow` failure reaches
  `AbMergeAuthoringResolver.Discover`, which still requests a static topology
  route after AB inventory moved to dynamic routes. This is the pending
  Application integration, not a reason to restore a parallel static AB path.

Application mode/session integration, UI, independent complete Dummy bytes,
Normal Golden execution and final review/verification remain pending. These
working-tree checks do not establish feature completion or release readiness.

### Application explicit-mode checkpoint (2026-09-07)

AB compilation and authoring now use the existing published dynamic compiler
and binding path; the static AB lookup was removed rather than retained as a
fallback. Generic compilation only forwards typed topology. NT51950 runtime
still requires explicit single/cascade; the registry's summary default does
not authorize a runtime default. The public authoring port accepts closed
`AbMergeDpMode.Normal/Dummy`, default Normal. This mode selects the declared
optional DP input or an empty selection independently of input-file presence.

- `dynamic-ab-authoring-red.trx`: all five existing input-layout tests failed
  at the obsolete static route lookup.
- `dynamic-ab-normal-authoring.trx`: 88/88 passed after dynamic wiring,
  including the full 60-case inspection group, five layouts and 23 AB cases.
- `dynamic-ab-explicit-mode-red.trx`: six Dummy binding tests failed because
  DP was still present; the separate missing-topology rejection passed.
- `dynamic-ab-explicit-mode-first.trx`: 35/35 passed after mode wiring.
- `dynamic-ab-mode-session-membership.trx`: 5/5 passed. Normal with DP and
  Dummy without DP accept; Normal without DP and Dummy with DP reject;
  undefined modes reject. The existing selected-slot compiler/readiness
  contract enforces required DP, without a second Application rule.
- `dynamic-ab-partial-inspection-red.trx`: reproduced partial TP inspection
  incorrectly recreating Normal from an accepted Dummy session. The inspection
  resolver now retains the current accepted exact capability; the canonical
  catalog checks its publication, workflow and IC before use.
- `dynamic-ab-mode-inspection.trx`: 101/101 passed after the partial-inspection
  correction, combining mode/session tests, AB compilation, existing layouts
  and the inspection regression group. No full-suite or Golden claim.

Mode-transition/stale-lease coverage, scoped review, UI and full output evidence
remain pending; these checkpoints do not complete the feature.

### Mode-transition review correction (2026-09-07)

Independent scoped review found that a current NT51950 single inspection
capability could still be paired with a cascade or missing topology token.
`dynamic-ab-inspection-topology-red.trx` reproduced both bypasses. Reuse now
also requires the existing canonical `TopologyRequirement.Matches(topology)`;
it does not derive topology from TP bytes or introduce another selector owner.

- `dynamic-ab-mode-transition-selection.trx`: 5/5 passed, including both
  mismatches rejected before reading bytes, bidirectional Normal/Dummy changes
  at the same revision producing different exact compilations, and partial
  Dummy inspection preserving its accepted contract.
- `dynamic-ab-stale-inspection.trx`: 1/1 passed. Normal inspection begun before
  Dummy activation cannot publish afterward; activation advances the revision,
  and the rejected completion leaves the current Dummy snapshot unchanged.
- `dynamic-ab-catalog-rollover.trx`: 2/2 passed. Both modes reject old exact
  inspection capabilities before input reads after catalog reload; subsequent
  authoring resolves the new publication token.
- `dynamic-ab-runtime-regression.trx`: all 50 AB runtime-admission tests passed
  after the correction. This is the named runtime group, not all Golden cases.
- Independent `ui_runtime_review` rechecked the topology gate and 5-case TRX
  and closed that P2. This scoped recheck is not final R3 acceptance.

The initial mode-transition test supplied DP to Dummy and correctly received
rejection (`dynamic-ab-mode-transition.trx`, 4/5). Its expectation was corrected
to model the approved confirmation flow retaining only TP; production rejection
was not weakened. Catalog rollover and full UI/byte evidence remain pending.

### UI state-model checkpoint (2026-09-07)

Model-only UI admission extends the existing Merge presentation and
WorkflowSession owners. Explicit confirmation commands now leave DP unchanged
until acceptance. The coordinator invalidates old inspection, clears only DP
selection with the existing clear helper, changes the typed mode, resets
prepared/readiness state and refreshes TP inspection once. `AbMergeSlots`
enumerates only current typed input bindings rather than every cached card.

- `dummy-ui-baseline.trx`: existing same-TP baseline 1/1 passed.
- `dummy-ui-mode-first.trx`: initial two state-model tests passed, covering
  default Off, request/Cancel preserving DP, confirmed On preserving TP and
  same-TP mode, original files retained, inactive DP programmatic selection
  excluded and Off not restoring DP.

These are ViewModel tests, not a rendered checkbox/modal. Pending confirmation
context invalidation, transition reentry/run guards, independent disabled DP
display, localization/accessibility, visual implementation and final model
review remain required. No screenshot or UI completion is claimed.

### 2026-09-07 pending confirmation context guard checkpoint

The existing Merge context publication now invalidates pending Dummy confirmation
when IC, topology or mode changes. Successful page navigation dismisses it only
after activation succeeds; the active-page predicate also rejects off-page toggle.
The WorkflowSession coordinator owns one transition flag spanning its awaited TP
inspection, released in `finally`; deterministic reentry/exception tests and
availability notifications remain pending.

- `dummy-ui-context-red.trx`: two expected failures reproduced stale IC/mode
  round-trip confirmation.
- `dummy-ui-context-green.trx`: four tests passed after context invalidation.
- `dummy-ui-context-topology.trx`: five tests passed, including NT51950
  single/cascade round-trip.
- `dummy-ui-page-red.trx`: one expected failure reproduced a prompt surviving
  navigation to Home.
- `dummy-ui-page-green.trx`: all six current Dummy state tests passed on the
  uncommitted candidate; no tests skipped.

Evidence is under `D:/NvtFwCombiner-TestArea/evidence/v114-dummy-dp/`.
This checkpoint is not final UI review, rendered evidence, full byte validation,
Golden execution or a completed commit. Remaining model coverage includes
reentry, run, rollback and catalog rollover, followed by visual implementation.

### 2026-09-07 transition reentry and command availability checkpoint

The coordinator's single transition guard is now exercised with a controlled
inspection pause. While confirmation awaits inspection, both the toggle command
and direct coordinator call cannot switch back to Normal. Completion and an
injected inspection failure both release the guard and permit a subsequent Off
operation. Command predicates and availability notifications reflect the same
guard and pending confirmation state, rather than a separate UI busy flag.

- `dummy-ui-reentry.trx`: initial controlled reentry case passed.
- `dummy-ui-availability-red.trx`: reproduced the toggle still reporting enabled
  during an in-flight confirmation.
- `dummy-ui-availability-green.trx`: seven tests passed after command predicates
  and transition/prompt availability notifications were added.
- `dummy-ui-reentry-failure.trx`: eight tests passed, zero skipped, including the
  injected inspection failure followed by a successful Off operation.

These results supersede the previous checkpoint's pending reentry/exception and
availability-notification items. Explicit cancellation, run, navigation rollback,
catalog rollover, independent model review and actual visual integration remain
pending. Full bytes and Off Golden evidence are still not provided by these tests.

### 2026-09-07 actual AB checkbox and confirmation render checkpoint

Implemented the accepted horizontal same-TP / Dummy DP checkbox row without new
checkbox/button styles. The existing modalSurface and semanticAction buttons own
confirmation presentation. The lazy modal host, shell interaction blocker and
action rail now participate in the same pending state. Cancel/Escape leaves Off;
reopening focuses Cancel. Focus entry must be queued even when the retained lazy
view does not yet have a VisualRoot at the open-property notification; checking
the root before queueing reproduced the second-open focus failure.

On displays an independent empty, disabled FirmwareSlotCard outside active input
bindings; no compiled range or Normal projection is invented for that display.
Off restores the active empty DP card. The incomplete-input message now uses
current compiled DP binding membership, so Dummy requests only TPA and TPB.

Reference: this document's accepted interaction contract plus the existing
AbSameTpConflictModal/shared checkbox and card styles. Actual complete-window
baseline and renders are retained in the external test evidence directory:

- `visual-before/dummy-1440-900-False-False-initial.png`
- `visual-after/dummy-1440-900-False-False-initial.png`
- `visual-after/dummy-1440-900-False-False-confirmation.png`
- `visual-after/dummy-1440-900-False-False-enabled.png`
- Matching 1440x900 Dark/Traditional Chinese, 980x640 Light/Traditional Chinese
  and Dark/English renders use the same filename pattern.

All are real production-window headless renders at scale 1 with NT51929,
AB Code and empty input selections, not generated mockups. Geometry tests measure
checkbox center alignment within 0.5 px and non-overlap. Tests exercise keyboard
Space activation, confirmation-before-check, Cancel focus, Escape, reopen focus,
background disabled state, On display isolation and Off empty DP.

- `dummy-ui-visual-red.trx`: four expected missing-control failures on baseline.
- `dummy-ui-visual-first.trx` / `dummy-ui-visual-focus.trx`: four focus failures;
  preserved as reproduction, not passing evidence.
- `dummy-ui-visual-focus-root.trx`: four complete-window cases passed.
- `dummy-ui-visual-and-same-tp.trx`: 21/21 passed, zero skipped; includes eight
  Dummy model cases, four real-window cases and existing same-TP regressions.

Primary visually compared the complete Light/English and Dark/Chinese On views
and the narrow Chinese confirmation. The disabled DP surface and TP cards retain
the existing card style; confirmation retains shared buttons and centered modal.
Independent review remains in progress. Native Windows/DPI/high-contrast, firmware
whole-output bytes, Off Golden and final integration are not claimed by this run.

### 2026-09-07 full-output evidence exposes initializer defect

`dummy-off-ab-golden-first.trx` passed all 15 tests in
`AbMergeGoldenRegressionTests` before the initializer correction below, with
unchanged expected output bytes/hashes. Test bundle pins were synchronized to the
then-current bundles and Normal direct-catalog compilation explicitly selects DP.
This group contains direct/alias product evidence and synthetic/reference/public
host cases; a count of 15 is not a claim of 15 direct certified Golden cases.

New independent `AbDummyDpOutputTests` specify the accepted 512-KiB map and TP B
relocations without reading candidate ranges or output. All three IC cases failed
at output offset zero: required `0xFF`, actual `0x00`, retained in
`dummy-two-bank-full-bytes.trx`. Earlier notes assuming blank initialization was
already FF were incorrect: all five profile output-image initializers were zero.

Independent review confirmed Normal seeds cover the whole output capacity,
including NT51950 cascade and NT51951 tails. The five output-image initializers
are now locally changed to 255 only; Combiner work-buffer zero initialization,
TP clone, relocations and imports remain unchanged. Bundle entry/content hashes,
trust and dynamic policy fingerprint consumers still need synchronization for
this correction before the next verifier/test run. The earlier 15/15 evidence
does not validate this new source. No new full-bytes pass is claimed yet.

UI review additionally retains a P2: confirmation with retained TP can finish
after the one-shot checkbox focus restoration was skipped while disabled. Add
controlled delayed-TP focus coverage and retry restoration after transition.
The generic reserved-region copy wording also needs to render the existing typed
initialization fact clearly, without reconstructing firmware maps in UI.

### 2026-09-07 FF candidate complete-output verification

The initializer correction is now synchronized through the existing
`ProfileBundleEntryArrayHasher`, trusted bundle declarations and runtime-produced
dynamic route fingerprints. Reviewed source pins were refreshed with
`python scripts/sync_derived.py --write --only reviewed-source-pins`.
No Golden expected bytes or output SHA changed.

- Family 919/929/932 bundle: `68527380d4e2de5994734b9357fc55963254e51382027d9f099699c9dc1a366f`.
- Family 950/951 bundle: `f60f5ef4f8c2a150c7dde47638d55aa35a84425146809263057939540ea0b6b9`.
- `dummy-ff-output-off-green.trx`: 18/18 passed, zero skipped, on synchronized
  FF source: three independent 512-KiB Dummy outputs plus the original 15 AB
  Golden/reference/public-host/runtime cases.
- `dummy-all-six-full-bytes.trx`: 6/6 passed, zero skipped, covering NT51919,
  NT51929, NT51932, NT51950 single, NT51950 cascade and NT51951.

Expected full byte arrays are specified from accepted map constants, synthetic
immutable TP inputs and independent scalar/CRC arithmetic, not candidate plan or
output. 950/951 invoke the actual pinned Combiner and compare complete output,
three exact postbuild import ranges and actual changed bytes. All six compare
the entire output including non-TP gaps/tails initialized to FF, and retain
original TP source bytes. These are map-based contract tests, not direct Dummy
product Golden or support promotion. Independent `ui_runtime_review` approved the
two oracle designs and inspected the six-case TRX. Its non-blocking suggestion
was adopted: 950/951 now use the existing address-sensitive pattern instead of a
256-byte repeating pattern. `dummy-all-six-address-sensitive.trx` reran all six
cases successfully. `dummy-ff-source-pins-green.trx` also passed both AB bundle
profile-source/entry-array identity checks.

The intermediate `dummy-ff-bytes-and-off.trx` contains six public-catalog failures
before policy fingerprints were synchronized; the final 18/18 supersedes them
for that scope. Prior passing UI evidence predates these FF fingerprints and
does not replace final candidate integration. The delayed-TP return-focus P2,
remaining contract/test-consumer synchronization and final verification/commits
remain open.

### Delayed confirmation return focus (2026-09-07)

The initial delayed-focus harness blocked the Avalonia dispatcher by using a
synchronous inspection callback. Moving service initialization off the UI thread
alone did not resolve it. Both live runs were explicitly stopped; neither is
passing or failing product evidence. The corrected test awaits a bounded gate
through the existing asynchronous inspection port and delegates actual inspection
unchanged. `dummy-ui-delayed-focus-red-async.trx` then reproduced the product
failure: after retained TP inspection, the enabled checkbox had no keyboard focus.

The modal now retains return-focus intent through the existing toggle command's
availability notification. It unsubscribes after successful restoration, a new
prompt, detachment or DataContext replacement. It does not enable the checkbox
before the coordinator finishes, change firmware behavior or add a polling timer.
`dummy-ui-delayed-focus-green.trx` passes 13/13, zero skipped, including the
delayed-focus regression, Dummy state cases and four production-window
viewport/theme/language cases. Independent `ui_runtime_review` inspected both
TRX files and the correction, returned scoped PASS and closed this focus P2.
This is not the final integration pass or a committed checkpoint.

### AB consumer regression synchronization (2026-09-07)

`dummy-ab-consumers-current.trx` ran 162 AB/profile/projection cases: 141 passed,
21 failed, none skipped. Existing ordinary-profile tests used the low-level
catalog's default empty optional selection while asserting DP seed behavior.
Their three fixture helpers and two cascade calls now explicitly select
`dp-ab-input`, as the ordinary contract requires. No expected output bytes,
write ranges or missing-input assertions were weakened.

`dummy-ab-consumers-explicit-normal.trx` reran the same 162 cases: 159 passed,
three failed, none skipped. Remaining failures are the two old candidate
compilation fingerprint assertions and the AB memory-layout test's old static
catalog lookup. These still require synchronization through the existing
compiler/publication owners; no blanket full pass is claimed. Firmware output
tests in this selection pass, including all six independent Dummy cases.

### Required checks

Latest consumer closure: `dummy-ab-consumers-green.trx` passes 165/165 with zero
skipped. The original 162-case selection plus all three other canonical memory
layout cases ran. The AB fixture now obtains its exact capability through public
AB authoring; Standard/Replace fixture lookup remains unchanged. The two source
candidate fingerprints were obtained from the existing compiler in
`dummy-ab-consumers-pin-diagnostic.trx`, not from generated output: NT51950
`1cc3e0faca583af4c22aeecf869922100637aeffe7d301b4d2f0a71df9c528ce`, NT51951
`68fd88c2ef1794d8b52943a40b6a574f641e9ca855b8ef98f09e2ebe94654d20`.
Independent `ui_runtime_review` returned scoped PASS for these four test files.
`dummy-profile-current.trx` also passes all 398 profile contract tests, zero
skipped. SPEC now explicitly distinguishes AB Normal/Dummy Off from the accepted
Dummy exception; the historical pilot and support-promotion constraints remain.
Final integration, remaining evidence review and local commits are not complete.

Additional current-source checks: `dummy-application-current.trx` passes 88
capability/AB/output-naming Application tests, and
`dummy-infrastructure-current.trx` passes 245 bundle/trust/policy Infrastructure
tests, all with zero skipped. `dummy-processor-inactive-input.trx` passes five
processor cases, including two new negative tests that reject an active
processor's staged-source or staged-artifact binding to an omitted optional
input. These use the existing profile fixture and compiler and assert rejection
without changing production behavior. They supplement, not replace, full-output
and final integration evidence. The owner subsequently approved explicit Dummy
tokens and Report `0xFF` provenance. `dummy-naming-preview-build-green.trx`
passes three cases, including public Dummy Preview/Build with explicit override,
serialized naming provenance and one informational issue, plus ordinary naming
and Normal Build. The earlier `dummy-naming-report-override.trx` reproduced a
real execution-entry defect: AB always requested DP as its first input. The
admitted fix now selects DP or required TPA from the compiled input bindings;
shared accepted-session validation and the executor remain unchanged.

The naming/public-execution scoped review passed but identified a Report P2:
the existing severity projection counted Info as Warning. The admitted correction
keeps informational rows visible and routes blocking checks through one
`IsBlocking` owner; unknown severity and legacy truncated-input warning behavior
are retained. `dummy-report-info-red.trx` reproduced the false warning in English
and Traditional Chinese. `dummy-report-info-green.trx` passes 48 scoped Report
tests, zero skipped. Independent `ui_runtime_review` inspected the three files
and TRX, returned scoped PASS and closed this Info/Warning P2. Naming and public
execution also have scoped approval; none of these replaces the fixed-head
whole-batch review or final verifier.

The structure-only verifier failed after 60 seconds on stale six-route manifest
fingerprints, ungoverned SPEC mutable-path entry and source-size ratchets. The
first two have been synchronized without changing expected Golden hashes.
The owner subsequently approved the exact source-size increment. Final source
measures 138542 full (+560), 99170 runtime (+280), 20757 Domain/Profiles,
42647 Application, 5039 Bootstrap/CLI and 30727 Infrastructure/Contracts/worker.
Existing allowances and corresponding actual-baseline assertions now match these
values with no headroom or measurement-policy change. All 19 code-size tests
pass (26.856 seconds). Do not claim structure or final verification passed.
Shared compiler scoped Polytail review passed; fixed-head review and final
evidence checkpoints remain open.

- Off preserves the existing AB behavior and its complete Golden outputs.
- On rejects DP input through the shared typed path, preserves the approved TP
  bytes, and fills every approved non-TP output range with `0xFF`.
- Boundary tests cover adjacent TP/non-TP sections, complete output length,
  invalid/incomplete requests and atomic failure without partial output.
- Toggle/retained-input/UI tests prove the switch cannot bypass Application
  admission or leave stale DP state participating in execution.
- Independent whole-output map-based comparisons and the exact write-range
  audit pass for every approved Dummy IC/topology; do not label these as
  certified Golden. Existing applicable Off-mode Golden comparisons still run.

Use the existing profile compiler and shared planner/executor. Do not implement
the fill in a UI click handler, add an AB-only executor, or regenerate expected
bytes from the changed implementation to make tests pass.
