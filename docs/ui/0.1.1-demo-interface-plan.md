# 0.1.1 Demo Interface Planning Scope

`0.1.1` is the first UI planning milestone after dev0 contract stabilization. It should not implement firmware semantics in the UI. It should define the first useful demo shape and confirm information architecture.

## Goals

- Confirm the initial desktop navigation model.
- Design a demo UI that can show profiles, inputs, shared Memory coverage, preview, logs, and reports without running real firmware parity flows.
- Keep UI backed by typed application view models and sample/synthetic data.
- Avoid copying merge/replace rules into XAML or ViewModel handlers.

## Selected top-level pages

```text
Settings
Merge
Replace
```

Home, Saved Rules, Reports, and Diagnostics are not top-level pages for the first usable UI. Reports and diagnostics remain secondary surfaces reached from Settings, status, or run results. Saved Rules appears only as actions/status inside General Merge or General Replace until the workflow is implemented.

## Merge demo content

- Mode selector: Standard / AB / General.
- IC selector from sample catalog.
- Compact input rows/cards using fake/synthetic data.
- Shared Memory coverage before/after visualization in the same layout position used by Replace.
- Mapping/operation preview table.
- Validation issue list.
- Build button disabled unless synthetic preview is valid.

## Replace demo content

- Persona selector: Display / TP HW / TP FW / General.
- Base reference BIN card using fake metadata.
- Replaceable region list.
- Overlay/mapping table.
- Shared Memory coverage before/after visualization in the same layout position used by Merge.
- Processor/tool readiness indicator.

## Settings demo content

- Profile catalog and tool folder settings.
- Strictness/theme options.
- Diagnostics/log/report access.
- Non-destructive configuration summaries.

## Secondary surfaces

- Saved Rules: shown as General Merge/Replace save/promote actions and status, not a page.
- Reports: opened from a preview/build result or Settings diagnostics area.
- Diagnostics: opened as a drawer, modal, or Settings subpage after owner decision.
- Logs: read-only, sanitized, and secondary.

## Non-goals

- Real firmware composition.
- Real combiner.exe execution.
- Editable profile schema UI.
- Golden regression UI.
- Release packaging UX.

## Exit criteria

- UI design document reviewed.
- Wireframe or XAML shell updated for the selected page structure.
- No ViewModel contains firmware copy/replace/CRC semantics.
- Synthetic data provider is clearly separated from production services.
- Terminal/log/report behavior is documented as secondary, not top-level navigation.
- Merge and Replace share a fixed-position Memory coverage before/after area.
