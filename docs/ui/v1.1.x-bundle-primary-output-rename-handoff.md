# v1.1.x bundle primary-output rename handoff

Status: the original rename feature is present in published `1.1.8`; the
`1.1.9` long-name follow-up below is locally in progress, not integrated or
published. Version allocation is owned by the
[NFC roadmap](../architecture/nfc_roadmap.md#current-release-sequence--2026-09-14).

This handoff records the owner request that enabling **Bundle output and
sources** must not prevent the operator from renaming the generated primary
output file. It is not part of `v1.1.1`, changes no firmware bytes or naming
tokens. Its scoped independent R2 host-delivery design review was completed
before implementation under `UI-114-BUNDLE-RENAME-12`.

## 1.1.9 long-name validation and recovery intake — 2026-09-16

The owner's reproduced report concerns a long **Bundle folder name**: no
output and no visible reason. A similarly long BIN name is a related case to
test, not yet a claimed reproduction. Live tracking is
[issue #434](https://github.com/Dennis40816/nvt_fw_combiner/issues/434);
the [roadmap](../architecture/nfc_roadmap.md#current-release-sequence--2026-09-14)
allocates the correction to `1.1.9`, outside the frozen `1.1.8` candidate.

Accepted recovery intent: show an actionable error while editing; reject an
invalid rename commit and restore the last accepted valid name. Do not reset
each keystroke, silently truncate, discard input selections, or mutate existing
files. Correcting the draft must restore normal confirmation behavior. Keep
Bundle-folder and primary-output filename recovery independent.

Distinguish the Windows filesystem component limit (normally 255 UTF-16 units
for an NTFS filename including its extension, or one directory name) from the
current product guard of 259 units for an entire path. A local .NET 10 probe
completed create/write/directory-move/read at total path length 390, while a
255-unit component succeeded and 256 failed. That does not certify native
pickers, external tools, UNC/SMB or a complete product Build at those lengths.
General removal of the 259-unit guard remains an assessment, not a silently
approved compatibility claim.

The atomic Bundle staging pattern currently appends 42 units to the user
folder name; the loose-output temporary filename adds 38. These are additional
implementation limits, not a requirement to reject every otherwise valid final
name. Assess bounded opaque sibling staging names through the existing atomic
writers, preserving same-volume promotion and failure/cancellation cleanup.
Regression coverage must reach actual invalid-edit visibility/recovery in
narrow and expanded-source layouts, final and staging component boundaries,
collision suffixes and no-partial-output behavior. Do not substitute passing
string validation for a real filesystem/Build check.

### Local unit 1: keep delivery failures visible — 2026-09-19

- Release base: `a2273c87`; documentation replay/pre-change head: `6c62a3f3`
  on `codex/1.1.9-long-name`, targeting local `1.1.9`. Original intake retained.
  Owner authorized continued `1.1.9` development and per-unit commits.
- R1 preflight was recorded in the owner task before editing. Existing
  `FileSystemCompositionOutputBundleDestinationValidator` produces the typed
  rejection; `OutputDeliveryConfirmationViewModel` projects `ValidationMessage`
  and `CanConfirm`; the shared modal owns display. No new validator or policy.
  Mutable production surface: `Views/OutputDeliveryConfirmationModal.axaml`.
  Regression: `ShellViewModelTests.OutputDeliveryValidation.cs`.
- Reproduction: a 300-character folder draft produces a path rejection and
  disables Continue, but the warning was inside the scrolled body. In a
  `980 x 620` expanded-source window the warning is below the visible viewport.
  This confirms one silent-looking UI path, not every user-reported path/tool
  failure or the exact unknown original filename.
- Move the same assertive live warning to the fixed action footer, above the
  buttons. Valid-state geometry, existing error text/style, input selections,
  actual validation, naming, bytes and write semantics are unchanged.
- Red: eight real-control geometry/visibility cases failed before correction.
  Green: 54 targeted cases passed; final expanded shared-output selection
  **68 passed, zero failed/skipped**. Eight new cases cover Light/English and
  Dark/Traditional Chinese, collapsed/expanded sources, `980 x 620` and
  `1280 x 900`, scroll top/bottom, invalid TextBox edits with no output,
  corrected drafts with re-enabled confirmation and actual successful Bundle
  execution. Test compilation/analyzers also passed. The first test-authoring
  compile failed on an invalid cleanup call/style issues and was corrected
  before the red run; it was not behavioral evidence.
- Command: `dotnet test tests/NvtFwCombiner.UiSmoke.Tests/NvtFwCombiner.UiSmoke.Tests.csproj
  --no-restore --filter 'FullyQualifiedName~OutputDeliveryLongNameErrorStaysVisibleAndRecovers|FullyQualifiedName~BundlePrimary|FullyQualifiedName~OutputConfirmation|FullyQualifiedName~OutputDelivery'`.
  Evidence: `D:/NvtFwCombiner-TestArea/evidence/v119-long-name/`,
  `error-red.trx`, `error-green.trx`, `error-final.trx`, and corresponding
  render folders. Final headless Light/Dark renders were visually inspected;
  these are not native Windows/DPI captures. The render test drains the next
  compositor frame to avoid recording the prior valid draft.
- Primary-agent scoped Polytail: **PASS** for this local R1 display unit;
  no parallel semantic owner, mutation or unsupported firmware claim.
  This does **not** close #434: invalid-commit last-valid-name recovery,
  component/staging/collision boundaries, loose BIN paths, long-input/tool
  behavior and total-path-cap assessment remain. Formal integration records,
  CI, native package and release Golden gates are separate; no full suite or
  publication was performed for this local correction.

## Pre-change behavior and reuse inventory

The sections below retain the original `1.1.4` implementation and local
evidence history. They are not outstanding release gates for the published
`1.1.8`. The active follow-up is the `1.1.9` intake above.

The product already has the required loose-output naming path and most of the
bundle UI. The gap is an explicit bundle-only restriction, not a missing
naming subsystem:

- `ICompositionOutputNaming` and
  `CompositionOutputNamingExperience.ResolveAcceptedOutput` are the live
  Presentation/CLI naming boundary. They carry the accepted session through
  `AcceptedSessionOutputNameResolver` to `CompiledOutputNameResolver`; the
  bundle change must extend this owner rather than route through an unused
  naming entry point. `CompositionRunRequest.IsOutputFileNameOverride` and
  `OutputNamingSummary.IsExplicitOverride` remain the execution/report
  provenance for manual host Build intent.
- `OutputDeliveryConfirmationViewModel` and
  `OutputDeliveryConfirmationModal` already expose `OutputFileName`, the edit
  action, keyboard focus, and localized labels. Today
  `CanEditOutputFileName` is false while bundling is enabled and switching the
  bundle on resets an operator draft to the canonical automatic name.
- `CompositionOutputBundleProposal`, `CompositionOutputBundleAdmission`, and
  `CompositionOutputBundleIntent` already retain the exact accepted session,
  prepared output name, immutable sources, and proposed folder destination.
  Today admission intentionally rejects an explicit output-name override.
- `FileSystemCompositionOutputBundleDestinationValidator` and the atomic
  bundle writer already own Windows-name, path, protected-input, collision,
  staging, promotion, cancellation, and partial-output safety. They remain the
  only destination/materialization owners.

No implementation may add a Presentation-only filename resolver, parse
firmware facts from a filename, or copy files after the atomic bundle commit.

## Intended interaction contract

1. `Bundle output and sources` may be on or off while the same output-filename
   edit action remains available.
2. The **Bundle folder name** and **Output filename** are independent fields.
   Editing or toggling either one must not silently rewrite the other. The
   existing automatic suggestions remain the initial defaults.
3. The override applies to the generated primary output BIN inside the atomic
   bundle. Accepted source filenames remain their accepted source identities;
   this item does not add source renaming.
4. The final primary filename, report identity, commit receipt, hash evidence,
   and Explorer/recent-output actions must all use the same effective accepted
   override and record it as explicit host Build intent.
5. Loose-output behavior, output bytes, compiled operations, profile-owned
   automatic naming, additional-delivery semantics, and bundle folder
   collision suffixing remain unchanged.

Whether a separately generated additional delivery should gain its own rename
field is deliberately unresolved and outside this request. It must not be
silently coupled to the primary filename or inferred from its stem.

## Safety and validation boundary

The bundle path must apply the same accepted manual-filename validation as the
loose path plus the existing atomic bundle destination validation. A proposed
name cannot carry path syntax or escape the selected folder, alias a protected
input, bypass Windows reserved/invalid-name and path-length checks, overwrite
another bundle member, or cause a visible partial bundle. Existing deterministic
collision allocation, private sibling staging, atomic promotion, cancellation,
and stale-session rejection remain fail closed.

The manual override is host delivery intent only. It cannot alter IC/profile
selection, naming-token extraction, Preview/Build firmware semantics, output
bytes, Golden evidence, or support status.

## Acceptance outline for the allocated release

- Turning bundle mode on preserves an already edited valid primary filename;
  editing remains keyboard- and screen-reader-accessible in both supported
  languages.
- Confirming bundle delivery creates no loose primary file and places the
  generated primary output under the selected custom filename in the promoted
  folder.
- Folder-name edits and primary-filename edits remain independent across
  toggle, Cancel/reopen, validation failure, and retry.
- When the typed renderer supplies naming provenance, the run report's
  `OutputNaming` records the delivered `ActualFileName` and
  `IsExplicitOverride = true`; commit and bundle receipts record the same
  actual delivered primary filename, size, and SHA-256 without gaining a
  duplicate intent flag. Bytes and SHA-256 equal the automatic-name run.
  Static renderers retain the existing optional/null `OutputNaming` contract;
  their `Output.FileName` and bundle receipts carry the effective name. Do not
  fabricate token/clock metadata merely to fill the optional summary.
- Invalid, colliding, protected-input, stale-session, cancelled, or failed
  requests leave no visible partial output and do not mutate any selected
  source.
- Source and optional additional-delivery names retain their existing typed
  behavior, including deterministic collision handling.
- Existing loose automatic/manual output tests remain green, and new
  Application, Infrastructure, UI-smoke, localization/accessibility, and
  packaged-Windows checks cover the bundle override end to end.

## Version boundary

This is a `v1.1.4` product/UI-delivery change. It is explicitly excluded
from the verification-only `v1.1.1` release and does not silently append to a
different allocated bundle.

## Local implementation and evidence — 2026-09-06

- The existing primary-name editor stays available with Bundle enabled. Folder
  and primary drafts remain independent across toggles, validation retry and
  Cancel/reopen of the same current accepted session. A changed session does not
  reuse a cancelled draft; a successful run starts a new preparation normally.
- Canonical admission remains automatic. One effective `OutputNameResolution`
  is derived afterward, reusing `CompositionRunRequest` filename/override
  validation, then carried through Intent, Delivery, destination and execution.
  Tokens, accepted sources and compiled A-only plans are not recomputed.
- Platform preflight applies existing Windows name rules to the primary name;
  invalid requests remain in the dialog without filesystem mutation. The
  existing atomic writer retains collision suffixes, staging and receipts.
- Owner follow-up changes `Sources` to `View source files` / `檢視來源檔案`.
  The existing toggle now shares ordinary `inlineDisclosure` styling, neutral
  text, stateful up/down arrows and keyboard focus behavior, rather than a blue
  link. No dialog layout redesign or second source-list owner was introduced.
- Both actual-control and actual-Bundle-execution regressions failed before
  production correction. Final scoped checks: UI **48/48**, Application
  naming/request/bundle **98/98**, Infrastructure bundle **263/263**. UI coverage
  includes full-byte equality between automatic/custom Standard Merge output,
  accepted-source hashes, output naming/report/bundle identity, invalid-name
  recovery, Cancel/reopen, stale-session rejection, loose flow, live modal and
  keyboard disclosure, English/Light and Traditional Chinese/Dark rendering.
  Application includes static-summary preservation and compiled override
  prohibition; Infrastructure covers the existing collision/atomicity boundary.
- Local evidence: `D:/NvtFwCombiner-TestArea/evidence/v114-bundle-rename/`;
  final TRX files are `bundle-final-ui.trx`, `bundle-final-application.trx`,
  `bundle-infrastructure.trx`. Full UI renders are under `render/` and were
  visually inspected by the primary agent; these are production Avalonia
  headless renders, **not native Windows/DPI screenshots**.
- Infrastructure's first narrow invocation was blocked by missing win-x64
  ReadyProbe runtime assets; restoring that existing project's win-x64 assets
  resolved the environment issue before the recorded 263/263 pass.
- Independent Terra/high review of the Application/Infrastructure/ADR diff
  found no P0/P1. Its static-renderer documentation finding was incorporated
  above instead of inventing clock/token provenance. Final committed-head
  integration verification, packaged Windows smoke and required release Golden
  execution remain separate; these local checks are not a release certification.
- The additional `python scripts/verify.py --structure-only` run failed after
  205.5 seconds on code-size ratchets in the accumulated dirty checkout
  (full production 138012 > 136413; runtime 98890 > 98559; Application
  42522 > 42205; Infrastructure/Contracts/worker 30697 > 30683). This run
  does not establish how much belongs to earlier slices versus this slice.
  Derived-file synchronization changed zero files. No ratchet was raised or
  bypassed, and the unchanged expensive check was not repeated. Integration is
  blocked until the accumulated code-size findings receive their proper review.
- Final VM/disclosure scoped review also passed. A proposed stale-refresh
  concern was withdrawn after checking the actual guarded CtrlRAM transition:
  explicit preservation is required when that transition replaces the accepted
  session. An additional **2/2** CtrlRAM automatic/manual-name refresh regression
  passed (`bundle-ctrlram-refresh.trx`); the folder and primary draft survive
  that trusted refresh. Total local scoped cases across these four final test
  runs: **411 passed**, no skipped cases.

### Approved alignment and Desktop default follow-up

Owner accepted [this reference](references/v1.1.4-bundle-delivery-alignment-approved.png)
on 2026-09-06 (generated layout preview, not a native capture). The latest
implementation supersedes the visible `View source files` text described above:
the localized string remains only as tooltip/accessibility name. The output
row is filename, adjacent pencil, then far-right disclosure chevron. The
folder-name pencil and parent picker share a right-hand column and are centered
on their respective value rows, not on label/value groups. Explicit Grid rows
provide the row alignment; the later chevron-only optical adjustment below
does not move these row anchors or hit targets.

Initial `ParentDirectory` uses the OS `DesktopDirectory` known folder, including
Windows redirection, without creating a folder or hard-coding a username/path.
Later operator selections are retained across toggles and Cancel/reopen. If
the OS supplies no existing desktop path, existing required-destination
validation requests a selection; there is no silent fallback destination.

Primary-agent scoped review: no new firmware, execution, validation-policy or
writer change. The real-control layout regression failed in both languages
before correction. The corrected scoped run passed **45/45**
(`alignment-green.trx`), followed by **2/2** desktop-state renders
(`alignment-desktop-render.trx`), including measured center alignment within
1 px, pencil-before-chevron order, absence of visible source text, keyboard
expansion, localized names, desktop initialization and retained custom parent.
Full light/English and dark/Traditional Chinese headless renders were visually
compared with the accepted layout; evidence is in `alignment-render/` under the
same local evidence directory. They are not native Windows/DPI certification.
The previously recorded integration/code-size blocker remains; no full-suite
rerun or ratchet change was performed for this R1 follow-up.

### Source chevron optical follow-up

Owner requested an unfilled expanded arrow and a slightly lower optical center.
The existing inline disclosure now overrides the Fluent checked presenter fill
with transparency, including expanded hover/pressed states; keyboard focus and
collapsed hover remain. Both chevron glyphs translate down 1 DIP without moving
the 34-DIP hit target. A first unitless transform failed at runtime; the final
transform uses explicit `px` units. The final focused source-disclosure and
CtrlRAM reference run passed **6/6**, no skips (`disclosure-final.trx`). Light
English and dark Traditional Chinese expanded headless renders in `arrow-render/`
were visually inspected; these are not native Windows captures. The already-open
native app retains its previous in-memory build until restarted. No firmware
behavior, integration gate, or release status changed.

The owner subsequently approved another 2 px downward adjustment from the
clipboard reference `codex-clipboard-e82d5735-834b-4f3a-b52e-a5a3fa7595b5.png`:
both glyphs now have a total 3-DIP translation. The existing real-control test
first failed **2/2** with expected 3 versus actual 1 (`arrow-3px-red.trx`), then
passed **2/2**, no skips (`arrow-3px-green.trx`). Updated headless renders are
in `arrow-3px-render/`; light collapsed and dark expanded layouts were inspected.
Scoped R1 review: PASS for this glyph-only adjustment; hit targets, focus,
disclosure behavior and firmware contracts are unchanged. Existing integration
blockers remain outside this local visual result.

### Per-item local commit boundary

The owner authorized a separate local commit per completed item. This Bundle
checkpoint includes only its delivery/naming, modal, related tests, reference
and handoff changes. Shared styles and localization are staged by hunk; unrelated
CtrlRAM, Report and metadata implementation remains in the working tree. The
existing sole shared-path design records 10 and 06 accompany record 12 without
claiming their other implementation is committed. Earlier local test results
apply to the documented working-tree snapshots, not an isolated final-head full
gate. Design records stay active; fixed-head integration review, code-size and
release evidence remain open. This commit is not merge or publication approval.
The pre-commit Bundle naming/edit/refresh and Build settings reference selection
passed **16/16**, zero failed/skipped (`bundle-precommit.trx`), against the
accumulated working tree. The partial-index diff also passed `git diff --check`.

### Closure preparation (2026-09-07)

The pending `finalReview.evidence` field in record 12 is cleared to its required
empty pending value. The local Terra/high review and the 411 scoped passing
tests remain recorded above; they are not a committed-head final review. No
admitted design fields, path grants, risk, or test outcomes are changed.

A focused invocation of the existing code-size measurement/validator on
`3a87241e` found 138,010 full-production lines against 136,413 (+1,597),
98,890 runtime against 98,559 (+331), 42,522 Application against 42,205 (+317),
and 30,697 Infrastructure/Contracts/worker against 30,683 (+14). Domain/Profiles
and Bootstrap/CLI/Desktop slices equal their current ceilings. This is a fresh
code-size-only result, not a fresh full structure or product-test pass. The
older 138,012 measurement above remains dated evidence for its earlier tree.
No allowance or base ratchet has been changed. Final closure remains pending.

The subsequent `python scripts/verify.py --structure-only` lane failed in
59.7 seconds with those four size errors and two record errors caused by the
primary agent running before staging the corrected record. The exact record
was then staged for the targeted lifecycle recheck; the failed structure run
is not presented as a pass. Derived-file synchronization changed zero files.
Partial-aggregate warnings are separate review findings, not extra blocking
size errors. No product test or Golden rerun was needed for this record-only
correction, and no previous test result is relabeled as a new pass.

Independent read-only closure audit (`ui_closure_admission`) found the 12
records' 91 unique mutable paths exactly cover the 91 governed changed paths.
The staged-record recheck through the existing
`validate_capability_reuse_governance` function completed historical validation
and reported 11 committed-active-record errors, with no reported path-coverage,
immutable-design or historical-tampering error. Record 12 was not listed only
because its staged blob differs from HEAD; it remains active, not finalized.
The correction's scoped review passed; the complete batch still lacks its
exact-head final review. This is a non-mergeable local checkpoint only.

## 1.1.9 accepted-name recovery — 2026-09-19

Local unit following `f155e10d`: the existing platform name validator now exposes
component validation through the existing Application naming facade. No second
Windows rule owner, firmware change or automatic shortening is introduced.
Blank, reserved, invalid and over-255 UTF-16-unit names (extension included)
are rejected. The existing total-path guard is unchanged.

Primary BIN and Bundle folder edits retain independent accepted names. Done or
Enter accepts a valid edit or restores that field's prior accepted value. A
valid draft is not silently accepted by reopening the editor. Both current
drafts are revalidated after completion; a recovery notice cannot hide a
remaining destination error. Loose output is guarded before picker and run.

Evidence under `D:/NvtFwCombiner-TestArea/evidence/v119-long-name`:

- `component-red.trx`: 2 expected failures / 2 passes before the length fix.
- `recovery-red.trx`: 8 expected failures before edit recovery and loose guards.
- `recovery-final.trx`: 88 UI passes, zero failures/skips; shared modal controls,
  Enter/Done, independent drafts, reopen, parent failures and both delivery modes.
- `name-platform-final.trx`: 29 platform/writer passes, zero failures/skips.
  An old total-path test initially failed because its component was itself 260
  units; it now constructs a legal component with an overlong full path, with
  a separate overlong-component regression. Neither boundary was relaxed.
- `name-architecture-final.trx`: 256 passes, zero failures/skips. The initial
  run had two planning-document failures: a supplemental table was inside the
  canonical version-list section, and the UI handoff exceeded its line ceiling.
  A section boundary and linked cross-version handoff extraction fixed these
  without dropping audit rows or changing checks.

Test-authoring compilation/analyzer failures were corrected without suppressions.
Independent pre-freeze review found uncommitted drafts could become recovery
baselines on reopen; the final run includes the fix and both-field regressions.
Light English Bundle and dark Traditional Chinese loose recovery renders were
visually inspected (`recovery-final/name-recovered-*.png`); these are headless
renders, not native Windows or release-package validation.

Admission: `UI-119-NAME-RECOVERY-02`; exact-head review and record closure follow
the implementation commit. This evidence is local and scoped, not a full-suite,
CI, Golden, integration or publication claim. Issue #434 still includes staging,
collision and final picked-path boundaries; those are not declared fixed here.
