# v1.1.x AB DP metadata layout handoff

Status: locally implemented and verified on 2026-09-06; actual screenshots
recorded below. Not committed, integrated or published. This handoff owns scope, acceptance and diagnosis. Version
allocation is owned by the [NFC roadmap `v1.1.4` milestone](../architecture/nfc_roadmap.md#114-ui-corrections-and-ab-dummy-dp).

The owner-provided AB Code Merge screenshot shows both DP bank project values
truncated while the right half of the facts region remains unused. This handoff
records the later UI correction only. It is excluded from `v1.1.1` and changes
no input decoding, firmware range, route, output byte, naming, or release
content.

## Published baseline checkpoint (2026-09-06)

The first item in the roadmap's initial implementation phase started with
actual-app observation and a screenshot-based proposal, before production edits.
The published `v1.1.3` Windows portable app was opened in Light/English with
`NT51929` and AB Code selected. Loading the public release fixture from case
`nt51929-ab-t05-d06` exposes `D06-00` and `AUTO_PRJ-594` for both DP banks.
No Build was executed and neither input nor expected firmware bytes changed.

At the captured 1920 x 1032 full-window size, both concatenated values fit,
but only the first two of the four desktop fact cells are used. At the restored
1182 x 792 window size, the existing compact two-column layout also displays
these particular values without truncation. This confirms the current combined
fact representation and unused desktop cells, not a fresh reproduction of the
historical long-value truncation. Keep the long-tracker rendered regression in
the acceptance outline below. The owner subsequently approved the final
screenshot-based four-field proposal; the subsequent local implementation and
test results are recorded below.

### Approved reference

- [Published full-window baseline](references/v1.1.3-ab-dp-metadata-baseline.jpg):
  1920 x 1032, Light, English, NT51929 AB Code, DP_AB selected and both TP slots
  empty. SHA-256:
  `076615a22388e769c505c01e7fd8f4814281f1a4ef3c4e8ffcfd9b73d6047ad3`.
- [Owner-approved four-field proposal](references/v1.1.4-ab-dp-metadata-approved.png):
  generated preview at 1710 x 919, based on that actual-app capture with the same
  representative state. SHA-256:
  `75ad91a1a54910944f51a08907ea8ce174b1051c60e439bda11d3fd930dabe61`.
- The owner accepted the final image in response to the four-column placement
  question. Approval covers DP1 Version, DP1 Jira Index, DP2 Version and DP2
  Jira Index, in that order. Other wording, values, geometry and controls remain
  governed by the actual `v1.1.3` baseline and the unchanged-layout contract
  below, not incidental image-generation differences. Render the implementation
  at the baseline viewport and the existing compact breakpoint for comparison;
  do not treat the preview raster dimensions as a new application viewport.

The preview fixture's actual A/B CMD bytes are both `52 06 02` at
`[0x401A,0x401D)` / `[0x4401A,0x4401D)`, so its sub-version is correctly `00`.
The separately certified metadata input has `5F 09 12` and sub-version `01`;
these are different inputs, not evidence that `v1.1.3` reverted to the retired
compact-header reader. This UI correction changes neither read source nor bytes.

## Local implementation and verification (2026-09-06)

The owner authorized implementing the approved reference and providing an actual
screenshot. Local branch `1.1.4` starts at the published `v1.1.3` commit
`e5202e2707d272076d24216222188d478314a07d`; pre-existing documentation edits were
preserved. `FirmwareInspectionProjection.ApplyAbInputFacts` now projects separate
bank facts from the existing typed observations and removes the combined
formatter. The shared fact container binds its existing `StateAutomationText`.
No card geometry, CMD Page reader, profile, firmware bytes or naming changed.

- [Actual desktop capture](references/v1.1.4-ab-dp-metadata-actual.jpg):
  unmodified Windows screenshot, 1920 x 1032, Light/English, the same public
  `nt51929-ab-t05-d06` DP_AB input and empty TP slots as the baseline.
  SHA-256: `db87a2bcbd00eb1f71bac32ff37536a5acd7fa2b47314911eec8951f4b03cf98`.
- [Actual compact capture](references/v1.1.4-ab-dp-metadata-actual-compact.jpg):
  unmodified 1182 x 792 screenshot with the same state. The existing two-column
  layout puts each bank's Version/Jira pair on its own row.
  SHA-256: `1b4c9e2f5162c7c15cda7c4c2ed07b9b641720c092f70caee3a2ea08f22d7c2f`.
- These are the locally built `NvtFwCombiner.Desktop.exe`, not a generated
  proposal or a modified release executable. The caption remains `1.1.3 desktop`
  because this local UI patch does not bump the release version. The full-page
  comparison preserves the shell, card outlines/height, identity, actions, TP
  slots and Output layout; only the four DP metadata cells change. From fresh
  Home, AB Code opens with NT51929 and a populated Mode; loading DP_AB becomes
  Verified. No Build was invoked during screenshot capture.

All test commands loaded the existing user-level `NFC_TEST_AREA_ROOT` and set
`TEMP`, `TMP`, `TMPDIR` to its `temp` child. Evidence is under
`D:\NvtFwCombiner-TestArea\evidence\v114-ab-dp-metadata`.

| Check | Observed result |
| --- | --- |
| Red: new projection/rendered regressions against the old projection | 14 executed: 13 expected failures (combined labels/two facts), TP unchanged case passed; `red.trx`. |
| Narrow tests: `AbDpMetadataTests`, `AbDpFactsUseExistingResponsiveGeometry`, `AbMergeInputsInspectOnLoadAndPreviewThroughSharedRuntime` | 15 passed, 0 failed/skipped; `green.trx`. Covers long/absent/zero Jira, localized Unknown, TP unchanged, real load/preview/Build, and Light/Dark + English/Traditional Chinese geometry at 1362/1050/620 card widths. |
| `dotnet test tests/NvtFwCombiner.UiSmoke.Tests/NvtFwCombiner.UiSmoke.Tests.csproj --no-build --no-restore` | 869 passed, 0 failed/skipped; `ui-smoke.trx`, reported test duration 2 m 6 s. |
| `dotnet build src/NvtFwCombiner.Desktop/NvtFwCombiner.Desktop.csproj --no-restore --verbosity minimal` | Passed, 0 warnings/errors; actual screenshot build. |
| Scoped capability-reuse governance validator | Passed with the staged design-active `UI-114-AB-DP-METADATA-01` record. |
| Scoped Polytail/correctness review | GPT-5.6 Terra, high, read-only: PASS, no P0-P3 findings. Root retains implementation and actual-reference acceptance responsibility. |

The full UI run and screenshots precede only a two-line XAML indentation cleanup;
no behavior changed afterward. This is the bounded local step-1 gate, not a
fresh full-repository verification, Golden run, integration or release claim.
The record remains design-active until an authorized committed review checkpoint;
the frozen `1.1.4` integration/release retains its own required gates.

## Reuse inventory and exact pre-change diagnosis

The screenshot symptom has two coupled Presentation causes; it is not evidence
that the underlying DP observations have lost their structure.

- Application already owns independent typed `Major`, `Minor`, and `TrackerId`
  values in `CompiledInputVersionObservation`. That contract remains unchanged.
- `FirmwareInspectionProjection.ApplyAbInputFacts` already owns the AB slot
  projection, but the former `FormatAbVersion` concatenated one bank's version
  and tracker into a single value such as `D06-00 · AUTO_PRJ-4095`.
- The shared `FirmwareSlotCard` deliberately uses the existing
  `FirmwareSlotInformationFactTemplate` in a four-column desktop `UniformGrid`
  and a two-column compact layout below its 820-pixel card breakpoint. Because
  DP_AB previously contributed only two combined facts, they occupied the first
  two quarter-width cells while the remaining two cells are empty. The shared
  fact template then correctly ellipsizes each combined value inside its own
  narrow cell. This explains the apparent unused width in the screenshot.
- Ordinary DP projection in `UiCompositionRunner.GetDpFirmwareSlotFacts`
  already presents DP Version and Jira Index as separate
  `FirmwareSlotFactViewModel` values. AB must reuse that fact model, template,
  tooltip, trimming, and responsive host; it does not need a new bank-group
  component, layout engine, or metadata model.

## Intended interaction and layout contract

1. A known DP_AB input is projected in this deterministic logical and visual
   order: **DP1 Version**, optional **DP1 Jira Index**, **DP2 Version**, optional
   **DP2 Jira Index**. Version values remain invariant `Dxx-xx`; Jira Index
   values remain invariant `AUTO_PRJ-n`. The middle dot is no longer used as
   semantic structure.
2. When both Jira Index values exist, the four independent facts fill the
   existing four desktop columns. In the existing two-column compact layout,
   DP1 Version/Jira Index form the first row and DP2 Version/Jira Index form the
   second row. No generic `FirmwareSlotCard` geometry is redesigned solely for
   AB, and no fixed two-bank row claim is made when an optional Jira fact is
   absent.
3. A missing tracker, including a decoded tracker value of zero, follows the
   existing Standard DP policy and omits that bank's Jira Index fact instead of
   fabricating a Not applicable placeholder. An unknown bank retains a
   localized Unknown Version fact and exposes no Jira Index fact.
4. Unknown help text is localized through `ShellTextResources`. `DP1`, `DP2`,
   `Version`, `Jira Index`, `Dxx-xx`, and `AUTO_PRJ-n` remain invariant
   technical vocabulary shared with Standard DP; AB does not introduce a new
   `Project` label.
5. Each value retains the shared independent character ellipsis and exact-value
   tooltip. Assistive reading order exposes the rendered label/value pairs in
   the same DP1-then-DP2 order; if the current fact container cannot expose
   those pairs, the smallest shared repair binds its existing
   `StateAutomationText` rather than adding an AB-specific automation model.
6. The selected filename/reveal action, Verified state, Browse/Clear actions,
   desktop card height, identity/action geometry, spacing tokens, minimum
   height, keyboard order, selection/admission, latest-generation inspection,
   and all firmware semantics remain unchanged. Compact height may grow only by
   the existing second fact row; no new wrapper, padding, or breakpoint is
   introduced.

## Acceptance outline for the allocated release

- A projection test with long valid trackers such as `AUTO_PRJ-4095` asserts
  four ordered Version/Jira Index facts with exact independent values and no
  value containing ` · `. `FormatAbVersion` is removed when it has no remaining
  caller; it is not replaced by another concatenation helper.
- Missing-tracker and unknown-bank cases assert that Jira Index is omitted,
  Version remains in DP1-then-DP2 order, and unknown Version help is localized
  in English and Traditional Chinese.
- A rendered production `FirmwareSlotCard` regression first reproduces the
  owner screenshot geometry. At that owner screenshot/approved desktop width,
  all four values, including `AUTO_PRJ-4095`, render without ellipsis and the
  DP2 Jira Index cell occupies the rightmost column. At narrower widths each
  field may trim only within its own cell and exposes the exact value through
  the shared tooltip. In the both-tracker compact fixture, row 1 is DP1
  Version/Jira Index and row 2 is DP2 Version/Jira Index.
- Long-filename coverage proves filename ellipsis/tooltip, Verified, Browse,
  Clear, focus order, and action geometry are unchanged in both themes and both
  languages.
- Accessibility coverage verifies the rendered label/value pairs and
  deterministic reading order. Existing AB inspection/admission and shared selector
  four-/two-column regressions remain green.

## Version boundary

This is the owner-authorized step-1 `v1.1.4` Presentation correction under the
consolidated UI allocation. It is not part of the verification-only `v1.1.1`
release, and its completion does not authorize another UI item or publication.
