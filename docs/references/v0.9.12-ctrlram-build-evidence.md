# v0.9.12 CtrlRAM Build Evidence

Recorded on 2026-07-21. The production-path Build ran from implementation
commit `3dfd00d9133bd3b7d672e72babaaf4ec8372035b`; the subsequent test-only
stabilization commit `99d1bde54b42a52948a45364350741c54289cb29` is the clean
canonical-verification subject. This is R3 regression and release-candidate
evidence for one NT51926 plan. It does not promote public support or substitute
for the independent evidence gates recorded for other IC/plan combinations.

## Environment and clean verification

- Windows 11 Enterprise `10.0.26200`, build `26200`, x64.
- .NET SDK `10.0.302`.
- `python scripts/verify.py --all`: PASS at `99d1bde54b42a52948a45364350741c54289cb29`
  in clean detached worktree `C:\nfcv3e`.
- Release build: zero warnings and zero errors.
- .NET: 2,214 passed, two declared platform skips, zero failed.
- CRC worker: 28 passed with 98.88% branch-aware coverage.
- Repository Python/policy inventory: 108 passed.
- Repository structure, Polytail fast checks, Ruff, Pyright, Pylint, profile
  contracts, golden fixture validation, and package-policy tests passed.

The primary development worktree still contains the pre-existing ignored
owner-handoff archive
`testdata/golden/owner-handoff/combiner-and-51929/intake/929_golden_combiner_source_code.7z`.
That archive remains outside the accepted inventory and was neither copied to
nor accepted by the clean verification worktree.

## Successful production-path Build

The command ran from `C:\nfcv3d` at
`3dfd00d9133bd3b7d672e72babaaf4ec8372035b` and used the real CLI/Workbench
path with the manifest-approved Legacy Combiner 1.13.0 package.

```text
dotnet run --project src/NvtFwCombiner.Cli/NvtFwCombiner.Cli.csproj \
  -c Release --no-build -- ctrlram-replace build \
  --profile NT51926 --ic-num cascade \
  --base testdata/golden/ctrlram-replace/fixtures/derived/20260717/base/nt51926-cascade-tp-base-self-regression.bin \
  --ctrlram replace-ctrlram-vn=testdata/golden/ctrlram-replace/fixtures/20260705/inputs/nt51926-cascade-self-20260705/vn.bin \
  --output C:\nfcv12e\nt51926-ctrlram-build.bin \
  --report C:\nfcv12e\nt51926-ctrlram-build.report.json
```

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| TP base | 245,760 | `9e5321fb7673736c6c52e61549e33347e3852488bd253088db7239d4d0f371fc` |
| VN input | 5,728 | `b855cbc3ce8f9b57e83ee16ce4c902a8089689dfc951e9138dc4294acbf6c5c7` |
| Built output | 245,760 | `f26b6366bc858a751bd0b7bc3be1b6a1ac6edfb4fa25b92b57bea140e193e13a` |
| Manifest expected output | 245,760 | `f26b6366bc858a751bd0b7bc3be1b6a1ac6edfb4fa25b92b57bea140e193e13a` |
| Build report | 13,438 | `c23143c0c06476fec1595e69e0b018bfb56150505ee143472d526d8c9c98ab39` |

The selected profile was
`nt51926-ctrlram-replace-fw141-runtime-cascade`. The processor was
`nfc.nt51926.ctrlram-postbuild-fw1.4.1`, using tool binding
`legacy-combiner-1.13.0` and two ordered commands. The package-pinned
`Combiner.exe` SHA-256 was
`ed6b58289cc780f73d36b831f5424cef44ad93187ba7518d36df6a77ad0c76bf`.

The VN self-replacement changed zero payload bytes. The independent base/output
diff found exactly 16 changed bytes in four complete half-open CRC/header words:

```text
[0x1C,0x20)
[0xFC,0x100)
[0x32F6C,0x32F70)
[0x3304C,0x33050)
```

Base and VN SHA-256 values were recomputed before and after Build and were
identical. The output was atomically committed only after processor completion
and final changed-range validation.

## Blocked Number-mismatch Build

The same immutable inputs were submitted with `--ic-num single`, while readable
FWConfig reported two ICs:

```text
dotnet run --project src/NvtFwCombiner.Cli/NvtFwCombiner.Cli.csproj \
  -c Release --no-build -- ctrlram-replace build \
  --profile NT51926 --ic-num single \
  --base testdata/golden/ctrlram-replace/fixtures/derived/20260717/base/nt51926-cascade-tp-base-self-regression.bin \
  --ctrlram replace-ctrlram-vn=testdata/golden/ctrlram-replace/fixtures/20260705/inputs/nt51926-cascade-self-20260705/vn.bin \
  --output C:\nfcv12e\must-not-exist.bin \
  --report C:\nfcv12e\number-mismatch.report.json
```

The real CLI path returned exit code 1. The intended output
`C:\nfcv12e\must-not-exist.bin` does not exist. The failure report is 8,534
bytes with SHA-256
`1d5202284366c859f190f05ad3afab035339eeea12a494a48dec02ba8053fc8b`;
it records an uncommitted zero-size output result and typed issue
`replace.ctrlram.ic-number-mismatch`:

```text
Selected Number is 1 IC, but the base firmware FWConfig reports 2 IC.
Switch Number to the matching plan and review the CtrlRAM inputs before Build.
```

The failure report remains at `C:\nfcv12e\number-mismatch.report.json`. Both
source hashes remained unchanged. This proves that Cancel/keep-current UI
behavior cannot bypass the backend plan-consistency gate or publish a partial
output.

## Interpretation and residual gates

This run proves actual Windows output publication, exact expected-output parity,
source immutability, declared changed ranges, processor identity, and actionable
failure diagnostics for the NT51926 Common FW 1.x cascade fixture. It does not
prove all 31 runtime plan pairs have independent owner expected outputs. Those
per-plan evidence gaps, firmware-owner R3 approval, support promotion, protected
CI, clean-machine package smoke, and the final release decision remain separate.
