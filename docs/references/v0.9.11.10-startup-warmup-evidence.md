# v0.9.11.10 first-frame and background warm-up evidence

## Scope and decision

`0.9.11.10` is the owner-selected integration branch name, not a four-part
release version. The package and in-app version remain `0.9.11`. The owner
approved composite ReadyToRun and exact 80,000,000-byte ceilings for both the
main EXE and complete release ZIP.

The startup lifecycle is now:

1. create and open the Home window;
2. warm immutable catalog/profile projections on a worker thread;
3. materialize Device Context, Replace, Merge, Settings, and Hex Editor visual
   trees one `DispatcherPriority.Background` step at a time; and
4. retain the materialized controls for subsequent navigation while the
   existing deferred state path applies current profile projections on demand.

Warm-up cannot navigate, open a modal, read a user firmware path, launch an
external processor, select support, change a profile, or acquire Build
authority. A user navigation that happens first retains the original lazy
first-use path, and warm-up never replaces already materialized content.

## Exact implementation nodes

- `d8227f11` fixes the overlapping active-run TextBox that hid the editable IC
  Number selector and adds a dedicated structural regression for every editable
  IC Number surface.
- `2320f94b` adds the first-frame/background startup phases, low-priority page
  materialization, cancellation, fallback, and trace stages.
- `44d232fd` enables compressed composite ReadyToRun publishing and applies the
  owner-approved 80,000,000-byte EXE and complete-ZIP ceilings.
- `f61cf03a` composes IC Number visibility from existing XAML state and removes
  an unnecessary warm-up API so the `MainWindowViewModel` aggregate remains at
  its exact 4,034-line ratchet.

## Exact package evidence

The final package was built from `f61cf03a` with:

```text
./scripts/package.ps1 -Version 0.9.11 -Commit f61cf03acb5e686be7022ccecf28f6d49bfea3a2
./scripts/smoke-release.ps1 -PackagePath ./artifacts/release/NvtFwCombiner-v0.9.11-win-x64.zip
```

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `artifacts/release/NvtFwCombiner-v0.9.11-win-x64.zip` | 75,324,046 | `474a8958c132c5815bc3f8887b0d0c8a59fcd0b0a1486b8dfc31524b2cad69a0` |
| Packaged `NvtFwCombiner.exe` | 69,956,761 | `ebdaffde0537625e8ed2a319d5bce5d0e8a4bb655805e9a4b779c77d956b75c4` |

The deterministic release smoke passed with visible UI launch, closed package
allowlist, hashes, manifests, SBOM/provenance sidecars, and CRC Worker self-test.
Both owner-approved byte ceilings pass. This local smoke is not the required
clean Windows x64 review without separately installed .NET or Python.

## Same-machine startup observation

`scripts/measure-startup.ps1` ran one warm-up and five measured launches of the
exact packaged EXE. The retained observation is:

| Evidence file | Bytes | SHA-256 |
| --- | ---: | --- |
| `artifacts/startup-measurements/packaged-home-0.9.11.10-f61cf03a-working-set.json` | 36,572 | `014a955be4724b0cfc9e8066dd14e4a16f222e9f81af4cbc9bb9d8e03b06fedb` |

| Metric | Median |
| --- | ---: |
| Process start to main window | 682.859 ms |
| Managed entry to `main-window.opened` | 426.221 ms |
| Managed entry to completed background warm-up | 2,422.192 ms |
| Working set when the window first appeared | 193,978,368 bytes |
| Working set after background warm-up | 285,974,528 bytes |
| Peak working set during observed startup | 285,974,528 bytes |

The retained median background deltas were 1,823.560 ms for immutable catalog
warm-up, 26.580 ms for Device Context, 5.780 ms for Replace, 2.410 ms for Merge,
5.720 ms for Settings, and 23.310 ms
for Hex Editor visual-tree materialization. Dispatcher waits are included in
those per-step deltas. They prove the common pages were actually built, not
merely assigned as hidden DataContext values.

The working-set increase from first window to completed warm-up is 91,996,160
bytes. This is the measured memory trade-off for removing common-page cold
materialization on this machine. No universal startup, navigation, or memory
claim is made from five local samples.

## Verification and residual gates

- dedicated IC Number selector test: passed;
- focused startup warm-up tests: passed;
- full UI smoke: `247/247` passed;
- Architecture: `96/96` passed before packaging;
- release package policy: `11/11` passed;
- code-size policy: `5/5` passed;
- visible local release smoke: passed.

Final handoff still requires the canonical verifier, independent R2
architecture/UI review, independent R3 release review, and a clean-machine
Windows x64 package smoke. The pre-existing owner-handoff archive under
`testdata/golden/owner-handoff/combiner-and-51929/intake/` remains outside this
scope and must not be deleted, re-inventoried, or used to weaken verification.
