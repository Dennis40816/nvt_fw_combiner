# v0.10.4 Home startup 700 ms evidence

Status: candidate evidence; the exact-review-commit package measurement and
complete repository verifier pass are recorded. The measured median remains
above 700 ms, so the performance gate is open. Owner review and the official
release-pipeline rerun remain required before release.

## Contract

The controlled package gate uses the repository's
`scripts/measure-startup.ps1` runner and the exact compressed single-file,
self-contained `win-x64` publish shape. One launch warms the OS/runtime path but
is not scored. The following five Home launches are measured from external
process creation to a nonzero main-window handle. Their median must be at or
below 700 ms. The unscored cold launch is retained as a separate observation;
this local gate is not a universal CI timing threshold.

Publish flags are `PublishSingleFile=true`,
`EnableCompressionInSingleFile=true`, `PublishReadyToRun=true`,
`PublishReadyToRunComposite=true`, `PublishTrimmed=false`,
`IncludeNativeLibrariesForSelfExtract=true`, and self-contained `win-x64`.

## Controlled environment

- Windows 11 Enterprise `10.0.26200` build `26200`
- Intel Core i9-10900F @ 2.80 GHz
- Ultimate Performance power scheme
- Home page, one unscored warm-up, five measured runs, 30-second timeout
- official `v0.10.3` release ZIP SHA-256:
  `51ff7f63d579e6c1248bca26fe1eb7f6ed2c24929ce4a2e824114f0a8686a180`
- `v0.10.4` candidate base: official `v0.10.3` commit
  `6301049e59432b093abe2bea676d0e223fa488cd`
- measured production patch id against the official `v0.10.3` base:
  `833eabea281a2641d3789feb54504c28a5a7e835`
- candidate EXE SHA-256:
  `ae025849a6fa854b1efa3bcdc09616fdef43aa45d2f85f3008c6431427f6bc54`

## Exact review commit result

- source commit:
  `ce8aa67b1bd253ec81b30c88bc2e1fe1088d2ec3`
- application SHA-256:
  `a683e685aec09e5cd29885318552359468b991b880ed8df74186cf1af3521619`
- package ZIP bytes: `77,156,319`
- package ZIP SHA-256:
  `b9bf323ae0ff20075c6d2086357a4dae2e2cc31c2a4f65337e41d5d559821d5d`
- measurement JSON SHA-256:
  `4c176429a5a9bfbe93997433c1252d5367f6b3045ebbe8dfab737d54f9c8eeaf`

| Observation | exact review commit |
| --- | ---: |
| Unscored cold process-to-window | 1,129.395 ms |
| Five-run process-to-window minimum | 784.604 ms |
| Five-run process-to-window median | **795.627 ms** |
| Five-run process-to-window maximum | 868.720 ms |
| Managed entry to opened median | 460.150 ms |
| First-frame synchronous UI work median | 154.245 ms |
| Allocated bytes at first window median | 10,315,784 |
| Working set at first window median | 207,228,928 |

The five scored runs were `784.604`, `868.720`, `807.002`, `789.992`, and
`795.627` ms. This misses the gate by `95.627` ms and is not a pass. Under the
same current workstation conditions, the preceding package without the
percentage surface measured `803.429` ms median, so the percentage change does
not show a startup regression; that relative comparison does not replace the
absolute gate.

The package closed-allowlist smoke, manifest/hash checks, bundled CRC-worker
self-test, and visible-window startup all pass for this exact package.

## Previous controlled result

| Observation | official v0.10.3 | v0.10.4 candidate |
| --- | ---: | ---: |
| Unscored cold process-to-window | not used as gate | 1,135.034 ms |
| Five-run process-to-window minimum | 3,681.52 ms | 659.894 ms |
| Five-run process-to-window median | 3,739.346 ms | **661.784 ms** |
| Five-run process-to-window maximum | 3,771.79 ms | 679.622 ms |
| Managed entry to opened median | 3,471.571 ms | 391.754 ms |
| First-frame synchronous UI work median | 3,207.586 ms | 143.362 ms |
| Allocated bytes at first window median | 922,031,848 | 10,327,416 |
| Working set at first window median | 256,888,832 | 202,649,600 |
| Main EXE bytes | 71,976,916 | 72,033,633 |

The candidate improves the measured process-to-window median by
3,077.562 ms (82.3 percent) and passes the 700 ms gate. All five scored runs
remain below the threshold: `659.894`, `679.622`, `660.199`, `661.784`, and
`677.527` ms. The complete candidate measurement JSON SHA-256 is
`010ed2175790533fb998b0a4b526fa823ad49dad5f5e9f5f598ee5d42038536c`;
the baseline JSON SHA-256 is
`e97e6e39dceeb2d512ca6ec15feae0e89a0f700f107e92399fd3eee279557453`.

This result predates the foreground percentage surface and is retained as
historical evidence only. It is not the exact-review-commit measurement for the
current candidate.

## Root cause and correction

The first Home shell constructed `WorkflowSessionPresentationViewModel`, which
immediately requested AB profile summaries, all IC ids, and the default IC.
Those read-only Presentation queries called the lazy canonical catalog's first
publication synchronously. A diagnostic release build localized that one call
to about 3.2 seconds and about 0.9 GB of allocation before the first window.

The correction restores the already-specified startup lifecycle:

1. shell construction creates Home without reading the catalog publication;
2. Home workflow commands remain disabled while the canonical publication is
   unavailable;
3. the existing post-open worker performs `WarmCanonicalCapabilities`;
4. the UI-thread continuation publishes the exact canonical IC, default IC,
   and AB-profile choices once; and
5. only then are the six Home workflow-entry commands and the Merge/Replace
   navigation commands enabled, the requested catalog-backed launch page is
   applied, and catalog-backed workflow localization/materialization is
   allowed.

If canonical publication fails, those entry points remain disabled and the
failure is traced; Presentation does not substitute inferred or stale support.
The visible Home window is covered by the reusable foreground loading surface
while publication is pending, so pointer/keyboard selection cannot race the
catalog. The startup coordinator reports only observable lifecycle milestones:
foreground preparation (`0%`), catalog work starting (`10%`), background
catalog warm complete (`80%`), UI publication starting (`90%`), and UI
publication complete (`100%`). The surface shows those exact milestones beside
the Preparing title; it does not estimate elapsed time, bytes, or
external-processor work. Failure keeps the blocking surface visible with an
explicit localized retry action. Retry uses the existing explicit catalog
reload contract rather than replaying a cached cold-start failure. The
percentage is included in the polite accessible status. Reduced-motion mode
keeps the same determinate value without decorative animation.

No IC is hard-coded, no second catalog/cache is introduced, and Presentation
does not infer support. The broader observable, cancellable, bounded, and
user-controllable preload lifecycle remains `v0.10.5` scope.

## Evidence and residual gates

- A focused UI-smoke behavior test proves shell construction exposes no IC
  choices and no enabled Home workflow entry before warm-up, then publishes
  canonical choices and enables entry after the existing warm-up.
- The startup structure test locks publication after `await catalogWarmup` and
  before post-startup diagnostics/page materialization.
- The firmware compiler, profiles, ranges, processors, naming, reports, and
  golden fixtures are untouched. The focused UI suite passes 454/454 and
  Architecture passes 213/213. GoldenRegression's xUnit runner passes 17/17;
  that runner count covers a canonical inventory of 46 cases: 32 direct
  complete-output goldens, 2 input-only direct-evidence cases, and 12
  fact/workflow aliases. The canonical manifests reference 234 BIN artifacts
  backed by 231 physical BIN files, with no fixture or expected-hash changes.
  `python scripts/verify.py --all` passes all structure, Python, .NET,
  coverage, and repository policy lanes on the frozen diff.
- The official release pipeline must repeat the measurement from the exact
  review commit after a future candidate first meets the local threshold. The
  current cold launch and warm median are both above 700 ms and are not
  represented as passing the contract.
