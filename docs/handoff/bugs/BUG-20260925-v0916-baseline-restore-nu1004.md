# BUG-20260925-v0916-baseline-restore-nu1004: pinned v0.9.16 executor restore fails NU1004

Status: open
Severity: P1 while it blocks the 1.1.12 C-3 comparison as dispatched; P2 once the commander accepts a documented deviation, because the certifying consumer of this contract is deferred to 2.0.0 (ADR 0057)
Found: 2026-09-25, Claude Code (Opus 5.5, WS-PARITY sub-agent), while building the v0.9.16 predecessor executor for the non-certifying 1.1.12 comparison, at `feature/1.1.12/v0916-alignment`@`5648d0fd1`
Where: `docs/contracts/v0916-baseline-executor-v1.json` (`restore.arguments`); the seven CLI-graph `src/*/packages.lock.json` files at tag `v0.9.16` (peeled `462590e8b993b8e42d088bc07377571a4bb9f25d`); consumer `scripts/v0916_parity_certification.py` `_run_verified_build`
Observed: on SDK `10.0.303`, with a `git archive v0.9.16` export whose `global.json`, seven lock files and seven external-tool files all match the contract's sizes and SHA-256, the exact pinned restore `dotnet restore src/NvtFwCombiner.Cli/NvtFwCombiner.Cli.csproj --locked-mode --runtime win-x64` exits 1 with `NU1004` for all seven projects. The message names the runtime identifier change: the lock files have no `net10.0/win-x64` target. They also still declare first-party project ranges `[0.9.2, )` while the projects are `0.9.16`; whether that alone would also trigger `NU1004` was not isolated. The pinned build then fails because no assets exist. The failure is deterministic and does not depend on the network or cache.
Expected: ADR 0057 and the executor contract state that the exact locked restore and no-restore Release build produce CLI assembly SHA-256 `093212528d1048acdb43563ba795353e1ac872f8b6a3aa99251782e958ad1a30`; `_run_verified_build` requires both commands to exit 0.
Evidence: logs in the test area under `parity-1112/logs/b-restore.log` and `b-build.log`. Feasibility probe (not used for any comparison): the same export restored with `--force-evaluate --runtime win-x64` (unlocked), then built with the exact pinned build command, gives `NvtFwCombiner.Cli.exe` 162304 bytes with SHA-256 `093212528d1048acdb43563ba795353e1ac872f8b6a3aa99251782e958ad1a30`, equal to the contract; its runtime closure has the contract's 431 files but 88,359,996 bytes and closure SHA-256 `e77fe0742b46780bb6839ba4184beebd42364e41e935790270a11ab62644f42a`, against the contract's 88,367,164 bytes and `18da1123...c2b3b0`. Hypothesis, not verified: the 7,168-byte gap is commit metadata (SourceLink) that a Git-less archive export cannot embed. The probe's lock rewrite only added empty `net10.0/win-x64` targets and changed first-party project ranges from `0.9.2` to `0.9.16`; no package id, version or content hash changed (logs `bp-restore.log`, `bp-build.log`).
Owner: unassigned; the commander decides whether option A may use the probe executor, and the 1.1.13 formal comparator owns the contract fix
Resolution:
