# 1.1.12 board

Writer: Claude Code (commander). Workers update only their own workstream
logs, `1.1.12/<WS-ID>.md` on their own branches. Protocol:
[handoff README](README.md).

Last updated: 2026-09-25, after the base refresh and a review of every handoff
document against the published `v1.1.11`.

## Identity

- Branch model from 1.1.12 on (WS-GOV decision 10): trunk `1.1.x`; feature
  branches `feature/1.1.12/<topic>` merge into it by pull request; the release
  branch `1.1.12` is cut from `1.1.x` only at feature freeze.
- Trunk `1.1.x` (local until checklist A-5) sits on the published `v1.1.11`:
  tag object `2a80cebe6`, peeled `1c37bd718` (equal to `origin/main`), tree
  `f7dcfbd03`; GitHub Release published 2026-09-25 06:46 UTC; release run
  `36103035279` with `candidate`, `promote` and `published-smoke` successful
  (v0.9.16 parity jobs skipped). This is a valid release base
  ([branch authority](../governance/branch-version-and-release-governance.md#establish-branch-authority)).
- Worktrees live in one sibling folder, written `<worktrees>/<name>` in these
  logs. Keep worktree folder names short: long names push Golden input paths
  past the Windows path limit and `git worktree add` fails. The `1.1.x`
  worktree belongs to the commander.
- Allocation owners, now on the trunk: roadmap
  [1.1.12 startup optimization](../architecture/nfc_roadmap.md#owner-approved-1112-startup-optimization--2026-09-25),
  [TP Header SVN modeling](../architecture/nfc_roadmap.md#owner-deferred-tp-header-svn-modeling--2026-09-25),
  [Header backup CRC investigation](../architecture/nfc_roadmap.md#owner-deferred-header-backup-crc-investigation--2026-09-25);
  audit handoff [startup detail](../architecture/post-v1.1.8-audit-handoff.md#startup-optimization-assigned-to-1112--2026-09-25).

### Base refresh after `v1.1.11` is published: done 2026-09-25

1. Done: `git fetch origin --tags --prune`.
2. Done: `git rebase --onto v1.1.11 a90c640f2 1.1.12`, then
   `git branch -m 1.1.12 1.1.x`; the worktree moved to `nfc-worktrees\1.1.x`.
   21 handoff commits sit on the tag; none conflicted (`v1.1.11` has no
   `docs/handoff/`).
3. Done: all ten feature branches rebased onto `1.1.x` in their own worktrees,
   each keeping its commit count.
4. Done: identity above; the roadmap and audit handoff carry the 1.1.12
   sections, linked above.
5. Pending owner approval (checklist A-5): push `1.1.x`; delete the merged
   `1.1.11` and `feature/1.1.11/reliability-and-info` remote branches (release
   closure); enable automatic deletion of merged pull request branches; tell
   Codex to name branches `feature/X.Y.Z/<topic>`; then create the allowlist
   ruleset (WS-GOV decision 9) with `refs/heads/main`, `refs/heads/*.*.*`
   (covers `1.1.x` and `X.Y.Z`), `refs/heads/feature/*.*.*/**` and
   `refs/heads/dependabot/**`, rule `creation`, no bypass actor.

After any 1.1.12 commit is pushed or pinned by a change record, bring newer
tags in only by merge: records bind exact SHAs.

### Review against `v1.1.11`, 2026-09-25

Every handoff document was re-checked against the published tag
(`v1.1.10..v1.1.11`: 180 files, +9,502/-686).

- Unchanged in 1.1.11, so the WS-GOV and WS-AI premises hold: `AGENTS.md`,
  the skill manifest and routing, the execution runbook, branch governance,
  `CONTRIBUTING.md`, `ci.yml`, `release.yml`, `verify.py`, and every source
  file WS-FLOW F1, F2, F3 and F9 cite (their line numbers still match).
- Changed and updated in the logs: 1.1.11 moved all eight AB Merge profiles to
  a new full-copy schema 2.17 (WS-FLOW F4); it added 22 capability-reuse
  records, 9 attestations, a second hard-coded cutover SHA and two new record
  rules (WS-GOV evidence); the reviewer-independence check moved to
  `validate_repository.py:3003,3029`; two skills changed only in body text
  (WS-AI verdicts unchanged).
- Bugs: `record-filename-taskid` fixed in `v1.1.11`; the other six re-verified
  as still open.

## Roles

| Role | Who | Owns |
| --- | --- | --- |
| Owner | Dennis | Decisions, R3 approval, push, PR, GitHub writes, release |
| Commander and integrator | Claude Code | This board, dispatch, review, integration into `1.1.x` |
| Worker | Codex | Assigned feature branches and their logs |

## Owner decisions 2026-09-25

These stay here until the governance-reset batch records them in their
canonical owners: the roadmap (1, 5), the governance ADR (2, 3, 4, 7),
`AGENTS.md` delegation (6) and `AGENTS.md` plus the handoff README (7).
Decision 8 was temporary and ended when `v1.1.11` was published on
2026-09-25.

1. 1.1.12 also carries the agent-collaboration and engineering-efficiency track
   below, in addition to its roadmap allocation (linked under Identity).
2. One governance reset is authorized: one ADR and one admitted batch instead of
   per-file R2 ceremony.
3. Claude Code commands; Codex collaborates from 1.1.12.
4. Capability-limiting skills in `~/.codex/skills` may be removed after
   per-item owner confirmation (WS-AI decision 4).
5. Close 1.1.11 first; 1.1.12 collaboration starts after `v1.1.11` is
   published.
6. Assign Codex models by demonstrated fit, not by permanent role.
7. Every agent, Codex included, records found bugs in the
   [bug ledger](README.md#bug-ledger) instead of leaving them in chat.
8. Nothing may disturb the 1.1.11 release: until `v1.1.11` is published, no
   GitHub setting, ruleset, Codex environment, shared-checkout or 1.1.11-branch
   change comes from the 1.1.12 track. 1.1.12 changes stay inside the 1.1.12
   branches.
9. **Release by 2026-09-28** (owner, 2026-09-25; supersedes decision 1 and the
   phase order below for this release). 1.1.12 ships C-1 startup
   optimization, C-3 v0.9.16 alignment and the handoff protocol with the bug
   ledger. C-2 (WS-IO F07/F08) ships only if its pull request is green by
   2026-09-27 18:00 +08:00; otherwise it moves to 1.1.13. If startup misses
   500 ms / 2,000 ms at freeze, the measured gains ship, the release notes
   state the gap and its blocker, and the targets stay. Everything else moves
   to 1.1.13: Phase B, WS-AI, WS-TEST, WS-FLOWFIX, WS-HDR and WS-DOC
   follow-ups. Release path: the proven 1.1.11 path (integration branch
   `1.1.12` from `main`, `feature/1.1.12/<topic>` pull requests into it, one
   release pull request into `main`); the `1.1.x` trunk model starts in
   1.1.13. The commander may push `1.1.12` and `feature/1.1.12/*` and open
   pull requests into `1.1.12`; merges, parity-difference dispositions and
   the release stay per-action owner approvals. The commander launches Codex
   headless with `codex exec`.

10. **Accepted commander proposals** (owner, 2026-09-25, amends 9):
    - v0.9.16 alignment in 1.1.12 is a non-certifying local comparison of
      the 37 plan routes that have canonical inputs (option A). The harness
      and the difference table live in the WS-PARITY lane; `scripts/`,
      workflows, contracts and ADR 0057 stay unchanged; the 27 routes
      without inputs are reported as not covered. The formal 1.x comparator
      moves to 1.1.13.
    - The commander may merge pull requests into `1.1.12` when CI is green,
      the cross-runtime review has no P0/P1 finding and no firmware
      semantics change. `main`, the release pull request and the release
      stay owner approvals.
    - `dotnet-trace` and `dotnet-counters` are installed for startup work.
    - Codex quota is limited: Codex keeps WS-IO and the cross-runtime review
      of Claude's pull requests. Startup B (WS-WINDOW) folds into WS-PERF
      (Claude); WS-PARITY runs as a Claude sub-agent.
11. **Startup and memory** (owner, 2026-09-25, accepted commander proposals):
    - First window within 500 ms: option A. 1.1.12 keeps compressed
      composite ReadyToRun (the 2026-09-11 decision and the 80,000,000-byte
      EXE ceiling stand); the release notes state the gap and its blocker
      (single-file decompression before managed entry). Trimming and
      composite exclusions are evaluated in 1.1.13.
    - Memory joins the startup acceptance: peak private bytes and peak
      working set must not regress against the `v1.1.11` package shape
      (330 MB and 335 MB), and the GC heap after warm-up stays at or below
      50 MB. Each measurement records these.
    - The commander may draft an ADR 0049 amendment for bounded parallel
      bundle loading inside the catalog stage and implement it (R2, Codex
      review); it merges only if units 2 and 3 leave the 2,000 ms target
      unmet and it stays within the memory budget.
12. **Parity, flash capacity and preload** (owner, 2026-09-26):
    - v0.9.16 alignment: the NT51951 and NT51950 2-IC cascade Diff NF
      preservation differences are approved. The 27 routes without canonical
      input have no Golden and stay not covered. The NT51950 2-IC cascade
      full-flash rejection (both versions) is investigated for its most
      likely cause and corrected if needed.
    - NT51950 and NT51951 flash capacity is no longer fixed (Display OSD; see
      the NT51950 AB Code family). Remove the hard size limits from their
      CtrlRAM maps (R3 profile change, firmware owner = owner; design review,
      Golden evidence and exact write-range audit apply).
    - Startup: bounded parallel bundle preload (ADR 0075, 4 workers) is
      adopted. The owner accepts its 2-4 MB peak private-bytes increase over
      the `v1.1.11` package shape; the other memory gates stand.
13. **Scope maintained** (owner, 2026-09-26): the 2026-09-28 release keeps the
    decision 9 to 12 scope. WS-IO F08, TP Header SVN display (C-7) and further
    architecture optimizations (the VersionManagement local-reference rule and
    duplicate JSON contexts) stay in 1.1.13 with the governance reset, test
    architecture and WS-FLOW corrections.
14. **1.2.1 additions** (owner, 2026-09-26): (a) an architecture review that
    simplifies profiles around the core concepts (Perfect family, Partial
    family and the like) and removes duplicated authoring; judges which flows
    are over-engineered or redundant and can be deleted or simplified; and
    finds where new ICs need more flexible adaptation; (b) a review of the UI
    interaction code. Both join the existing 1.2.1 profile reference
    convergence and Launcher scope. Also on 2026-09-26: every 1.1.12 item must
    ship, including the NT51950/NT51951 capacity change; AB flash is decided by
    finding two NVT markers.
15. **Owner verification reminders** (owner, 2026-09-26): the 1.1.x line is
    expected to conclude with a 1.2.0 release. At the 1.2.0 release approval
    (owner correction 2026-09-26: not at 1.1.x releases), the commander reminds
    the owner to verify: (1) NT51929 Perfect family DP CMI reads cmd1page0
    `0x16`-`0x18` completely; (2) the owner's NT51950 AB Code with Display OSD
    merges; (3) the public NT51950 2-IC B Code bank is at `0x8A000`.
16. **Private-bytes allowance** (owner, 2026-09-26, amends 12): the frozen
    candidate `badc545b0` measured peak private bytes 331.7-334.8 MB against
    327.9-329.2 MB for the `v1.1.11` package in the same quiet session
    (medians 334.1 and 328.8), while peak working set (at most 334.5 MB) and
    GC heap after warm-up (at most 34.8 MB) passed. The private-bytes
    allowance is raised to at most 6 MB above the `v1.1.11` baseline, and
    1.1.12 ships on the planned date; the release notes state the measured
    increase.
17. **Nonstandard CtrlRAM Base lengths** (owner, 2026-09-26): 1.1.12 supports
    NT51950/NT51951 CtrlRAM Bases of any length, not only the published
    Standard lengths (256 KiB, 512 KiB, 1 MiB). This needs its own R3 record
    (CTRLRAM-OSD-ENVELOPE-CLASSIFY-1112-01) because admitted record contracts
    are immutable.
18. **Working-set gate** (owner, 2026-09-26, amends 11): the final product
    source `dc773e967` measured peak working set 331.3-335.6 MB (median
    333.2) against 331.7-335.3 MB (median 333.7) for the `v1.1.11` package in
    the same quiet session; one launch exceeded the fixed 335 MB bound by
    0.6 MB, as did `v1.1.11` itself. The per-launch working-set gate becomes
    the same-session `v1.1.11` maximum plus 1 MB (here 336.3 MB); the
    difference is treated as measurement noise and 1.1.12 ships.
19. **Review findings before release** (owner, 2026-09-26): the first release
    run stopped on an unresolved P1 automated review thread (pull requests #448
    and #449). The owner chose to fix in 1.1.12 the two F07 P1 findings and its
    small P2 findings, the pre-Build nonstandard-size warning and the
    exact-capability prefix classification (new R3 record), and to ship the
    Display OSD NVT marker case as a documented fail-closed known issue for
    1.1.13 ([bug](bugs/BUG-20260926-osd-tail-nvt-marker-blocks-base.md)).
20. **NVT marker rule direction for 1.1.13** (owner, 2026-09-26, draft, owner
    still refining): the Display OSD NVT marker fix belongs to 1.1.13, not
    1.1.12. Direction: AB evidence should come from the NVT marker at the
    position after each bank's FWConfig Backup region (the NVT End Flag), with
    at least one each in the A and B code, instead of counting markers across
    a whole bank or TP range, so DP and Display OSD bytes never count. Open
    points for the R3 design: at least one versus exactly one, and whether the
    Standard FWConfig Backup checks use the same position rule.
21. **Release workflow cleanup** (owner, 2026-09-26): the owner finds the
    release workflow full of odd steps; clean it up in 1.1.13 together with
    the release re-run conflict (WS-GOV). The commander inventories the
    workflow for owner confirmation first.
22. **1.1.13 integration branch** (owner, 2026-09-26): 1.1.13 integrates on the
    `1.1.x` trunk with `feature/1.1.13/<topic>` branches (WS-GOV decision 10).
    Before its first push the trunk's handoff history is squash-sanitized, and
    the GitHub settings of checklist A-5 still need their own approval.
23. **Trunk sync rule** (owner, 2026-09-26, option A of the release re-run
    analysis): the `1.1.x` trunk's first push is squash-sanitized and rebased
    onto `main`, never merged with it; after every release pull request merges,
    the trunk is fast-forwarded to `main` before anything is finalized again.
    The root fix (retiring capability-reuse history replay) is 1.1.13 WS-GOV work.
24. **1.1.13 GitHub writes** (owner, 2026-09-26): as in 1.1.12, the commander
    may push `feature/1.1.13/*` branches and open pull requests into `1.1.x`;
    merging into `1.1.x` or `main` and releasing stay per-action approvals.
    The checklist A-5 GitHub settings are asked item by item after the trunk's
    first pull request has passed.
25. **CLI exit code after a post-commit report failure** (owner, 2026-09-26):
    the exit code stays 0 when the output is committed and only `--report`
    fails; the CLI prints the `cli.report.failed` partial-success issue, as for
    a failed loose delivery after the primary commit.
26. **Wave 1 merge** (owner, 2026-09-26): pull request #457 merges into `1.1.x`
    with a merge commit once its checks pass and no P1 review thread remains
    unresolved.
27. **F08 presentation** (owner, 2026-09-26): a failed local-state save shows a
    persistent status-bar notice with a Retry action that stays until a save
    succeeds; it does not block other work.
28. **CI H1 deadline split** (owner, 2026-09-26): the write-lease test helper
    gets a separate budget for starting its child process; the readiness
    deadline after start stays 10 s.
29. **Release workflow inventory** (owner, 2026-09-26): the seven-item
    inventory on the 1.1.13 board is accepted as the cleanup scope; the owner
    may add items later. The cleanup is R3 and its design returns to the owner
    before implementation.
30. **NVT marker rule** (owner, 2026-09-26, final form of decision 20): each
    bank's layout-declared end-flag position right after its FWConfig Backup
    region (TP section end minus 4) must hold `00 4E 56 54`; a marker anywhere
    else is neither counted nor rejected. AB inputs need one in bank A and one
    in bank B; Standard inputs use the same position rule for their one bank.
31. **TP SVN undefined flag bits** (owner, 2026-09-26): only `0x80`, `0x40` and
    `0x20` are flags (A-9). When the SVN flag byte has any other bit set, the
    stamping script may be faulty: the display shows the parsed raw value with
    a small warning icon on its right whose tooltip says the stamping script
    `InsertPID.py` is suspect. This explains the three `0xC3` Golden TP inputs
    as an anomaly instead of a seventh revision digit. The `0x60` combination
    (DIFF_EXIST with NO_SVN_RECORD, without LOCAL_BUILD) is normal: it marks
    firmware built by Jenkins, and gets no icon.
32. **Navigation underline** (owner, 2026-09-26): only the selected page's
    navigation tab shows the blue underline. Keyboard focus stays visible but
    must not look like selection, and startup focus goes to the selected page's
    tab instead of always Home
    ([bug](bugs/BUG-20260926-nav-focus-looks-selected.md)).
33. **UI reference images for F08 and C-7** (owner, 2026-09-26): the owner
    approved the Codex-generated mockups built from the real NT51923 Standard
    Merge screen as the development references:
    [TP SVN warning](../ui/references/v1.1.13-tp-svn-warning-approved.png) and
    [save-failure status strip](../ui/references/v1.1.13-f08-save-failure-approved.png).
    Implementations align with their layout and look, while text sizes, strip
    height and button size follow the existing specifications (13 px text as in
    the startup preparation strip; Retry styled like Browse). New UI work starts
    from such a reference image built on the real screen.
34. **F08 and TP SVN presentation revision** (owner, 2026-09-26; amends 31 and
    33): the TP SVN field shows only the raw 4 bytes, followed by an icon in
    every case — an info icon whose tooltip shows the parse result (flags and
    revision), or, for an anomaly, a warning icon whose tooltip adds the
    warning to the parse result. Anomalies are undefined flag bits
    (`InsertPID.py` may be faulty) and an unstamped `00 00 00 00` value
    (`InsertPID.py` may not have run). `0x60` comes from Jenkins on the server,
    where a non-zero revision is normal. The F08 Retry button shows only its
    icon, with the text in a hover tooltip. Icons are crisp vector icons. The
    owner prefers Codex's generated mockup style; the reference images are
    regenerated to match. The owner approved the regenerated set, which replaces
    the decision 33 images:
    [TP SVN warning](../ui/references/v1.1.13-tp-svn-warning-approved.png),
    [TP SVN states](../ui/references/v1.1.13-tp-svn-states-approved.png) (in
    that sheet only the SVN field is normative; card headers follow the real
    screen) and
    [save-failure status strip](../ui/references/v1.1.13-f08-save-failure-approved.png).
35. **NVT marker scope** (owner, 2026-09-26): the decision 30 position rule
    applies in 1.1.13 only to NT51950/51951, which fixes the Display OSD case;
    NT51923/926/927/928 move to a later small R3 item. NT51919/929/932 keep
    the single-marker search within the template range for now; the owner
    notes their Backup end is computable from the IC count, a later item. The
    meaning of "Standard" in decision 30 is still being clarified.
36. **TP SVN card details** (owner, 2026-09-26): a card shows the TP SVN of the
    TP code (TP start + `0x24`); an input that contains AB code also shows the
    B code's TP SVN, with the same icon and tooltip rules. FWConfig SVN bytes
    and FWConfig Backup content never appear on screen, and the SVN copy inside
    the Header copy is not shown on the card. Warning icons keep the existing
    outline triangle (info uses an outline circle); the TP Version warning
    stays unchanged. Correction: the NT51950 T80 TP input is stamped at TP
    start + `0x24` (`0xA024`); the all-zero Golden example is
    `nt51932-gen-flash`.
37. **NVT marker rule coverage** (owner, 2026-09-26, answers the open point of
    decision 35): for NT51950/51951 the position rule applies to every use of
    the marker — CtrlRAM Base classification, the FWConfig Backup validations,
    and reading a TP input's FWConfig in Merge and DP Replace. Changing the
    shared Standard Merge and General Merge profile family and re-pinning it is
    accepted even though saved General Merge custom rules may become stale:
    General Merge is hidden and will be reimplemented in a later version.
38. **Pre-built profile catalog** (owner, 2026-09-26, after evaluating a local
    database): built-in profiles stay reviewed JSON under the hash-pinned trust
    index; the build derives a pre-built catalog snapshot with the same catalog
    code and ships it, and a profile update regenerates the snapshot. A local
    database is not adopted for built-in profiles (not reviewable as diffs, not
    byte-stable for hash pins, no query need). Online updates stay possible
    through the trust index. In 1.1.13 as an R2-R3 item: ADR and independent
    design review first, then implementation, accepted by measured startup
    time and snapshot-versus-JSON catalog equivalence.
39. **1.1.14 display-convention consistency** (owner, 2026-09-26): 1.1.14 adds a
    repository-wide inventory and improvement of display conventions. Examples
    from the 1.1.13 mockup reviews: field icon shape, size and spacing (outline
    circle info versus triangle warning; 12 px next to the value), tooltip
    styles (plain text, the issue-details card, invented callouts) and their
    placement and content order, keyboard focus versus selection indicators
    (decision 32), status strips (startup preparation and the F08 notice),
    icon-only versus labelled buttons (Retry), A/B naming (`TPA`/`TPB`, `(A)`/
    `(B)`, "A code"/"B code"), and bilingual string consistency. Until then,
    1.1.13 work follows the existing conventions.
40. **TP SVN reference images, fourth set** (owner, 2026-09-26): approved as
    the C-7 development references, replacing the earlier TP SVN images:
    [Standard anomaly](../ui/references/v1.1.13-tp-svn-warning-approved.png),
    [AB Base](../ui/references/v1.1.13-tp-svn-ab-approved.png) and
    [states](../ui/references/v1.1.13-tp-svn-states-approved.png). Rules: a
    12 px outline icon sits about 4 px after the value, vertically centred;
    the tooltip uses the standard tooltip look (small box, thin border, no
    arrow, content-sized, at most 360 px wide) below and right of the icon
    without covering the value; normal content is two lines ("Flags: …",
    "Revision: …"); an anomaly adds a first line with the warning triangle and
    the warning text. AB inputs label the fields `TP SVN (A)` and `TP SVN (B)`,
    following the card's existing `(A)`/`(B)` convention.
41. **Public failed-test evidence** (owner, 2026-09-26): when a CI test project
    fails, its TRX is uploaded unchanged (error messages and stack traces
    included, three-day retention); the same content is already public in the
    shard log. Recorded for `CI-FAILURE-EVIDENCE-1113-01`.
42. **F08 Retry tooltip placement** (owner, 2026-09-26): the Retry tooltip opens
    to the left of the button, so it covers neither the side rail's arrow
    button nor the notice text; this deviates from the reference image, which
    shows it above the button.
43. **Header-copy TP SVN** (owner, 2026-09-26): the SVN copy inside the byte-code
    Header copy is modeled read-only but not displayed, keeping the 1.1.12
    P0-3 rule that both copies are modeled separately.
44. **B-code TP SVN locations** (owner as firmware owner, 2026-09-26): B-bank start
    + TP start + `0x24` is `0x47024` for NT51919/929/932, `0x4A024` for NT51950
    512 KiB, the 1 MiB Display OSD image, the NT51950 Desay cascade and the
    NT51951 Desay maps, and `0x8A024` for the NT51950 Common cascade and
    NT51951 Common 1 MiB maps (no Golden yet; synthetic evidence only).
45. **Header-copy TP SVN locations** (owner as firmware owner, 2026-09-26):
    Header copy start + `0x24` is `0x30334` for NT51923, `0x32DE4` for
    NT51917/927/928, `0x27F14` for NT51919/929/932 and `0x2D330` for
    NT51950/51951 (bank-local in AB images). NT51926 is declared per firmware
    version (`0x32F74` for fw1.4.1, `0x32A94` for fw2.0.0) on the existing
    declared variants, never inferred from version bytes in the input.
46. **ADR 0077 accepted** (owner, 2026-09-26): the pre-built profile catalog ADR
    is accepted after two independent design reviews; implementation starts
    with the Step 0 attribution measurement and continues only past its gate.
47. **RO-1: v0.9.16 terminal certification** (owner, 2026-09-26): stays deferred
    to 2.0.0 (option C); retain or retire it when 2.0.0 is planned. Until
    then no 2.x release may be promoted without the terminal chain, and 1.x is
    unaffected. The 1.1.13 rolling parity is an extra check, not a replacement.
    Evidence reviewed with the owner: of the 27 routes without canonical input,
    22 have contract-only evidence, 4 have synthetic-oracle evidence and 1 has
    none; none has owner-certified Golden bytes (decision 12 keeps them not
    covered by parity).

## Released: `v1.1.12` — 2026-09-26

Published 2026-09-26 04:39 UTC from `main` `30b17e699` (release pull request
#456; tag `v1.1.12` object `ec3104f0e`) by release run `36217032510`:
candidate, owner-approved protected promote and published-download smoke all
succeeded (v0.9.16 parity jobs skipped by design for 1.x). Ten assets: the
portable ZIP, the Launcher EXE, checksums, SBOMs, provenance and the candidate
manifest. The earlier release run `36178939238` stopped on unresolved P1
review threads before anything was published (decision 19). Follow-up work
continues in the [1.1.13 board](1.1.13.md).

## Release plan to 2026-09-28

Baseline, 2026-09-25, source identical to `v1.1.11` (Release build, one
warm-up and five scored launches, `scripts/measure-startup.ps1
-RequirePreloadLifecycle`; raw data in the test area under
`evidence/v1112-startup-baseline-a5b8c0658/`): process to window 1,235 ms;
managed entry to `main-window.opened` 1,162 ms (13.5 MB allocated);
`main-window.opened` to `startup-warmup.catalog-state.applied` **5,005 ms and
770 MB allocated**; warm-up completed 6,498 ms. The catalog interval is the
dominant cost.

| Lane | Owner, model | Branch | Due (+08:00) |
| --- | --- | --- | --- |
| Startup A and B: the whole startup path (B folded in per decision 10) | Claude, Opus 5.5 | `feature/1.1.12/startup-performance` | PR 09-27 12:00 |
| C-2 WS-IO F07/F08 | Codex `gpt-6-sol` xhigh | `feature/1.1.12/io-persistence` | green 09-27 18:00, else 1.1.13 |
| C-3 WS-PARITY (option A) | Claude sub-agent, Opus | `feature/1.1.12/v0916-alignment` | differences to owner 09-26 evening |
| Release prep: roadmap amendment, CHANGELOG, release notes | Claude, then Codex `gpt-6-luna` max when a thread frees | `feature/1.1.12/release-prep` | 09-27 evening |
| Cross-runtime review | a fresh Codex thread reviews Claude's pull requests; Claude reviews Codex's | - | as each PR opens |
| B: NT51950/NT51951 CtrlRAM Display-OSD envelope and two-NVT AB rule (decision 12) | Claude, Opus 5.5; two-NVT classifier by a Claude sub-agent | `feature/1.1.12/ctrlram-osd-envelope` (merges `feature/1.1.12/ab-two-nvt`) | with the freeze |

Status 2026-09-26 (local, integration head `7e9d7acc6`): every lane is merged
into `feature/1.1.12/integration`. B is implemented under
CTRLRAM-OSD-ENVELOPE-1112-01 (R3), its R1 view record
CTRLRAM-OSD-ENVELOPE-VIEW-1112-01 and AB-TWO-NVT-1112-01 (R3). Evidence so
far: the owner's 0x80000 AUTO_PRJ-599 case on the NT51950 2-IC route
reproduces the owner expected output, and with the registered Combiner differs
only in the four approved CRC words; registered-Combiner equivalence for
NT51950/NT51951 single and 2-IC Bases at 512 KiB and 1 MiB; 27 two-NVT
characterization cases; full Bootstrap suite green on the two-NVT branch.
Lesson: every record field except the finalized fields is immutable once
admitted, and each governed path has exactly one record. A nonstandard-length
prefix classification rule outside the admitted contracts was therefore
withdrawn from local history before any push; Base classification recognizes
only the published Standard lengths (256 KiB, 512 KiB, 1 MiB) pending an owner
decision.

Status 2026-09-26 (local, integration head `aa8ae537b`): owner decision 17 was
implemented under CTRLRAM-OSD-ENVELOPE-CLASSIFY-1112-01 (R3, independent design
review incorporated). The production source `dc773e967` passed the full local
verifier (every .NET suite including Golden regression; the remaining script
failures also fail on `main`) and the frozen gate measurement under decisions
16 and 18; later commits change documents only. The Codex fixed-head final
review of `439f89a81` approved six records and required F-9 (parity wording,
fixed in `aa8ae537b`). F-10 (P3: the SourceEnvelopeExtent root comment still
describes one full-container root) moves to 1.1.13 so the production source
stays identical to the verified and measured source.

Status 2026-09-26 (published branch): Codex approved all seven records at
`aa8ae537b`; `206da2581` finalizes them and `e75b81c01` adds the owner's
attestations for the four R3 items (owner reply: four R3 items approved).
Structure passes. `feature/1.1.12/integration` is pushed and pull request #448
into `1.1.12` is open; merging it, the release pull request into `main` and
the release stay separate owner approvals because firmware semantics change.

Status 2026-09-26: the first CI run failed the roadmap line ceiling (2,501 of
2,500 lines, from a post-measurement documentation edit). ROADMAP-LINE-CEILING-1112-01
(R2, Codex design ACCEPT and final APPROVED) re-wrapped the bullet without word
changes; CI then passed 10/10 at `6b3140827` and, with owner approval, pull
request #448 merged into `1.1.12` as `86b88bfcc`. Next: the release pull request
`1.1.12` -> `main`, the owner's human release-notes review and three firmware
checks (decision 15), then the release workflow; each is a separate owner
approval.

Integration (commander, following the `v1.1.11` practice): one batch branch
`feature/1.1.12/integration` from `1.1.12` merges every product lane
(startup, WS-IO F07, and ADR 0075 only if adopted) plus release documents.
Each lane carries one capability-reuse record; the batch freezes one reviewed
head, gets one cross-runtime final review (Codex for Claude-owned records,
Claude for Codex-owned records), and one evidence commit finalizes all records
as the head's direct child. One pull request merges it into `1.1.12` with a
merge commit (never squash or rebase: records pin commit SHAs). Schedule:
lanes ready 09-27 10:00; frozen head 09-27 12:00; review and evidence 09-27
afternoon; pull request green by the 09-27 evening window. `scripts/package.ps1`
is R3 in the validator and stays unchanged (decision 11).

Owner approval windows: 09-26 evening, 09-27 evening (freeze), 09-28
morning (release). Intermediate startup evidence uses per-stage allocation,
which CPU contention does not distort, plus timing as a direction signal;
the final timing is the commander's quiet-machine run on the packaged
candidate at freeze.

## Track scope in 1.1.12

- **Roadmap-owned product work:** startup performance (priority), F07/F08 and
  residual F20/F21, the NT51950 Header backup CRC investigation, TP Header SVN
  modeling after owner confirmation.
- **Track:** shared handoff and bug ledger; AI documents and skills for both
  runtimes; removal of capability-limiting skills; development-flow reset;
  release workflow limited to removing 0.9.x sources and parallelizing the
  candidate stage (WS-GOV decision 7); one-time v0.9.16 alignment; stale
  document, directory and branch cleanup; a decision table for architecture
  conflicts; test-architecture ADR, categories and one pilot split; WS-FLOW
  F2 and F1 if the owner admits them.
- **Later:** listed at the end of the master checklist.

## Workstreams

| ID | Goal | Owner | Model | Branch | State | Next |
| --- | --- | --- | --- | --- | --- | --- |
| WS-0 | Collaboration base: this board, protocol and bug ledger | Claude | Opus 5.5 | `1.1.x` | local | Owner review |
| WS-GOV | Development and release flow reset | Claude, Codex review | Opus 5.5; review `gpt-6-sol` xhigh | `feature/1.1.12/governance-reset` | ADR draft done (P0-4) | A-10 review, then Phase B |
| WS-GOVREV | Independent design review of the ADR draft, WS-GOV and WS-AI | Codex | `gpt-6-sol` xhigh; fallback a fresh Claude session | `feature/1.1.12/governance-design-review` | dispatched | A-10 |
| WS-AI | Dual-runtime AI documents and skills | Claude, Codex review | Opus 5.5; review `gpt-6-sol` high | `feature/1.1.12/agent-docs` | decisions 1-5 accepted | B-11 |
| WS-FLOW | Merge and Replace flow review | Claude | Opus 5.5 | `feature/1.1.12/flow-review` | local (report) | P0-3 owner review of F1-F11 |
| WS-DOC | Stale document and conflict inventory | Codex | `gpt-6-luna` max | `feature/1.1.12/doc-inventory` | held | A-8; feeds B-2 and C-8 |
| WS-PERF | **Startup performance, the 1.1.12 product priority** | Codex | `gpt-6-sol` xhigh, escalate to `gpt-6-astra` | `feature/1.1.12/startup-performance` | planned | C-1 step 1 after the base refresh |
| WS-IO | F07/F08 and residual F20/F21 | Codex | `gpt-6-sol` xhigh | `feature/1.1.12/io-persistence` | planned | C-2 after the base refresh |
| WS-PARITY | One-time alignment with v0.9.16 (ADR 0057); owner confirms each difference | Codex runs, Claude reviews, owner disposes | `gpt-6-sol` xhigh | `feature/1.1.12/v0916-alignment` | planned | C-3 early after the base refresh |
| WS-HDR | NT51950 Header backup CRC investigation; TP Header SVN modeling | Codex with firmware owner | `gpt-6-astra` | `feature/1.1.12/header-integrity` | planned | C-6; C-7 after P0-3 |
| WS-TEST | Test architecture | Claude design, Codex migration | Opus 5.5; migration `gpt-6-luna` max | `feature/1.1.12/test-architecture` | planned | C-4 design |
| WS-FLOWFIX | WS-FLOW F2, F1 first half, F11 CRC worker retirement | Codex, Claude reviews | `gpt-6-sol` xhigh; F11 deletion `gpt-6-luna` max | `feature/1.1.12/flow-fixes` | planned | C-5, C-10 after Phase B |

WS-GOV and WS-AI land first (Phase B) because they change the rules every
other workstream follows. WS-IO starts after the base refresh because 1.1.11
changes the same picker and input-result consumers.

Model fit, from vendor positioning and public evaluations as of 2026-09-25:
`gpt-6-luna` at max takes bounded checklist, read-heavy or exact-file-boundary
work and stops on ambiguity; its output is synthesized by the commander.
`gpt-6-sol` takes multi-step implementation and review. `gpt-6-astra` takes
multi-constraint firmware or architecture problems. Cross-runtime review
(Claude authors, Codex reviews, or the reverse) is the default independent
review; when the other runtime is unavailable, a fresh session of the same
runtime reviews (WS-GOV decision 3). Headless dispatch passes
`--sandbox workspace-write` explicitly.

Parallel-work rules, until Phase B lands and for as long as workstreams run
concurrently:

- One writer per path (each envelope's write lock). A worker never edits
  another workstream's log or the board; it records the need under `Open`.
- Only the commander runs `git fetch`, `git worktree add/remove/prune`,
  `git gc` and branch deletion; workers never touch shared refs outside their
  own branch.
- Never commit in a worktree while a verifier or test run is active there.
- At most three Codex threads run at once. WS-PERF timing runs only while no
  build, test or verifier runs on the machine; the log records that condition
  with each measurement.
- Evidence gathered before Phase B lands states the rules it was produced
  under. After Phase B merges, the commander rebases every open feature
  branch; workers do not rebase or merge the trunk themselves.
- WS-PERF and WS-IO both touch `src/NvtFwCombiner.Presentation.Avalonia/`:
  their write locks list exact files, and an overlap goes to the commander
  before either side edits.
- Headless Codex workers run in the `workspace-write` sandbox, which keeps
  `.git` read-only for the sandbox. A worker edits files and writes its
  checkpoint entry but does not stage or commit; the commander commits each
  checkpoint on the worker's branch, with Codex as co-author.
- A WS-TEST split moves whole test files. It runs only on classes no active
  write lock includes (both WS-PERF and WS-IO lock "their tests", which can
  reach `ShellViewModelTests` partials); the commander schedules each split.

## Master checklist

The single list of everything 1.1.12 must do, consolidated 2026-09-25 before
development starts and corrected after an independent omission review. Cite
item IDs in logs and commits; the commander keeps states current. Allocation
stays owned by the roadmap; this list owns order, owners and completion.
Anything unfinished is reallocated explicitly, never dropped.

### Phase 0: while 1.1.11 closes (nothing may disturb it)

- [x] P0-1 Done: `v1.1.11` published 2026-09-25 (identity above).
  `BUG-20260925-record-filename-taskid` was fixed in it (`4fe9cb9bb`).
- [x] P0-2 Done at release; outcome per the `v1.1.11` CHANGELOG Known issues:
  AB CtrlRAM routes keep Candidate status, and independent expected outputs
  plus firmware-owner review remain open for uncovered cases. That work is not
  in 1.1.12 unless the owner reallocates it (the roadmap keeps NT51950/51 AB
  evidence later). The 32-byte Header difference moved to 1.1.12 as C-6. The
  release hid the Customized entry points in the UI; the Customized
  CLI/direct-launch decision stays open for the owner.
- [x] P0-3 Owner decisions, done 2026-09-25: WS-FLOW findings (recorded in
  WS-FLOW "Accepted owner decisions": F2 and F1 first half in 1.1.12, F11
  retire, F5 and F1 second half in 1.1.13, F6 unchanged); TP SVN facts below.
  Only the SVN first-byte flag definition remains (A-9).
  Owner facts recorded 2026-09-25 (C-7 moves them into the firmware-family
  definitions):
  - SVN sits at **TP start + `0x24`**, not TP Header start + `0x24`. NT51950
    is always at `0xA024` (TP start `0xA000`). The 929 Type AB descriptor at TP
    start + `0x100` is not an SVN location.
  - Confirmed table (offsets inside the TP input file), every IC a 4-byte
    unsigned little-endian value: 917/923/926/927/928 TP start `0x0`, SVN
    `0x24`; 919/929/932 TP start `0x7000`, SVN `0x7024`; 950 and 951 TP start
    `0xA000`, SVN `0xA024`.
  - FWConfig's `u8AutoBuildSvnVer1-4` bytes (structure offsets 29-32) are no
    longer filled: all 60 FWConfig instances in the 164 canonical Golden BINs
    read `00 00 00 00` (commander scan, 2026-09-25). They are not an SVN
    source; model them separately and never sync them.
  - Open for re-confirmation (evidence, same scan): at TP start + `0x24` in 20
    unique Golden TP inputs, the low 16 bits repeat across unrelated files
    (`0x19C0` in 10 of 18 nonzero values; also `0x80C3`, `0x17C0`) while the
    high 16 bits vary (2,167-33,089). One NT51932 input reads 0. AB Golden:
    TP A and TP B carry equal values, and the merged output keeps one copy in
    each bank at its bank-local TP start + `0x24`.
  - Decisive owner evidence (commander diff, 2026-09-25, of the owner's
    `nt51950_fw_T02.bin` against
    `nt51950_fw_expected_diff_svn_revision.bin`, private owner evidence):
    exactly six ranges differ. `[0xA026,0xA028)` `45 78` to `46 31`, and the
    same change in a second copy at `[0x2D332,0x2D334)`; CRC words at
    `0xA11C`, `0xA130` and at `0x2D428`, `0x2D43C`. Bytes `0xA024-0xA025`
    (`c0 20`) do not change. Reading: the SVN is **2 bytes at TP start +
    `0x26`**, not 4 bytes at `+0x24`; NT51950 carries a **backup Header** near
    `0x2D30C` with its own SVN copy and CRC pair (the structure C-6
    investigates). Byte order is not decidable from bytes alone: big-endian
    reads 17,784 to 17,969 (+185), little-endian 30,789 to 12,614.
  - **Owner correction, supersedes the "little-endian" confirmation above:**
    the SVN field is **4 bytes, big-endian, at TP start + `0x24`**; the
    unchanged `+0x24`-`+0x25` bytes are slow-changing high-order bytes (T02
    `0xC0204578`, SVN-revision file `0xC0204631`, +185). A leading `0xC0`
    marks a **local build**. The repository's 923/926/927
    `svn-auto-build-version` model (`byteOrder: little`, unsigned integer) is
    wrong and C-7 corrects it.
  - **Encoding (owner, 2026-09-25):** bytes 1-3 hold the revision as six BCD
    digits, so `C0 20 45 78` is a local build of rev 204578 and `C0 20 46 31`
    is rev 204631 (53 revisions apart, not 185). Verified: bytes 1-3 are valid
    BCD in 20 of 20 unique Golden TP inputs. Leading bytes seen: `0xC0` (15),
    `0xC3` (3, revisions `80xxxx`), `0x60` (1), `0x00` (1, whole field zero,
    likely unstamped). Still to confirm in C-7: the meaning of `0xC3` and
    `0x60`, and whether an all-zero field means "no SVN".
  - **Main and backup Header SVN (owner, 2026-09-25):** the two copies are not
    required to match. The SVN is stamped by a separate step after postbuild.
    Model both copies read-only and separately; a difference is not a
    diagnostic. In the owner's files both copies and both Header CRC pairs
    changed together. These rules are sufficient: NFC work does not need the
    stamping step itself (owner, 2026-09-25).
  - **AB banks (owner, 2026-09-25):** each bank carries its own SVN at its
    bank-local TP start + `0x24`; A and B may legitimately differ. Read and
    display both; a difference is not an error. (Golden A/B values happen to
    be equal.)
  - With these facts P0-3's SVN part is complete except the first-byte flag
    definition (A-9).
- [x] P0-4 Commander, done 2026-09-25: B-1 ADR draft at
  `feature/1.1.12/governance-reset:docs/handoff/1.1.12/ADR-DRAFT-governance-reset.md`
  for owner approval at B-13.
- [x] P0-5 Commander, done 2026-09-25: dispatch envelopes (all seven fields plus the bug-ledger
  rule) for WS-PERF, WS-IO, WS-PARITY, WS-HDR, WS-FLOWFIX and WS-TEST; the
  WS-DOC envelope already targets the `1.1.x` trunk.
- [ ] P0-6 Owner: run `<worktrees>/branch-cleanup/delete-unmerged-branches.ps1`
  (45 branches) and review its 6 commented branches.
- [ ] P0-7 Owner, optional: housekeeping of personal tools outside the
  repository; the commander keeps the details out of Git.

### Phase A: base refresh, after `v1.1.11` is published

- [x] A-1 to A-4 Done 2026-09-25: base refresh steps 1-4 above.
- [ ] A-5 Owner-approved: base refresh step 5 (push `1.1.x`, delete the two
  merged 1.1.11 branches, auto-delete setting, naming ruleset).
- [x] A-6 Done 2026-09-25: `ponytail`, `ponytail-review`, `taste-skill` moved
  to `~/.codex/skills-disabled/` (reversible; WS-AI decision 4).
- [ ] A-7 Owner relays to Codex: board, handoff protocol, bug ledger, branch
  naming, model assignments, its envelopes.
- [ ] A-8 Start WS-DOC; its verdicts feed the B-2 archive and the C-8 table.
- [x] A-9 Done 2026-09-25. Owner definition of the SVN field's first byte:
  flags combined with bitwise OR, only these three: `0x80` LOCAL_BUILD
  (stamped on a Windows host), `0x40` DIFF_EXIST (the working copy had local
  modifications), `0x20` NO_SVN_RECORD (no last-changed revision; the
  revision is written as 0). In the owner's stamping logic the modification
  check runs only when a revision record exists. Commander check of the 21
  unique Golden TP inputs: `0xC0` in 16 (LOCAL_BUILD and DIFF_EXIST); `0xC3`
  in 3 (the same flags plus low nibble 3, which is no flag: read as a seventh
  BCD revision digit, revisions 3801947, 3801330, 3801955); `0x60` in 1
  (NT51927: DIFF_EXIST and NO_SVN_RECORD with revision 183151, a combination
  the current stamping logic cannot produce); `0x00` in 1 (NT51932, whole
  field zero: not stamped, since the logic sets NO_SVN_RECORD whenever it
  writes revision 0). Owner disposition (2026-09-25): `0xC3` does not conform
  to the definition (the seventh-digit reading is not confirmed); it affects
  only info parsing and display, never bytes. `0x60` is plausible: a Jenkins
  build (no LOCAL_BUILD). `0x00` means the field was not written; the info
  display should mark it with a warning indicator (for example "!") next to
  the value. C-7 (1.1.13): read flags as byte 0 `& 0xE0`, show bits outside
  the three flags as non-conforming rather than guessing their meaning, and
  keep all SVN reading read-only.
- [ ] A-10 WS-GOVREV: independent design review of the ADR 0074 draft,
  WS-GOV 1-10 and WS-AI 1-5 (the current rules require it before R2/R3
  implementation). P0/P1 findings are resolved in the drafts, with owner
  answers where needed, before B-0.

### Phase B: rules first (one ADR, one batch admitted under the old rules)

- [ ] B-0 Open one `design-active` capability-reuse record for the batch under
  the current rules (file name equal to its `taskId`).
- [ ] B-1 Governance ADR covering WS-GOV decisions 1-10 and WS-AI decisions
  1-5, naming each rule's canonical owner.
- [ ] B-2 Retire capability-reuse validation; move records, attestations,
  checkpoint, waiver and WS-DOC-approved governance history to
  `docs/governance/archive/`; rewrite the 29 links; add pull request template
  fields for owner search, semantic owner, disposition and the review mode
  used.
- [ ] B-3 Review rule (decision 3) in the runbook and `AGENTS.md`; CODEOWNERS on
  R3 paths.
- [ ] B-4 Tiered CI (decision 4): no feature-push runs, path-mapped tests on
  pull requests into `1.1.x`, full suite plus Golden at release-branch cut and
  release pull request, weekly full run when `1.1.x` changed, manual dispatch;
  build once and reuse the build in test shards; the `ui` shard split waits
  for C-4 categories.
- [ ] B-5 Hotspot size block and one consolidated ADR 0021 (decision 5).
- [ ] B-6 Rename the structure check; drop `python-worker / verify` from
  required checks; owner updates ruleset `22009240` at merge (decision 6).
- [ ] B-7 `release.yml`: remove 0.9.x sources, parallel candidate jobs; keep the
  v0.9.16 parity jobs, enabling them for the 1.1.12 candidate if C-3 needs the
  release run (decisions 7 and 7b).
- [ ] B-8 Branch governance, `CONTRIBUTING.md` and `AGENTS.md` for the `1.1.x`
  model, naming allowlist and the closure step that merges `main` back into
  `1.1.x` (decisions 9 and 10); set `target-branch: 1.1.x` for all three
  ecosystems in `.github/dependabot.yml` (today they target `main`).
- [ ] B-9 Roadmap amendment: this track's scope, the v0.9.16 alignment, no
  further 0.9.x maintenance releases (7c), 1.1.13 rolling parity (7b) and the
  WS-FLOW dispositions from P0-3.
- [ ] B-10 `AGENTS.md` and `development-execution-workflow.md`: runtime-neutral
  wording, pointers to `docs/handoff/` and the bug ledger, single-runtime rule,
  one owner for rules now repeated (full suite in six places, test-area setup
  in four, preflight fields in four).
- [ ] B-11 WS-AI decisions 1-5: `sync_derived.py` projections and drift check,
  validator frontmatter, `nfc-` rename, three deletions, `nfc-review`, two
  rewrites, trims (`release-readiness`, `golden-regression`,
  `ui-experience-change`), fixes (`locate-golden-evidence` uses
  `NFC_TEST_AREA_ROOT`; delete the `diagnosing-bugs` web template; update
  `implement` and `nfc-architecture-change` for the reset; trigger wording for
  `capture-firmware-ui` and `open-firmware-example`; `github-review-polling`
  explicit only), routing, hybrid interview style in `nfc-grill-with-docs`.
- [ ] B-12 Close bugs `verify-all-help-text`, `version-branch-ci-gap`,
  `grilling-authority-conflict`, `skill-stale-persona-rules`.
- [ ] B-13 Cross-runtime review of the batch (Codex `gpt-6-sol` xhigh, or a
  fresh Claude session); owner approves the ADR text; seal the final record as
  the last checkpoint; owner R3 attestation; green CI.

### Phase C: product and quality work on `1.1.x`

- [ ] C-0 Integration gate for every workstream: pull request into `1.1.x`
  (owner handles push and pull request), path-mapped CI, review mode stated,
  bugs cited; the commander removes merged local worktrees and branches.
- [ ] C-1 **WS-PERF, the product priority: startup.** Limits from process
  launch, every launch, cold and warm: first window visibly presented within
  500 ms; all startup loading (catalog validation, required page readiness,
  deferred startup views) within 2,000 ms. Warm baseline (window-handle
  proxy): 1.333 s to window, then 4.846 s to catalog applied, 6.518 s to
  warm-up finished.
  1. Measure first: same machine, settings, build flavor and arguments; the
     one-warm-up, five-scored warm baseline plus separate controlled cold
     launches; raw stages; visible-presentation evidence.
  2. Break down the 4.846 s interval. Source hypotheses: catalog and profile
     loading, validation and compilation, repeated work, UI materialization.
     Commander's hypothesis from code mapping: catalog publication compiles
     every static route at startup (`CanonicalCapabilityCatalogSource.Load`).
  3. Optimize through existing startup and catalog owners, preserving
     validation, catalog completeness, honest loading and error feedback, page
     readiness, independent page instances and bounded lifetime. Background
     work still counts toward 2,000 ms.
  4. Check that first Merge and Replace navigation does not inherit the wait.
  5. Report gains, memory and allocation trade-offs and residual delays; a
     missed target names its blocker and is never relaxed.
  6. Final acceptance with the actual package on the owner machine.
- [ ] C-2 WS-IO. Revalidate each finding against current code; attach F18
  behavioral or interleaving evidence to every repair. F07: loose delivery
  keeps its committed path, size and hash after cancellation or report failure
  without rerunning processors (atomic bundles keep one transaction). F08:
  visible, retryable persistence failures. F20 residual: remaining picker and
  I/O consumers capture context before awaiting and reject stale results. F21
  residual: atomic local Report replacement preserving the original on
  precommit failure; best-effort disclosure for nonlocal providers. Keep the
  shipped snapshot, reentry, disposal and Report Save fixes. One writer with
  WS-FLOW F1, F5 and F6 in `CompositionRunService`.
- [ ] C-3 WS-PARITY: run the ADR 0057 v0.9.16 comparison for the 1.1.12 line
  early; the owner disposes every difference; an unexplained difference
  becomes a bug.
- [ ] C-4 WS-TEST: test-architecture ADR; categories; path-to-category map
  (feeds B-4 and the `ui` shard split); one pilot split with before/after
  timing. Proposed, to confirm in the design: pilot `ShellViewModelTests`, the
  largest class, and a review of architecture tests that assert document
  prose.
- [ ] C-5 WS-FLOWFIX, as decided at P0-3 (WS-FLOW accepted decisions): F2 the
  engine applies `ChangedRangePolicy` after every external processor (R2,
  Golden unchanged); F1 first half, processor acquisition derived from the
  compiled plan for all five routes. F1 second half, F5 and F6 are 1.1.13.
- [ ] C-10 WS-FLOWFIX: retire the Python CRC worker (F11) as a deletion-only
  batch: `tools/crc-worker`, its CI lane and dependabot entries (close #340
  and #437), the `crc-worker-contract` skill, the Domain stage type and
  normalizer branch; ADR 0002 superseded; keep the external-processor
  boundary and archive the worker contracts for a possible Python Combiner.
- [ ] C-6 WS-HDR: the 32-byte Header backup/CRC difference in the NT51950
  single-IC, 1 MiB OSD, Normal CtrlRAM Both candidate. Start from the P0-3
  owner SVN rules (no stamping script is needed): compare both Headers' SVN
  fields and CRC words, then trace the legacy Combiner copy and CRC order and
  when firmware uses the main or backup Header;
  only then decide the shared processor contract. No IC special case, second
  CRC, masking or expected-byte change; Both does not certify A-only or
  B-only. R3 with firmware owner and Golden evidence if the contract changes.
- [ ] C-7 WS-HDR: TP SVN common modeling per the P0-3 owner facts (4 bytes
  big-endian at TP start + `0x24`: build-origin flag plus six BCD revision
  digits; main and backup Header copies modeled separately; one per AB bank),
  read-only, as a separate scoped admission; fixes
  `BUG-20260925-svn-model-byte-order`; flag meanings from A-9; not a release
  blocker.
- [ ] C-8 Commander: architecture-conflict decision table (change code or amend
  ADR) from WS-DOC section 5 and WS-FLOW.
- [ ] C-9 Route `BUG-20260925-cli-invariant-exception-uncaught` to the
  controlled-error work (F17/F26, 1.1.14) in the roadmap.

### Phase D: freeze and release

- [ ] D-1 Feature freeze; cut release branch `1.1.12` from `1.1.x`; full CI and
  Golden (automatic once B-4 lands, otherwise dispatched).
- [ ] D-2 Startup acceptance (C-1), WS-PARITY dispositions and the C-6 outcome
  complete.
- [ ] D-3 `VERSION`, `CHANGELOG.md` and release notes per the release-note
  contract; independent human review of the frozen release notes against
  evidence; owner visual, firmware R3 (where touched) and release gates.
- [ ] D-4 Pull request `1.1.12` to `main`; `release.yml` with the parallel
  candidate; publish.
- [ ] D-5 Closure: Release Closure Record (tag, peeled SHA, run, assets and
  digests, provenance, smoke result, retry history, residual gates); merge
  `main` back into `1.1.x`; branch and pull request inventory with an
  owner-approved exact deletion list; review the bug ledger; open the 1.1.13
  board.

### Not in 1.1.12 (roadmap-owned later work)

WS-FLOW F3, F4 and F9 profile convergence (R3); F1 second half with F5
(1.1.13); F6 re-evaluation (1.1.13); F7 and F8 when their files are touched; F10 with the F17/F26 controlled-error work (1.1.14); release-pipeline
redesign and rolling parity (1.1.13); restoring the local .NET/Python verifier
overlap once its flakiness is fixed; remaining oversized test classes; hotspot
refactors; CtrlRAM cold first-open and F14/F15 (1.2.8); broad profile
reference convergence and Launcher (1.2.1).

## Open P0/P1 bugs

None. [BUG-20260925-record-filename-taskid](bugs/BUG-20260925-record-filename-taskid.md)
was fixed in `v1.1.11`.

## Cross-branch blockers

None: 1.1.11 is published and the base refresh is complete.
