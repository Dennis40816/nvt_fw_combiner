# v1.1.2 repository document convergence handoff

Status: D1-D7 approved deletion batch implemented; awaiting independent review
and final gate. The repository-wide remainder is `KEEP_PENDING_EVIDENCE`;
`v1.1.2` is not wholly complete.

## Boundary

`v1.1.2` Phase 1 completed an owner-approved, exact seven-path document
deletion batch from its inventory. The repository-wide remainder remains
`KEEP_PENDING_EVIDENCE`. It does not change product behavior, firmware facts,
release evidence, or semantic authority. Post-cleanup semantic
architecture/version-rule convergence and the dead-code/minimality review
remain `v1.1.14` scope in the [NFC roadmap](nfc_roadmap.md).

The CtrlRAM selector contract/SVG version-label rename in this planning diff is
separately authorized roadmap-allocation maintenance, outside the cleanup
candidate table. It is not a `MOVE_CANDIDATE` omission.

## Open execution control

| Open TODO | Owner | Blocker | Next action |
| --- | --- | --- | --- |
| Independently review the exact D1-D7 deletion batch. | Independent reviewer | Frozen review is pending. | Check the [v1.1.2 manifest](v1.1.2-repository-document-convergence-manifest.md), exact path absence, frozen hashes, and caller audit. |
| Run the final repository gate. | Implementation owner | Independent review must pass first. | Run the canonical final verifier on the frozen candidate; do not broaden the seven-path batch. |
| Classify the repository-wide remainder. | Documentation owner | No path-specific evidence or approval. | Keep every unlisted tracked document `KEEP_PENDING_EVIDENCE` until a later owner-approved batch. |

## Classification and manifest gate

For each path, record its canonical owner, status, incoming links/callers, and
one of these outcomes:

1. Keep an active canonical authority.
2. Retain required immutable release, firmware, or governance evidence.
3. Archive only when a concrete future or traceability need exists.
4. Otherwise propose owner-approved removal.
5. Regenerate or remove derived/generated documents from their existing source.

For a retained relocation, use `git mv` and audit every exact incoming
link/caller. The future manifest must prove no broken links, no duplicate
current authority, and explicit historical-versus-current status. Do not invent
an archive tree without evidence. Selection and every exact deletion manifest
require review and owner approval before future deletion.

## Evidence and completion boundary

The completed batch output is the [approved deletion record and frozen
evidence](v1.1.2-repository-document-convergence-manifest.md), not a
repository-wide classification. It preserves the removed paths' pre-deletion
hashes and explicit owner decision. Independent review and the final gate are
still required; any later mutation uses separately approved evidence before it
claims completion.
