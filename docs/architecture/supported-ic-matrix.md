# Supported IC / Workflow Matrix Draft

This is a planning inventory, not a support claim. A row becomes supported only after profile validation, integrity disposition, golden regression, and owner sign-off. `Unknown` never means `None`.

Current owner priority as of 2026-07-18:

- focus on normal Merge and normal Replace for DP Replace and CtrlRAM Replace workflows;
- keep AB Code Merge hidden from UI/CLI until firmware-owner approval; NT51929 and NT51950 have direct owner golden evidence, NT51919/NT51932 use the owner-approved NT51929 fact scope, and NT51951 uses the owner-approved NT51950 workflow-logic scope with its distinct `0x80000` synthetic topology locked independently;
- include NT51950 and NT51951 normal Merge with the confirmed DP Perspective TP overlay range `0x0A000-0x36FFF (len 0x2D000)`; owner golden fixtures are recorded and firmware-owner sign-off remains required;
- require Replace UI to collect IC num before profile-specific regions are shown. ICs with only single/cascade choices use text labels; ICs with three or more concrete choices such as NT51917/NT51927/NT51928 use numeric count selection, optionally with an Other/custom path later;
- expect CtrlRAM Replace CRC/header recalculation through approved legacy `combiner.exe` postbuild sequences.

The per-IC Merge/Replace flowchart reference is [`ic-workflow-flowcharts.md`](ic-workflow-flowcharts.md). When adding a new IC workflow, follow [`adding-ic-merge-replace-workflow.md`](adding-ic-merge-replace-workflow.md). Update both documents together when IC workflow status changes.

The active 0.8.0 cleanup goal and milestone acceptance criteria are tracked in
[`0.8.0-goal-and-acceptance.md`](0.8.0-goal-and-acceptance.md). New IC support should start from `IcSupportCatalog`, then update only the relevant flash-map, TP header, postbuild, family-policy, profile, test, and documentation rows.

## Availability, evidence, and IC-family vocabulary

Workflow availability and golden verification are separate axes:

- **Golden verified** means direct or owner-approved fact-scoped parity is recorded. It is not a product-support promise.
- **Evidence open** means the executable/safety contract exists and authoring remains available, while direct golden or firmware-owner review is still open. A missing golden alone does not ban the workflow.
- **Not available** is reserved for a missing executable/safety contract, such as NT51931 Replace, or a workflow whose profile/ranges have not been declared. Its UI badge must expose the reason and opening condition on hover.

Owner-declared family facts are typed in `IcSupportCatalog`; they never expand executable ranges by themselves:

| Family | Canonical IC | Member | Relationship and reusable scope |
| --- | --- | --- | --- |
| `nt51927-family` | NT51927 | NT51917 | Perfect alias across the owner-declared family facts. |
| `nt51927-family` | NT51927 | NT51928 | Partial alias for Replace; LDC differs and NT51928 NB is excluded. |
| `nt51929-nt51932-family` | NT51929 | NT51919 | Perfect owner-declared family alias. |
| `nt51929-nt51932-family` | NT51929 | NT51932 | Perfect family alias for the declared facts; NT51932 still owns its distinct cascade product path. |

## Standard Merge and DP Replace relationship

Standard Merge and DP Replace must use the same canonical IC memory-map facts. Therefore, an IC cannot expose DP Replace unless it also exposes Standard Merge. The inverse is a product-roadmap expectation, not an automatic runtime promotion: each Standard Merge IC should become DP Replace-eligible only after the shared map explicitly derives DP-owned write ranges, preserved TP/customer/header ranges, accepted capacities, input normalization, integrity/postbuild behavior, tests, golden evidence, and firmware-owner review.

Current DP Perspective input contract for NT51950/NT51951:

- the UI calls `reference-base` **Reference FlashCode**, not `base.bin`;
- it must be one complete final Standard/Normal Merge `.bin` for the same selected IC and exactly one declared capacity: `0x40000`, `0x80000`, or `0x100000` bytes;
- the DP replacement is a DP/FlashCode-shaped `.bin` no larger than that reference capacity; a shorter input is padded with `0x00`, while an oversized input is rejected;
- the output clones Reference FlashCode, replaces the declared full DP container, then restores the reference TP overlay. The existing owner rule keeps customer information from the replacement DP image.

A same-capacity complete Standard/Normal FlashCode is therefore a valid *shape* for the current DP replacement slot, but parity/support claims stay limited to recorded evidence. Future AB FlashCode input must use an AB-specific profile-declared artifact shape/extractor and explicit A/B bank, header-copy, preserved-range, and Legacy Combiner rules. Normal FlashCode offsets must not be guessed or reused for AB input.

NT51930 uses a different profile over its existing canonical 256 KiB Standard Merge map:

- Reference FlashCode is one complete same-IC Standard/Normal Merge output of exactly `0x40000` bytes;
- the replacement is a same-IC DP/FlashCode-shaped BIN containing the complete canonical DP range `[0x0000, 0x6000)`; `0x40000` is the expected outer-container length, any other length that still covers this range produces the declared warning, and shorter input is rejected;
- the output clones Reference FlashCode and replaces only `[0x0000, 0x6000)`. The gap `[0x6000, 0x7000)` and TP `[0x7000, 0x40000)` remain byte-for-byte from Reference FlashCode;
- the executable V2 safety contract and deterministic full-output oracle are present, so authoring is available as **Evidence open**. Direct owner DP Replace golden parity and firmware-owner release review remain open.

## Current executable inventory

| Area | IC coverage | Source of truth | What this currently means | Not enough for 1.0 until |
| --- | --- | --- | --- | --- |
| IC support exposure | NT51917, NT51919, NT51920, NT51923, NT51926, NT51927, NT51928 non-NB, NT51929, NT51930, NT51931, NT51932, NT51950, NT51951 | `IcSupportCatalog` | Central workbench inventory for IC workflow exposure, owner-approved aliases, and onboarding notes. It does not replace detailed flash-map/header/postbuild/profile catalogs. | Every exposed workflow row is backed by the matching detailed catalog, executable profile or workbench path, tests, and owner evidence. |
| Standard Merge profiles | NT51917, NT51919, NT51920, NT51923, NT51926, NT51927, NT51928, NT51929, NT51930, NT51931, NT51932, NT51950, NT51951 | trusted V2 bundles plus `TrustedV2CompositionCompiler` | Executable canonical V2 routes exist for the golden-backed gen_flash set, owner-confirmed aliases NT51917 -> NT51927 and NT51919 -> NT51929, NT51930 flash-map golden, and NT51950/NT51951 DP Perspective. NT51950/NT51951 select the V2 map matching only DP input sizes `0x40000`/`0x80000`/`0x100000`, output the selected DP length, overlay TP `0x0A000-0x36FFF (len 0x2D000)`, and preserve customer info. | Firmware-owner sign-off before support claim/exposure; direct owner samples for optional aliases or remaining 950/951 capacities if selected for release. |
| TP flash-map catalog | NT51917, NT51919, NT51920, NT51923, NT51926, NT51927, NT51928 non-NB, NT51929, NT51930, NT51931, NT51932, NT51950, NT51951 | hash-pinned `profiles/built-in/ctrlram-postbuild-v2/flash-map.json` plus `BuiltInTpFlashMapCatalog` | UI can display DP, CtrlRAM, customer-info, IC-count visibility, and declared TP/full-Flash base shapes from current evidence. Static C# range facts are retired. | Every released workflow maps these rows through profiles, compiler checks, report evidence, and direct TP/full-Flash parity. |
| CtrlRAM postbuild command catalog | NT51917, NT51919, NT51920, NT51923, NT51926, NT51927, NT51928 non-NB, NT51929, NT51930, NT51931, NT51932, NT51950, NT51951 | hash-pinned `profiles/built-in/ctrlram-postbuild-v2/catalog.json`, Combiner `1.13.0` | Typed command sequences exist for single/cascade or numeric branches and are tested against argv shape. NT51926 selects 1.4.1 versus 2.0.0 from base FWConfig Common FW version. NT51930 accepts only stable Common FW 1.x; its inspected 2.0.0 BAT row is evidence-only and cannot be selected. Owner confirms only one actual NT51930 Jira project, INX; cataloged no-Jira branches are not separate golden gaps or support claims. Workbench UI exposes per-region CtrlRAM slots and runs staged Combiner postbuild for Preview/Build reports and output. NT51920 AUTO_PRJ-72/PID `0xF401`/Common FW 1.2.0/single and AUTO_PRJ-55/PID `0x1403`/Common FW 1.2.0/cascade 2 route through exact-base V2 contracts with V1/V2 full-byte, two-command argv, processor-authority, report-identity, and input-immutability parity. Their owner expected controls differ from the recalculated output only at four approved source/header-copy CRC words, with CtrlRAM payload difference zero. NT51923 AUTO_PRJ-662/PID `0x6005`/Common FW 1.4.1/single and AUTO_PRJ-734/PID `0x4C03`/Common FW 1.4.1/cascade 3 likewise route through exact-base V2 contracts; canonical Standard Merge reconstructs both owner bases byte-for-byte, and their V1/V2 bytes, two-command argv, processor authority, report identity, and immutable inputs match with the same four CRC-only owner differences. NT51927 AUTO_PRJ-529/PID `0x5709`/Common FW 1.4.1/single routes through an exact-reference V2 contract; canonical Standard Merge reconstructs the owner base byte-for-byte, its V1/V2 output and seven-command process evidence match, and the owner output differs only at six CRC words with payload drift zero. NT51926 Common FW 1.4.1 cascade plus exact 2.0.0 PID `0x1309` chip-count 1/3 cases, all without a version edit, route through V2 `0x3C000` TP/`0x40000` full-Flash TP-prefix contracts. NT51930 AUTO_PRJ-302/PID `0x110D`/Common FW 1.3.0/cascade 3, NT51932 AUTO_PRJ-525/PID `0x5601`/Common FW 2.0.0/cascade 3, and NT51951 AUTO_PRJ-695/PID `0x5901`/Common FW 2.0.0/single route through full-Flash V2 contracts whose V1/V2 bytes and registered-1.13 command sequences are identical. NT51932 and NT51951 additionally require exact Standard Merge reference SHA. NT51951's 1.11-produced expected differs from registered 1.13 only at four CRC words, so cross-version full-byte equality is not claimed. | Final per-IC support exposure and firmware-owner review. Closure is limited to each exact project/variant/topology; NT51920, NT51923, NT51927, NT51930, and NT51931 exact routes require the full reference SHA as a build discriminator, while NT51932 and NT51951 have independently reconstructable Standard Merge bases whose full SHA is the variant discriminator. Other bases/projects/counts remain on the retained fallback. |
| Production Replace profiles | NT51930 and NT51950/NT51951 DP Replace UI/CLI workbench paths route V2 runtime-admitted profiles; CtrlRAM workbench profiles are generated from FlashMap/postbuild evidence; General Replace UI/CLI workbench supports DP-kind mappings and TP/CtrlRAM mappings with postbuild refresh | `WorkbenchCompositionService`, trusted V2 bundle, hash-pinned FlashMap/Postbuild config | NT51930 DP Replace clones an exact `0x40000` Reference FlashCode and replaces only canonical DP `[0x0000, 0x6000)`, preserving every later byte. Its exact INX CtrlRAM AUTO_PRJ-302/PID `0x110D`/FW 1.3.0/cascade-3 route now uses a support-neutral V2 profile with full-byte V1 parity; no-Jira variants remain fallback-only. NT51950/NT51951 clone an exact Reference FlashCode of `0x40000`, `0x80000`, or `0x100000`, replace the padded selected-length DP container, restore only TP `0x0A000-0x36FFF (len 0x2D000)`, and keep customer info `0x37000-0x37FFF (len 0x1000)` from DP. The V2 profiles directly lock each distinct map, input, and operation shape. NT51950/NT51951 have archived owner-approved legacy comparison plus public synthetic expected hashes. General Replace can build explicit mappings inside cataloged DP-kind ranges from UI rows or repeated CLI `--mapping <target-start+length=path>`; TP/CtrlRAM-touching mappings append the selected legacy Combiner postbuild refresh after the explicit mapping. Header/customer/project/backup rows remain protected. | NT51930 CtrlRAM exact-route R3 review and explicit support promotion; independent direct DP Replace golden parity; real CtrlRAM/General Replace profiles per released IC/mode; TP-touching General Replace golden outputs; and finalized owner-approved safety envelopes. |
| General Merge workbench | Catalog IC ids for selection only; no IC-specific support claim | `WorkbenchCompositionService.RunGeneralMergeAsync`, `general-merge` CLI, Merge page General mode, `saved-rule` CLI | General Merge v1 initializes a caller-declared blank output length with reserved `0x00` bytes, then compiles explicit source-start/target-start/length mappings as `CopyRange` operations through the shared profile compiler. It supports Preview/Build from CLI and UI, rejects target overlap and out-of-bounds ranges, writes structured reports, and never invokes postbuild. CLI can validate saved-rule JSON, print normalized mapping fragments, and consume General Merge saved-rule rows via `--rule` plus explicit `--slot` bindings; reports mark those operations as `saved-rule`. | Saved-rule promotion into normal workflows, UI Saved Rules, owner-approved reusable General Merge policies, General Replace saved-rule execution, and any future postbuild-dependent variant must be separately reviewed. |

## First-sample gap priority

| Priority | Gap | Current state | Needed before `v1.0.0` |
| --- | --- | --- | --- |
| P0 | Support matrix lock | Candidate IC/workflow rows exist, but candidates are not support claims. | Owner chooses the exact IC/workflow subset to ship; non-selected rows stay hidden or clearly candidate-only. |
| P0 | Standard Merge golden closure | Golden-backed canonical V2 profiles are executable; NT51930 and the recorded NT51950/NT51951 owner golden outputs from `merge_bin.7z` are covered; NT51917/NT51919 are executable owner-confirmed aliases and are tested against NT51927/NT51929 golden bytes. NT51950/NT51951 V2 also retain legacy byte parity for all declared capacities. | Firmware-owner sign-off for every released Standard Merge profile; direct owner samples for an additional 950/951 capacity if it is selected for release, and optional direct NT51917/NT51919 alias samples if desired. |
| P0 | DP Replace production closure | NT51930 and NT51950/NT51951 DP Replace UI/CLI workbench paths route V2 runtime-admitted profiles. NT51930 has a deterministic full-output oracle and Standard Merge self-replacement control but stays Evidence open. NT51950/NT51951 retain their deterministic six-capacity oracle, customer-padding boundaries, and archived comparison. | Direct owner golden for the exact NT51930 INX Jira project/variant/topology and per-IC DP/LD maps for any further explicitly released DP Replace IC. No-Jira NT51930 content is not a missing case. |
| P0 | CtrlRAM Replace production closure | UI slots, memory layout, staged legacy Combiner execution, postbuild command trace, CLI multi-slot input, and private fixture handoff exist. | Private golden outputs, firmware-owner parity review, and promotion from workbench execution to released support claim. |
| P0 | General Replace production closure | Workbench/UI and CLI Preview/Build can run explicit mappings inside cataloged DP-kind ranges and TP/CtrlRAM ranges that have selected postbuild profiles. Protected/header/customer rows remain blocked. | Owner-approved General Replace safety envelopes, overlap/alignment policy, and golden outputs for released IC/modes. |
| P1 | General Merge production closure | General Merge v1 can run ad hoc explicit source-to-target mappings over a blank output from CLI and UI without postbuild. Saved-rule JSON validation and General Merge CLI consumption exist, with operation provenance in reports. | UI Saved Rules, normal-workflow promotion, final policy UX, and owner-approved reusable mapping evidence before support-style claims. |
| P1 | Unified workflow data model | Merge uses executable profiles; Replace still mixes synthetic profiles, workbench-specific planning, and flash-map facade data. | `0.6.0` should evaluate a unified profile/template/catalog model so UI, CLI, and tests call the same runner contracts. |
| P1 | Report/history completeness | Preview/Build report modal exists; first-peak errors, save flow, output artifact path display, local persistent report history, single-entry deletion, persisted audit metadata, structured operation evidence cards for status, overlap, normalized ranges, processor/tool id, read/write ranges, and refresh argv exist. Report issues now carry schema-level severity, known truncation diagnostics render as warnings rather than blocking issues, and Build automatically runs validation/report generation without a separate manual Preview gate. | Finish support-ready audit review and visual polish. |
| P1 | Settings persistence/readiness | Settings surface is catalog-backed; theme, strictness, language, and report history persist locally. `Warn only` is recorded as a UI preference but firmware gates still fail closed. | Define any future execution-affecting setting through Application contracts before it can change Preview/Build behavior; continue polishing support-status readiness. |
| P2 | Deferred workflows | AB Merge remains hidden from UI/CLI. NT51929 and NT51950 have direct owner golden evidence; NT51919/NT51932 and NT51951 use the explicitly approved fact-scoped aliases while retaining their independent configuration/topology tests. All runtime exposure and release promotion remain pending. UI Saved Rules, General profile promotion, General Replace saved-rule execution, and future REG Replace remain planned. | Promote only after processor/integrity evidence, firmware-owner review, and explicit support priority. |

## Owner validation package needed

- NT51917/NT51919 Standard Merge: owner confirmation is recorded as NT51917 -> NT51927 and NT51919 -> NT51929; direct IC-specific samples are optional audit evidence rather than an exposure blocker.
- NT51930 Standard Merge: DP input, TP input, expected output, expected file name, input/output SHA-256, and owner approval are recorded in `testdata/golden/standard-merge-gen-flash/manifest.json`.
- NT51930 DP Replace: provide the owner-promised exact INX Jira project/variant/topology package with one same-build `0x40000` Reference FlashCode, replacement DP/FlashCode BIN, expected final FlashCode, command/tool identity, and hashes. The checked-in public synthetic oracle and Standard Merge self-replacement are migration controls only and do not close direct golden parity or imply support for no-Jira projects.
- NT51950/NT51951 Standard Merge: canonical V2 map/plan/runtime golden tests cover the owner NT51950 `0x40000` and NT51951 `0x80000` DP cases, with TP input covering `0x0A000-0x36FFF` and customer info preserved from DP. Legacy/V2 parity covers all six declared IC/capacity combinations. Direct owner golden evidence for the other combinations remains required if any is selected for release exposure.
- NT51950/NT51951 DP Replace: public deterministic oracle covers six IC/capacity cases plus customer-padding boundaries, with TP restored from base and customer info from DP. The owner accepts archived pre-V2 comparison plus the checked-in static expected hashes for V2 runtime admission; `knownDeviations` is empty, so full-byte parity is required before routing. This is migration evidence, not an independent hardware golden or a product support claim.
- CtrlRAM Replace per released IC/mode: base firmware, per-region CtrlRAM replacement BINs, expected final output after Combiner postbuild, Combiner version/tool binding, command order, declared read/write ranges, allowed diff ranges, and FWConfig Common FW version when an IC has multiple postbuild categories.
- IC aliases/counts: NT51928/NT51929/NT51951 alias behavior and NT51928 NB exclusion are owner-confirmed. NT51930 validation is limited to the topology in the exact INX Jira golden; no-Jira count branches are neither missing golden cases nor support claims.
- General Replace: protected ranges beyond the current header/customer/project/backup gate, allowed explicit-mapping envelope, overlap/alignment rules, TP-touching postbuild expected outputs, and final release-scope IC/mode approvals.

| IC | Standard Merge | AB Merge | Replace planning | Integrity evidence | Current evidence | 1.0 status |
| --- | --- | --- | --- | --- | --- | --- |
| NT51917 | canonical V2 Standard Merge route; map-bound alias of NT51927 | no evidence | DP/CtrlRAM priority | CtrlRAM Replace uses the NT51927 reference flow | owner alias confirmation + NT51927 golden-byte alias regression + trusted V2 map-bound alias/plan/golden parity + packaged Bootstrap routing | Candidate; firmware-owner migration review pending |
| NT51919 | canonical V2 Standard Merge route; map-bound alias of NT51929 | fixed `0x80000` V2 candidate with an explicit fact-scoped NT51929 region-set alias; application run requests are rejected | DP/CtrlRAM priority | CtrlRAM Replace uses the NT51929/NT51932 reference flow | complete AB candidate-output parity through the manifest-declared NT51929 alias; no direct NT51919 product golden, and firmware-owner approval of the alias scope remains pending | Candidate; firmware-owner migration review pending |
| NT51920 | reference candidate; canonical V2 Standard Merge route | no evidence | Exact CtrlRAM V2 routes for Common FW 1.2.0 single/cascade 2; other Replace shapes retain the V1 fallback | AUTO_PRJ-72/PID `0xF401`/single and AUTO_PRJ-55/PID `0x1403`/cascade 2 each run registered Combiner 1.13.0 as one staged session with two ordered `CRC_Enable` commands. V1 and V2 are byte-identical; each differs from its owner expected control only at four approved CRC words (16 bytes), with zero CtrlRAM payload drift. | owner-approved exact inputs/expected outputs + trusted V2 profiles + full-byte V1/V2 parity + argv/read-write/report/input-immutability evidence; wrong base/PID/version/chip-count shapes retain the fallback | Support-neutral exact candidates; no public support promotion, with independent R3 review required before integration |
| NT51923 | canonical V2 Standard Merge route | no evidence | Exact CtrlRAM V2 routes for Common FW 1.4.1 single/cascade 3; other Replace shapes retain the V1 fallback | AUTO_PRJ-662/PID `0x6005`/single and AUTO_PRJ-734/PID `0x4C03`/cascade 3 each reconstruct the exact reference through Standard Merge, then run registered Combiner 1.13.0 as one staged session with two ordered `CRC_Enable` commands. V1 and V2 are byte-identical; each differs from owner expected only at four CRC words (16 bytes), with zero CtrlRAM payload drift. | owner-approved DP/TP/physical inputs and expected outputs + trusted V2 profiles + full-byte V1/V2 parity + argv/read-write/report/input-immutability evidence; wrong base/PID/version/chip-count shapes retain the fallback | Support-neutral exact candidates; no public support promotion, with independent R3 review required before integration |
| NT51926 | canonical V2 Standard Merge route | no evidence | DP/CtrlRAM/General TBD | CtrlRAM Replace postbuild category selected by FWConfig Common FW version: `1.4.1` -> `0x32F50` and VN len `0x1660`, `2.0.0` -> `0x32A70` and VN len `0x149E`. V2 routes without a version edit cover exact 1.4.1 cascade and 2.0.0 PID `0x1309` chip-count 1/3 shapes, accept `0x3C000` TP work or `0x40000` full Flash, stage only the TP prefix, and preserve the Flash tail. Single/full-Flash/file-backed DP-only General Replace also uses its V2 candidate; other General shapes retain existing behavior. | `gen_flash_bin_v2` config + trusted V2 Standard Merge parity + exact 1.4.1 and 2.0.0 CtrlRAM three-way evidence. For 2.0.0, V1 equals V2 full-byte; owner expected differs only at four approved CRC words (16 bytes), with zero CtrlRAM payload drift. Other projects/counts/edits and firmware-owner support review remain pending. | Executable candidates routed for exact slices; no support promotion |
| NT51927 | canonical V2 Standard Merge route | no evidence | Exact Common FW 1.4.1 single CtrlRAM V2 route; two/three-chip CtrlRAM evidence remains candidate-only | AUTO_PRJ-529/PID `0x5709`/single runs seven ordered `MERGE_MODE` and `NT51927BASED_GEN_CRC_MODE CRC32` commands. V1 and V2 are byte-identical at `fdb8fef0...20ab9`; the owner reference differs only at six CRC words (24 bytes), with payload drift zero. | owner-approved DP/TP/physical inputs and expected output + canonical Standard Merge reconstruction SHA `fc4d2f97...69c55a` + exact V2 family/profile + full-byte/process/report/input-immutability parity; wrong base/PID/version/chip-count shapes retain V1 | Support-neutral exact single candidate; no public support promotion. Two/three-chip exact routing and independent R3 release review remain. |
| NT51928 | canonical V2 Standard Merge route with LDC | no evidence | DP/CtrlRAM priority | CtrlRAM Replace uses the NT51927 reference flow for non-NB only; NT51928 NB is a separate IC and is not covered | `gen_flash_bin_v2` config + trusted V2 map/plan/golden parity + packaged Bootstrap routing + owner alias confirmation | Candidate; firmware-owner migration review pending |
| NT51929 | canonical V2 Standard Merge route | fixed `0x80000` V2 candidate; full DP first, TPA overlay, TPB `+0x40000` relocation; application run requests are rejected | DP/CtrlRAM priority | CtrlRAM Replace uses the NT51932 reference flow; TPA/TPB CRC explicitly None for AB evidence; TPB relocation required | direct tracked owner fixture with full-byte V2/reference parity (`c7e1e263...3d66abe2`) plus named Python-configuration synthetic parity; firmware-owner promotion review pending | Priority candidate; firmware-owner migration review pending |
| NT51930 | canonical V2 Standard Merge route | no evidence | DP Replace available as Evidence open; CtrlRAM priority | DP Replace reuses the canonical `0x40000` map and changes only `[0x0000, 0x6000)`. CtrlRAM Replace routes only AUTO_PRJ-302/PID `0x110D`/Common FW 1.3.0/cascade 3 to `NT51930BASED_NORMAL_MODE CRC8`; V1/V2 bytes and argv match, while wrong project/version/count/selector shapes retain V1. The inspected 2.0.0 and other no-Jira content remain evidence-only and do not establish product support. | trusted V2 Standard Merge and DP Replace map/plan routing + exact INX CtrlRAM manifest/SHA/V1-V2 parity + registered Combiner 1.13 identity; owner-final deltas classified as CRC/header-copy/declared DiffDLM because no independent pre-replacement base exists | Support-neutral exact CtrlRAM candidate; independent R3 release review remains, and DP Replace direct-golden closure is separate |
| NT51931 | canonical V2 Standard Merge route | no evidence | Exact CtrlRAM V2 route materialized; Replace remains Not available by support-neutral policy | AUTO_PRJ-158/PID `0x131B`/cascade 6 expected-derived control has zero payload drift and 108 classified header/header-copy CRC bytes. Retained V1 and exact V2 output SHA are both `f38fdecd...c594`; registered 1.13.0 `NT51931BASED_NORMAL_MODE CRC8` is byte-identical to the owner 1.2.0.4 `NT51930BASED_NORMAL_MODE CRC8` control, while the rejected 1.13.0/51930-based pairing access-violates. The supplied D8DfT82 FlashCode is not the same build and remains outside the exact hash-pinned route. | exact manifest/BAT-conflict/argv/input/hash evidence, real registered-tool regression, hash-pinned V2 bundle/profile/family, V1/V2 full-byte/process parity; 1.2.0.4 remains evidence-only and uncommitted | No owner input is pending and no support promotion is claimed. Other builds, PID/version/count/selector shapes retain the fail-closed legacy boundary until the final V1 retirement phase. |
| NT51932 | canonical V2 Standard Merge route | fixed `0x80000` V2 candidate; full DP first, TPA overlay, TPB `+0x40000` relocation; application run requests are rejected | region inventory TBD | TPA/TPB CRC explicitly None; TPB relocation required | owner-approved fact-scoped NT51929 golden applicability plus direct named Python-configuration synthetic parity (`cd54e124...7de10ce`); firmware-owner review pending | Priority candidate; firmware-owner migration review pending |
| NT51950 | canonical V2 DP Perspective Standard Merge route, exact-capacity selected | repository-only fixed `0x80000` AB V2 candidate stages full DP, A/B bank artifacts, and TPB DIFF `+0x40000`; Combiner 1.13.0 then writes B-header ILM/DLM/CRC; application run requests are rejected | DP and CtrlRAM priority | Standard Merge direct `0x40000` DP golden from `merge_bin.7z`; all three capacity variants have legacy/V2 parity. The V2 DP Replace route uses exact base, padded DP, TP-only restore, and customer-from-DP. CtrlRAM Replace postbuild uses `NT51950BASED_NORMAL_MODE CRC8`. | hash-anchored V2 family/profile bundle + two owner-approved AB golden fixtures with full-byte Python/Combiner parity + deterministic DP Replace hashes including padding boundaries; firmware-owner review remains required before AB runtime exposure | Priority candidate |
| NT51951 | canonical V2 DP Perspective Standard Merge route, exact-capacity selected | one-mebibyte V2 compilable candidate copies full DP, stages `0x80000` A/B artifacts, overlays TPA/TPB, relocates TPB DIFF `+0x80000`, then declares the Combiner B-header stage; application run requests are rejected. A Windows-only synthetic Python/Combiner regression verifies the distinct `0x80000` command shape. | DP and CtrlRAM priority | Standard Merge direct `0x80000` DP golden from `merge_bin.7z`; all three capacity variants have legacy/V2 parity. The V2 DP Replace route uses exact base, padded DP, TP-only restore, and customer-from-DP. CtrlRAM Replace uses the NT51950 reference flow. | hash-anchored V2 family/profile bundle + direct Standard Merge golden + deterministic DP Replace hashes + owner-approved NT51950 AB workflow-logic applicability + distinct NT51951 synthetic topology; firmware-owner review remains required before AB promotion | Candidate |

## Workflow promotion gate per IC/mode

- authoritative memory map, region atomicity, and owner;
- blank/reference initializer and canonical profile;
- explicit integrity disposition for every processor stage;
- valid/invalid fixtures and expected output SHA-256;
- mutation/processor diff review;
- UI catalog visibility and terminology decision;
- release/support owner sign-off.

## Replace-specific evidence still required

- DP Replace: DP partition map and allowed atomicity; NT51930 uses its canonical Standard Merge map with a static full-output oracle but remains Evidence open until exact INX Jira project/variant/topology direct owner golden parity and R3 review. No-Jira NT51930 content is not a missing evidence row or support claim. NT51950/NT51951 exact-base replacement uses V2 runtime-admitted profiles whose migration parity is full-byte equality against static public expected hashes and archived owner-approved legacy comparison, with no known deviations. Other IC DP Replace mappings remain gated pending their own evidence;
- CtrlRAM Replace: complete named CtrlRAM regions/groups and post-processing dependencies;
- IC num: UI and request model must bind Replace to the selected IC before presenting region choices; two-option ICs use text choices such as `single`/`cascade`, while three-or-more concrete count ICs use numeric count selection with future room for Other/custom exceptions;
- CRC/header: exact legacy `combiner.exe` version, invocation, read/write ranges, execution order, and golden evidence;
- General: globally forbidden/protected ranges, alignment, overlap, and post-processing trigger rules.
