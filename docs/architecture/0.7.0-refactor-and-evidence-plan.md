# 0.7.0 Refactor and Evidence Plan

Status: active planning note for 0.7.0 development.
Last updated: 2026-07-05.

This document tracks the remaining 0.7.0 cleanup and evidence work after Standard Merge, DP Replace workbench paths, and CtrlRAM postbuild execution became traceable. It is not a release support claim. The full universal special-value validation table is tracked in [`ctrlram-universal-sentinel-validation-table.md`](ctrlram-universal-sentinel-validation-table.md).

## Owner-provided evidence checklist

For each IC/mode selected for release, provide these artifacts or hashes:

| Evidence | Required details |
| --- | --- |
| Flash-map workbook or export | IC, revision/date, sheet/section name, sanitized source label, SHA-256 hash, and owner approval. Do not record confidential source paths. |
| Flash header / mmap reference | Header-copy source/target/length, FWConfig start, CtrlRAM ranges, customer-info/protected ranges, overlay/map requirements, SHA-256 hash. |
| Postbuild BAT/CMD/log | Exact command order, mode arguments, IC-number branches, map.txt requirement, and expected tool version. |
| Combiner identity | Tool binding id, executable version, SHA-256, source-code category if reviewed, and whether it is 1.x or 2.0-era behavior. |
| Base firmware | IC, IC-number branch, Common FW version, FW version/bar, PID, size, SHA-256, and confidentiality/commit approval. |
| Replacement BINs | Slot id/region, intended source offset, size, SHA-256, and whether the file is exact, oversized-to-truncate, or padded by policy. |
| Expected output | Final postbuild-clean output size/hash and allowed changed ranges. This is required before promotion from "executes" to "OK". |
| Review decision | Firmware-owner sign-off, unsupported branches, and whether alias behavior is accepted for release. |

For NT51926/NT51930 specifically, include FWConfig Common FW version in every CtrlRAM Replace case:

- NT51926 Common FW `1.4.1` selects `PostbuildSetup_51926_1.4.1.bat`; Common FW `2.0.0` selects `PostbuildSetup_51926_2.0.0.bat`.
- NT51930 Common FW `1.x.x` selects archived `PostbuildSetup_51930_1.4.0.bat`; Common FW `2.0.0` selects `PostbuildSetup_51930_2.0.0.bat`.

## Universal special-value BIN validation

A universal validation BIN can reduce owner effort before real golden outputs exist. It does not replace golden parity, but it catches mapping mistakes quickly.

Recommended pattern:

1. Use a base firmware whose target regions are not already the same repeated byte.
2. Generate one replacement BIN per slot with a deterministic sentinel pattern:
   - `normal`: seed `0xA1`
   - `mp`: seed `0xB2`
   - `diff/dlm`: seed `0xC3`
   - `vn`: seed `0xD4`
   - `nf`: seed `0xE5`
3. Within each BIN, encode offset as `byte = (seed + offset) & 0xFF`; this proves both start address and length.
4. Run Replace + required postbuild.
5. Verify that every selected declared region contains the matching sentinel/address pattern, except bytes explicitly owned by postbuild CRC/header/header-copy writes.
6. Verify that unselected regions match base, except declared postbuild writes.
7. For truncation policy, add one oversized file per CtrlRAM slot and assert the report contains the truncation warning and the output uses only the declared region length.

This universal case is useful for:

- confirming slot-to-address wiring;
- finding swapped `Normal/MP/VN/NF/DiffDLM` inputs;
- confirming `single/cascade/2/3` visibility;
- validating General Replace explicit mapping bounds.

It cannot prove:

- exact legacy combiner parity;
- customer real-output CRC correctness;
- overlay/map.txt behavior;
- NT51931 production mode selection.

## 0.7.0 detailed TODO

### P0 - firmware evidence and behavior

1. NT51926: collect expected final CtrlRAM Replace output for the 2026-07-05 Common FW `1.4.1` fixture.
2. Done: owner workbook TP Overview rows now document NT51926 `1.4.1` VN length `0x1660`, FWConfig backup length `0x800`, header target `0x32F50`, and `2.0.0` equivalents. Code catalog and UI/workbench selection are category-aware.
3. NT51930: collect CtrlRAM Replace expected output for a Common FW `1.x.x` base; current Standard Merge golden reads `1.3.0`, but CtrlRAM expected output is still missing.
4. Done: owner workbook TP Overview/category notes now document that NT51930 `1.x.x` consumes `MP_Ctrlram.bin`, uses VN length `0x195E`, and has a `14..29` extended cascade `DiffDLM` length `0x23000`, while `2.0.0` marks MP as overview-only and uses VN length `0x1960`.
5. NT51931: wait for owner review of 1.13.0 source or official tool/version evidence before changing production mode from official `NT51930BASED_NORMAL_MODE`.
6. Done for current public evidence: NT51950/NT51951 DP Replace has golden-derived self-replacement regression using the owner-approved Standard Merge `merge_bin.7z` fixtures. It proves the selected-base workbench path preserves the approved flash image for the current `0x40000` and `0x80000` cases, but it is not a substitute for independent DP Replace expected-output sign-off.
7. Confirm every release-scope IC/mode has expected output hash or documented private golden gate.

### P0 - UI/workbench consistency

1. Done: CtrlRAM slot visibility/ranges are category-aware after the base BIN is loaded and FWConfig is read.
2. Done: selected postbuild category is shown beside the base BIN metadata with Common FW version, FW/bar validity, and PID.
3. Done: General Replace workbench Preview/Build can compile and run explicit mappings inside cataloged DP-kind ranges while protecting header/customer/project/backup rows.
4. Done: General Replace mappings that touch TP/CtrlRAM rows now append the selected legacy Combiner postbuild refresh after the explicit mapping; golden parity and release exposure remain gated.
5. Done: Preview/Build reports show selected processor id, tool binding, target range, and processor read/write ranges as structured operation evidence instead of one long meta string; command argv stays in a fixed-width block. Current NT51926 fixture test locks `fw1.4.1` and `0x32F50`, and NT51950 General Replace self-replacement locks TP-touch postbuild traceability.
6. Done: NT51950/NT51951 DP Replace memory coverage waits for the selected base BIN before drawing an address range; approved base lengths `0x40000`, `0x80000`, and `0x100000` render as their actual selected range, unsupported lengths render as blocked instead of falling back to a max container, CLI IC/profile selectors route through the same selected-base workbench path, and golden-derived self-replacement tests lock the current `0x40000`/`0x80000` fixture outputs.
7. Done: UI-triggered Build reports carry the output artifact path as UI history metadata without persisting host paths in report JSON. The report modal now has local persistent report history entries that restore prior loaded/Preview/Build reports across app restarts, reopen them without re-running firmware workflows, and persist derived audit metadata such as run id, IC/mode context, output hash, command summary, issue summary, and evidence counts. Full 1.0 traceability still needs support-ready audit review and visual polish.
8. Done: report review treats warning diagnostics as non-blocking, so successful CtrlRAM oversize-truncation flows show `Succeeded with warning(s)` and history cards report warnings separately from issues.
9. Done: UI Build gates now generate reopenable reports with the explicit `ui.build.preview-required` issue code when the user tries to Build after IC/mode/input changes without a fresh valid Preview.
10. Done: run reports now emit issue `Severity` (`error` by default, `warning` for approved input truncation diagnostics). Report review prefers the schema severity and keeps the legacy `input.address-space.truncated` fallback for older reports.

### P1 - core/data model refactor

1. Done for workbench/UI: replace IC-only postbuild lookup assumptions with explicit default/category-aware helpers.
2. Done in code catalog and owner workbook evidence for NT51926/NT51930: versioned TP Overview differences are applied from the selected postbuild category instead of UI-only conditions.
3. Done: Workbench Standard Merge, DP Replace, CtrlRAM Replace, and General Replace now share one Bootstrap preview/build runner for request creation, output writer setup, external-processor binding, preview-token build approval, and build-output input-alias protection. Workflow-specific profile, mapping, and postbuild planning stay outside the runner.
4. Convert large static postbuild rows into compact row builders only where it reduces duplicated firmware facts without hiding address evidence.
5. Keep firmware semantics in Application/Profile catalogs; UI and CLI only adapt typed requests.
6. Done: catalog tests now fail if `LegacyCombinerPostbuildCatalog.All` gains duplicate IC rows without an explicit Common FW category selection policy; current approved versioned ICs are NT51926 and NT51930.

### P1 - test and report cleanup

1. In progress: shared `NvtFwCombiner.TestSupport` now owns repository root helpers, generic temporary workspaces, fake artifact readers, and fake clocks for Application/Bootstrap/Infrastructure/GoldenRegression/UiSmoke tests. Large combiner-manifest-specific workspaces remain local until they can be reduced without hiding firmware evidence.
2. In progress: split oversized UI smoke setup into focused helpers while keeping golden-byte assertions in regression tests; General Replace, Standard Merge, DP/950/951, and CtrlRAM smoke setup now use shared temporary workspaces and repository/manifest path helpers. Report projection tests now use focused report JSON sample builders instead of repeating long inline JSON blocks.
3. Done: universal sentinel Replace aid now has a deterministic generator script, and Application tests verify the documented `0x23000` sentinel length covers every cataloged staged CtrlRAM postbuild block.
4. Done: CtrlRAM Replace private/committed golden manifest schema `0.2` now requires `commonFwVersion` and `postbuildCategory`; the verifier checks the manifest Common FW against the base BIN FWConfig and verifies known NT51926/NT51930 versioned categories fail closed when mismatched.
5. Done for local UI history scope: report smoke tests now lock report history capture, ordering, error-report capture, output artifact path display, reopening an earlier report without adding a new run, snapshot import/export, and local history file round-trip/fail-closed behavior.
6. Done for local shell preferences: Settings theme, strictness, and language selections are persisted under the current user's local app data and restored on startup. Invalid or unreadable preference files fall back to `System` / `Strict` / `English`; `Warn only` remains a saved UI preference and does not relax firmware validation gates.

## XML/core hard-coding audit

Current assessment for 0.7.0:

| Area | Finding | Refactor need |
| --- | --- | --- |
| `MainWindow.axaml` | Large file with repeated slot/report/coverage visual structures and hard-coded colors/sizing. Slot card, slot fact, and report row templates are now shared resources instead of repeated inline blocks. | Continue extracting reusable coverage styles/templates and keep typography/buttons/dropdowns consistent. This is UI quality debt, not firmware behavior risk. |
| ViewModels | Mode labels and UI strings are still constants in ViewModel partials. | Acceptable short term, but should move toward a localization/resource model and catalog-driven mode descriptors. |
| Workbench service | Preview/build request execution is now shared, legacy postbuild capacity/write-range policy now lives in `LegacyCombinerPostbuildPlanner`, NT51950/NT51951 selected-base DP Replace profile construction now lives in `BuiltInReplaceProfiles` instead of Bootstrap, workbench run-report projection is isolated in a report partial, Replace planning failure projection is isolated in a planning partial, DP Replace mode assembly lives in a DP partial, Replace display projection lives in a display partial, General Replace mapping/profile adapters live in focused partials, and CtrlRAM Replace profile/planning projection now lives in focused partials. The remaining CtrlRAM runner still owns mode request assembly, postbuild-category selection, and binding validation. | Continue splitting command handlers/use-case adapters so Bootstrap remains an adapter, not a second firmware semantics layer. |
| `LegacyCombinerPostbuildCatalog` | Static firmware facts are intentionally hard-coded evidence, but versioned categories and branch variants make IC-only lookup unsafe. | Category/branch-specific helpers and tests now cover NT51926 and NT51930, including NT51930 1.x `14..29` extended cascade. Continue avoiding UI range logic. |
| `TpFlashMapCatalog` | Base rows remain IC-level, with category-specific overrides applied from selected postbuild plans. | Code applies NT51926/NT51930 overrides after FWConfig category selection; owner workbook TP Overview now records the same versioned evidence. |
| CLI | Standard Merge, NT51950/NT51951 selected-base DP Replace, real-IC CtrlRAM Replace multi-slot Preview/Build, and real-IC General Replace repeated `--mapping <target-start+length=path>` Preview/Build are exposed. Synthetic General Replace single mapping remains available as contract evidence. | Add richer report screenshot automation only if the UI surface needs scripted visual capture parity. |
| Settings | Catalog/tool rows are application-backed; report history and theme/strictness/language preferences persist locally. | Execution-affecting settings still fail closed and should not become policy switches until Application-level contracts define their effect. |

Bad-sense items to avoid in 0.7.0:

- more IC-specific `if NTxxxxx` logic in UI/XAML;
- command strings assembled outside `LegacyCombinerPostbuildCommandLineBuilder`;
- using max DP length labels when actual DP/base length is variable;
- treating FWConfig read failure as "use latest postbuild";
- adding expected golden hashes that only match an unexplained implementation change.
