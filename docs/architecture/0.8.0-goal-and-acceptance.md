# 0.8.0 Goal and Acceptance Criteria

Status: active milestone goal.

## Version decision

This work is a `0.8.0` milestone, not another `0.7.x` polish patch.

Reason: the scope crosses profile catalogs, Application flash-map/header categories, Bootstrap workbench routing, report presentation, contract tests, UI smoke expectations, and onboarding documentation. A `0.7.x` patch is still appropriate for isolated wording, spacing, color, or one-screen UI fixes. The changes below alter how future IC support is added and reviewed.

## Goal

Make Merge and Replace support easier to extend and easier to audit:

1. A new IC or IC mode should be introduced through a small set of explicit C# catalogs/categories instead of scattered UI, CLI, and profile special cases.
2. Report screens should read like human review reports, not debug logs: the first view explains pass/fail and byte differences, while detailed trace data remains available for audit.
3. Existing byte behavior must not change unless a profile/catalog row and test explain the change.

## Acceptance Criteria

### IC onboarding architecture

- `IcSupportCatalog` is the first C# entry point for IC workflow exposure, alias facts, and high-level onboarding notes.
- `TpFlashMapCatalog` remains the canonical flash-map region source for TP/DP/CtrlRAM/customer/protected rows.
- `TpHeaderCatalog` remains the canonical TP header/write-section source for report labels, postbuild write classification, overlap priority, header copy, backup, CRC, and TP window sections.
- `LegacyCombinerPostbuildCatalog` remains the only source for legacy Combiner postbuild command categories, Common FW version rules, IC-number branch rules, staged-file names, and command order.
- Family policies that affect more than one workflow have a dedicated catalog. Current example: `DpPerspectiveCatalog` owns NT51950/NT51951 DP Perspective supported lengths and TP/customer-info ranges used by both Standard Merge and DP Replace.
- FlashCode output naming is created by a catalog-backed Bootstrap policy from selected slot roles and metadata. DP version metadata is decoded as contiguous main/sub bytes (`Dxxxx`). UI code must not decide DP version offsets, FWConfig token priority, or the final file-name format.
- Workbench/UI/CLI code may adapt catalog/profile data into requests, but must not introduce new firmware ranges, operation order, postbuild rules, or output naming semantics.
- Architecture tests enforce that Presentation reaches firmware workflow/catalog data through the Bootstrap workbench facade and does not directly reference Application flash-map/header/postbuild catalogs or profile catalogs.
- Adding one supported IC/mode updates only the relevant catalog/profile rows plus tests and docs. Any extra code path requires an explicit justification in the PR.

### Large-file containment

- Large files are tracked as 0.8.0 refactor targets when they exceed roughly 700 physical lines or when one file owns multiple UI/report/catalog concerns.
- First repository shape guard is complete: architecture tests now fail if `.cs`, `.axaml`, or `.md` files under `src/`, `tests/`, or `docs/` grow past the 700-line cleanup threshold, excluding build output folders.
- Current XAML split status: `src/NvtFwCombiner.Presentation.Avalonia/MainWindow.axaml` is below the large-file threshold and now mostly contains event-bound Merge/Replace authoring markup. Further extraction should use focused `UserControl` boundaries so file-picker and drag/drop event wiring stays explicit instead of moving event-bound markup into passive resource dictionaries.
- First Bootstrap cleanup is complete: workbench DTOs and display helpers were split out of `src/NvtFwCombiner.Bootstrap/WorkbenchCompositionService.cs`, reducing the facade to roughly 450 lines without changing firmware behavior.
- Second Workbench facade cleanup is complete: Standard Merge display/run logic, catalog/settings snapshots, and shared adapter helpers were split into focused `WorkbenchCompositionService.StandardMerge`, `Catalog`, and `Common` partials, leaving the root facade file as a thin shell without changing UI-facing method contracts or firmware behavior.
- First CLI support cleanup is complete: Standard Merge and Replace command handlers share preview-before-build, run-profile adaptation, output/report path protection, report writing, issue printing, IC-number alias formatting, and range formatting through `CliCompositionRunSupport` without changing option parsing or command output contracts.
- Second CLI/Workbench execution cleanup is complete: Standard Merge CLI, Replace CLI, and the desktop workbench facade now share one Bootstrap preview-before-build gate through `CompositionRunExecutionSupport`, and General Merge CLI uses the common output-target and hex range helpers instead of private duplicates.
- Third CLI/Workbench adapter cleanup is complete: General Merge CLI, saved-rule mapping adaptation, General Replace CLI, and Workbench General Merge/Replace range parsing now share `BootstrapRangeText`, removing the hidden dependency where General Merge parsed ranges through a Replace partial.
- First Bootstrap IC-number selection cleanup is complete: CLI and Workbench adapters construct `IcNumberSelection` through `WorkbenchIcNumberSelections`, and architecture tests reject direct Bootstrap construction outside that helper.
- Root CLI cleanup is complete: top-level command routing and process-level exception handling remain in `CliApplication.cs`, while profile listing, Standard Merge handling, option parsing, result printing, and usage text live in focused partials without changing command-line contracts or exit-code behavior.
- First General Merge CLI cleanup is complete: saved-rule loading, compatibility checks, slot binding, and saved-rule-to-workbench mapping adaptation were split into `MergeCliCommandHandler.SavedRules.cs`, leaving `MergeCliCommandHandler.cs` focused on command flow, manual mapping parsing, output/report guarding, and result printing.
- Second General Merge CLI cleanup is complete: manual mapping parsing, option parsing, result/report printing, and usage text were split into focused `MergeCliCommandHandler.*` partials, leaving the primary handler focused on command dispatch, output/report guarding, and workbench invocation without changing command-line contracts.
- First General Merge workbench cleanup is complete: `WorkbenchCompositionService.GeneralMerge.cs` now owns display/run orchestration only, while mapping compilation, profile/capacity policy, and blocked-report generation live in focused `GeneralMerge.Mapping`, `GeneralMerge.Profile`, and `GeneralMerge.Report` partials without changing operation order or report schema.
- Second General Merge workbench cleanup is complete: `WorkbenchGeneralMergeIds` now owns Bootstrap-generated General Merge profile ids such as the `general-output` target region, and profile generation, mapping projection, and saved-rule validation consume that shared id.
- Third Workbench facade cleanup is complete: Standard Merge catalog/profile lookup, memory-map/coverage display projection, and UI run orchestration were split into focused `WorkbenchCompositionService.StandardMerge*` partials without changing profile selection, DP Perspective behavior, operation order, or output reports.
- First Replace CLI compile cleanup is complete: built-in Replace profile compilation and the legacy one-row General Replace explicit mapping parser were split into `ReplaceCliCommandHandler.ProfileCompile.cs`, leaving `ReplaceCliCommandHandler.cs` focused on command dispatch, input binding, output/report guarding, and result printing.
- Second Replace CLI cleanup is complete: option parsing/run-id creation, result printing/unknown-profile errors, and usage text were split into `ReplaceCliCommandHandler.Options`, `ReplaceCliCommandHandler.Result`, and `ReplaceCliCommandHandler.Usage`, leaving the primary handler focused on command dispatch, IC-number selection, and fixed input binding.
- Third Replace CLI cleanup is complete: legacy General Replace CLI materialization ids are shared through the `ReplaceCliCommandHandler` partial class constants instead of being repeated between input binding and profile compilation.
- First saved-rule loader cleanup is complete: mapping-row validation and operation-fragment validation were split into `SavedCompositionRuleLoader.MappingRows.cs` and `SavedCompositionRuleLoader.OperationFragments.cs`, leaving `SavedCompositionRuleLoader.cs` focused on file/root parsing, compatibility, input-slot templates, and overall rule compatibility.
- Second saved-rule loader cleanup is complete: saved-rule schema allowlists/enums, compatibility policy, and input-slot template parsing now live in focused `SavedCompositionRuleLoader.*` partials, leaving the root loader focused on file reading and root parse orchestration without changing saved-rule validation semantics.
- Third saved-rule loader cleanup is complete: saved-rule JSON scalar helpers, range/integer parsing, grammar regexes, and validation issue construction now live in focused partials, keeping range semantics and ID grammar out of generic JSON helper code without changing validation behavior.
- Fourth saved-rule loader cleanup is complete: `SavedRuleSchemaTokens` now owns stable saved-rule JSON enum tokens such as composition kind, overlap policy, operation kind, support status, protected-range policy, and slot cardinality; schema validation and General Merge saved-rule CLI projection consume those tokens instead of repeating contract literals.
- First workbench run-id cleanup is complete: mode-specific UI run-id prefixes now live in `WorkbenchCompositionService` runner helpers, so successful runs and blocked planning reports use the same traceability prefix for Standard Merge, General Merge, DP Replace, CtrlRAM Replace, and General Replace.
- First saved-rule issue-code cleanup is complete: `SavedRuleIssueCodes` now owns the stable Bootstrap CLI saved-rule validation codes, loader partials and saved-rule CLI tests consume that contract, and architecture tests reject duplicate `saved-rule.*` literals in Bootstrap production code.
- First profile compiler cleanup is complete: profile/header input validation, fixed operation validation, and explicit mapping validation helpers were split into focused `CompositionProfileCompiler.*.cs` partials, leaving `CompositionProfileCompiler.cs` as the small public compile entry point without changing profile semantics.
- First Replace profile catalog cleanup is complete: synthetic contract-only Replace profiles and DP Perspective production policy adapters were split into focused `BuiltInReplaceProfiles.*` partials, leaving the root file as the stable exposure-order list without changing profile definitions.
- Second Replace profile catalog cleanup is complete: DP Perspective Replace profile-facing helpers use generic `DpPerspective...` names instead of legacy `Nt51950Family...` aliases, keeping the family policy owned by `DpPerspectiveCatalog`.
- First report ViewModel cleanup is complete: `ReportReviewViewModel` factory, summary, JSON helper, and common helper code were split into focused partials, reducing the primary binding surface file to roughly 300 lines without changing report schema or firmware behavior.
- Second report ViewModel cleanup is complete: bindable report properties were split into `ReportReviewViewModel.Bindings.cs`, leaving the primary report view-model file focused on constructor projection and empty-report initialization without changing report presentation text, JSON parsing, or UI bindings.
- Third report ViewModel cleanup is complete: report line rows, badges, facts, range-table rows, input groups, byte-difference summary rows, and operation-flow nodes now live in focused ViewModel files without changing report schema, UI bindings, or firmware behavior.
- Fourth report ViewModel cleanup is complete: report input classification tokens and CtrlRAM slot/coverage group keys now live in focused Presentation helpers, so input grouping, operation flow, and replace-region grouping no longer repeat UI-only classification strings.
- First report contract cleanup is complete: Replace output-difference machine classifications now live in `NvtFwCombiner.Contracts.Reports.OutputDifferenceClassifications`; Application report generation uses the contract values, Bootstrap projects them to UI adapters, and Presentation no longer repeats classification literals.
- Second report contract cleanup is complete: the `report.output-difference.unexpected` issue code now lives in `NvtFwCombiner.Contracts.Reports.ReportIssueCodes`, so report generation and UI smoke assertions consume the same contract token.
- Third report contract cleanup is complete: composition engine input length/truncation issue codes now flow through Domain-owned `CompositionIssueCodes` and Bootstrap-projected `WorkbenchCompositionIssueCodes`, so Bootstrap report planning and Presentation report guidance no longer repeat those literals.
- Fourth report contract cleanup is complete: workbench/UI planning issue codes now live in `WorkbenchIssueCodes`, so Bootstrap run planning, Presentation fallback reports, and Bootstrap tests consume the same `ui.*`, `replace.*`, and `input.artifact.*` tokens.
- First shell ViewModel cleanup is complete: `MainWindowViewModel` binding properties, commands, and observable generated properties were split into `MainWindowViewModel.Bindings.cs`, reducing the constructor/helper coordinator file to roughly 350 lines without changing UI behavior.
- Second shell ViewModel cleanup is complete: shell construction/state and context refresh/mode-selection logic were split into `MainWindowViewModel.Construction` and `Context` partials, leaving the primary file focused on local slot-file assignment without changing command wiring, page navigation, or selected-context refresh behavior.
- First report shell cleanup is complete: session report history state, persistence snapshots, history commands, and relocalization were split into `MainWindowViewModel.ReportHistory.cs`, leaving `MainWindowViewModel.Report.cs` focused on the current loaded report, modal state, toast state, and UI-generated run-error reports.
- First firmware slot ViewModel cleanup is complete: slot file state, icon presentation, requirement-state brushes, fact badges, and slot kind definitions now live in focused ViewModel files without changing slot UI behavior or introducing firmware semantics into Presentation.
- Second firmware slot ViewModel cleanup is complete: display-only slot-kind resolution now lives in `FirmwareSlotKindResolver` and consumes Bootstrap workbench slot ids before title fallback, leaving `FirmwareSlotViewModel.Icons` focused on visual icon resources without changing slot UI behavior.
- First UI composition runner cleanup is complete: catalog/settings calls, firmware fact/output naming projection, Merge adapters, Replace adapters, and shared ViewModel projection helpers were split out of `UiCompositionRunner.cs`, leaving the primary Presentation runner as a thin shell over Bootstrap workbench contracts without adding UI-owned firmware semantics.
- First shell text cleanup is complete: dynamic localized status/summary methods and `PlanningCardText` were split out of `ShellTextResources.cs`, leaving the primary resource contract focused on language selection and bindable label properties without changing UI copy.
- Second shell text cleanup is complete: bindable labels were split into core, workflow, settings, and report resource partials, leaving `ShellTextResources.cs` focused on language selection and preference conversion without changing UI copy.
- First postbuild catalog cleanup is complete: branch-rule helpers, command builders, alias helpers, and NT51927 2/3-chip command templates were split out of `LegacyCombinerPostbuildCatalog`, reducing the primary profile-row file to roughly 540 lines without changing command data.
- Second postbuild catalog cleanup is complete: IC profile rows were split into `LegacyCombinerPostbuildCatalog.Profiles.cs`, leaving `LegacyCombinerPostbuildCatalog.cs` focused on `All`, lookup, default profile selection, and Common FW category resolution without changing command data.
- Third postbuild catalog cleanup is complete: alias-heavy NT51927-family and NT51950-family profile rows were split into dedicated family partials, keeping reference profiles adjacent to their aliases while preserving `LegacyCombinerPostbuildCatalog.All` order and command data.
- Fourth postbuild catalog cleanup is complete: NT51930/NT51931 and NT51932/NT51929/NT51919 profile rows were split into dedicated family partials, keeping Common FW variants and alias chains adjacent without changing command data.
- First postbuild contract type cleanup is complete: legacy combiner enums, Common FW version matching, branch token rules, block arguments, command declarations, command plans, and profile aggregates now live in focused files under `Application/ExternalTools` without changing Protocol 2 staging authority, branch resolution, command validation, or public type names.
- First postbuild planner cleanup is complete: branch selection/capacity/staged-file discovery, public write-range APIs, header/CRC integrity helper ranges, and write-section normalization/priority selection now live in focused `LegacyCombinerPostbuildPlanner.*` partials without changing command order, range values, or Protocol 2 staging authority.
- First legacy combiner processor cleanup is complete: staging source projection, staged BIN materialization, shortened-output normalization, and staging-tree validation were split into `LegacyCombinerPostbuildProcessor.Staging.cs`, leaving `LegacyCombinerPostbuildProcessor.cs` focused on transform flow, manifest/executable resolution, and independent diff/write-range enforcement without changing Protocol 2 authority.
- Second legacy combiner processor cleanup is complete: manifest/executable resolution and output/write-range verdict formatting were split into focused `LegacyCombinerPostbuildProcessor.Tools` and `Results` partials, leaving the primary processor file focused on staged transform orchestration without changing approved tool resolution, SHA-256 checks, staging cleanup, or declared-write-range enforcement.
- First TP flash-map catalog cleanup is complete: TP Overview profile rows and row-construction helpers were split into `TpFlashMapCatalog.Profiles.cs`, leaving `TpFlashMapCatalog.cs` focused on lookup, IC-number visibility, postbuild range overrides, and mapped CtrlRAM filtering without changing any cataloged range.
- Second TP flash-map catalog cleanup is complete: TP Overview value types, IC-number choices/visibility, and postbuild range/mapping policy were split into focused `TpFlashMapTypes` and `TpFlashMapCatalog.*` files, leaving the root catalog focused on lookup and visible-region projection without changing any cataloged range or postbuild-derived override.
- First DP version catalog guard is complete: `GenFlashVersionCatalog` exposes owner-evidenced DP main/sub version rules for tests, and Application coverage now rejects duplicate rule ids, non-contiguous main/sub bytes, and DP version offsets outside the declared DP range.
- Second DP version catalog guard is complete: every gen_flash DP version rule is checked against the executable Standard Merge `dp-region`, preventing output naming offsets from drifting away from profile copy ranges.
- Third DP version/output naming cleanup is complete: missing gen_flash DP version evidence now displays a warning `Pending` slot badge while FlashCode file names keep the stable `Dxxxx` fallback, and architecture tests keep DP offset/catalog reads in the Bootstrap firmware metadata facade instead of output-name formatting code.
- First DP Perspective catalog ownership cleanup is complete: the supported IC family label now comes from `DpPerspectiveCatalog`, Bootstrap uses DP Perspective private names instead of NT51950-specific workflow names, and an architecture guard rejects hard-coded NT51950 family labels in Bootstrap diagnostics.
- First DP Replace authoring catalog cleanup is complete: the NT51928 LDC authoring exception now lives in `DpReplaceAuthoringCatalog`, and Bootstrap uses catalog rules for additional DP Replace slots and planned source address spaces.
- First Replace mode-id cleanup is complete: `WorkbenchReplaceModes` now owns the workbench `DP` / `CtrlRAM` / `General` Replace mode ids consumed by UI and CLI adapters, and architecture tests reject Bootstrap-side duplicate literals outside that catalog.
- First shell default IC cleanup is complete: the initial workbench IC now comes from `IcSupportCatalog.DefaultIcId` through the Bootstrap facade, and an architecture guard rejects hard-coded default IC ids in Presentation code.
- First IC workflow-id guard is complete: `IcWorkflowIds.All` is the catalog-owned workflow id allowlist, and `IcSupportEntry` rejects empty or unknown workflow declarations before invalid support rows can be surfaced.
- Second IC workflow-id cleanup is complete: Bootstrap command routing, saved-rule compatibility, generated workbench profiles, built-in profile definitions, and the profile compiler now consume `IcWorkflowIds` instead of repeating standalone workflow-id literals; contract tests keep `IcWorkflowIds.All` aligned with the Domain runtime `ExperienceCatalog`.
- Third IC workflow-id cleanup is complete: Presentation fallback report paths consume Bootstrap-projected `WorkbenchWorkflowIds` instead of duplicating workflow-id strings or referencing the Profiles assembly directly.
- Fourth IC workflow-id cleanup is complete: `ExperienceIds` now owns the stable Domain workflow string tokens, while `ExperienceCatalog`, `IcWorkflowIds`, Bootstrap, and Presentation consume those constants instead of repeating token literals.
- First address-space id cleanup is complete: `CompositionAddressSpaceIds` now owns shared composition address-space tokens such as `output-image`, `reference-base`, `dp-input`, `tp-input`, and replacement input ids; Bootstrap projects them through `WorkbenchAddressSpaceIds` for UI adapters, and architecture tests reject duplicate production literals.
- First composition issue-code cleanup is complete: core engine issue codes for input length/missing/truncation, capacity, and external-processor failures now live in `CompositionIssueCodes`, and architecture tests reject duplicate literals in Domain engine code and tests.
- First workbench slot-id cleanup is complete: `WorkbenchSlotIds` now owns UI/CLI workbench slot keys such as `merge-dp`, `merge-tp`, `merge-ld`, `replace-base`, `replace-dp`, and dynamic CtrlRAM replacement prefixes; Domain owns the cross-layer dynamic CtrlRAM replacement prefix used by report classification, and architecture tests reject duplicate production literals in Bootstrap and Presentation.
- First Merge mode-id cleanup is complete: `WorkbenchMergeModes` now owns the Standard/AB Code/General workbench mode tokens consumed by Presentation mode selection and dynamic readiness text, matching the existing `WorkbenchReplaceModes` boundary.
- First dynamic CtrlRAM replacement label cleanup is complete: `DynamicCtrlRamReplacementIds` now owns parsing and human-readable labels for `replace-ctrlram-*` ids, while Application report differences and Presentation report input rows consume the shared convention through Domain/Bootstrap instead of duplicating suffix parsing.
- First IC number token cleanup is complete: `IcNumberSelectionTokens` now owns stable `single` / `cascade` selector tokens used by flash-map choices and postbuild branch rules, and Bootstrap projects them through `WorkbenchIcNumberTokens` for UI/CLI adapters.
- First firmware-fact display cleanup is complete: postbuild category display names are shortened inside the Bootstrap workbench metadata facade, and an architecture guard rejects external `PostbuildSetup_` tool-file naming details in Presentation code.
- Second firmware-fact display cleanup is complete: postbuild display categories now come from `LegacyCombinerPostbuildProfile.DisplayCategory`, so Bootstrap no longer parses external `PostbuildSetup_` evidence file prefixes.
- Second DP Perspective catalog ownership cleanup is complete: Standard Merge and DP Replace DP Perspective profile lists are generated from `DpPerspectiveCatalog.SupportedIcIds`, so adding or removing a DP Perspective IC no longer requires duplicating profile-list entries.
- Third DP Perspective catalog ownership cleanup is complete: DP Perspective operation ids, operation order, and the shared container region id now live in `DpPerspectiveCatalog`; Standard Merge profiles, DP Replace profiles, Bootstrap planning reports, and golden/profile assertions consume the same constants.
- First composition run-service cleanup is complete: input binding, artifact-read handling, output-difference report classification, and preview-token fingerprinting were split out of `CompositionRunService`, reducing the primary execution/report file to roughly 280 lines without changing execution, report, or preview-token behavior.
- First composition run-service test cleanup is complete: preview-token tests and external-processor/report tests were split into focused partial test files, reducing `CompositionRunServiceTests` to roughly 670 lines without changing Application coverage.
- Second composition run-service test cleanup is complete: request builders, fake output writer, and fake external processor fixtures were split into `CompositionRunServiceTests.TestSupport.cs`, leaving the primary file focused on run-service behavior assertions.
- Third composition run-service test cleanup is complete: shared service fixtures, request builders, and external-processor fixtures were split into focused `CompositionRunServiceTests.*` support partials without changing Application assertions.
- First composition engine production cleanup is complete: input validation/normalization, host operations/mutation trace, and external-processor execution were split into focused `CompositionEngine.*` partials, leaving the primary file as the shared Merge/Replace execution flow without changing issue codes, operation order, range semantics, or mutation hashes.
- First composition engine test cleanup is complete: external-processor execution tests and shared plan/input helpers were split into focused `CompositionEngineTests.ExternalProcessor` and `CompositionEngineTests.TestSupport` partials, leaving the primary Domain test file focused on initialization, input normalization, mutation, and issue semantics.
- First composition plan production cleanup is complete: initialization validation and operation/external-processor validation were split into focused `CompositionPlan.*` partials, leaving the primary plan file focused on construction, address-space indexing, and public plan contracts without changing exception text, overlap policy, or range checks.
- First composition plan test cleanup is complete: overlap validation, address-space/input policy, external-processor policy, and shared plan helpers were split into focused `CompositionPlanTests.*` partials without changing Domain invariants.
- First Replace CLI test cleanup is complete: DP Replace, CtrlRAM Replace, General Replace, validation/safety, and shared CLI helpers were split into focused `ReplaceCliCommandTests.*` partials without changing command-line assertions or workbench/golden fixture coverage.
- First saved-rule CLI test cleanup is complete: saved-rule schema/security validation, mapping projection, General Merge rule consumption, and shared JSON/CLI helpers were split into focused `SavedRuleCliCommandTests.*` partials without changing command-line assertions or saved-rule contract coverage.
- First postbuild catalog test cleanup is complete: version-selection, alias/branch, and command-line argv contract tests were split out of `LegacyCombinerPostbuildCatalogTests`, leaving the primary file focused on planner/write-range/sentinel coverage.
- First legacy combiner processor test cleanup is complete: failure/closed-behavior tests and shared fake-runner/workspace fixtures were split out of `LegacyCombinerPostbuildProcessorTests`, leaving the primary file focused on successful staging and transform coverage without changing Infrastructure behavior.
- First real combiner smoke test cleanup is complete: CRC smoke, 16-byte self-replacement matrix, pure pasteback equivalence, test data, and manifest/golden helpers were split into focused `LegacyCombinerPostbuildRealToolSmokeTests.*` partials without changing real-tool coverage.
- First architecture test cleanup is complete: UI boundary/shell assertions, roadmap/document assertions, and shared repository parsing helpers were split into focused partial test files without changing repository-boundary coverage.
- Second architecture test cleanup is complete: Bootstrap/Application/Presetation structural boundary guards were split into `RepositoryBoundaryTests.Structure.cs`, leaving the primary repository-boundary file focused on dependency-free setup and shell/UI surface assertions.
- Third architecture test cleanup is complete: structural boundary guards were split into focused Presentation, Bootstrap, and Profile/Postbuild partials, so future 0.8 structure rules can be added beside their owning layer without growing one mixed guard file.
- Fourth architecture test cleanup is complete: shell surface and localization assertions now live in focused partials, leaving the root repository-boundary test file focused on dependency-free setup only.
- First UI smoke cleanup is complete: report review, report history, shell-preference persistence, DP Perspective/DP Replace, and CtrlRAM Replace tests were split out of `ShellViewModelTests`, reducing the primary workflow smoke file to roughly 600 lines without changing UI assertions.
- Second UI smoke cleanup is complete: shared report-diff, pattern, CtrlRAM-region parsing, brush, and icon assertion helpers were split into `ShellViewModelTests.TestSupport.cs`, leaving the primary shell smoke file below the large-file threshold.
- Third UI smoke cleanup is complete: shell settings/navigation, Merge/General Merge, firmware-slot facts/visuals, and General Replace smoke assertions were split into focused `ShellViewModelTests.*` partials, leaving the primary shell smoke file as a small test-class shell without changing UI behavior assertions.
- Fourth UI smoke cleanup is complete: report modal/review, accepted output differences, report history persistence, warning classification, triage command evidence, and shell preference persistence were split into focused `ShellViewModelTests.Report*` and `Preferences` partials without changing report readability assertions.
- Fifth UI smoke cleanup is complete: CtrlRAM Replace display/selection, report preview behavior, and build/fixture/sentinel coverage were split into focused `ShellViewModelTests.CtrlRamReplace*` partials without changing UI smoke assertions.
- First profile compiler test cleanup is complete: input length/padding/truncation policy tests and processor ownership policy tests were split into focused partial test files, reducing `CompositionProfileCompilerTests` to roughly 490 lines without changing compiler behavior or test coverage.
- Second profile compiler test cleanup is complete: explicit mapping compilation, General Replace TP postbuild gating, region/access validation, and shared compiler fixtures were split into focused `CompositionProfileCompiler*` partials without changing profile compiler assertions.
- First Standard Merge profile cleanup is complete: DP Perspective and synthetic profile builders were split out of `BuiltInStandardMergeProfiles`, and owner-confirmed gen_flash aliases now derive from `IcSupportCatalog.StandardMergeSourceIcId` instead of duplicating reference TP/DP ranges.
- Second Standard Merge profile cleanup is complete: `BuiltInStandardMergeProfiles` is now the stable exposure-order root, while gen_flash rows, owner-confirmed aliases, the FlashMap 51930 row, and shared `StandardMergeRegion` helpers live in `BuiltInStandardMergeProfiles.GenFlash`; DP Perspective exposure now lives beside the DP Perspective builder.
- First FWConfig metadata cleanup is complete: reviewed FWConfig offsets now live in `FirmwareConfigLayout`, and the reader plus postbuild-category tests consume that catalog instead of duplicating private magic offsets.
- Second FWConfig metadata cleanup is complete: common-FW reading for postbuild category selection now belongs to `WorkbenchCompositionService.FirmwareMetadata` instead of the CtrlRAM Replace postbuild partial, keeping metadata ownership separate from workflow-specific command formatting.
- Third workbench firmware metadata cleanup is complete: FlashCode output naming and CtrlRAM display-region projection were split out of `WorkbenchCompositionService.FirmwareMetadata`, keeping firmware fact reading, output-name policy, and UI display projection in separate Bootstrap partials without changing behavior.
- First Standard Merge profile test cleanup is complete: gen_flash, alias, DP Perspective, and synthetic/flash-map profile contract tests were split into focused partials without changing assertions or executable profile coverage.
- First XAML cleanup is complete: shared `Window.Styles` were split into `Styles/MainWindowStyles.axaml`, reducing `MainWindow.axaml` by roughly 460 lines without changing bindings or layout.
- First report-template XAML cleanup is complete: pure report row/chip/history/operation templates were split into `Resources/MainWindowReportTemplates.axaml`, reducing `MainWindow.axaml` below roughly 1.95k lines while keeping event-bound templates in place.
- First shared-template XAML cleanup is complete: pure memory tooltip, firmware fact chip, and stretch presenter resources were split into `Resources/MainWindowSharedTemplates.axaml`; duplicate Replace base-slot markup now uses the existing firmware slot card template, reducing `MainWindow.axaml` to roughly 1.7k lines without changing firmware behavior.
- Second shared-template XAML cleanup is complete: Merge/Replace memory coverage bar/list/group templates and Merge/Replace memory-map row templates now live in `Resources/MainWindowSharedTemplates.axaml`, reducing `MainWindow.axaml` to roughly 1.37k lines while preserving existing ViewModel bindings and visual behavior.
- Second report-template XAML cleanup is complete: the report audit-details tabs now live in `Resources/MainWindowReportTemplates.axaml`, reducing `MainWindow.axaml` to roughly 1.11k lines while keeping event-bound report save/close shell wiring in the root window.
- First report-panel XAML cleanup is complete: report modal header, history panel, and outcome summary panel now live in `Resources/MainWindowReportPanels.axaml`, reducing `MainWindow.axaml` to roughly 990 lines while keeping report save and file-input event handlers in the root window.
- First shell-panel XAML cleanup is complete: navigation trail, device context selector, report toast, and footer now live in `Resources/MainWindowShellPanels.axaml`; Merge/Replace output layout panels now live in `Resources/MainWindowSharedTemplates.axaml`, reducing `MainWindow.axaml` to roughly 790 lines while leaving event-bound workflow authoring rows in the root window.
- Second page-template XAML cleanup is complete: pure Home and Settings page layouts were split into `Resources/MainWindowPageTemplates.axaml`, leaving `MainWindow.axaml` focused on shell navigation plus event-bound Merge, Replace, file-input, and report surfaces.
- First MainWindow code-behind cleanup is complete: report launch/save/toast handlers, Build save handlers, and file input drag/browse/remove handlers were split into focused partial files, leaving `MainWindow.axaml.cs` as lifecycle/preference coordination only.
- First modal XAML cleanup is complete: Replace selection review and report review overlays were split into focused `Views/ReplaceSelectionModal` and `Views/ReportModal` controls, and firmware/report save picker definitions were centralized in `FirmwareFilePickerDialogs` without changing ViewModel state, report schema, or build output behavior.
- Third shared-template XAML cleanup is complete: the pure CtrlRAM firmware slot-group template now lives in `Resources/MainWindowSharedTemplates.axaml`, leaving `MainWindow.axaml` below the large-file threshold and focused on event-bound workflow authoring surfaces.
- Third report-template XAML cleanup is complete: report audit-details tabs moved to `Resources/MainWindowReportAuditTemplates.axaml`, leaving `MainWindowReportTemplates.axaml` focused on reusable report rows, chips, operation nodes, range tables, and history entries.
- First firmware-slot control cleanup is complete: firmware input slot cards now live in `Views/FirmwareSlotCard`, shared drag/drop state and BIN open dialogs are centralized, and `MainWindow.axaml` is roughly 630 lines while keeping file-selection events explicit and free of firmware semantics.
- First mapping-row control cleanup is complete: General Replace and General Merge mapping rows now live in focused `Views/GeneralReplaceMappingRow` and `Views/GeneralMergeMappingRow` controls, so `MainWindow.axaml` is roughly 500 lines and no longer owns browse/drop/remove handlers for row-level file inputs.
- Split work must preserve behavior first: extract reusable XAML resources, focused report view-model projections, catalog row groups, and test fixtures only when existing UI smoke/catalog/golden tests stay equivalent.
- Large-file cleanup is not allowed to change firmware ranges, operation order, output naming, or report semantics unless that semantic change is separately cataloged and tested.

### Report readability

- The default report view states the outcome, generated artifact name, and whether differences are accepted, unexpected, or blocking.
- SHA-256 and raw hashes are kept in saved/raw report details, not exposed as primary screen text unless the user opens audit details.
- Diff rows are grouped by the best-known TP flash/header section, for example `Header`, `Header copy`, `DLM CRC`, `NF CtrlRAM`, `VN CtrlRAM`, or `Unclassified`.
- `Header` and `Header copy` are shown as distinct sections when `TpHeaderCatalog` can classify them.
- Byte differences use a readable bytes view: offset, before bytes, after bytes, changed length, and section label. Long byte runs are chunked instead of rendered as one continuous line.
- Operations render as a vertical ordered timeline using sequence numbers such as `100`, `200`, `300`, with one clear action per step.
- Postbuild details are separate from normal copy/replace operations. A postbuild command may reference the operation it refreshes, but it is not duplicated as an operation step.
- Inputs are presented as aligned human-readable rows with role, file name, size, detected metadata, and status. Detailed path/hash data is audit-only.
- Empty/debug-only sections are hidden by default and available only in raw/audit detail mode.

### Verification

- Profile contract tests cover every centralized policy that replaces duplicated constants or workflow exposure rules.
- IC onboarding contract tests reject duplicate IC rows, unknown workflow ids, orphan alias sources, and Standard Merge aliases without executable source profiles.
- Application catalog tests prove that supported ICs have matching flash-map/header/postbuild coverage when the workflow requires it.
- Current cross-catalog tests start from `IcSupportCatalog` and verify every CtrlRAM Replace exposure has TP flash-map rows, postbuild profiles, selectable IC-number choices, and staged BIN blocks that map back to profile-adjusted TP Overview CtrlRAM rows.
- Postbuild staged-block to TP Overview CtrlRAM row checks derive their tested selections from profile branch rules, including extended numeric branches such as 14/29 instead of only hand-picked 2/3 cases.
- Current cross-catalog guards verify every postbuild planner write-section id resolves to a declared `TpHeaderCatalog` section, so report labels and write-range classification stay in the same catalog.
- Repository boundary tests verify that Bootstrap keeps a single preview-before-build gate, preventing CLI and Workbench build validation from drifting.
- Repository boundary tests verify that General mapping text parsing stays centralized in `BootstrapRangeText`, preventing CLI-specific helpers or cross-partial Workbench helpers from becoming hidden parser owners.
- UI/report smoke tests cover at least one successful CtrlRAM Replace self-replacement report and one blocking report.
- Golden/self-replacement tests remain byte-for-byte stable for existing approved fixtures.
- `python scripts/verify.py --all` passes locally before the milestone is considered complete.

### Release gate

- No new firmware BIN, private source, or generated release package is committed unless it is an owner-approved golden fixture.
- Existing 0.7.x behavior remains available while 0.8.0 refactoring is in progress.
- The milestone is not promoted as a support claim until owner-selected IC/mode rows have matching evidence, tests, and sign-off.
