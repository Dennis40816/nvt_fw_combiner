# 0.9.x Codebase Readiness Audit

Status: active, evidence-backed planning inventory.

Audit date: 2026-07-17.

Audited branch: local `0.9.10` at `762c777d`, based on
`origin/0.9.10` at `164e4f35`. This inventory is not a firmware support claim
and does not authorize a byte, range, processor, schema, or release change.

Parallel-development update, 2026-07-18: local `0.9.10` was rebased onto the
current clean `0.9.9` committed tip `7e18cfa0`. `git range-diff` confirmed all
four existing `0.9.10` commits remained patch-equivalent and the ancestry count
became `0/4`. This is a temporary parallel-development baseline: `0.9.9` is
still active, the remote `0.9.10` ref was not rewritten, and a final rebase plus
canonical verification remain mandatory after `0.9.9` closes.

## Purpose and method

This audit converts important current codebase gaps into explicit `0.9.x`
release work. It inspected repository instructions, SPEC, accepted/proposed
ADRs, production projects, tests, profiles and evidence manifests, living
support/retirement reports, local/remote branch lineage, CI workflows, package
scripts, and the canonical verifier.

The scan distinguished:

- **confirmed** issues that have direct source, test, branch, or workflow
  evidence;
- **evidence gates** where code can remain candidate-only until the owner
  supplies firmware or release proof; and
- **research** that must be measured before an implementation decision.

An issue is assigned to a milestone; this does not authorize its implementation
without the risk and review gate in this document. A version branch may exclude
an optional workflow only through an explicit owner scope decision. It may not
hide an unresolved release-scope blocker by moving it to an unspecified later
major/minor line.

## Verified baseline

- The repository contains eight production C# projects and nine test/support
  projects. Production builds with nullable analysis and warnings-as-errors.
- No production `TODO`, `FIXME`, `NotImplementedException`, unpinned per-run
  executable path, or production dependency on `refcode/` was found.
- `python scripts/verify.py --all` on 2026-07-17 passed repository structure,
  Polytail, 19 repository Python tests, Ruff, Pyright, Pylint, 28 CRC-worker
  tests at 98.88% coverage, restore, format, and Release build with zero
  warnings/errors.
- The same full gate passed 1,605 .NET tests but failed one Infrastructure test:
  `RunAsyncTimeoutKillsChildProcessBeforeReturning` did not observe the child
  PID before the three-second runner timeout. The isolated test then passed in
  three seconds. The current `origin/0.9.9` branch already extends that timeout
  and adds headless process-start coverage; current `origin/0.9.10` does not
  contain those commits. The full gate is therefore still red for the audited
  `0.9.10` lineage and may not be represented as passing.
- Two Unix-special-file bundle tests are expected to skip on Windows. CI runs
  .NET tests only on `windows-latest`, so the FIFO and Unix-domain-socket cases
  are not currently exercised by protected CI.
- At the 2026-07-17 audit, `origin/0.9.9...origin/0.9.10` had five commits
  unique to `0.9.9` and two unique to `0.9.10`; the branches were siblings
  after `v0.9.8`. The 2026-07-18 local rebase now provides a temporary ancestor
  chain, but multiple active `feature/0.9.9/*` branches and the eventual final
  `0.9.9` tip still require a final rebase and explicit merge, supersede, or
  close decisions.
- The documented `v0.9.9` production-source goal is 54,000 nonblank lines, but
  `origin/0.9.9` ratchets 56,731 and audited `0.9.10` ratchets 56,742. Firmware
  safety must not be weakened to force the old number; the milestone must
  either complete exact proven retirement or record an owner-accepted measured
  ratchet.

## Important issue inventory

| ID | Priority / risk | Confirmed issue or gate | Evidence | Assigned completion milestone |
| --- | --- | --- | --- | --- |
| `NFC-0910-01` | P0 / R2 integration | The audited `0.9.10` was not based on `0.9.9`; the 2026-07-18 local rebase fixes current ancestry but `0.9.9` is still active, so later commits can still be missed. | Historical remote left/right count `5/2`; current local temporary ancestry `0/4` at `0.9.9` `7e18cfa0`; final rebase pending. | `v0.9.10` final predecessor reconciliation after `0.9.9` closes. |
| `NFC-0910-02` | P0 / R1 | The audited canonical full gate is red because the timeout process-tree test is load-sensitive in the current lineage. | Full-suite failure before child PID publication; isolated pass; candidate fix already exists on `origin/0.9.9`. | `v0.9.10`; reconcile, review the candidate fix, and rerun the full gate once. |
| `NFC-0910-03` | P1 / R0 governance | The 54,000-line `v0.9.9` promise does not match either active branch ratchet. | `origin/0.9.9` = 56,731; audited `0.9.10` = 56,742. | `v0.9.10` owner ratchet decision; deletion remains evidence-driven. |
| `NFC-0910-04` | P0 / R2 + security/dependency review | Candidate IC intake has a Proposed ADR, two unmerged contract branches, no accepted workbook/dependency/security decision, and no canonical C# use case/UI implementation. | ADR 0023 remains Proposed; workbook and workspace contract branches are ahead of `origin/0.9.10`. | `v0.9.10` candidate-intake closure. |
| `NFC-0911-01` | P1 / R2 | Workbench automatic Build performs a full Preview and then a full Build, duplicating engine and processor work. | `CompositionRunExecutionSupport.PreviewOrBuildAsync`. | `v0.9.11` single-pass automatic Build. |
| `NFC-0911-02` | P1 / R1-R3 by phase | UI firmware inspection rereads selected files; byte ownership crosses redundant copies; CtrlRAM postbuild performs `2C + 1` host full-image reads per session; no deterministic performance counters separate these costs. | Workbench firmware metadata/context paths, `ToArray` inventory, and `LegacyCombinerPostbuildProcessor`. | Instrument/baseline in `v0.9.10`; safe UI/Application work in `v0.9.11`; R3 adapter convergence in `v0.9.15` after golden closure. |
| `NFC-0912-01` | P1 / R2 | Firmware/path inspection and ambient run metadata remain spread across Bootstrap and Presentation instead of explicit Application ports with Infrastructure adapters. Local preference/history persistence is an intentional UI adapter and is not included in this finding. | Direct `File`, `FileInfo`, `Directory`, clock, and GUID use in Workbench/CLI/UI composition paths. | `v0.9.12` boundary convergence. |
| `NFC-0912-02` | P1 / R2 contract | Workbench returns report JSON, while UI and CLI reparse field names and infer fallback labels. This weakens compile-time report evolution and contributes to the `DeclaredReplacement`/version-edit ambiguity. | `WorkbenchRunResult.ReportJson`, `ReportReviewViewModel.*`, and CLI `JsonDocument` report parsing. | `v0.9.12` typed report projection and semantic migration. |
| `NFC-0912-03` | P1 / R1-R2 | Architecture coverage is dominated by source-text shape ratchets and does not comprehensively enforce project dependency, filesystem/process, report-contract, and port ownership rules. Unix bundle special-file cases also lack a protected Linux .NET job. | 78 architecture tests use 302 `ReadText` calls and approximately 1,097 string-shape assertions; CI .NET job is Windows-only. | Semantic boundary tests in `v0.9.12`; cross-platform CI evidence in `v0.9.16`. |
| `NFC-0913-01` | P0 before runtime convergence / R2-R3 | CtrlRAM and General Replace still depend on legacy `CompositionProfileCompiler`, `TpFlashMapCatalog`, `IcMetadataFacade`, and `LegacyCombinerPostbuildCatalog` authority. | Active consumers listed in the legacy-retirement matrix and production-source references. | `v0.9.13`, one workflow/evidence row at a time. |
| `NFC-0914-01` | P0 support gate / R3 | CtrlRAM Replace lacks release-scope expected outputs and owner parity; NT51931 official command crashes; full-flash versus TP-work-image slicing is undecided. | CtrlRAM status report and private fixture manifest. | `v0.9.14` firmware evidence closure. |
| `NFC-0914-02` | P0 if selected / R3 | General Replace lacks final owner safety envelopes, alignment/overlap decisions, and TP/CtrlRAM expected outputs. Reusable General Merge/Saved Rule policy is also not approved for support-style promotion. | Supported-IC matrix and owner-input checklist. | `v0.9.14` scope/evidence decision; explicitly exclude unapproved rows. |
| `NFC-0914-03` | P0 support gate / R3 | The release support matrix is not locked. Standard Merge aliases/capacities, DP Replace beyond NT51950/NT51951, and AB candidates still require selected-case evidence and firmware-owner review. | Supported-IC matrix, AB manifest, owner handoff checklist. | `v0.9.14` support-scope lock and golden review. |
| `NFC-0915-01` | P1 / R1-R3 | End-to-end operability is not closed: run progress/cancellation branches are split, report/history audit review is incomplete, performance has no accepted baseline, and postbuild I/O cannot be optimized safely before golden parity. | Unmerged `feature/0.9.9/*` UX branches, report roadmap, and Replace optimization rationale. | `v0.9.15` UX, soak, performance, and postbuild-adapter closure. |
| `NFC-0916-01` | P0 release gate / R3 | Stable release still needs independent clean-machine UI launch and representative Preview/Build, signing/legal and golden-redistribution decisions, protected required-check/branch evidence, independent reviewer identity, and final package/provenance verification. Current package and release workflows pass `-SkipUiLaunch`. | Release workflows, CODEOWNERS, package/smoke scripts, license scope, and verification report. | `v0.9.16` release/security closure. |
| `NFC-0916-02` | P1 / R0 | Living truth is split across an 0.9.8 SPEC header, an older supported matrix, a 2026-07-05 CtrlRAM status header, a checklist ending at v0.9.6, and a roadmap that formerly deferred stabilization to 0.10.x-0.12.x. | Current documentation headers and milestone references. | Start in `v0.9.10`; final truth audit in `v0.9.16`. |
| `NFC-0914-04` | P2 scope decision / R0 or R3 if activated | REG Replace, UI Saved Rules, General Replace saved-rule execution, and unsupported AB/IC branches are deferred capabilities, not hidden completion work. | SPEC and supported-IC matrix. | Owner explicitly includes with evidence or records post-1.0 exclusion in `v0.9.14`. |

## Architecture disposition

The primary use cases remain normal Merge and normal Replace from the same UI
workbench and CLI. Both create typed Application requests, compile through one
profile/compiler boundary, execute through one Domain composition engine, and
use Infrastructure only through ports for filesystem, staging, process, and
report persistence. Candidate intake produces reviewable candidate records; it
does not become another runtime loader or executor.

The affected durable contracts are V2 family/map/profile bundles, compiled
composition plans, external-processor declarations, immutable run/report
projections, and the candidate-intake records proposed by ADR 0023. The
required dependency direction is Presentation/CLI -> Application -> Domain,
with Infrastructure implementing Application ports. Domain remains free of
filesystem, process, JSON, and UI dependencies.

The selected strategy is staged convergence inside `0.9.x`: measure first,
introduce typed ports/reports before retirement, prove V2 parity one workflow
at a time, lock the support subset, then close package/release evidence. The
following alternatives are rejected:

- defer important stabilization to unnamed later minor versions;
- reuse Preview output as Build authority or let a UI cache authorize bytes;
- infer report semantics in Presentation from operation ids or reason strings;
- add a second compiler/executor or a UI-only firmware restriction;
- delete safety, test, evidence, or compatibility paths to reach a line count;
- move UI-local preference/history persistence merely to satisfy a blanket
  layer rule.

Stable report classifications and schema tokens remain compatible until a
separately reviewed versioned migration exists. Firmware output, operation
order, processor authority, ranges, hashes, and support state remain unchanged
by R0-R2 convergence. Each R3 workflow phase requires its direct golden and
owner gate. Release metadata changes only at the final reviewed milestone, so
candidate branches cannot imply package or support promotion.

## Milestone and feature-branch allocation

| Version branch | Required outcomes | Suggested independently reviewable feature branches |
| --- | --- | --- |
| `0.9.10` | Reconcile reviewed `0.9.9`; make the canonical full gate green; resolve the code-size ratchet; accept/reject ADR 0023 choices; review or close candidate workbook/workspace branches; implement the one typed candidate-intake boundary and Settings UI if accepted; record deterministic Replace baselines; refresh living documentation. | `feature/0.9.10/predecessor-reconciliation`, `feature/0.9.10/candidate-intake-contracts`, `feature/0.9.10/typed-candidate-intake`, `feature/0.9.10/replace-performance-baseline`. |
| `0.9.11` | Single-pass automatic Build, one UI inspection snapshot, deterministic execution/read/process counters, and safe same-owner buffer-copy convergence. Build retains authoritative reread/hash, final validation, and atomic promotion. | `feature/0.9.11/single-pass-build`, `feature/0.9.11/firmware-inspection-snapshot`, `feature/0.9.11/buffer-ownership`. |
| `0.9.12` | Introduce/finish Application ports for composition file inspection and run metadata, keep Infrastructure as adapter owner, return typed report data to UI/CLI, distinguish version-edit semantics, and add semantic dependency/boundary tests. | `feature/0.9.12/io-port-boundary`, `feature/0.9.12/typed-report-projection`, `feature/0.9.12/architecture-enforcement`. |
| `0.9.13` | Route CtrlRAM and General Replace through trusted V2 compile/runtime authority; retire legacy compiler/catalog projections only after direct output/report/UI/CLI parity and the retirement matrix close each consumer. | `feature/0.9.13/ctrlram-v2-authority`, `feature/0.9.13/general-replace-v2-authority`, `feature/0.9.13/legacy-catalog-retirement`. |
| `0.9.14` | Owner locks the shipped IC/workflow/capacity/topology set; close selected Standard/DP/CtrlRAM/General/AB golden and processor evidence; decide NT51931 and TP-slice behavior; explicitly exclude unapproved/deferred capabilities. | One `feature/0.9.14/<workflow>-<ic-or-scope>` branch per independent R3 evidence set; never combine unrelated IC goldens. |
| `0.9.15` | Finish report/history/run-progress/accessibility, cancellation and long-run soak; enforce evidence-backed performance budgets; apply the R3 postbuild I/O optimization only after exact golden parity. | `feature/0.9.15/run-operability`, `feature/0.9.15/report-audit-ux`, `feature/0.9.15/postbuild-performance`. |
| `0.9.16` | Add missing protected cross-platform/security coverage, run independent clean-machine package/UI/representative workflow smoke, close signing/legal/redistribution and branch-protection evidence, and produce the final reviewed 0.9.x support/release candidate. | `feature/0.9.16/cross-platform-security-ci`, `feature/0.9.16/clean-machine-smoke`, `feature/0.9.16/signing-release-governance`. |

No feature branch targets `main`. Each feature branch is created from and
reviewed back into its exact version branch. A later version branch starts only
from the reviewed predecessor. `v1.0.0` follows `v0.9.16`; important
stabilization work is not assigned to an unspecified `0.10.x`, `0.11.x`, or
`0.12.x` bucket.

## Dependency order and stop conditions

1. Reconcile branch lineage before accepting any `0.9.10` implementation or
   benchmark result.
2. Restore a green canonical gate before using the branch as a performance or
   migration baseline.
3. Accept the candidate-intake ADR/security/dependency choices before merging
   the proposed workbook/workspace contracts or adding UI.
4. Instrument before optimizing; keep Build authority independent of UI cache.
5. Establish typed ports/report contracts before retiring compatibility
   projections.
6. Complete V2 runtime parity before firmware support promotion; complete
   product goldens before R3 postbuild optimization.
7. Lock the support scope before final UX/package claims.
8. Close signing, legal, redistribution, clean-machine, protected-CI, and
   independent-review gates before a stable tag or release.

Any changed firmware range, operation order, CRC/header behavior, processor
read/write authority, golden hash, or support promotion stops for R3 evidence
and firmware-owner review. Any workflow excluded from `v1.0.0` remains visibly
candidate/deferred and cannot be used to block closure of the selected support
subset.

## Items inspected but not promoted to important issues

- Central package versions, lock files, NuGet audit, the exact repository SDK
  installer, and warnings-as-errors are already enforced.
- External processes use `ProcessStartInfo.ArgumentList` with shell execution
  disabled; `origin/0.9.9` additionally records headless process start.
- The two Windows skips are platform conditions, not fake tests. The issue is
  protected Linux .NET coverage, not removal of the skip reason.
- Localization `CS1591` suppressions and large partial aggregates require
  narrow justification/ownership review during `v0.9.12`, but no correctness
  failure was found from their presence alone.
- Code-size reduction is not an independent product goal. Only exact semantic
  retirement with retained evidence may lower a ratchet.

## Audit completion rule

This inventory is complete when every P0/P1 row is either closed by its named
milestone with tests/review evidence or explicitly excluded by an owner decision
that does not contradict the selected `v1.0.0` support scope. New confirmed
issues receive a stable id and a specific remaining `0.9.x` milestone; they are
not appended to a generic post-0.9 stabilization bucket.
