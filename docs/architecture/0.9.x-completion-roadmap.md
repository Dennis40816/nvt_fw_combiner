# 0.9.x Completion Roadmap

Status: active owner roadmap.

This document is the execution goal for the remaining `0.9.x` milestones. It
separates a completed migration milestone from a firmware-support claim and
keeps each later release small enough to verify independently.

## Program goal

Deliver the owner-approved AB, CtrlRAM, typed family/evidence, support-matrix,
and legacy-retirement scope through `v0.9.9` using the existing V2 boundary and
one composition engine. Reconcile that result into `v0.9.10`, then measure and
remediate process, I/O, allocation, report, history, and UI bottlenecks without
changing firmware behavior or support truth. The milestone includes a
read-only Hex Diff review workspace and typed progress. Runtime FWConfig facts
always come from the unique NVT Backup copy at terminal `T - 0xFFF`; a primary
flash-map address is evidence only and is never a runtime fallback.

## Completed-milestone audit

| Milestone | Audit result | Evidence | Carry-forward action |
| --- | --- | --- | --- |
| `v0.9.1` | Functional scope complete and tagged. | `v0.9.1` points at `1d910b45`; trusted V2 bundle/map/profile compilation routes covered Standard Merge and NT51950/NT51951 DP Replace. The release evidence records local `verify.py --all` success and the remote CI outage. | No functional migration item is moved to `v0.9.3`. It remains a candidate/support-neutral migration milestone. |
| `v0.9.2` | Functional scope complete and tagged. | `v0.9.2` points at `57fefbdf`; content-addressed schema source, deterministic closed-root materialization, all built-in bundle materialization, and removal of the Standard Merge legacy runtime fallback are merged into `0.9.2`. | `v0.9.3` starts from this reviewed release baseline. This milestone did not add AB behavior. |

The following are deliberately not omissions from `0.9.1` or `0.9.2`:

- Remaining legacy Replace/General runtime routes and compatibility projections
  are governed by the [Legacy Retirement Matrix](../governance/legacy-retirement-matrix.md)
  and belong to `v0.9.5` only after exact V2 replacement evidence exists.
- Unsigned or private CtrlRAM/General Replace golden evidence for unrelated ICs
  is not silently promoted by either migration milestone. `v0.9.3` closes the
  evidence required by its own AB and version-edit work only.
- Product support remains a firmware-owner decision. A V2 runtime route or a
  tagged migration milestone never changes a profile to `Supported`.

## Release order

```text
v0.9.1 (tagged)
  -> v0.9.2 (tagged release baseline)
  -> v0.9.3 AB Code + CtrlRAM version edit
  -> v0.9.4 candidate IC intake
  -> v0.9.5 V2 workflow convergence and legacy retirement
  -> v0.9.6 release/support consolidation
  -> v0.9.7 UI token consolidation and code-size discipline
  -> v0.9.8 feature-frozen source/package convergence
  -> v0.9.9 legacy convergence and verified patch closure
  -> v0.9.10 end-to-end performance and Change Report remediation
  -> post-remediation route selected after reviewed v0.9.9 and v0.9.10 merge
  -> v1.0.0 first signed-off stable sample
```

`0.9.3` integration and release must start from the reviewed `v0.9.2` tag.
Candidate AB work may start earlier under the policy below, but it must rebase
or merge onto that tag before integration. Do not backfill the tag after AB
integration has started: that would make the AB evidence baseline ambiguous.

### Pre-tag candidate-development policy

Owner policy, 2026-07-14: development of a later `0.9.x` feature branch is
allowed before its preceding release tag and without owner firmware evidence.
This permission is limited to deterministic candidate, architecture, intake,
or test work. It does not grant runtime support, change a promotion stage,
replace direct golden/legacy parity, permit a release tag, or allow a merge to
`main`. Each early branch names its target milestone and must rebase or merge
onto the required reviewed predecessor tag before that target can integrate or
release.

## v0.9.3: AB Code and CtrlRAM version edit

Risk class: R3. A separate `feature/0.9.3/<topic>` branch is required for each
independently reviewable firmware slice, with firmware-owner review before the
release tag.

### Current AB evidence inventory (2026-07-15)

All five V2 candidates copy the complete submitted DP_AB container before
their declared TP overlays, and all five are rejected by the Application run
request boundary. No row below is runtime support.

| Target | Verified candidate evidence | Remaining R3 owner gate |
| --- | --- | --- |
| NT51919 | Explicit owner-approved fact-scoped NT51929 region-set alias with complete candidate-output parity to the tracked NT51929 case. | A direct product golden is still absent; runtime promotion review remains separate. |
| NT51929 | Direct owner fixture and complete V2/Python-reference parity, output `c7e1e263...3d66abe2`. | Firmware-owner promotion review. |
| NT51932 | Owner-approved fact-scoped NT51929 golden applicability plus direct named Python-configuration synthetic parity, output `cd54e124...7de10ce`. | Firmware-owner promotion review. |
| NT51950 | Two direct owner fixtures reproduce the Python reference and Combiner 1.13.0 command byte-for-byte. The command consumes no `map.txt`; C# relocates only TPB DIFF and never writes header CRC. | Firmware-owner approval of the exact staging/command sequence before runtime exposure. |
| NT51951 | Owner-approved NT51950 workflow-logic applicability plus synthetic full-byte Python/Combiner parity for `NT51950BASED_MERGE_AB_MODE CRC8 A.bin B.bin output.bin 0x80000`. | Firmware-owner promotion review; the alias is not a direct NT51951 product golden. |

### Scope

1. Implement AB Merge for NT51919, NT51929, NT51932, NT51950, and NT51951
   through V2 bundle data, the existing compiler, and the existing composition
   engine. There is no AB-only executor or UI-owned byte logic.
2. Every AB profile initializes its output from the complete submitted DP_AB
   container, then applies only profile-declared TPA/TPB overlays. Customer
   information follows the submitted DP_AB container unless owner evidence
   declares a narrower exception.
3. NT51919 may reuse NT51929/NT51932 AB facts only through an approved
   fact-scoped map binding with direct parity evidence. It is not a whole-map
   alias.
4. NT51950 topology-dependent AB behavior derives its chip count from the
   canonical FWConfig Backup layout. Profile applicability selects the needed
   CMD/CMD-BK facts; it must not expose eight synthetic modes when aliases
   produce the same executable behavior.
5. Implement the generic `transform-scalar` operation only where a profile
   declares the approved TPB relocation field. It remains checked for width,
   byte order, expected-before value, and overflow.
6. Verify whether the external Python CRC transform and legacy Combiner are
   equivalent separately for each approved 950/951 AB branch. No equivalence
   is assumed from their names or from Normal/CtrlRAM postbuild behavior.
7. On CtrlRAM Replace Build only, present a pre-Build review choice to retain
   the TP FW major/sub-version or edit both values. Preview never edits the
   version. The version reader and any approved writer use
   `FirmwareConfigLayout` and the NVT Backup copy only; fields, write ranges,
   ordering relative to Combiner postbuild, and output differences require
   owner evidence and tests.

### Required evidence and tests

- Archive each owner-supplied AB golden outside Git unless it is an approved
  fixture. Commit only manifests, hashes, provenance, and explicit evidence
  decisions.
- Add direct V2/reference output parity and negative tests for every approved
  IC/topology/capacity branch. Test initializer order, overlays, customer-info
  ownership, scalar relocation, CRC/header declared writes, and rejection of
  malformed/ambiguous metadata.
- Test both CtrlRAM version choices against the same base image: retain is
  byte-identical to the existing approved branch; edit changes only the
  declared version and postbuild ranges. Confirm an invalid or missing NVT
  Backup fails closed.
- Run the affected contract, engine, worker, Bootstrap, UI, and golden tests;
  finish with `python scripts/verify.py --all`, Polytail, independent review,
  and firmware-owner review. A failed or unavailable remote CI is recorded,
  never represented as a passing firmware gate.

### Exit criteria

`v0.9.3` may be tagged only when all five approved AB targets and the selected
CtrlRAM version-edit branches have their required R3 evidence, owner review,
and immutable golden/legacy parity records. Unapproved ICs, modes, capacities,
or CtrlRAM postbuild variants remain candidate data with explicit blockers.

## v0.9.4: automated IC intake

Provide a manifest-driven intake command or UI that reads declared evidence
into a caller-selected empty staging directory and emits candidate bundle rows
plus validation reports. It must be deterministic, read Office files without
macros, reject path escapes/overwrite/lock files, and have no runtime profile
registration, support promotion, range inference, or automatic schema allowlist
change. A human still reviews and promotes a candidate through the normal
bundle materialization path.

## v0.9.5: V2 workflow convergence

Migrate one workflow family at a time until its V2 runtime/profile/map path
has direct byte, naming, processor, report, CLI, and UI parity. Only then
retire the corresponding legacy definition named in the retirement matrix.
This includes the remaining Replace/General compiler authority, flash-map and
metadata compatibility projections, and legacy Combiner postbuild catalog when
their R3 evidence permits replacement. Do not delete a legacy catalog merely
because it is old or because one adjacent workflow has migrated.

## v0.9.6: release and support consolidation

Remove compatibility projections whose `v0.9.5` retirement rows are closed;
reconcile the IC/workflow support matrix; close clean-machine Windows package,
SBOM/provenance, hash, signing-policy, report/history, and release-manifest
evidence. This milestone does not introduce new firmware behavior or use
packaging as a substitute for firmware-owner support approval.

## v0.9.7: UI token consolidation and code-size discipline

Risk class: R1/R2 according to the affected UI boundary. Candidate work may
start before the reviewed `v0.9.6` tag under the pre-tag policy, but integration
and release follow that tag. It consolidates exactly equivalent Avalonia visual,
text, and spacing tokens so one semantic UI concept has one owner. It also
records a production/UI/test line-count inventory and removes only definitions,
adapters, or compatibility paths that have an exact retained replacement and
direct regression coverage.

It must not:

- place firmware ranges, profile behavior, integrity policy, or firmware
  terminology decisions in UI tokens;
- replace accessibility labels, localization resources, or meaningful state
  distinctions with visually similar shared values;
- remove required tests, manifests, golden evidence, or review records to
  improve a line-count metric; or
- introduce a second theme, localization, or token hierarchy.

Exit evidence records the baseline and resulting line counts, every retired
definition and its retained owner, UI smoke/accessibility coverage for affected
surfaces, and an independent review that confirms no semantic token collapse or
firmware-boundary drift. A smaller codebase is an outcome of exact replacement,
not a standalone acceptance target.

## v0.9.8: feature-frozen source and package convergence

Risk class: R2 for source ownership and R3 for release-package changes. Any
firmware-contract change is deferred to its own evidence-bearing R3 branch.
This milestone is feature-frozen and support-neutral: user-visible firmware
behavior, declared ranges, processor commands, output bytes, and support states
do not expand.

Exit gates:

- hand-written production C# and AXAML retain the owner-accepted 56,742-line
  final ratchet recorded on 2026-07-16; the earlier 56,000-line stretch gate is
  superseded and further source convergence moves to v0.9.9;
- the portable release package is no more than 1% larger than the reviewed
  v0.9.7 baseline;
- the exact Windows inner-ZIP baseline is 57,501,699 bytes and the maximum is
  58,076,715 bytes;
- canonical verification enforces production size, exact profile/schema
  duplication, and oversized partial-type aggregates;
- the reviewed package/release workflow builds a clean artifact and
  `smoke-release.ps1` enforces the package byte ceiling;
- redundant compatibility projections, hard-coded registrations, duplicate
  validation, and pass-through adapters are deleted only when one retained
  owner has direct regression evidence; and
- trust-boundary validation, immutable input handling, atomic output,
  processor diff checks, golden evidence, and required tests are not weakened
  to reach the size target.

## v0.9.9: legacy convergence and verified patch closure

Production C#/AXAML must be at or below 54,000 nonblank lines, and the portable
package remains within the same 1% growth ceiling. Every refactored legacy
compiler, profile, catalog, facade, and runtime path must be retired after its
exact V2 replacement is reviewed. The explicit exception is Legacy Combiner:
the approved executable and constrained runner remain.

Static Postbuild declarations move to the hash-pinned repository catalog only
with exact command, tool binding, branch, staging, and read/write range parity.
That support-neutral retirement does not promote an IC: full-output golden and
firmware-owner evidence remain R3 support gates. This milestone also closes confirmed patch
gaps, including bundle/package reparse or TOCTOU findings, without speculative
checks or support promotion. NT51919, NT51932, NT51951, and every other
candidate retain their recorded direct/fact-scoped evidence and owner gates.

## v0.9.10: end-to-end performance remediation

`v0.9.10` owns comprehensive process, I/O, allocation, report, history, and UI
performance work. It consumes the reviewed `v0.9.9` contracts and support truth;
it does not redo legacy retirement, family/evidence semantics, profile exposure,
or support-matrix decisions. Candidate-IC intake is no longer assigned to this
milestone.

The milestone is support-neutral. It may not change firmware bytes, ranges,
atomicity, operation or processor command order, CRC/header behavior, output
naming, report classifications, profile promotion, or the one compiler/engine
boundary merely to improve elapsed time. [ADR 0026](../adr/0026-typed-composition-run-progress.md)
owns typed UI progress and [ADR 0027](../adr/0027-evidence-preserving-performance-remediation.md)
owns the performance and inspection boundaries.

### Performance CHANGE rationale

| Area | Confirmed cost or usability gap | Required result |
| --- | --- | --- |
| Automatic Build | A historical Build click could execute complete Preview and Build paths. | One authoritative Application execution and one commit, with identical bytes/report facts. |
| Legacy Combiner | Sequential command plans repeat process launch and staging-firmware readback work. | Run exact argv/order as one private staging pipeline, keep fail-closed process/file checks, perform one final full read plus only evidenced selective normalization, and bind manifest discovery/registry construction to one explicit process lifetime. |
| Build responsiveness | Synchronous planning, composition, or report projection can delay first paint or block the dispatcher. | Publish active state first; keep CPU/I/O/projection off dispatcher with cancellation and latest-generation ownership. |
| Firmware inspection responsiveness | Slot facts, IC/context hints, CtrlRAM display selection, and the Build version-confirmation preflight can synchronously reread the same BIN on the dispatcher. | Read one immutable, file-stamped inspection snapshot off dispatcher per latest slot generation, reuse it only for UI facts, and reject stale completion without weakening Build-time reads. |
| Report generation | Fragmented changes create many ranges, hashes, semantic rows, allocations, and JSON fields before Presentation starts. | Measure discovery, semantic splitting, hashing, report construction, and serialization separately; remove repeated work without losing exact evidence. |
| Report review/history | Full JSON parsing, eager detail, duplicate live projections, persistence, reopen, and relocalization can amplify memory and UI-thread cost. | Sequential indexing, bounded pages, lazy detail, compact inactive history, off-dispatcher projection/persistence, and atomic latest-result publication. |
| Change review | A list-first Changes tab and 32-byte previews do not explain spatial byte changes. | A read-only virtualized Hex Diff with typed reason/verdict, range navigation, address jump, and optional verified original rows. |
| Raw Hex Editor search | Search/Next historically copied the complete work buffer and later still rescanned the unchanged snapshot for every same-query Next action. | Retain one immutable snapshot and one bounded immutable result per document revision/query, keep next/wrap/fallback semantics in Application, reject stale results at the mutation boundary, and preserve cancel-before-copy and typed no-document behavior. |
| Raw Hex Editor document model | The source-identity map retained one nullable integer for every byte, amplifying memory at the 8 MiB document limit. | Keep exact insert/delete identity with one compact integer and a private named sentinel; preserve nullable addresses only at the public viewport boundary. |
| Raw Hex Editor change navigation | Every edit eagerly projected every fragmented changed block into a non-virtualized inspector, while viewport cells repeatedly searched the complete range set. | Keep the complete typed range snapshot, project only one 64-row navigator window, preserve global traversal/reason/address semantics, and restrict viewport lookups to a lower-bound value search plus the structural subset. |
| Buffer ownership | Exact-length firmware images cross several defensive copies. | Retain copies between trust owners; remove only measured same-owner copies and lock backing identity in tests. |
| Measurement | Overall timestamps alone do not distinguish engine, processor, I/O, report, persistence, and UI costs. | Same-source B/C counts and local timing/allocation/working-set/dispatcher evidence. |

Source size remains a maintainability measurement, not a `v0.9.10` performance
KPI. Owner decision on 2026-07-19 sets a 60,000 nonblank production C#/AXAML
hard ceiling for this release. Safety, evidence, or tests are never removed to
reduce lines. A dedicated code-size convergence phase begins after `v0.9.11`
and restores a measured descending ratchet from its reviewed baseline.

### Completion and residual ledger

| Priority | Item | Status and remaining evidence |
| --- | --- | --- |
| Complete | B0 deterministic baseline | [Replace Performance Baseline](../references/replace-performance-baseline.md) records DP 256/512/1024 KiB, CtrlRAM 2/13 command counters, processor sessions, artifact reads, commits, and failed-input zero-commit executable evidence. |
| Complete path-independent slice | Per-run shared artifact snapshots | Commit `ab0e710e` snapshots each successfully read exact artifact id once inside one Application run. When General Merge or General Replace binds the same immutable source to multiple address spaces, the reader and SHA-256 work occur once while every binding still receives an independent Application-owned buffer, input summary, normalization, and validation. With `K` bindings over `U` unique successful artifact ids, reads and hashes are `U` instead of `K`, while trust-boundary buffer copies remain `K`. Failed reads are deliberately not cached, so separate bindings retain separate typed issues and retry behavior. There is no path normalization or cross-run cache. Full Application passes `174/174`, B0 execution metrics pass `7/7`, and Architecture passes `91/91`; no universal elapsed-time claim is made. |
| Complete path-independent slice | CtrlRAM runtime-reference base snapshot | Commit `9d65e219` lets the routed NT51926 Common FW 1.4.1 V2 CtrlRAM compile and its immediately following Application run consume the same host-read immutable base snapshot. The route therefore performs one physical base read instead of compiling from one file state and rereading a potentially different file state for execution; replacement reads and the Application-owned trust-boundary copy remain authoritative. There is no UI or cross-run cache and no tool/golden physical path is encoded. CtrlRAM CLI, archived TP-base golden, and B0 counters pass `45/45`; Architecture passes `92/92`. The formerly separate external Combiner final-read gate is closed in the stable B/C rows below. |
| Complete | Single authoritative Automatic Build | Application executes once and commits that exact output; tests lock execution/read/session/launch/commit counts, complete output bytes/hash, and the named mutation, validation, issue, and processor-command facts. |
| Complete | Typed progress and dispatcher entry | Application phases flow through Bootstrap to a localized step UI with a polite live region and reduced-motion behavior; composition and report projection leave the dispatcher. Commit `980f95bd` adds non-color completed/active/pending markers and executable high-contrast evidence without changing Application-owned phase truth. |
| Complete | Large-report Presentation projection | Difference scanning is sequential, detail is lazy, visible collections are paged, inactive history is compact, and manual reopen/persistence/relocalization execute off dispatcher. Persistence is serialized, latest-wins, cancellation-safe before atomic promotion, fault-isolated, and drained on normal close with a bounded best-effort timeout; stale projection completions are rejected. Commit `65da6758` defers each indexed row's `Lazy<T>` and closure until access: the same 10,000-range cold/repeated projection allocation falls from `10,883,824`/`10,869,728` to `9,203,800`/`9,189,704` bytes, exactly `1,680,024` bytes (`15.4%`) per projection. Commit `c22c2a68` then lets the production shell/history path share the original report string instead of retaining a second full-report UTF-8 mirror: an isolated temporary full-GC diagnostic reduced retained delta from `7,567,592` to `1,491,936` bytes (`80.3%`), while projection allocation increased to `9,553,408`/`9,531,816` bytes (`3.8%`/`3.7%`). The reachability diagnostic was removed, standalone callers are not generalized, and output/report/bounded-page/jump semantics remain identical. Stable-predecessor report p50/p95 and allocation evidence is closed in the Node B/C row below without a serializer claim. Packaged first-page and Windows working-set evidence remain in the manual package gate. |
| Complete path-independent slice | Report-history size accounting | Commit `64a45efd` carries the UTF-8 byte count already observed by a successful report projection into active history capture and relocalization. Build/report publication therefore no longer performs a second full JSON scan on the dispatcher solely to calculate the storage warning. Persisted metadata-only entries and invalid JSON retain the original fallback calculation, artifact-path bytes remain included, and history schema/reopen behavior do not change. Full UI passes `226/226` and Architecture passes `91/91`; packaged click-to-history publication remains part of the Windows gate rather than an elapsed-time claim. |
| Complete path-independent slice | Local report-history bounds | Commit `7185c0e2` adds a 16 MiB UTF-8 soft budget in addition to the existing 12-entry limit. The newest report is always retained; only older best-effort local entries are evicted when the byte budget is crossed. A persisted history file over 64 MiB is rejected from the opened handle before BOM inspection or JSON deserialization, bounding recovery from legacy or corrupted local state. Report JSON/schema, active review, entry ordering, atomic latest-wins persistence, and reopen behavior inside the retained set are unchanged. Full UI passes `228/228` and Architecture passes `91/91`. The dev.2 package opened its four-entry/11.8 MB history without materializing the 10,000-row report, but the exact 12-entry/16 MiB packaged working-set gate remains open. |
| Complete path-independent startup-allocation slice | Local UI state streaming restore | Commit `c47b28c9` deserializes current UTF-8 history/preferences directly from the bounded opened file and streams legacy UTF-16/UTF-32 BOM input through a UTF-8 transcoder instead of allocating a second whole-file text value. The warmed 4 MiB current-format history observation was `25,227,920` current-thread allocated bytes before the change; both current UTF-8 and legacy UTF-16 paths now pass a `12,582,912`-byte ceiling while preserving the complete report string. Readers share delete, not write, so an atomic save can publish a new path while the existing handle retains one complete old snapshot; projection begins after that handle closes. Missing, malformed, oversized, unsupported-schema, five BOM encodings, held-reader replacement, full UI `237/237`, Architecture `94/94`, Polytail, and independent R2 UI/architecture review pass with no P0-P3 finding. Schema, latest-wins persistence, dispatcher ownership, and firmware behavior are unchanged. This is an allocation/concurrency bound, not packaged startup latency or working-set evidence. |
| Complete H0 | In-session Replace inspection bytes | Successful Replace preserves the canonical reference and final output without report serialization or an extra output copy. Merge, read failure, and final publication failure expose no snapshot. |
| Implemented H1; rendered visual gate remains | Read-only Hex Diff consumer | The session-local snapshot drives a focused 16-byte read-only viewport, typed output-space address jump, reason/verdict/evidence panel, original-row toggle, and persisted-report fallback. The review-first navigator replaces pages at 64 rows and retains at most one page-external selection, so a 10,000-range report exposes at most 65 navigator controls/models. Commit `85336306` locks non-color Changed/Original/Selected/verdict cues and a two-pixel selected outline, then routes the production run result through the shared cancellable projector with its verified snapshot and monotonic generation. Pre-cancel publishes no report/history/JSON; an identity-matched 10,000-range stale run cannot overwrite the newer report or append history. Small/fragmented/10,000-range, bounds, fallback, bilingual, keyboard/screen-reader, non-color structure, static-motion, cancellation, stale-completion, Polytail, full UI `213/213`, Architecture `91/91`, and independent UI/architecture review pass. The development package additionally completed a real NT51950 DP Replace whose final output differed from its reference only at `0x0`; one final diff row, operation trace, reason/verdict, address jump, range list, and original-row toggle rendered and remained operable. The [Windows manual performance acceptance](../references/v0.9.10-windows-manual-performance-acceptance.md) retains the final clean-machine contrast-theme gate; development rendering does not substitute for effective-theme review. |
| Complete slices; final A8 gate remains | Raw Hex Editor Search/Next | An unchanged document revision first captures one immutable search snapshot instead of copying the complete work buffer for every scan. Pre-canceled loaded searches capture nothing; no-document still returns its typed issue. Load and every edit/undo/redo attempt publish a new revision before touching the editor, so an in-flight old result is rejected and a failed edit safely discards both caches. Commit `509dedd7` additionally reuses one immutable 4,096-address completed result for the same ordinal query and revision. Application owns negative/start/Next/wrap and truncated-index fallback; Bootstrap owns only revision/query cache, cancellation, immutable snapshot, and publication. Selection through retained index 4,095 performs no new scan, while a request at 4,096 of a truncated dense result falls back to the authoritative scan. Application raw-search tests pass `26/26`, Bootstrap search-performance tests pass `11/11`, Hex Editor UI passes `25/25`, Architecture passes `94/94`, Polytail and independent R2 review pass. Within a 254,208-match dev.4 result set, the observed first four selections advanced `0x0 -> 0x21 -> 0x42 -> 0x63` without the dev.3 per-Next working-set growth pattern; exact observations and their uncontrolled-session limit are recorded in the [Windows manual performance acceptance](../references/v0.9.10-windows-manual-performance-acceptance.md). Final candidate A8 remains open. |
| Complete slices | Raw Hex Editor document state | Commit `703f5555` replaces the internal per-byte `int?` source-address map with `int` plus a private `-1` inserted-byte sentinel. The public viewport continues to expose nullable original addresses, and insert/delete/change-range/undo/redo behavior is unchanged. The exact 1 MiB Load observation falls from `10,486,120` to `6,299,752` allocated bytes, removing `4,186,368` bytes (`39.9%`); the offset map saves approximately 32 MiB at the 8 MiB document limit. Commit `725dc041` then stops full-document dirty-byte scans while structural edits leave working/original lengths unequal; a full scan still occurs when a later insert/delete restores equal length. The 1 MiB insert observation changes from `13.007` to `3.151` ms (`75.8%`) but is not a percentile claim. Commit `d056c829` replaces the successful value-only edit path's approximately three complete-document change-range scans with an affected/touching-span rescan plus work proportional to the current changed-run count. Insert/delete keeps the exact structural fallback, which is cached once per revision; immutable snapshots prevent callers from corrupting the cache. The deterministic 8 MiB one-byte edit plus 101 lookups observation is `6.681` ms and `976` current-thread allocated bytes, but remains one local sample rather than a percentile claim. Commit `300dd235` then bounds the Presentation inspector to one replace-on-page 64-row window, preserves all global indices/reasons/counts/address jumps and full Next wrap, and replaces per-cell all-range searches with a lower-bound value lookup plus the structural subset. The 10,000-range regression materializes 64 initial and 16 tail ViewModels rather than all 10,000; its latest single command observation is `10.600` ms and `3,015,056` current-thread allocated bytes. The compact two-row pager, auto-width global index, EN/ZH action-plus-total automation name, keyboard buttons, polite page status, focused UI `34/34`, full UI `231/231`, Architecture `94/94`, and independent UI/architecture review pass. Commit `5e30f1e1` further makes a one-byte edit inside an existing 10,000-range snapshot rebuild only the affected/touching records: all other immutable records are shared between stable revision snapshots, and one read-only backing list replaces the prior full record/wrapper rebuild plus duplicate reference array. The latest single observation is `0.477` ms and `80,816` current-thread allocated bytes with a `128 KiB` regression ceiling. Raw behavior passes `23/23`, Application `181/181`, Bootstrap `6/6`, UI Hex `25/25`, Architecture `94/94`, and independent immutability review. These are deterministic bounds and local samples, not percentile claims. Packaged 8 MiB load/edit/search working-set and interaction evidence remains in the Windows manual gate. |
| Dominant cold-allocation attribution complete; no serializer change | Upstream report generation/serialization | The deterministic 10,000-range baseline skips unused split-boundary collections, reuses only range-independent immutable semantics inside one run, borrows the Domain-owned output during report creation, and encodes lowercase hashes/previews without temporary uppercase strings. Run-through-report allocation first fell from `20,162,040` to `11,719,480` bytes (`41.9%`). Commit `6b4379d2` then reuses the universal SHA-256 string for each of the 256 possible single-byte report slices: the immediate same-command observation falls from `11,646,384` to `7,541,912` allocated bytes, removing `4,104,472` bytes (`35.2%`), while output SHA, `11,720,520` JSON characters, JSON SHA, every range/hash/classification/semantic/export fact, and multi-byte hashing stay exact. Its one-sample run-through time changed from `132.869` to `79.421` ms but is not a percentile claim. Cold/repeated indented serialization measured `58,057,368`/`23,761,648` bytes and `132.240`/`52.627` ms; repeated allocation is only `320,608` bytes (`1.37%`) above the final UTF-16 string floor. A clean-process attribution assigns `33,538,536` bytes, or `97.79%` of the `34,295,720` cold-versus-repeated gap, to the shared pool's 16 KiB-through-16 MiB geometric buckets; the post-return first serialization remains `758,296` bytes above repeated. A 16 MiB initial-buffer sample preserved JSON SHA and reduced allocation to `41,267,016` bytes but took `208.190` ms versus the `132.240` ms cold baseline sample, so it did not demonstrate a latency improvement; pre-sizing/warm-up was rejected rather than shifting cost or over-renting ordinary reports. No production serializer/schema change is justified by the dominant attribution. Stable-predecessor report timing/allocation is closed in the Node B/C row below; packaged working set remains a manual gate. |
| Complete path-independent startup slice | Pinned catalog canonical hashing | Commit `d7bd4ddc` hashes already-canonical ASCII+LF catalog bytes directly from their immutable span and writes SHA-256 into a stack buffer. The two production catalogs are 192,703 and 46,947 bytes and satisfy that exact fast-path predicate. A deterministic 256 KiB observation falls from `786,688` to `152` allocated bytes, removing `786,536` bytes (`99.98%`) of hash-normalization allocation. CR/CRLF, form feed, every non-ASCII sequence (including NEL/LS/PS and invalid UTF-8), hash mismatch, and strict JSON retain the prior decode/line-normalize/re-encode or fail-closed path. Catalog hashes, profiles, argv, support, and firmware behavior do not change. Focused catalog tests pass `26/26`, full Infrastructure passes `243/243` with two platform skips, Bootstrap passes `465/465`, Architecture passes `94/94`, Polytail passes, and independent R2 review reports no P0-P3 finding. No startup-latency claim is made before final packaged first-frame evidence. |
| Complete path-independent startup-allocation slice | Private profile-bundle JSON snapshots | Commit `c627f273` lets the already bounded, hash-verified, private immutable `ProfileBundleFileSnapshot` parse its own bytes without creating a second complete defensive copy. General strict-JSON callers still receive an isolated snapshot, and an architecture lock permits exactly one owned-memory production call. A warmed 1,048,590-byte observation allocates `376` bytes on the owned path versus `1,048,992` bytes on the defensive path. BOM, duplicate-key, invalid UTF-8, framing, depth, schema, content-hash, stable-file, and caller-mutation gates remain unchanged. Full Infrastructure passes `244/244` with two platform skips, Architecture passes `95/95`, Polytail and independent R2 review pass with no P0-P3. This is an allocation bound, not a universal startup-latency claim. |
| Complete path-independent slices | Same-owner buffer copies | Report generation no longer clones `CompositionExecutionResult.OutputBytes` solely to hash/diff/preview it. `OverlayArtifactReader` now returns the same adapter-owned immutable backing on repeated reads while its constructor still isolates caller bytes and Application still creates the trust-boundary work copy. Commit `8ea8caf5` removes the clone-after-clone inside both successful external-processor result factories: each processor operation avoids two full-image allocations while Application and Domain still perform one defensive copy at their respective ownership boundaries. Commit `5f1ad87b` writes the generic/Legacy request-owned immutable input and generic staged artifacts directly to private staging, removing one more full-image allocation per initial processor input plus one allocation per generic staged artifact. Commit `8481fd2a` lets the internal Application run result and inspection snapshot retain the Domain-owned immutable output while the public `byte[]` constructor still isolates arbitrary callers; the deterministic 4 MiB publication fixture falls from `12,592,016` to `8,397,696` allocated bytes (`33.3%`, `4,194,320` bytes removed). Commit `3d40a017` reuses the caller-isolated Domain input for exact normalization and builds padding/truncation directly into the final normalized buffer; the 4 MiB exact fixture falls from `4,204,352` to `2,712` allocated bytes (`99.94%`, `4,201,640` bytes removed). Commit `3f2fcac8` lets engine-created staged source/artifact values adopt only their fresh `ReadSlice` arrays; the 4 MiB staged-artifact fixture falls from `8,401,336` to `4,207,008` allocated bytes (`49.9%`, `4,194,328` bytes removed), while public constructors still copy arbitrary caller buffers and preserve validation ownership. The reviewed Legacy exact-file copy is deliberately retained because it becomes the mutable, coalesced staging-file projection used for filename conflict checks and profile-approved block assembly. Its separate mutable staged-source projection remains necessary to apply approved overrides without mutating request-owned input. Commit `151310d8` preserves complete post-process immutable-artifact verification but streams exact length/content through pooled windows requested at no more than 128 KiB, removing the prior artifact-sized `ReadAllBytesAsync` allocation from both generic and Legacy adapters. Length/content mismatch remains fail-closed, buffers are cleared on return, focused coverage passes `11/11`, full Infrastructure `241/241` with two platform skips, Architecture `94/94`, Polytail and independent R3 review pass; stable physical golden parity is closed by the Node B/C capture below while final firmware/security owner approval remains. The atomic output writer already streams the same immutable memory to a private temp file before atomic promotion and adds no full-buffer copy. Warmed 1 MiB processor result tests pass `1/1` in each layer, full Domain passes `357/357`, full Application passes `172/172`, B0 execution metrics pass `7/7`, focused Infrastructure processor coverage passes `37/37`, focused UI inspection/Hex Diff passes `5/5`, Architecture passes `91/91`, and independent R2 ownership reviews pass. Required trust boundaries remain: the Domain execution snapshot, public-constructor isolation, independent processor diff, and atomic promotion. The completed layout-neutral final-read pipeline below removes the remaining repeated complete staging-file reads; its stable physical tool/golden parity is closed in the final-read and Node B/C rows below. |
| Implemented code slices; manual UI/AT gate remains | Firmware inspection dispatcher I/O | Commit `b17da5b6` moves the CtrlRAM Build version-confirmation preflight off the dispatcher. Commit `2205c419` adds a Bootstrap inspection batch that validates the complete request before any read and reads each exact distinct DP/TP path once. Commit `3c1e22da` removes the public synchronous slot-selection API: views await one generation-owned worker batch whose before/after file identities, IC/Number/mode, slot paths, and TP dependency reject stale publication. Commit `7211204d` supersedes the earlier output-name projection cache: every output-name binding now projects the current path-matched immutable inspection DTO without `FileInfo`, existence checks, or a path-backed firmware read. Selection and explicit same-path reselection immediately publish unknown version tokens while the worker is pending; only the latest generation may publish inspected tokens, and a stale completion produces no notification. Firmware facts, IC hints, verified context, CtrlRAM display, DP length, and output naming reuse one immutable selected-file snapshot; Number/mode/localization/other-workflow refreshes do not stat or reread it. Merge and Replace memory refresh ownership is separated, typed CtrlRAM slots apply after generic context construction, compatible mismatch acceptance retains and reinspects the same safe SlotId, and unavailable retention is announced by a bilingual polite live region. Composition and Build continue authoritative rereads. Bootstrap inspection passes `7/7`, output-name parity/priority passes `4/4`, full UI passes `209/209`, Architecture passes `89/89`, and independent architecture/UI/Polytail review passes. The remaining first-frame, real slow/unavailable storage, and Narrator/NVDA gates use the [Windows manual performance acceptance](../references/v0.9.10-windows-manual-performance-acceptance.md); deterministic source tests do not substitute for those sessions. |
| Complete path-independent inspection-allocation slice | Firmware IC hint header probe | Commit `7aa09be3` scans the existing bounded 256 KiB header span for the first ASCII `519dd` token instead of decoding the complete probe to a UTF-16 string before regex matching. The warmed complete-inspection allocation changes from `525,680` bytes to a committed ceiling of at most `65,536` bytes. An outgoing-regex oracle preserves optional NT/TT backtracking, digit boundaries, non-ASCII replacement, first-match, filename-first, unreadable-file, and probe-limit semantics. Bootstrap passes `467/467`, Architecture passes `94/94`, Polytail and independent R2 UI/architecture review pass with no P0-P3. The advisory hint still cannot select support or Build authority; file reads, metadata contracts, firmware bytes, and the packaged storage/dispatcher gate are unchanged. |
| Implemented; packaged first-frame/storage gate remains | Startup report restoration | Commit `7b6f1ab6` removes report-history I/O, JSON validation, latest-review projection, and explicit `--load-report` I/O/projection from the window constructor. `OnOpened` yields before dispatching those operations to workers; persisted history still precedes startup argument diagnostics, an explicit report still wins last, and `--open-report` runs only after complete publication. Generation ownership rejects a slow history or explicit report before expensive projection when a newer UI report wins; close cancellation publishes no partial state. Normal, source-failure, malformed JSON, invalid-shape degradation, stale-generation, and cancellation regressions pass; full UI passes `223/223` and Architecture passes `91/91`. The development package opened a 6,163,824-byte/10,000-difference synthetic startup report with every test-owned `Responding` poll returning true, published keyboard-invoked Settings before the complete report modal in a separate cold run, and bounded the persisted Changes automation tree to 121 elements/eight section summaries; exact observations and limitations are in the [Windows manual performance acceptance](../references/v0.9.10-windows-manual-performance-acceptance.md). No first-frame latency improvement is claimed until the exact packaged candidate passes the 12-entry history and explicit-report cold-start rows there. |
| Implemented; packaged interaction gate remains | Local UI preferences | Commit `9bfc780f` removes synchronous preference serialization/temp-file promotion from the dispatcher property-change handler. Report history and immutable preference snapshots share one typed latest-wins persistence coordinator; superseded queued work is cancelled, writes remain serialized and atomic, faults are isolated, and close drains both coordinators under the existing bounded best-effort timeout. Commit `1c0dc687` resolves the persisted language before ViewModel creation and aligns the selector during guarded initialization, avoiding the prior English construction followed by a second full-shell relocalization while preserving normal post-construction switching. Commit `69a59441` then caps that intentionally synchronous pre-ViewModel preference load at 64 KiB from the opened file before payload deserialization: an exact-limit valid UTF-8 document still loads, while an equally valid 64 KiB+1 document falls back to System/English/reduced-motion off. Commit `c47b28c9` adds the streaming and atomic-replacement guarantees recorded above. Save/schema/normal switching remain unchanged. Focused local persistence passes `14/14`, full UI `237/237`, Architecture `94/94`, and independent R2 UI/architecture review passes with no P0-P3. The dev.2 package persisted reduced motion on and back off without losing System/English and closed normally, but rapid theme/language cycling, exact initial-language rendering, and final-candidate restoration remain in the [Windows manual performance acceptance](../references/v0.9.10-windows-manual-performance-acceptance.md). |
| Stable B/C parity complete; final human/release gate remains | Legacy Combiner final-read pipeline | The private sequential command pipeline no longer reads the complete firmware before and after every command. It performs one complete read only after the final successful command, then independently diffs against the pre-pipeline baseline and union of declared write ranges. An evidenced `MERGE_MODE` shortened-output path captures only the predecessor tail after minimum declared block coverage and appends the required suffix; other command families fail closed on shortening. Process exit/timeout, cancellation, hidden-window, immutable artifacts, length, and unexpected staging entries remain per-command gates. Stable B `32c37e25` and C `6f3698dd` use the same Combiner 1.13.0 hash and NT51926 Common FW 1.4.1 two-command golden. Both produce output SHA `f26b6366…e193e13a`, and common physical/headless tests pass `21/21` per node. Complete firmware reads change from `2C+1` to one per successful session, while launches remain `C`; 2/13-command, short-output and source-audit gates pass. The fresh-testhost physical case did not show elapsed improvement, so the claim is deterministic I/O reduction only. Intermediate images are not parity evidence. Final firmware-owner/R3 release approval remains. |
| Stable physical-layout parity complete; final human/release gate remains | External processor discovery lifetime | Bootstrap publishes one lazily initialized immutable processor environment per OS process with execution-and-publication thread safety. Successful router construction, an unavailable root, or an invalid initialization failure is evaluated once; concurrent and later runs do not repeat parent-root search, recursive manifest parsing, registry construction, or router construction. Restart is the explicit refresh boundary for any tool/manifest layout change. Per-run private staging, process sessions, final validation, and executable SHA-256 verification remain independent. Stable B/C evidence trees are byte-identical for external tools and golden fixtures; the catalog, manifest, and executable hashes are recorded in the [Replace Performance Baseline](../references/replace-performance-baseline.md). No physical path is embedded. Final domain/firmware/security owner and release review remain. |
| Complete path-independent staging slice | Process/staging diagnostics | stdout and stderr are now drained concurrently into independent 65,536-character caps. Small output remains exact; truncated output preserves a scalar-safe prefix, explicit marker, and ordered tail. Real-process success and timeout tests lock dual-stream drainage, process-tree kill/wait behavior, partial diagnostics, and the hard cap. Commit `de52c894` builds the allowed staging-tree policy once per nonempty command plan, retains the directory-first fail-closed scan after every command's artifact verification and short-output normalization, and removes only the identical-state final scan after which no host or awaited-process staging mutation occurs. The source-audited two-command path therefore changes from three policy builds/scans to one policy build and two scans; the 13-command path changes from 14 policy builds/scans to one policy build and 13 scans. A two-command behavior test proves an unexpected entry from command one blocks command two; full Infrastructure passes `230/230` with two platform-specific skips, B0 execution metrics pass `7/7`, Architecture passes `91/91`, and independent R2 review passes. The final-read work was implemented layout-neutral and its predecessor-bound physical tool/golden parity is now closed by the stable capture below. |
| Complete | Canonical architecture drift | The four stale source-shape expectations were reconciled against current production truth without exclusions or firmware/profile changes. Standard Merge resolver ownership, the mutually exclusive Automatic Build progress overloads, NT51926 candidate non-promotion, and asynchronous report-history reopen remain locked; the full Architecture suite passes `88/88`. |
| Complete | Progress high contrast | Completed `✓`, active `▶`, and future `○` remain distinct without green/blue color, animation, or one language. The visual-only glyph stays in the raw automation view while the parent keeps the localized phase/state name and polite live region, preventing duplicate screen-reader announcements. Seven steps occupy 272 px inside the 360–430 px panel; marker contrast meets AA. Focused progress/XAML tests pass `12/12`, full UI passes `210/210`, Architecture passes `91/91`, Polytail passes, and independent UI review passes. |
| Gate | Source ceiling and final verifier | Owner decision on 2026-07-19 sets a 60,000 nonblank production C#/AXAML hard ceiling. Stable-reconciled candidate `6f3698dd` measures 59,429, leaving 571 lines of headroom. Named partials are exact at 4,405 lines for `WorkbenchCompositionService` and 4,069 for `MainWindowViewModel`; duplicate JSON is exact at 1,055 and the general partial maximum remains 2,500. Focused policy tests pass `5/5`, independent R2/Polytail review reports no P0-P3, and detached clean-tree structure passes. The final canonical verifier remains open. |
| Stable predecessor complete; v0.9.10 publication gate remains | Release lineage and version metadata | Stable `v0.9.9` is protected-main commit `32c37e254271de507be49d0f5ef38faaa122dba6`; annotated tag object `1590a27f…28bd8f96` peels exactly to it, `VERSION=0.9.9`, and the GitHub Release/package CI is green. The v0.9.10 stack was replayed onto that exact commit with 130/130 patches range-diff equal and zero conflicts. Final `v0.9.10` still requires VERSION/changelog/package/provenance alignment, required feature and main CI, a tag on the protected main merge commit, and immutable GitHub Release/package verification. |
| Development package evidence; final gate remains | Windows self-contained package | Commit `35d1fe66` corrects `SHA256SUMS.txt` path encoding from lossy ASCII to strict UTF-8 without BOM after the first development smoke reproduced a hash failure for a manifest-approved Unicode reference path. The deterministic dry run now rejects BOM, invalid UTF-8, and non-exact Unicode path round-trips; independent R2 review and release-policy tests pass `7/7`. Development packages dev.1 through dev.4 passed the canonical fresh-extraction allowlist, manifest/hashes, SBOM/provenance, bundled CRC-worker vector, and packaged-application smoke. Dev.3 retained the before-observation for repeated Search/Next; dev.4 at `509dedd7` is 57,738,537 bytes with SHA-256 `bab881e6af99f992b4155b5eab7e3d86579cf00d51ddb5dbb887c9fe9e7c90b0` and supplies the targeted after-observation. Package names, full SHAs, sizes, and limitations are in the [Windows manual performance acceptance](../references/v0.9.10-windows-manual-performance-acceptance.md). All are development evidence only. The exact final reviewed candidate must repeat clean-machine, interaction, accessibility, external-tool/golden-layout, firmware-owner, and release-security gates. |
| Stable-predecessor B/C complete; packaged/human/final gates remain | Node B/C and final review | Stable B `32c37e25` and C `6f3698dd` use byte-identical tool/golden trees. DP public oracle and NT51926 two-command golden filters pass `14/14` per node; real-tool/headless tests pass `21/21`; command catalog/argv tests pass `33/33`. NT51926 full output SHA remains `f26b6366…e193e13a`. The B0 surface passes `7/7` and locks one authoritative Build, exact report/mutation/validation/issue parity, DP three-capacity reads, CtrlRAM 2/13-command sessions/launches, and failed-input zero commit. Ten final report pairs retain the exact output/JSON hashes and 10,000 differences while run-through p50/p95 falls `22.40%/25.88%` and allocation falls `64.57%`. The two-command physical fresh-testhost observation is slightly slower and does not support a wall-clock claim; only the full-read reduction `5→1` is claimed. The [Replace Performance Baseline](../references/replace-performance-baseline.md) records protocol, hashes and raw samples. Packaged dispatcher/first-step/first-page/accessibility evidence, final R2/R3/firmware-owner review, canonical verification, CI and publication remain open. |

### Change Report read-only Hex Diff workspace

The default desktop layout is a resizable two-column workspace:

- The left area uses roughly two thirds of the width for a read-only,
  bounded/virtualized viewport aligned with the standalone Hex Editor's visual
  grammar. It shows address, 16 output bytes, and ASCII. It does not reuse the
  editor's mutable work buffer, undo history, Save As path, edit commands, or
  per-byte structural source map.
- `Show original rows` inserts the verified reference row below each visible
  changed output row. The toggle changes row projection only; it does not copy
  or persist the complete firmware again.
- The upper-right information panel leads with typed modification reason,
  expected/review-required verdict, evidence, semantic subject, section,
  changed-byte count, and before/after hashes. “Declared replacement” means an
  accepted declared write range; it does not by itself mean a version change.
- The lower-right navigator lists review-required ranges first, then accepted
  ranges in address order. Each half-open range is explicitly in the
  Application-transported compiled output address space. Selecting it validates
  the offset against the snapshot length, synchronizes the information panel,
  and jumps/focuses the viewport at its first address. Accessible labels include
  the output-space id; Presentation never infers it from profile or IC text.
  Paging replaces a fixed 64-range window instead of accumulating controls; a
  page-external jump pins only that selected range until its page becomes
  visible, for a hard 65-model/control maximum.
- Color is never the only signal. Keyboard use, screen-reader names, high
  contrast, and reduced motion expose the same status.

The full binary source is the non-serialized, session-local snapshot from
[ADR 0027](../adr/0027-evidence-preserving-performance-remediation.md). A
reopened persisted report has only its stored ranges, hashes, semantics, and
bounded hex preview; it must say that complete Hex Diff bytes are unavailable
instead of rereading mutable source paths or fabricating a full comparison.

### Baseline comparison protocol

Node B is the reconciled before state and node C is the optimized state. Each
comparison records commit and clean state, Windows/.NET version, CPU, warm-up,
run count, input and expected-output SHA-256, Legacy Combiner manifest hash,
cold/warm distinction, and exact feature flags. Counts and byte/report equality
are deterministic assertions. Elapsed p50/p95, allocations, GC, peak working
set, and UI-thread blocked intervals are local evidence, not universal CI
thresholds.

The first parity matrix includes public deterministic NT51950/NT51951 DP Replace
at 256/512/1024 KiB and the NT51926 Common FW 1.4.1 cascade two-command
full-output case. The 13-command case is count/timing-only. General Replace has
synthetic contract coverage but no owner full-output golden and cannot be used
to imply product support or firmware parity.

## Post-v0.9.10 decision inventory

No `v0.9.11` or later milestone is assigned before reviewed `v0.9.9` and
`v0.9.10` merge. The owner will version the next route using final B/C evidence
and the support matrix. The inventory remains explicit:

- any process, buffer, I/O, report, history, Hex Diff, progress, startup,
  cancellation, accessibility, or working-set residual that misses the
  `v0.9.10` target;
- a dedicated production code-size convergence phase after `v0.9.11`, with a
  fresh ownership inventory and descending ratchets established from the
  reviewed result rather than from the temporary 60,000 ceiling;
- a compact/versioned report representation only if upstream fragmented-report
  evidence still fails after repeated work is removed; it must preserve exact
  ranges, typed semantics, hashes, processor trace, complete export, and
  backward reading;
- controlled `DiffNFMerge.exe` preprocessing for NT51919/NT51929/NT51932 and
  NT51950/NT51951 cascade CtrlRAM. Until exact tool/hash/argv/order/size/error,
  golden, and owner evidence exist, users provide the already-built
  `NF_Ctrlram.bin`;
- reviewed custom postbuild profiles. They must compile through the same
  profile/planner/executor/staging/diff boundaries and may not expose arbitrary
  scripts, per-run command editing, or guessed write authority;
- General Replace owner full-output golden parity. Synthetic/contract tests are
  not a product golden or support claim;
- future AB Reference FlashCode. It requires an AB-specific profile and
  extractor; normal offsets, DP Replace acceptance, or filenames cannot be
  guessed into AB support;
- candidate-IC intake through deterministic declared evidence. It remains
  human-reviewed candidate authoring with no automatic firmware inference,
  runtime registration, or support promotion; and
- final support-matrix, clean-machine package, SBOM/provenance, signing/legal,
  long-run, and stable-release gates for `v1.0.0`.

The first signed-off stable sample remains `v1.0.0` under canonical release
governance. No inventory item receives a version merely because it is listed.

## Scope fence

No milestone may add a per-IC executor, a second compiler, a second runtime
schema/loader hierarchy, UI firmware semantics, primary-FWConfig fallback, or
unchecked binary patch. Any proposed exception requires an ADR, precise R3
evidence, and owner approval before implementation.
