# 0.9.x Completion Roadmap

Status: active owner roadmap.

This document is the execution goal for the remaining `0.9.x` milestones. It
separates a completed migration milestone from a firmware-support claim and
keeps each later release small enough to verify independently.

The evidence-backed issue inventory and dependency order are maintained in the
[0.9.x Codebase Readiness Audit](0.9.x-codebase-readiness-audit.md). All P0/P1
items selected for the first stable sample are assigned to `v0.9.10` through
`v0.9.16`; they are not deferred to an unspecified later minor line.

## Program goal

Deliver the owner-approved AB Code and CtrlRAM version-edit work through the
existing V2 family/profile/compiler boundary and the one composition engine,
then standardize IC intake, retire only fully replaced legacy routes, and close
release/support evidence. Runtime FWConfig facts always come from the unique
NVT Backup copy at terminal `T - 0xFFF`; a primary flash-map address is
evidence only and is never a runtime fallback.

## Completed-milestone audit

| Milestone | Audit result | Evidence | Carry-forward action |
| --- | --- | --- | --- |
| `v0.9.1` | Functional scope complete and tagged. | `v0.9.1` points at `1d910b45`; trusted V2 bundle/map/profile compilation routes covered Standard Merge and NT51950/NT51951 DP Replace. The release evidence records local `verify.py --all` success and the remote CI outage. | No functional migration item is moved to `v0.9.3`. It remains a candidate/support-neutral migration milestone. |
| `v0.9.2` | Functional scope complete and tagged. | `v0.9.2` points at `57fefbdf`; content-addressed schema source, deterministic closed-root materialization, all built-in bundle materialization, and removal of the Standard Merge legacy runtime fallback are merged into `0.9.2`. | `v0.9.3` starts from this reviewed release baseline. This milestone did not add AB behavior. |

The following are deliberately not omissions from `0.9.1` or `0.9.2`:

- Remaining legacy Replace/General runtime routes and compatibility projections
  are governed by the [Legacy Retirement Matrix](../governance/legacy-retirement-matrix.md)
  and belong to `v0.9.5` only after exact V2 replacement evidence exists.
- Unsigned or private CtrlRAM/General Replace golden evidence for unrelated ICs
  is not silently promoted by either migration milestone. `v0.9.3` closes the
  evidence required by its own AB and version-edit work only.
- Product support remains a firmware-owner decision. A V2 runtime route or a
  tagged migration milestone never changes a profile to `Supported`.

## Release order

```text
v0.9.1 (tagged)
  -> v0.9.2 (tagged release baseline)
  -> v0.9.3 AB Code + CtrlRAM version edit
  -> v0.9.4 candidate IC intake
  -> v0.9.5 V2 workflow convergence and legacy retirement
  -> v0.9.6 release/support consolidation
  -> v0.9.7 UI token consolidation and code-size discipline
  -> v0.9.8 feature-frozen source/package convergence
  -> v0.9.9 legacy convergence and verified patch closure
  -> v0.9.10 predecessor reconciliation + candidate IC intake
       + Replace performance baseline
  -> v0.9.11 Replace reliability and safe performance convergence
  -> v0.9.12 Application boundaries and typed reports
  -> v0.9.13 V2 Replace convergence and legacy retirement
  -> v0.9.14 firmware evidence and support-scope lock
  -> v0.9.15 operability, soak, and evidence-backed performance
  -> v0.9.16 release, security, and clean-machine closure
  -> v1.0.0 first signed-off stable sample
```

`0.9.3` integration and release must start from the reviewed `v0.9.2` tag.
Candidate AB work may start earlier under the policy below, but it must rebase
or merge onto that tag before integration. Do not backfill the tag after AB
integration has started: that would make the AB evidence baseline ambiguous.

### Pre-tag candidate-development policy

Owner policy, 2026-07-14: development of a later `0.9.x` feature branch is
allowed before its preceding release tag and without owner firmware evidence.
This permission is limited to deterministic candidate, architecture, intake,
or test work. It does not grant runtime support, change a promotion stage,
replace direct golden/legacy parity, permit a release tag, or allow a merge to
`main`. Each early branch names its target milestone and must rebase or merge
onto the required reviewed predecessor tag before that target can integrate or
release.

## v0.9.3: AB Code and CtrlRAM version edit

Risk class: R3. A separate `feature/0.9.3/<topic>` branch is required for each
independently reviewable firmware slice, with firmware-owner review before the
release tag.

### Current AB evidence inventory (2026-07-15)

All five V2 candidates copy the complete submitted DP_AB container before
their declared TP overlays, and all five are rejected by the Application run
request boundary. No row below is runtime support.

| Target | Verified candidate evidence | Remaining R3 owner gate |
| --- | --- | --- |
| NT51919 | Explicit fact-scoped NT51929 region-set alias with complete candidate-output parity to the tracked NT51929 case. | Firmware-owner approval of the alias scope; a direct product golden is still absent. |
| NT51929 | Direct owner fixture and complete V2/Python-reference parity, output `c7e1e263...3d66abe2`. | Firmware-owner promotion review. |
| NT51932 | Direct named Python-configuration synthetic parity, output `cd54e124...7de10ce`. | Direct product golden or owner-approved fact-scoped alias evidence, then firmware-owner promotion review. |
| NT51950 | Two direct owner fixtures reproduce the Python reference and Combiner 1.13.0 command byte-for-byte. The command consumes no `map.txt`; C# relocates only TPB DIFF and never writes header CRC. | Firmware-owner approval of the exact staging/command sequence before runtime exposure. |
| NT51951 | Synthetic full-byte Python/Combiner parity for `NT51950BASED_MERGE_AB_MODE CRC8 A.bin B.bin output.bin 0x80000`. | Direct NT51951 product golden and firmware-owner approval; NT51950 output is not substitute evidence. |

### Scope

1. Implement AB Merge for NT51919, NT51929, NT51932, NT51950, and NT51951
   through V2 bundle data, the existing compiler, and the existing composition
   engine. There is no AB-only executor or UI-owned byte logic.
2. Every AB profile initializes its output from the complete submitted DP_AB
   container, then applies only profile-declared TPA/TPB overlays. Customer
   information follows the submitted DP_AB container unless owner evidence
   declares a narrower exception.
3. NT51919 may reuse NT51929/NT51932 AB facts only through an approved
   fact-scoped map binding with direct parity evidence. It is not a whole-map
   alias.
4. NT51950 topology-dependent AB behavior derives its chip count from the
   canonical FWConfig Backup layout. Profile applicability selects the needed
   CMD/CMD-BK facts; it must not expose eight synthetic modes when aliases
   produce the same executable behavior.
5. Implement the generic `transform-scalar` operation only where a profile
   declares the approved TPB relocation field. It remains checked for width,
   byte order, expected-before value, and overflow.
6. Verify whether the external Python CRC transform and legacy Combiner are
   equivalent separately for each approved 950/951 AB branch. No equivalence
   is assumed from their names or from Normal/CtrlRAM postbuild behavior.
7. On CtrlRAM Replace Build only, present a pre-Build review choice to retain
   the TP FW major/sub-version or edit both values. Preview never edits the
   version. The version reader and any approved writer use
   `FirmwareConfigLayout` and the NVT Backup copy only; fields, write ranges,
   ordering relative to Combiner postbuild, and output differences require
   owner evidence and tests.

### Required evidence and tests

- Archive each owner-supplied AB golden outside Git unless it is an approved
  fixture. Commit only manifests, hashes, provenance, and explicit evidence
  decisions.
- Add direct V2/reference output parity and negative tests for every approved
  IC/topology/capacity branch. Test initializer order, overlays, customer-info
  ownership, scalar relocation, CRC/header declared writes, and rejection of
  malformed/ambiguous metadata.
- Test both CtrlRAM version choices against the same base image: retain is
  byte-identical to the existing approved branch; edit changes only the
  declared version and postbuild ranges. Confirm an invalid or missing NVT
  Backup fails closed.
- Run the affected contract, engine, worker, Bootstrap, UI, and golden tests;
  finish with `python scripts/verify.py --all`, Polytail, independent review,
  and firmware-owner review. A failed or unavailable remote CI is recorded,
  never represented as a passing firmware gate.

### Exit criteria

`v0.9.3` may be tagged only when all five approved AB targets and the selected
CtrlRAM version-edit branches have their required R3 evidence, owner review,
and immutable golden/legacy parity records. Unapproved ICs, modes, capacities,
or CtrlRAM postbuild variants remain candidate data with explicit blockers.

## v0.9.4: automated IC intake

Provide a manifest-driven intake command or UI that reads declared evidence
into a caller-selected empty staging directory and emits candidate bundle rows
plus validation reports. It must be deterministic, read Office files without
macros, reject path escapes/overwrite/lock files, and have no runtime profile
registration, support promotion, range inference, or automatic schema allowlist
change. A human still reviews and promotes a candidate through the normal
bundle materialization path.

## v0.9.5: V2 workflow convergence

Migrate one workflow family at a time until its V2 runtime/profile/map path
has direct byte, naming, processor, report, CLI, and UI parity. Only then
retire the corresponding legacy definition named in the retirement matrix.
This includes the remaining Replace/General compiler authority, flash-map and
metadata compatibility projections, and legacy Combiner postbuild catalog when
their R3 evidence permits replacement. Do not delete a legacy catalog merely
because it is old or because one adjacent workflow has migrated.

## v0.9.6: release and support consolidation

Remove compatibility projections whose `v0.9.5` retirement rows are closed;
reconcile the IC/workflow support matrix; close clean-machine Windows package,
SBOM/provenance, hash, signing-policy, report/history, and release-manifest
evidence. This milestone does not introduce new firmware behavior or use
packaging as a substitute for firmware-owner support approval.

## v0.9.7: UI token consolidation and code-size discipline

Risk class: R1/R2 according to the affected UI boundary. Candidate work may
start before the reviewed `v0.9.6` tag under the pre-tag policy, but integration
and release follow that tag. It consolidates exactly equivalent Avalonia visual,
text, and spacing tokens so one semantic UI concept has one owner. It also
records a production/UI/test line-count inventory and removes only definitions,
adapters, or compatibility paths that have an exact retained replacement and
direct regression coverage.

It must not:

- place firmware ranges, profile behavior, integrity policy, or firmware
  terminology decisions in UI tokens;
- replace accessibility labels, localization resources, or meaningful state
  distinctions with visually similar shared values;
- remove required tests, manifests, golden evidence, or review records to
  improve a line-count metric; or
- introduce a second theme, localization, or token hierarchy.

Exit evidence records the baseline and resulting line counts, every retired
definition and its retained owner, UI smoke/accessibility coverage for affected
surfaces, and an independent review that confirms no semantic token collapse or
firmware-boundary drift. A smaller codebase is an outcome of exact replacement,
not a standalone acceptance target.

## v0.9.8: feature-frozen source and package convergence

Risk class: R2 for source ownership and R3 for release-package changes. Any
firmware-contract change is deferred to its own evidence-bearing R3 branch.
This milestone is feature-frozen and support-neutral: user-visible firmware
behavior, declared ranges, processor commands, output bytes, and support states
do not expand.

Exit gates:

- hand-written production C# and AXAML retain the owner-accepted 56,742-line
  final ratchet recorded on 2026-07-16; the earlier 56,000-line stretch gate is
  superseded and further source convergence moves to v0.9.9;
- the portable release package is no more than 1% larger than the reviewed
  v0.9.7 baseline;
- the exact Windows inner-ZIP baseline is 57,501,699 bytes and the maximum is
  58,076,715 bytes;
- canonical verification enforces production size, exact profile/schema
  duplication, and oversized partial-type aggregates;
- the reviewed package/release workflow builds a clean artifact and
  `smoke-release.ps1` enforces the package byte ceiling;
- redundant compatibility projections, hard-coded registrations, duplicate
  validation, and pass-through adapters are deleted only when one retained
  owner has direct regression evidence; and
- trust-boundary validation, immutable input handling, atomic output,
  processor diff checks, golden evidence, and required tests are not weakened
  to reach the size target.

## v0.9.9: legacy convergence and verified patch closure

The original planned target was at most 54,000 nonblank production C#/AXAML
lines, with the portable package within the same 1% growth ceiling. Every
refactored legacy compiler, profile, catalog, facade, and runtime path may be
retired only after its exact V2 replacement is reviewed. The explicit
exception is Legacy Combiner: the approved executable and constrained runner
remain.

`LegacyCombinerPostbuildCatalog` declarations may move into V2 processor data
only with exact command, tool binding, staging, read/write range, full-output
golden, and firmware-owner evidence. Until then the catalog remains an R3
authority, not removable debt. This milestone also closes confirmed patch
gaps, including bundle/package reparse or TOCTOU findings, without speculative
checks or support promotion. NT51919, NT51932, NT51951, and every other
candidate retain their recorded direct/fact-scoped evidence and owner gates.

The 2026-07-17 audit found that this numerical goal was not met:
`origin/0.9.9` records 56,731 nonblank lines, while the audited `0.9.10`
lineage records 56,742. Before `0.9.10` can integrate, the owner must accept the
exact measured ratchet or the branch must complete only evidence-backed
retirement. Safety checks, tests, firmware evidence, and required compatibility
paths must not be deleted merely to force 54,000.

## v0.9.10: UI-driven candidate IC intake toolchain

`0.9.10` first reconciles the reviewed `0.9.9` predecessor. On 2026-07-18 the
owner authorized parallel development, so local `0.9.10` was rebased onto the
current clean `0.9.9` committed tip `7e18cfa0`; all four existing `0.9.10`
commits remained patch-equivalent. This is not final predecessor closure.
After `0.9.9` finishes, `0.9.10` must rebase again, every unmerged
`feature/0.9.9/*` and `feature/0.9.10/*` branch must receive an explicit merge,
supersede, or close decision, and the canonical full verifier must pass before
intake or performance results become a release baseline.

The proposed durable boundary and alternatives are recorded in
[ADR 0023](../adr/0023-ui-driven-candidate-ic-intake.md). It recommends one
typed in-process C# use case, exact four-record parity followed by retirement of
the Python executable boundary, and a secondary Settings UI. The ADR remains
Proposed until the owner accepts its implementation/runtime, workbook-format,
UI-placement, and security/dependency-review gates; no UI or new generated
contract is authorized yet.

The user-facing goal is to add an IC by supplying declared information through
the UI rather than manually constructing every intermediate artifact. The
intake accepts owner-provided Excel workbooks, mmap files, BAT command evidence,
sample BINs, expected outputs, and provenance through the one typed C# use case.
During migration, `scripts/create_candidate_ic_intake.py` is only the parity
oracle for deterministic `candidate-bundle-rows.json`,
`candidate-evidence-manifest.json`, `missing-evidence.json`, and
`validation-report.json` bytes; it is retired after exact parity.

The toolchain reads Office input without macros and parses BAT files only as
declared command evidence; submitted commands are never executed. It uses a
caller-selected empty staging directory, rejects path escapes, overwrites, and
lock files, and preserves the existing contract in which those four records are
the only files at the selected output root. Persisting generated family/profile/
bundle/evidence artifacts or PR-ready diagnostics requires a separately
versioned candidate-workspace contract, schema, and negative tests; until then
they remain typed previews. Those records are not a runtime profile bundle.
Generating any new family, map, profile, processor, or bundle contract requires
a separately reviewed contract/schema change before implementation. The
toolchain never infers firmware facts from filenames or adjacent ICs, edits
shared schemas or allowlists automatically, registers a runtime profile, or
promotes support. Human architecture and firmware-owner review remain the
promotion boundary.

### Replace optimization CHANGE rationale (owner-added 2026-07-17)

`v0.9.10` also carries a bounded, measurement-first optimization lane for the
existing DP Replace and CtrlRAM Replace paths. Candidate IC intake remains the
primary milestone. This lane is support-neutral and may not change firmware
bytes, declared ranges, atomicity, processor argv/order, profile promotion, or
the one compiler/engine boundary merely to improve elapsed time.

The current branch provides the following concrete reasons to investigate a
change:

| Area | Current evidence | Why change |
| --- | --- | --- |
| Automatic Build orchestration | `CompositionRunExecutionSupport.PreviewOrBuildAsync` runs a complete Preview and then a complete Build after a successful Preview. | The same artifact reads, reference clone, engine operations, external processors, validation, hashes, diff, and report work can run twice for one Build click. |
| CtrlRAM postbuild I/O | For each command, `LegacyCombinerPostbuildProcessor` reads the full staged firmware before the command and again during shortened-output normalization, then reads the final firmware once more. A session with `C` commands therefore performs `2C + 1` host full-image reads. | A 13-command branch costs 27 host reads per processor session. Under the current automatic Build path, it also means 26 process launches and 54 host reads before other artifact I/O is counted. |
| Workbench file inspection | Slot selection asks independent firmware-fact, IC-mismatch, context-suggestion, metadata, and postbuild projections to inspect the same selected BIN. | Repeated synchronous full-file reads increase selection latency and UI-thread pressure, especially for category-dependent CtrlRAM paths such as NT51926 and NT51930. |
| Buffer ownership | Artifact, Application, Domain, normalization, external-processing, result, and report boundaries include multiple defensive byte-array copies. | Domain must keep its defensive snapshot, but copies repeated inside one owning layer add allocation and memory-bandwidth cost without adding trust. |
| Output-difference explanation | `DeclaredReplacement` is the stable classification for accepted differences inside declared Replace write ranges. Generic non-processor operations, including a TP FW version `patch-scalar`, may therefore receive this classification. | The term does **not** mean "firmware version changed". A generic "Declared replacement" label can hide whether bytes came from a CtrlRAM payload, an explicit TP FW version edit, FWConfig propagation, or CRC/header refresh. |
| Measurement | Run reports have overall timestamps, but automatic Build returns only the final Build report and external process records do not expose duration or deterministic invocation/read counters. | Optimization cannot be reviewed reliably without separating engine, processor, I/O, and UI-inspection costs. |

#### Proposed direction and ownership

1. Add deterministic counters and a local benchmark harness before changing
   execution. Cover DP container lengths `0x40000`, `0x80000`, and `0x100000`;
   CtrlRAM two-command and 13-command plans; and NT51926/NT51930 base-file
   selection. Record p50/p95 locally, allocations, full-image read counts,
   engine executions, processor sessions, and process launches. Do not add a
   runtime dependency or hard timing assertion to CI.
2. Add one Application-owned automatic-Build use case that snapshots and
   validates current inputs, executes the compiled composition once, performs
   final validation, and atomically promotes exactly those validated bytes.
   Standalone Preview remains non-committing. An explicit Preview-token Build
   remains bound to compiled fingerprint, hashes, and output options and must
   fail closed when they change.
3. Give the UI one immutable asynchronous inspection snapshot per selected
   file identity/hash and project firmware facts, mismatch guidance, context,
   metadata, and postbuild readiness from it. Build must still perform its own
   authoritative read and hash; a UI cache is never Build authority.
4. Document byte-buffer ownership and remove only redundant same-owner copies.
   Retain the ADR 0015 Domain snapshot, immutable input identity, processor
   before/after evidence, and immutable report/result projections.
5. Only after byte-identical golden coverage, let the Infrastructure postbuild
   adapter retain the authoritative command result in memory and converge to
   one full staging-image readback per command. The adapter may not skip
   shortened-output normalization, executable hash verification, artifact
   immutability checks, host diff validation, or declared-write-range checks.
6. Keep stable report classifications unless a versioned contract migration is
   approved. Application should provide typed semantic subjects such as
   `CtrlRAM replacement`, `TP FW version edit`, `FW configuration / postbuild
   update`, and `Postbuild CRC/header refresh`; Presentation only localizes and
   renders them. It must not branch on operation ids or reason strings to infer
   firmware meaning.

Reusing an earlier Preview output as the later Build artifact is rejected: it
creates stale-input and stale-policy authority. Skipping hashes or independent
diff validation, parallelizing ordered Combiner commands, or removing Domain's
defensive snapshot is also rejected. Optimizing only Presentation is
insufficient because the dominant CtrlRAM costs cross Application and
Infrastructure boundaries.

#### Acceptance, risk, and gates

- Automatic Build executes the engine once and opens one processor session;
  the 13-command case reduces process launches from 26 to 13.
- One UI selection performs at most one full-file read for its inspection
  snapshot. Changing file identity or content invalidates the snapshot.
- After the separately reviewed adapter phase, a 13-command automatic Build
  reduces host staging-firmware reads from the current worst case of 54 to at
  most 13. Staged-source artifact immutability reads are tracked separately and
  are not hidden from evidence.
- Golden output bytes and SHA-256, operation/mutation traces, exact processor
  argv and order, difference classifications, warnings, output naming, and
  failed-run atomicity remain identical unless a separately approved contract
  change explicitly names a report-only semantic difference.
- Self-replacement that produces identical final bytes still emits no output-
  difference row. A `DeclaredReplacement` row proves an accepted changed range,
  not that a version edit occurred.
- Counters and byte assertions are deterministic CI gates. Timing results are
  recorded as benchmark evidence rather than flaky pass/fail thresholds.
- Instrumentation and isolated UI inspection work are R1 unless they alter a
  public report or use-case contract. Automatic-Build orchestration, report
  semantics, and buffer-ownership changes are R2 and require architecture/
  contract review. External postbuild I/O convergence is R3 and requires exact
  CtrlRAM golden parity plus firmware-owner review.
- Any changed range, operation order, checksum/header behavior, processor
  authority, public classification token, or output byte is outside this
  optimization authorization and must stop for its normal R3 evidence and
  human gate.

Implementation proceeds as separate reviewable phases. `v0.9.10` owns only the
deterministic baseline and counters. `v0.9.11` owns automatic-Build
orchestration, the UI inspection snapshot, and safe same-owner buffer
convergence. `v0.9.12` owns typed report semantics. The R3 postbuild adapter is
deferred to `v0.9.15` and may start only after `v0.9.14` closes the applicable
golden evidence. `v0.9.10` cannot integrate or release until its branch is
reconciled with the reviewed `v0.9.9` baseline and each included phase passes
its own narrow tests, Polytail, the canonical final verifier, and required
independent review.

## v0.9.11: Replace reliability and safe performance convergence

Risk class: R1 for instrumentation and UI inspection; R2 for Build
orchestration and buffer ownership.

Deliver one Application-owned automatic-Build execution, one immutable
asynchronous UI inspection snapshot per selected file identity/hash,
deterministic execution/read/process counters, and removal of only redundant
same-owner copies. Build still performs an authoritative read/hash, validation,
and atomic output promotion. Output bytes, processor argv/order, operation
trace, stable classifications, and support state remain unchanged.

## v0.9.12: Application boundaries and typed reports

Risk class: R2 architecture/contract review.

Move composition firmware inspection and ambient run metadata behind explicit
Application ports with Infrastructure adapters. UI-local preference/history
persistence remains a Presentation adapter. Replace the UI/CLI dependence on
reparsed `WorkbenchRunResult.ReportJson` with typed immutable report
projections, and make semantic subjects such as TP FW version edit explicit
without redefining the stable `DeclaredReplacement` machine classification.
Add semantic dependency and trust-boundary tests instead of relying only on
source-text shape ratchets.

## v0.9.13: V2 Replace convergence and legacy retirement

Risk class: R2-R3 by workflow.

Converge CtrlRAM and General Replace on the trusted V2 family/profile/compiler
authority. Retire `CompositionProfileCompiler`, `TpFlashMapCatalog`,
`IcMetadataFacade`, and `LegacyCombinerPostbuildCatalog` consumers only one
reviewed matrix row at a time, after direct output, report, CLI, UI, invalid
cases, processor, and owner evidence proves the exact replacement. Legacy
Combiner remains the approved constrained external-tool exception.

## v0.9.14: firmware evidence and support-scope lock

Risk class: R3 with firmware-owner review.

The owner selects the exact shipped IC/workflow/capacity/topology subset and
closes the matching Standard Merge, DP Replace, CtrlRAM Replace, General
Replace, and AB golden/processor evidence. The milestone explicitly decides
NT51931 postbuild handling, full-flash versus TP-work-image slicing, General
Replace safety envelopes, and whether REG Replace or saved-rule capabilities
are included or remain post-1.0. Unapproved rows remain hidden/candidate-only.

## v0.9.15: operability, soak, and evidence-backed performance

Risk class: R1-R3 by phase.

Finish report/history audit review, run progress, cancellation, accessibility,
representative long-run soak, and accepted deterministic performance budgets.
Only after the applicable `v0.9.14` full-byte golden parity may the
Infrastructure postbuild adapter converge repeated staging-image reads. Exact
tool binding, normalization, artifact immutability, before/after evidence,
host diff validation, and declared-write-range checks remain mandatory.

## v0.9.16: release, security, and clean-machine closure

Risk class: R2-R3 release/security review.

Add protected cross-platform .NET coverage for Unix special-file trust cases;
run an independent clean Windows x64 package test that launches the UI and
performs representative Preview/Build; close signing, legal, golden-fixture
redistribution, branch-protection/required-check, independent-reviewer, SBOM,
provenance, hash, and final documentation gates. This milestone produces the
reviewed final `0.9.x` release candidate; it does not bypass firmware-owner
approval for the selected support matrix.

## Formal stabilization target

`v0.9.16` is the final planned `0.9.x` stabilization milestone. `v0.9.10`
remains the reconciled IC-intake and measurement baseline, not a stable product
claim. The first signed-off stable sample remains `v1.0.0` and follows only
after the `v0.9.10` through `v0.9.16` gates selected for its support scope are
closed under the canonical release governance and package workflow.

## Scope fence

No milestone may add a per-IC executor, a second compiler, a second runtime
schema/loader hierarchy, UI firmware semantics, primary-FWConfig fallback, or
unchecked binary patch. Any proposed exception requires an ADR, precise R3
evidence, and owner approval before implementation.
