# v0.9.15–v0.9.17 AB Delivery Roadmap

Status: active owner roadmap, amended 2026-07-23.

This document assigns the next three milestones without turning a candidate,
golden input, observed version, or topology into a firmware support claim.
It supersedes only the future-scope assignments in earlier planning documents;
the released `v0.9.14` changelog remains historical release evidence.

## Common release and safety rules

- `0.9.15` starts only from official `v0.9.14` tag
  `58f5bbf4cdbfb4e02036c8c1b40c48aa88fa21f7`, peeled commit
  `9b15d8757ccb44167c471ca4e602036066bcdea9`.
- `0.9.16` may start only from the official reviewed `v0.9.15` release tag;
  `0.9.17` may start only from the official reviewed `v0.9.16` release tag.
  A feature branch from an earlier candidate, local commit, or stale worktree is
  not a substitute baseline.
- A release must retain one composition engine, typed profile authority, named
  address spaces, half-open ranges, immutable inputs, staged external tools, and
  independently verified write ranges. UI text, filenames, PID, TP version,
  Common FW version, a golden hash, and observed IC count do not select a route.
- Code-size measurement is a visible review metric only. Readability, clear
  ownership, executable tests, firmware safety, and evidence are release gates.
- Cross-workflow header/Evidence/Memory coverage unification, the shared Hex
  viewport, Changes redesign, and global Button feedback remain unassigned
  Presentation backlog. They do not enter `0.9.15`, `0.9.16`, or `0.9.17`
  without a separate owner milestone assignment.

## Milestone plan

| Milestone | Active scope | Explicit boundary | Exit evidence |
| --- | --- | --- | --- |
| `0.9.15` | Complete delivery readiness for the already enabled NT51919/NT51929/NT51932 perfect-family AB Merge path: typed execution-snapshot output names under [ADR 0034](../adr/0034-ab-output-name-contract.md), independent TPA/TPB inspection facts (version, Common FW, and PID when present), silent perfect-family catalog reconciliation, and reviewer-facing delivery automation. | No new IC or topology admission. NT51950/NT51951 remain candidates. Delivery automation may gather deterministic preflight, lineage, verification, changed-file, warning, and residual-gate evidence; it cannot merge, tag, publish, or bypass human review. | Exact baseline proof; narrow behavior tests; canonical verification and CI; review-ready provenance; Codex review; and the existing R3 firmware-owner/golden gates for the enabled AB route. |
| `0.9.16` | NT51950 AB Code candidate closure. Establish separately typed one-IC and two-IC topology evidence, DP/TP header and command authority, declared input/output length rules, output naming provenance, and candidate regression vectors. | The owner-supplied NT51950 single-topology golden is evidence for that topology only. It does not infer or enable a two-IC route, a command offset, padding/truncation, a profile, or support status. IC Number is shown only where the AB profile has evidence-backed topology choices. | Firmware-owner-reviewed map/operation/processor contract; direct vectors for every exposed topology and size boundary; negative topology/length tests; staged-write evidence; canonical verification; independent review. No support claim without all R3 gates. |
| `0.9.17` | NT51951 AB Code candidate closure, independent of NT51950. Establish its own one-IC and two-IC topology evidence, DP/TP header and command authority, declared input/output length rules, and candidate regression vectors. | No NT51951 range, split point, TP backup location, command placement, or output size may be copied from NT51950 or inferred from a presentation string. The owner-described layout remains an evidence hypothesis until an approved profile/ADR and direct vectors establish it. | The same R3 map/profile/processor, topology, boundary, staged-write, golden, canonical-verification, independent-review, and firmware-owner gates as `0.9.16`, evaluated from NT51951 evidence. |

## Delivery-to-review automation contract for `0.9.15`

The automation produces review evidence; it does not make release decisions. A
review handoff must identify:

1. the exact release tag and peeled commit used as baseline, current branch, and
   clean/dirty worktree state;
2. the complete changed-file inventory, commits, and whether a version branch
   or feature branch unambiguously descends from that baseline;
3. executed checks, CI state, code-size review warnings, and the precise
   distinction between a warning and a failed gate;
4. profile/IC/mode impact and all unchanged candidate boundaries; and
5. outstanding human firmware-owner, golden, packaging, release-owner, and
   Codex-review gates.

The automation must fail closed for ambiguous lineage or a dirty worktree before
it performs development actions. It never starts a build merely because a
release tag exists, and it never creates a release, alters a tag, merges a PR,
or promotes candidate support.

## Candidate admission ledger

| IC/mode | First scheduled milestone | Current planning status | Required fact source |
| --- | --- | --- | --- |
| NT51919/NT51929/NT51932 AB Merge | `0.9.15` | Existing enabled perfect-family route; delivery/readiness work only. | ADR 0032, ADR 0034, direct regression evidence, and firmware-owner review. |
| NT51950 AB Merge single | `0.9.16` | Candidate; one owner-supplied golden may inform evidence intake only. | Owner map, direct input/output vector, approved profile/ADR, and firmware-owner review. |
| NT51950 AB Merge two-IC | `0.9.16` | Candidate; no inference from single-topology evidence. | Independent two-IC map, vector, approved profile/ADR, and firmware-owner review. |
| NT51951 AB Merge single/two-IC | `0.9.17` | Candidate; independent evidence intake. | Independent owner map and vectors, approved profile/ADR, and firmware-owner review. |

## Deferred Presentation decision

[ADR 0028](../adr/0028-capability-driven-shared-hex-viewport.md) remains a
proposed design boundary, not an implementation authorization. Before any U0-U5
work starts, the owner must name a later milestone and its exact released
predecessor, then obtain the ADR's R2 architecture/UI review and accessibility
evidence. That future work must not be smuggled into an AB candidate release.
