<UserControl
    x:Class="NvtFwCombiner.Presentation.Avalonia.Views.GeneralMergeMappingRow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:NvtFwCombiner.Presentation.Avalonia.ViewModels"
    x:Name="Root"
    x:DataType="vm:GeneralMergeMappingViewModel">
  <Border
      Classes="subtleSurface"
      Margin="0,0,0,8"
      Padding="7">
    <Grid ColumnDefinitions="34,118,118,108,*,82,40" ColumnSpacing="8">
      <Border Classes="mappingIndex">
        <TextBlock Classes="mappingIndex" Text="{Binding Index}" />
      </Border>
      <TextBox
          Classes="addressInput"
          Grid.Column="1"
          Text="{Binding SourceStartAddress, Mode=TwoWay}"
          PlaceholderText="source" />
      <TextBox
          Classes="addressInput"
          Grid.Column="2"
          Text="{Binding TargetStartAddress, Mode=TwoWay}"
          PlaceholderText="target" />
      <TextBox
          Classes="addressInput"
          Grid.Column="3"
          Text="{Binding Length, Mode=TwoWay}"
          PlaceholderText="length" />
      <Border
          Classes="fileDropZone"
          Grid.Column="4"
          MinHeight="32"
          Padding="0"
          DragDrop.AllowDrop="True"
          DragDrop.DragEnter="DropZone_OnDragEnter"
          DragDrop.DragOver="DropZone_OnDragOver"
          DragDrop.DragLeave="DropZone_OnDragLeave"
          DragDrop.Drop="MappingDrop_OnDrop">
        <Grid>
          <Border Classes="dropZoneOverlay" />
          <StackPanel Margin="10,0" VerticalAlignment="Center" Spacing="2">
            <TextBlock Classes="bodyEmphasisText" Text="{Binding DisplayName}" TextWrapping="Wrap" />
            <TextBlock Classes="technicalCaption" IsVisible="{Binding HasFile}" Text="{Binding DisplayDetail}" TextWrapping="Wrap" />
          </StackPanel>
        </Grid>
      </Border>
      <Button
          Grid.Column="5"
          Classes="action browseAction"
          Click="BrowseButton_OnClick"
          Content="{Binding BrowseLabel, ElementName=Root}"
          ToolTip.Tip="{Binding SelectSourceBinTooltip, ElementName=Root}" />
      <Button
          Grid.Column="6"
          Classes="iconButton dangerIcon"
          Click="RemoveButton_OnClick"
          ToolTip.Tip="{Binding RemoveMappingTooltip, ElementName=Root}">
        <TextBlock FontSize="13" Text="X" />
      </Button>
    </Grid>
  </Border>
</UserControl>
