interface:
  display_name: "To Tickets"
  short_description: "Split a plan into tracer-bullet tickets"
  default_prompt: "Use $to-tickets to split this approved NFC plan into dependency-aware tracer-bullet tickets."
