interface:
  display_name: "Supervised Branch Development"
  short_description: "Coordinate multi-agent branch integration"
  default_prompt: "Use $supervised-branch-development for multi-agent, reconstruction, R3 migration, release integration, or large conflict resolution."
policy:
  allow_implicit_invocation: true
