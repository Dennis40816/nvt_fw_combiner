interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "Resolve merge and rebase conflicts"
  default_prompt: "Use $resolving-merge-conflicts to recover or resolve this Git operation while preserving branch intent."
