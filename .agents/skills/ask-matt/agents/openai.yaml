interface:
  display_name: "Ask Matt"
  short_description: "Find the right skill or workflow"
  default_prompt: "Use $ask-matt to route this NFC task to the smallest useful installed skill flow."
policy:
  allow_implicit_invocation: false
