# Test Data Policy

This specification package contains manifests only. It intentionally excludes real firmware and expected output BIN files.

The future repository may contain small synthetic fixtures generated from documented patterns. Proprietary golden firmware must remain in approved private storage and be materialized only on authorized CI runners.
