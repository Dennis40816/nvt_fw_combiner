# NT51932 cascade

既有 final expected、Initial DP、TP、Normal、VN、`DiffDLM.bin`、`NF_Diff_*.bin` 與一份
不完整的 `NF_Ctrlram.bin` 已在 Git，不要整包重傳。

請提供兩組證據：

1. 與 final expected 同一次 run 的正確 `DiffDLM.bin`；若既有檔確實正確，改附它的生成
   命令、完整 log 與 input/output SHA-256，解釋目前 target 中 4,095 bytes 的差異。
2. 經 DiffNFMerge 完成的最終 `NF_Ctrlram.bin`；若無法直接匯出，附所有
   `NF_Diff_*.bin` 的順序、DiffNFMerge 完整命令／log 與 input/output SHA-256。

`DiffNFMerge.exe` repository 已有，不要重傳。請保留原檔名與 `AUTO_PRJ` 資訊。
