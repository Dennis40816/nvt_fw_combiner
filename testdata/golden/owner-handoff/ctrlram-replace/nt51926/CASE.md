# NT51926 CtrlRAM Replace Handoff

## Required now

Put one file here:

```text
cascade/expected.bin   262,144 bytes
```

This must be the independent final output for the already tracked NT51926
Common FW 1.4.1 cascade base and five replacement inputs after the tracked
postbuild and Legacy Combiner 1.13 command.

Do not resend `base.bin`, the replacement inputs, BAT command, or Combiner
unless their official hashes differ from the committed manifest. `single/` is
not requested in this batch.
