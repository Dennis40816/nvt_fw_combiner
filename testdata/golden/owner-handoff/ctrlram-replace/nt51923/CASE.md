# NT51923 CtrlRAM Replace Handoff

Requested case folders inside this directory:

- `single/`
- `cascade/`

Requested files per case folder: `base.bin`, replacement files under `inputs/`, optional `expected.bin`, optional `combiner-cmd.txt`.
