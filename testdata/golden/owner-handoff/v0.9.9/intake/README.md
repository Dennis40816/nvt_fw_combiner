# Private v0.9.9 Evidence Drop

This directory and every payload below it are ignored by Git.

Start with `51926/ctrlram-replace/1.4.1/cascade/`. Copy the exact reference
inputs and expected output there, then copy `CASE.template.md` and
`case.template.json` into the case folder and fill in the owner-known facts.

Do not place an executable here when the recorded Legacy Combiner 1.13.0 tool
is already the correct tool. Record its exact version and SHA-256 instead.

See the tracked sibling `../README.md` for the complete requested folder list,
priorities, per-case files, AB invariants, and acceptance sequence.
