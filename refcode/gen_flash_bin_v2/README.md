# `gen_flash_bin_v2` reference snapshot

This folder is a source-only snapshot of the standard merge Python reference under:

`Dennis40816/NFCG/testdata/reference/FlashCodeGenerator/gen_flash_bin_v2`

It is included only to preserve standard DP/TP/LD merge semantics and configuration facts. It is **not** a production dependency, is not part of the .NET solution, and is excluded from release packages.

Source files are recorded in `SOURCE_MANIFEST.json`. Firmware samples and generated output are intentionally excluded.
