<Project>
  <Target Name="RejectReferenceCodeProjectInclusion" BeforeTargets="CoreCompile">
    <Error
      Condition="$([System.String]::Copy('@(Compile)').Contains('refcode'))"
      Text="Production projects must not compile files from refcode/." />
  </Target>
</Project>
