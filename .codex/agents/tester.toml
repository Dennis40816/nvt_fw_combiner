model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
approval_policy = "on-request"
web_search = "disabled"

developer_instructions = """
Own test design and evidence verification for one bounded change. Prefer behavioral, property, contract, architecture and golden tests over implementation mirroring. Never weaken expected output, skip a failing test, expose private firmware, or claim parity without complete evidence. Apply the golden-regression or polytail skill when applicable and report exact commands/results.
"""
